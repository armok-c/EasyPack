# Milestone v1.1: 体验增强与预设指令

**Status:** ✅ SHIPPED 2026-04-26
**Phases:** 6-10
**Total Plans:** 11

## Overview

修复命令执行核心问题，提升窗口体验和操作效率，引入预设指令系统降低配置成本。

## Phases

### Phase 6: 命令执行修复

**Goal**: 用户能正常点击任何项目的任何指令卡片，终端在正确目录下执行命令，路径含空格/中文/特殊字符也不出错
**Depends on**: Phase 5 (v1.0 完成)
**Requirements**: FIX-01, FIX-02
**Plans**: 1 plan

Plans:
- [x] 06-01-PLAN.md -- 使用 raw_arg 修复命令执行传参

**Details:**
使用 `raw_arg()` 替换 `.args()` 传参方式，修复 0x80070002 阻断性 bug。根因是 Rust `std::process::Command.args()` 使用 MSVC C 运行时转义规则，对 `cmd.exe /K` 的命令行解析不兼容。`raw_arg()` 直接传递原始命令行字符串，绕过 MSVC 转义。1 文件修改，1 测试新增。

---

### Phase 7: 无边框窗口与自定义标题栏

**Goal**: 应用拥有现代化的无边框窗口外观，顶部显示自定义标题栏（应用名称、拖拽区域、窗口控制按钮），窗口阴影和 resize 正常工作
**Depends on**: Phase 6
**Requirements**: WIN-01, WIN-02, WIN-03
**Plans**: 1 plan

Plans:
- [x] 07-01-PLAN.md -- 无边框窗口和自定义标题栏实现

**Details:**
自定义 TitleBar React 组件替代系统原生标题栏。通过 Tauri Window API 实现最小化/最大化/关闭。`startDragging()` + `data-tauri-drag-region` 实现窗口拖拽。TDD 完成（8 个测试全部通过）。修复 Windows 上 `data-tauri-drag-region` 不可靠的问题，改用 `appWindow.startDragging()`。6 文件修改，77 个测试通过无回归。

---

### Phase 8: Rust 后端扩展与快速 UI 修复

**Goal**: Rust 后端提供项目信息检测能力（图标识别、文件夹大小、Git 分支、打开文件夹），模态窗在窗口过小时可滚动不被截断
**Depends on**: Phase 7
**Requirements**: PROJ-07, PROJ-08, PROJ-09, PROJ-10, UI-09
**Plans**: 5 plans

Plans:
- [x] 08-01-PLAN.md -- Rust 后端命令实现（scan_project_icons + get_project_info）+ assetProtocol 配置
- [x] 08-02-PLAN.md -- DialogContent 自适应高度修复（max-h-[90vh] + flex-col + 内部滚动包装）
- [x] 08-03-PLAN.md -- 图标类型判别工具函数 + Sidebar 文件图标渲染
- [x] 08-04-PLAN.md -- ProjectSettingsDialog 自定义图标 UI（扫描 + 文件选择）+ 人工验证
- [x] 08-05-PLAN.md -- 前端项目信息集成（文件夹大小 + Git 分支显示 + App.tsx props 传递）

**Details:**
两个 Tauri 命令（scan_project_icons, get_project_info）13 单元测试。assetProtocol 配置启用 convertFileSrc 支持。DialogContent 改为 flex-col + max-h-[90vh] 自适应滚动。图标类型判别函数（isFileIcon / getFilePath）支持文件图标。ProjectSettingsDialog 自定义图标 UI。项目信息 8 秒超时保护，独立错误标志区分加载/成功/失败三态。

---

### Phase 9: 前端 UI 集成

**Goal**: 指令切换和控制操作以按钮样式呈现在同一行，用户可直接从应用内打开项目文件夹
**Depends on**: Phase 8
**Requirements**: PROJ-11, UI-10
**Plans**: 1 plan

Plans:
- [x] 09-01-PLAN.md -- Toggle Group 按钮行 + 打开文件夹按钮 + open_folder Rust 命令

**Details:**
文本链接切换器替换为 Toggle Group 按钮行（全局/项目指令拼合按钮 + 打开文件夹 outline 按钮）。Rust 后端 open_folder 命令使用 explorer.exe + raw_arg()。95 测试通过无回归。

---

### Phase 10: 预设指令系统

**Goal**: 用户通过分类下拉框快速选择预设指令（python/pip/git/rust/npm），降低手动配置成本
**Depends on**: Phase 9
**Requirements**: PRE-01, PRE-02, PRE-03, PRE-04
**Plans**: 3 plans

Plans:
- [x] 10-01-PLAN.md -- 预设库数据层扩展（presets.ts 重写 + icons.ts 扩展 + Select 安装）
- [x] 10-02-PLAN.md -- CommandDialog 双 Select 预设选择器 UI 集成
- [x] 10-03-PLAN.md -- Gap closure: CargoShip 替换为 Ship + scope 选择 UI (PRE-04)

**Details:**
分类预设库（4 分类 25 命令）+ 默认卡片精简为 git pull + claude 两个。双 Select 预设选择器（分类 → 命令）自动填充表单。修复 CargoShip 图标（lucide-react 不存在）替换为 Ship。Scope 选择器 UI（全局/项目 radio group），addCommand 支持 explicit scope 参数。79 测试通过。

---

## Milestone Summary

**Key Decisions:**

- Decision: raw_arg 替换 args (Rationale: 绕过 MSVC C 运行时转义，修复 0x80070002) — ✓ Validated
- Decision: startDragging() 替代 data-tauri-drag-region (Rationale: Windows 兼容性) — ✓ Validated
- Decision: Sync core + async Tauri command wrapper (Rationale: 测试可脱离 tokio 运行时) — ✓ Validated
- Decision: assetProtocol scope ** (Rationale: 个人工具，用户需从任意路径选图标) — ✓ Validated
- Decision: file: 前缀约定区分图标类型 (Rationale: 简单可靠) — ✓ Validated
- Decision: 独立 boolean error 标志 (Rationale: UI 明确区分加载/成功/失败三态) — ✓ Validated
- Decision: 手写 Button pair 替代 ToggleGroup (Rationale: 仅 2 按钮互斥切换，无需 Radix 抽象) — ✓ Validated
- Decision: Ship 替代 CargoShip (Rationale: lucide-react 不导出 CargoShip) — ✓ Validated
- Decision: explicit scope 参数 + commandMode fallback (Rationale: 向后兼容) — ✓ Validated

**Issues Resolved:**

- 修复命令执行 0x80070002 阻断性 bug
- 修复 Windows 无边框窗口拖拽不工作
- 修复 DialogContent 窗口过小时截断
- 修复 CargoShip 图标导致运行时崩溃
- 修复 shadcn Select 组件输出到错误路径

**Issues Deferred:**

- CommandDialog.test.tsx 16 个测试失败（Element type is invalid，pre-existing）
- npm run build 有 8 个 pre-existing TypeScript 错误
- Windows Snap Layout 不支持（WIN-04，需额外实现）
- 从 package.json/scripts 自动导入指令（PRE-05）

**Technical Debt Incurred:**

- build_full_command 函数有 dead_code warning
- 部分 TypeScript 类型错误未清理（dialog.tsx, Sidebar.tsx）
- 文件夹大小计算无异步进度条（PROJ-12 deferred）

---

_For current project status, see .planning/ROADMAP.md_
