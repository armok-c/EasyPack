---
phase: 02-sidebar-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/hooks/useProject.ts
autonomous: true
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "应用启动时从 tauri-plugin-store 加载项目列表和选中状态"
    - "添加项目后数据持久化到磁盘，重启后可恢复"
    - "删除项目后数据同步更新到磁盘"
    - "选中项目后选中状态持久化到磁盘"
    - "Store 加载失败时应用正常运行（内存降级模式）"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "tauri-plugin-store Rust crate 依赖"
      contains: "tauri-plugin-store"
    - path: "src-tauri/src/lib.rs"
      provides: "Rust 端 store 插件注册"
      contains: "tauri_plugin_store"
    - path: "src-tauri/capabilities/default.json"
      provides: "store 权限声明"
      contains: "store:default"
    - path: "src/hooks/useProject.ts"
      provides: "多项目管理 hook（store-backed state）"
      contains: "export interface ProjectItem"
  key_links:
    - from: "src/hooks/useProject.ts"
      to: "@tauri-apps/plugin-store"
      via: "load() + store.set/get"
      pattern: "import.*load.*from.*@tauri-apps/plugin-store"
    - from: "src-tauri/src/lib.rs"
      to: "tauri_plugin_store"
      via: "plugin registration"
      pattern: "tauri_plugin_store::Builder"
---

<objective>
安装 tauri-plugin-store 并重构 useProject hook 为多项目 store-backed 模式

Purpose: 建立数据持久化基础设施，将 Phase 1 的单项目 useState 模式重构为多项目管理 + tauri-plugin-store 持久化，为后续 UI 重构提供数据层支撑。
Output: 可工作的 store-backed useProject hook，支持多项目添加/删除/选中，数据自动持久化
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sidebar-persistence/02-CONTEXT.md
@.planning/phases/02-sidebar-persistence/02-RESEARCH.md
@.planning/phases/02-sidebar-persistence/02-UI-SPEC.md

@src/hooks/useProject.ts
@src-tauri/Cargo.toml
@src-tauri/src/lib.rs
@src-tauri/capabilities/default.json
@src/App.tsx
@src/components/Sidebar.tsx
@src/components/MainArea.tsx

<interfaces>
<!-- Phase 1 existing contracts that must be preserved or updated -->

From src/components/Sidebar.tsx (current):
```typescript
interface SidebarProps {
  currentProject: Project | null;
  onAddProject: () => void;
}
```

From src/components/MainArea.tsx (current):
```typescript
interface MainAreaProps {
  currentProject: Project | null;
  onExecute: (command: string) => void;
}
```

From src/App.tsx (current):
```typescript
const { currentProject, selectFolder, executeCommand } = useProject();
```

From src/hooks/useProject.ts (current):
```typescript
export interface Project {
  name: string;   // 文件夹名称
  path: string;   // 完整路径
}
export function useProject(): {
  currentProject: Project | null;
  selectFolder: () => Promise<void>;
  executeCommand: (shellCommand: string) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 tauri-plugin-store 并注册插件</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/capabilities/default.json, package.json</files>
  <read_first>
    - src-tauri/Cargo.toml (current Rust dependencies)
    - src-tauri/src/lib.rs (current plugin registration)
    - src-tauri/capabilities/default.json (current permissions)
    - package.json (current frontend dependencies)
  </read_first>
  <action>
1. **安装前端 npm 包:**
   Run: `pnpm add @tauri-apps/plugin-store`
   This adds `@tauri-apps/plugin-store` (version 2.4.x) to package.json dependencies.

2. **安装 Rust crate:**
   Run: `cd E:/GitLib/EasyPack/src-tauri && cargo add tauri-plugin-store`
   This adds `tauri-plugin-store = "2"` to Cargo.toml [dependencies].

3. **注册 Rust 端插件 (src-tauri/src/lib.rs):**
   在 `tauri::Builder::default()` 链中，在 `.plugin(tauri_plugin_dialog::init())` 后面添加:
   ```rust
   .plugin(tauri_plugin_store::Builder::default().build())
   ```
   完整的 run() 函数应为:
   ```rust
   pub fn run() {
       tauri::Builder::default()
           .plugin(tauri_plugin_dialog::init())
           .plugin(tauri_plugin_store::Builder::default().build())
           .invoke_handler(tauri::generate_handler![
               commands::shell::execute_command,
           ])
           .run(tauri::generate_context!())
           .expect("error while running tauri application");
   }
   ```

4. **添加 capabilities 权限 (src-tauri/capabilities/default.json):**
   在 permissions 数组中添加 `"store:default"`:
   ```json
   {
     "$schema": "../gen/schemas/desktop-schema.json",
     "identifier": "default",
     "description": "Capability for the main window",
     "windows": ["main"],
     "permissions": [
       "core:default",
       "dialog:default",
       "store:default"
     ]
   }
   ```

5. **验证编译:**
   Run: `cd E:/GitLib/EasyPack && pnpm build` (前端 TypeScript 编译)
   Run: `cd E:/GitLib/EasyPack/src-tauri && cargo check` (Rust 编译检查)
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && pnpm build && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - package.json 包含 "@tauri-apps/plugin-store" 依赖
    - src-tauri/Cargo.toml 包含 "tauri-plugin-store" 依赖
    - src-tauri/src/lib.rs 包含 "tauri_plugin_store::Builder::default().build()"
    - src-tauri/capabilities/default.json 包含 "store:default" 权限
    - 前端 TypeScript 编译通过
    - Rust cargo check 通过
  </done>
  <acceptance_criteria>
    - `grep -c "tauri-plugin-store" src-tauri/Cargo.toml` returns 1 or more
    - `grep -c "tauri_plugin_store" src-tauri/src/lib.rs` returns 1 or more
    - `grep -c "store:default" src-tauri/capabilities/default.json` returns 1 or more
    - `grep -c "@tauri-apps/plugin-store" package.json` returns 1 or more
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 重构 useProject hook 为多项目 store-backed 模式</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>
    - src/hooks/useProject.ts (current single-project hook to be rewritten)
    - .planning/phases/02-sidebar-persistence/02-RESEARCH.md (data schema, hook pattern, pitfall avoidance)
    - .planning/phases/02-sidebar-persistence/02-UI-SPEC.md (data persistence specification)
    - src/App.tsx (consumer of useProject hook)
    - src/components/Sidebar.tsx (consumer of useProject types)
    - src/components/MainArea.tsx (consumer of useProject types)
  </read_first>
  <action>
完全重写 src/hooks/useProject.ts，将 Phase 1 的单项目 useState 模式重构为多项目 store-backed 模式。注意：此任务只修改 hook 文件本身。App.tsx/Sidebar.tsx/MainArea.tsx 的接口适配将在 Plan 02 完成，此阶段先保持旧接口兼容。

**具体实现（参考 RESEARCH.md Pattern 1 + UI-SPEC Data Persistence Specification）：**

1. **新的类型定义（替换旧的 Project interface）：**
```typescript
export interface ProjectItem {
  id: string;       // 规范化路径作为 ID (lowercase, forward slashes)
  name: string;     // 文件夹名称 (per D-03)
  path: string;     // 原始完整路径 (preserves original casing)
  addedAt: number;  // Date.now() timestamp
}

// 兼容旧接口的类型别名（Plan 02 迁移完成后可移除）
export type Project = ProjectItem;
```

2. **项目 ID 生成函数（RESEARCH.md Pattern 2）：**
```typescript
function generateProjectId(path: string): string {
  return path
    .split(/[\\/]/)
    .filter(Boolean)
    .join('/')
    .toLowerCase();
}
```

3. **Store 常量：**
```typescript
const STORE_PATH = 'easypack-store.json';
const PROJECTS_KEY = 'projects';
const SELECTED_KEY = 'selectedProjectId';
```

4. **useProject hook 内部状态：**
- `projects: ProjectItem[]` — 项目列表（按添加时间排列）
- `selectedId: string | null` — 当前选中项目 ID
- `store: Store | null` — tauri-plugin-store 实例
- `loading: boolean` — store 初始化加载状态

5. **派生状态：**
- `currentProject` — 从 projects + selectedId 派生，保持与旧接口兼容：
  ```typescript
  const currentProject = selectedId
    ? projects.find(p => p.id === selectedId) ?? null
    : null;
  ```

6. **useEffect 初始化（参考 RESEARCH.md Pattern 1 + Pitfall 1）：**
```typescript
useEffect(() => {
  let mounted = true;
  async function init() {
    try {
      const s = await load(STORE_PATH, { autoSave: 100 });
      if (!mounted) return;
      const savedProjects = await s.get<ProjectItem[]>(PROJECTS_KEY);
      const savedSelectedId = await s.get<string>(SELECTED_KEY);
      if (savedProjects) setProjects(savedProjects);
      if (savedSelectedId) setSelectedId(savedSelectedId);
      setStore(s);
    } catch (error) {
      console.warn("Store 加载失败，使用内存模式:", error);
      // 降级：即使 store 失败，应用仍可正常运行（数据不持久化）
    } finally {
      if (mounted) setLoading(false);
    }
  }
  init();
  return () => { mounted = false; };
}, []);
```

7. **addProject 方法（per D-02 新项目在底部, D-04 重复检测, D-05 添加后自动选中）：**
```typescript
const addProject = useCallback(async (path: string, name: string) => {
  const id = generateProjectId(path);
  if (projects.some(p => p.id === id)) {
    toast.error("项目已存在"); // D-04
    return;
  }
  const newItem: ProjectItem = { id, name, path, addedAt: Date.now() };
  const updated = [...projects, newItem];
  setProjects(updated);
  setSelectedId(id);
  await store?.set(PROJECTS_KEY, updated);
  await store?.set(SELECTED_KEY, id);
}, [projects, store]);
```

8. **removeProject 方法（per D-10 自动选中邻近, D-11 删除最后一个回到空状态）：**
```typescript
const removeProject = useCallback(async (id: string) => {
  const idx = projects.findIndex(p => p.id === id);
  if (idx === -1) return;
  const updated = projects.filter(p => p.id !== id);
  setProjects(updated);

  // D-10: 自动选中邻近
  let newSelectedId: string | null = null;
  if (updated.length > 0 && id === selectedId) {
    newSelectedId = updated[Math.min(idx, updated.length - 1)].id;
  } else if (id !== selectedId) {
    newSelectedId = selectedId;
  }
  setSelectedId(newSelectedId);
  await store?.set(PROJECTS_KEY, updated);
  await store?.set(SELECTED_KEY, newSelectedId);
}, [projects, selectedId, store]);
```

9. **selectProject 方法：**
```typescript
const selectProject = useCallback(async (id: string) => {
  setSelectedId(id);
  await store?.set(SELECTED_KEY, id);
}, [store]);
```

10. **selectFolder 方法（沿用 Phase 1 逻辑，但调用 addProject）：**
```typescript
const selectFolder = useCallback(async () => {
  try {
    const selected = await open({
      directory: true,
      multiple: false,
      title: "选择项目文件夹",
    });
    if (typeof selected === "string") {
      const name = selected.split(/[\\/]/).filter(Boolean).pop() || selected;
      await addProject(selected, name);
    }
  } catch (error) {
    console.error("文件夹选择失败:", error);
  }
}, [addProject]);
```

11. **executeCommand 方法（沿用 Phase 1 逻辑，使用 currentProject 派生状态）：**
```typescript
const executeCommand = useCallback(async (shellCommand: string) => {
  if (!currentProject) return;
  try {
    await invoke("execute_command", {
      projectPath: currentProject.path,
      shellCommand,
    });
    toast.success(`已执行: ${shellCommand}`);
  } catch (error) {
    toast.error(`命令执行失败：${error}。请检查项目路径和命令是否正确。`);
  }
}, [currentProject]);
```

12. **返回值（保持旧接口兼容 + 新增多项目接口）：**
```typescript
return {
  // 旧接口（保持兼容，Plan 02 迁移完成前 Sidebar/MainArea 仍使用）
  currentProject,        // ProjectItem | null (兼容旧 Project | null)
  selectFolder,          // () => Promise<void>
  executeCommand,        // (shellCommand: string) => Promise<void>

  // 新增多项目接口
  projects,              // ProjectItem[]
  selectedId,            // string | null
  loading,               // boolean
  addProject,            // (path: string, name: string) => Promise<void>
  removeProject,         // (id: string) => Promise<void>
  selectProject,         // (id: string) => Promise<void>
};
```

**重要注意：**
- import `load` and `type Store` from `@tauri-apps/plugin-store`
- import `open` from `@tauri-apps/plugin-dialog`
- import `invoke` from `@tauri-apps/api/core`
- import `toast` from `sonner`
- import `useState, useEffect, useCallback` from `react`
- 保持 `Project` 类型别名以兼容现有 Sidebar/MainArea 的 `Project` import
- 所有 store 操作使用 `await`（RESEARCH.md Pitfall 3）
- useEffect 清理使用 `mounted` flag 防止卸载后 setState（RESEARCH.md Pitfall 1）
- Store 加载失败使用 console.warn 降级，不显示错误 toast
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - useProject.ts 导出 ProjectItem interface（含 id, name, path, addedAt 四个字段）
    - useProject.ts 导出 Project 类型别名指向 ProjectItem
    - useProject hook 返回 projects, selectedId, loading, addProject, removeProject, selectProject
    - useProject hook 保持 currentProject, selectFolder, executeCommand 旧接口
    - store 初始化使用 load('easypack-store.json', { autoSave: 100 })
    - 重复添加同一路径时 toast.error("项目已存在")
    - 删除选中项目后自动选中邻近项目
    - TypeScript 编译通过，旧组件 import 无报错
  </done>
  <acceptance_criteria>
    - `grep -c "export interface ProjectItem" src/hooks/useProject.ts` returns 1
    - `grep -c "export type Project" src/hooks/useProject.ts` returns 1
    - `grep -c "generateProjectId" src/hooks/useProject.ts` returns 1
    - `grep -c "easypack-store.json" src/hooks/useProject.ts` returns 1
    - `grep -c "autoSave" src/hooks/useProject.ts` returns 1
    - `grep -c "项目已存在" src/hooks/useProject.ts` returns 1
    - `grep -c "removeProject" src/hooks/useProject.ts` returns at least 2 (definition + return)
    - `grep -c "selectProject" src/hooks/useProject.ts` returns at least 2 (definition + return)
    - `pnpm build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 前端 TypeScript 编译通过: `pnpm build`
2. Rust 编译检查通过: `cd src-tauri && cargo check`
3. Store 插件正确注册: `grep "tauri_plugin_store" src-tauri/src/lib.rs`
4. Store 权限已配置: `grep "store:default" src-tauri/capabilities/default.json`
5. useProject hook 导出完整接口: `grep "removeProject\|selectProject\|projects\|selectedId" src/hooks/useProject.ts`
</verification>

<success_criteria>
- tauri-plugin-store 前端和 Rust 端均安装并注册
- capabilities/default.json 包含 store:default 权限
- useProject hook 管理多项目数组 + store 持久化
- 旧接口（currentProject, selectFolder, executeCommand）保持兼容
- 新接口（projects, selectedId, removeProject, selectProject）已导出
- pnpm build 和 cargo check 均通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-persistence/02-01-SUMMARY.md`
</output>
