# Phase 4: 自定义指令与项目级覆盖 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 04-custom-commands
**Areas discussed:** 自定义指令 CRUD 入口, 项目级覆盖机制, 弹窗表单设计, 指令数据结构与展示

---

## 自定义指令 CRUD 入口

### 添加指令入口
| Option | Description | Selected |
|--------|-------------|----------|
| 网格区域 + 按钮 | 在网格区域底部或右下角放添加按钮，点击弹出添加弹窗 | |
| 顶部工具栏 | 项目信息区域旁边放工具栏按钮 | |
| 卡片悬停操作 | 悬停卡片时显示操作按钮 | |

**User's choice:** 网格区域 + 按钮

### 添加按钮显示方式
| Option | Description | Selected |
|--------|-------------|----------|
| 编辑模式 | 进入编辑模式后显示添加按钮和卡片操作按钮，类似 iOS 编辑模式 | ✓ |
| 常驻添加占位卡片 | 网格中始终显示虚线边框占位卡片 | |
| 悬停时显示 | 悬停网格区域时显示添加按钮 | |

**User's choice:** 模式切换（编辑模式）

### 编辑模式入口
| Option | Description | Selected |
|--------|-------------|----------|
| 项目信息区域按钮 | 项目信息区域旁边的设置/编辑图标按钮 | ✓ |
| 右键空白区域 | 右键网格空白处弹出选项 | |
| 右上角固定按钮 | 主区域右上角固定位置 | |

**User's choice:** 项目信息区域按钮

### 编辑/删除触发方式
| Option | Description | Selected |
|--------|-------------|----------|
| 右键菜单 | 卡片右键显示上下文菜单（编辑、删除） | ✓ |
| 悬停显示图标 | 悬停卡片时显示编辑和删除图标 | |
| 点击后选择操作 | 点击卡片后弹出操作选择 | |

**User's choice:** 右键菜单

### 删除确认
| Option | Description | Selected |
|--------|-------------|----------|
| 直接删除，无确认 | 点击删除按钮直接删除 | ✓ |
| 弹出确认弹窗 | 删除前弹出确认弹窗 | |

**User's choice:** 直接删除，无确认

### 编辑触发方式
| Option | Description | Selected |
|--------|-------------|----------|
| 点击卡片编辑 | 编辑模式下点击自定义指令卡片弹出编辑弹窗 | ✓ |
| 点击编辑图标 | 卡片上显示编辑图标，点击图标编辑 | |

**User's choice:** 点击卡片编辑

---

## 项目级覆盖机制

### 覆盖含义
| Option | Description | Selected |
|--------|-------------|----------|
| 完全替换 | 项目级指令完全替代全局指令 | ✓ |
| 追加合并 | 项目级指令追加到全局指令后面 | |
| 混合模式 | 替换同名 + 追加新指令 | |

**User's choice:** 完全替换

### 项目级指令集初始内容
| Option | Description | Selected |
|--------|-------------|----------|
| 纯自定义 | 从零添加，不含预设 | |
| 预设 + 自定义 | 默认包含 4 个预设指令，可在此基础上增删 | ✓ |
| 用户选择包含 | 创建时弹出选择器勾选 | |

**User's choice:** 预设 + 自定义

### 创建/切换入口
| Option | Description | Selected |
|--------|-------------|----------|
| 项目信息区域显示 | 显示当前模式标签 + 切换入口 | ✓ |
| 侧边栏右键项目 | 右键侧边栏项目显示选项 | |
| 下拉选择器 | 项目信息区域下拉选择 | |

**User's choice:** 项目信息区域显示

### 回退全局指令
| Option | Description | Selected |
|--------|-------------|----------|
| 全部删除后自动回退 | 删除所有项目级指令后自动回到全局 | ✓ |
| 明确切换按钮 | 有切换按钮，保留项目级数据 | |

**User's choice:** 全部删除后自动回退

---

## 弹窗表单设计

### 表单字段
| Option | Description | Selected |
|--------|-------------|----------|
| 名称 + 命令 + 可选图标 | 名称（必填）+ Shell 命令（必填）+ 图标（可选，默认 Terminal） | ✓ |
| 名称 + 命令 + 描述 | 不提供图标选择，统一 Terminal 图标 | |
| 名称 + 命令 + 图标选择器 | 从 lucide-react 图标库中选择 | |

**User's choice:** 名称 + 命令 + 可选图标

### 图标选择方式
| Option | Description | Selected |
|--------|-------------|----------|
| 预设图标列表 | 8-10 个常用图标选择 | ✓ |
| 输入图标名称 | 输入 lucide-react 图标名称 | |
| 完整图标搜索器 | 搜索全部 lucide-react 图标 | |

**User's choice:** 预设图标列表

### 实时预览
| Option | Description | Selected |
|--------|-------------|----------|
| 实时预览卡片 | 弹窗底部展示卡片预览 | ✓ |
| 无预览 | 只看表单字段 | |

**User's choice:** 实时预览卡片

### 表单验证
| Option | Description | Selected |
|--------|-------------|----------|
| 即时验证 | 字段有内容时保存按钮可用，为空显示红色提示 | ✓ |
| 提交时验证 | 点击保存时统一验证 | |

**User's choice:** 即时验证

---

## 指令数据结构与展示

### 展示方式
| Option | Description | Selected |
|--------|-------------|----------|
| 混合展示，无区分 | 统一 CommandCard，不做区分 | |
| 混合展示 + 轻微标记 | 自定义指令有轻微视觉标记 | ✓ |
| 分区展示 | 预设和自定义分两个区域 | |

**User's choice:** 混合展示 + 轻微标记

### 展示顺序
| Option | Description | Selected |
|--------|-------------|----------|
| 全局在上，项目级在下 | 按类型分层 | |
| 统一排序 | 按添加顺序统一排列 | ✓ |

**User's choice:** 统一排序

### 预设指令删除
| Option | Description | Selected |
|--------|-------------|----------|
| 全局模式不可删预设 | 预设指令在全局模式下不可删除 | ✓ |
| 可隐藏预设指令 | 可以隐藏（软删除） | |

**User's choice:** 全局模式不可删预设

### 数据结构
| Option | Description | Selected |
|--------|-------------|----------|
| 统一数据结构 | CommandItem 统一接口 | ✓ |
| 分离数据结构 | 预设和自定义各自独立 | |

**User's choice:** 统一数据结构

### 项目级编辑位置
| Option | Description | Selected |
|--------|-------------|----------|
| 同一编辑体验 | 复用主区域编辑模式 | ✓ |
| 独立设置面板 | 侧边栏抽屉式面板 | |

**User's choice:** 同一编辑体验

---

## Claude's Discretion

- 自定义指令卡片的视觉标记具体样式
- 预设图标列表的具体图标选择
- 编辑模式的进入/退出动效
- "添加指令"占位卡片样式
- 右键菜单的实现方案
- CommandItem 的 id 生成策略
- 持久化数据 schema
- 项目信息区域模式标签布局

## Deferred Ideas

None — discussion stayed within phase scope
