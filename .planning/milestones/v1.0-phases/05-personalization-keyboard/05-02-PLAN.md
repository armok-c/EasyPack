---
phase: 05-personalization-keyboard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - src/components/Sidebar.tsx
  - src/hooks/useProject.ts
  - src/App.tsx
autonomous: true
requirements:
  - PROJ-06

must_haves:
  truths:
    - "用户可在侧边栏项目行左侧看到拖拽手柄(GripVertical)，hover 时显示"
    - "按住拖拽手柄可上下拖动项目调整排序"
    - "拖拽完成后项目顺序立即保存，重启应用后排序保持"
  artifacts:
    - path: "package.json"
      provides: "@dnd-kit/react 0.4.0 依赖"
      contains: "@dnd-kit/react"
    - path: "src/components/Sidebar.tsx"
      provides: "拖拽手柄 + DragDropProvider 排序"
      contains: "DragDropProvider"
    - path: "src/hooks/useProject.ts"
      provides: "reorderProjects 方法"
      contains: "reorderProjects"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "@dnd-kit/react"
      via: "DragDropProvider + useSortable"
      pattern: "useSortable"
    - from: "src/components/Sidebar.tsx"
      to: "src/hooks/useProject.ts"
      via: "onReorderProjects 回调"
      pattern: "onReorderProjects"
---

<objective>
实现侧边栏项目的拖拽排序功能。用户可通过拖拽手柄(GripVertical)上下拖动项目调整排序，排序结果立即持久化。

Purpose: 满足 PROJ-06 需求，让用户自定义项目在侧边栏中的显示顺序。
Output: 安装 @dnd-kit/react，修改 Sidebar.tsx 支持拖拽，修改 useProject.ts 新增 reorderProjects
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-personalization-keyboard/05-CONTEXT.md
@.planning/phases/05-personalization-keyboard/05-RESEARCH.md
@.planning/phases/05-personalization-keyboard/05-UI-SPEC.md
@.planning/phases/05-personalization-keyboard/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 完成后的接口状态 -->

From src/hooks/useProject.ts (Plan 01 扩展后):
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;   // Phase 5: from ICON_OPTIONS keys
  color?: string;  // Phase 5: CSS hex color from COLOR_OPTIONS
}

// 新增方法 (Plan 01):
updateProjectStyle: (projectId: string, style: { icon?: string; color?: string }) => Promise<void>;
```

From src/components/Sidebar.tsx (Plan 01 修改后):
```typescript
interface SidebarProps {
  projects: ProjectItem[];
  selectedId: string | null;
  onAddProject: () => void;
  onSelectProject: (id: string) => void;
  onRemoveProject: (id: string) => void;
  onUpdateStyle: (projectId: string, style: { icon: string; color: string }) => void;
}
```
Sidebar 现在包含: ContextMenu 右键菜单、图标显示、彩色左边框、ProjectSettingsDialog。

From @dnd-kit/react 0.4.0 API (per RESEARCH.md):
```typescript
import { DragDropProvider } from '@dnd-kit/react';
import { useSortable, isSortable } from '@dnd-kit/react/sortable';

// useSortable returns: { ref, handleRef, isDragging }
// isSortable(source) checks if source is a sortable operation
// DragDropProvider: onDragEnd callback receives event with event.operation.source
// sortable source has: { initialIndex, index }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 @dnd-kit/react + useProject reorder 方法 + Sidebar 拖拽集成</name>
  <files>package.json, src/hooks/useProject.ts, src/components/Sidebar.tsx, src/App.tsx</files>
  <read_first>
    - src/hooks/useProject.ts (Plan 01 扩展后的完整代码)
    - src/components/Sidebar.tsx (Plan 01 修改后的完整代码，含右键菜单和图标颜色)
    - src/App.tsx (当前 props 传递模式)
    - .planning/phases/05-personalization-keyboard/05-RESEARCH.md (@dnd-kit/react API 用法示例)
    - .planning/phases/05-personalization-keyboard/05-UI-SPEC.md (拖拽手柄规范)
    - package.json (确认当前依赖列表)
  </read_first>
  <action>
1. 安装 @dnd-kit/react 精确版本:
   ```bash
   cd E:/git/EasyPack && npm install @dnd-kit/react@0.4.0 --save-exact
   ```
   使用精确版本（不带 ^ 前缀），per RESEARCH.md Pitfall 1 建议。

2. 在 `src/hooks/useProject.ts` 中新增 `reorderProjects` 方法:
   ```
   const reorderProjects = useCallback(
     async (reordered: ProjectItem[]) => {
       setProjects(reordered);
       await store?.set(PROJECTS_KEY, reordered);
     },
     [store]
   );
   ```
   - 使用不可变模式：接收已排好序的新数组，直接 setProjects
   - 在 return 对象中导出: `reorderProjects`

3. 修改 `src/components/Sidebar.tsx` 集成拖拽排序:
   - 在 SidebarProps 新增: `onReorderProjects: (reordered: ProjectItem[]) => void`
   - 新增导入:
     ```typescript
     import { DragDropProvider } from '@dnd-kit/react';
     import { useSortable, isSortable } from '@dnd-kit/react/sortable';
     import { GripVertical } from 'lucide-react';
     ```
   - 从 Sidebar 组件中提取项目行为独立组件 `SortableProjectItem`:
     ```typescript
     function SortableProjectItem({
       project,
       isSelected,
       onSelect,
       onRemove,
       onContextMenu,
     }: {
       project: ProjectItem;
       isSelected: boolean;
       onSelect: (id: string) => void;
       onRemove: (id: string) => void;
       onContextMenu: (id: string) => void;
     }) {
       const { ref, handleRef, isDragging } = useSortable({ id: project.id });
       // ... 项目行内容（保留 Plan 01 的图标、颜色边框、右键菜单、X 删除按钮）
     }
     ```
   - SortableProjectItem 内部布局 (per D-07 + UI-SPEC):
     ```
     [GripVertical 手柄 (left)] [Icon (if set)] [Project Name (flex-1)] [X delete (right)]
     ```
     - 拖拽手柄: `ref={handleRef}` 绑定到 GripVertical 的父容器 div
     - 手柄样式 (per D-09): `opacity-0 group-hover:opacity-100 transition-opacity duration-150 cursor-grab`
     - 手柄大小: GripVertical `className="size-3 text-muted-foreground"`
     - 手柄点击区域: 包裹 div `p-0.5 rounded hover:bg-white/10`
     - 拖拽中态: 整个 SortableProjectItem 外层 div 添加 `isDragging ? 'opacity-50' : ''`
   - 项目行外层包裹 DragDropProvider:
     ```typescript
     <DragDropProvider onDragEnd={handleDragEnd}>
       <div className="flex flex-col gap-1">
         {projects.map((project) => (
           <SortableProjectItem key={project.id} ... />
         ))}
       </div>
     </DragDropProvider>
     ```
   - handleDragEnd 回调:
     ```typescript
     const handleDragEnd = useCallback((event: any) => {
       if (event.canceled) return;
       const { source } = event.operation;
       if (isSortable(source)) {
         const { initialIndex, index } = source;
         if (initialIndex !== index) {
           const newProjects = [...projects];
           const [moved] = newProjects.splice(initialIndex, 1);
           newProjects.splice(index, 0, moved);
           onReorderProjects(newProjects);
         }
       }
     }, [projects, onReorderProjects]);
     ```
   - 项目行 div 上保持 ContextMenu 包裹（Plan 01 已有的），拖拽手柄用 handleRef 绑定，两者不冲突因为手柄和 ContextMenu 绑定在不同元素上 (per RESEARCH.md Pitfall 3)

4. 修改 `src/App.tsx`:
   - 从 useProject 解构新增: `reorderProjects`
   - 在 Sidebar 组件调用中新增 prop: `onReorderProjects={reorderProjects}`
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 包含 `"@dnd-kit/react": "0.4.0"` (精确版本，无 ^ 前缀)
    - `node_modules/@dnd-kit/react` 目录存在
    - `src/hooks/useProject.ts` 包含 `reorderProjects`
    - `src/components/Sidebar.tsx` 包含 `DragDropProvider`
    - `src/components/Sidebar.tsx` 包含 `useSortable`
    - `src/components/Sidebar.tsx` 包含 `GripVertical`
    - `src/components/Sidebar.tsx` 包含 `handleRef`
    - `src/components/Sidebar.tsx` 包含 `isDragging`
    - `src/components/Sidebar.tsx` 包含 `opacity-0 group-hover:opacity-100` (手柄 hover 显示)
    - `src/components/Sidebar.tsx` 包含 `onReorderProjects`
    - `src/App.tsx` 包含 `reorderProjects` (解构)
    - `src/App.tsx` 包含 `onReorderProjects` (传递给 Sidebar)
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>拖拽手柄在侧边栏项目行左侧 hover 显示，按住手柄可上下拖动项目，松开后顺序立即保存到 store，重启应用排序保持</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drag interaction -> Store | 拖拽操作产生新的数组顺序写入 store |
| Store -> UI | 应用启动时从 store 恢复项目顺序 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | T (Tampering) | reorderProjects | accept | 排序操作由用户 UI 交互触发，不涉及外部输入。reorder 仅改变数组元素顺序，不修改元素内容 |
| T-05-07 | D (DoS) | Drag handler | accept | 拖拽操作频率受 UI 交互限制，无法程序化高频触发 |
| T-05-08 | S (Spoofing) | Drag source | accept | @dnd-kit/react 库内部验证 drag source 合法性，本项目不暴露拖拽 API 给外部 |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. 现有测试通过: `npx vitest run`
3. 侧边栏项目行左侧 hover 时显示 GripVertical 拖拽手柄
4. 拖拽项目到新位置后松开，项目顺序立即更新
5. 拖拽后项目顺序持久化（可通过 store JSON 文件验证）
</verification>

<success_criteria>
- @dnd-kit/react 0.4.0 已安装（精确版本）
- 拖拽手柄 hover 显示，与 X 删除按钮行为一致
- 拖拽完成后项目顺序立即保存
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/05-personalization-keyboard/05-02-SUMMARY.md`
</output>
