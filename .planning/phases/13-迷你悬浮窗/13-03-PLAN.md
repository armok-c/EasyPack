---
phase: 13-迷你悬浮窗
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - src/hooks/useFloatWindow.ts
  - src/components/FloatApp.tsx
autonomous: true
gap_closure: true
requirements: [FLOAT-01, FLOAT-02, FLOAT-03, FLOAT-04, FLOAT-05, FLOAT-06, FLOAT-07]

must_haves:
  truths:
    - "托盘菜单点击可切换悬浮窗显示/隐藏，不再出现'无法创建悬浮窗'错误"
    - "悬浮窗可独立关闭（X 按钮），关闭后可从 TitleBar 或托盘菜单重新打开"
    - "createFloat 部分失败时已创建的窗口被回滚销毁，不会残留孤儿窗口"
    - "悬浮窗宽度缩小至 220px，高度缩小至约 300px，减少屏幕遮挡"
  artifacts:
    - path: "src/hooks/useFloatWindow.ts"
      provides: "悬浮窗生命周期管理，含窗口 adopt 和 create 回滚逻辑"
      contains: "adoptFloatWindow"
    - path: "src/components/FloatApp.tsx"
      provides: "缩小尺寸的悬浮窗 UI"
      contains: "w-[220px]"
  key_links:
    - from: "src/hooks/useFloatWindow.ts"
      to: "WebviewWindow.getByLabel"
      via: "adopt + toggle 分支判断"
      pattern: "adoptFloatWindow|existing.*!floatWindowRef\\.current"
---

<objective>
修复 Phase 13 UAT 中发现的 3 个问题：托盘菜单切换悬浮窗失败、悬浮窗关闭/重建不稳定、悬浮窗尺寸过大。

Purpose: 三个问题共享同一根因 -- toggleFloat 中条件 `existing && floatWindowRef.current` 过于严格，导致 ref 失效时无法复用已存在的窗口。修复此核心逻辑并缩小窗口尺寸。

Output: 稳定的悬浮窗 toggle/close/reopen 行为 + 更紧凑的窗口尺寸。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-迷你悬浮窗/13-UAT.md
@.planning/phases/13-迷你悬浮窗/13-CONTEXT.md
@.planning/phases/13-迷你悬浮窗/13-01-SUMMARY.md
@.planning/phases/13-迷你悬浮窗/13-02-SUMMARY.md
@src/hooks/useFloatWindow.ts
@src/components/FloatApp.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 useFloatWindow 窗口 adopt 和 create 回滚逻辑</name>
  <files>src/hooks/useFloatWindow.ts</files>

  <read_first>
    - src/hooks/useFloatWindow.ts (当前完整文件，重点：toggleFloat 第 160-193 行、createFloat 第 91-157 行)
  </read_first>

  <action>
    修改 `src/hooks/useFloatWindow.ts`，修复三个核心问题：

    **1. 新增 `adoptFloatWindow` 函数（在 createFloat 之后定义）**

    当 `WebviewWindow.getByLabel("float")` 返回窗口但 `floatWindowRef.current` 为 null 时，重新领养该窗口：
    ```typescript
    async function adoptFloatWindow(win: WebviewWindow): Promise<void> {
      // 重新注册 onCloseRequested 监听器
      const unlistenClose = await win.onCloseRequested(() => {
        floatWindowRef.current = null;
        setFloatVisible(false);
        cleanupListeners();
      });
      unlistenersRef.current.push(unlistenClose);

      // 如果旧监听器不存在，也重新注册 execute 监听器
      const unlistenExecute = await listen<{ command: string }>(
        "float:execute",
        (event) => {
          const { command } = event.payload;
          if (typeof command === "string" && command.length > 0) {
            onExecuteRef.current(command);
          }
        }
      );
      unlistenersRef.current.push(unlistenExecute);

      // 恢复 ref 引用
      floatWindowRef.current = win;
    }
    ```

    **2. 修改 toggleFloat 逻辑（第 160-193 行区域）**

    将原来的条件分支 `if (existing && floatWindowRef.current)` 改为三段逻辑：

    ```typescript
    const toggleFloat = useCallback(async () => {
      const existing = await WebviewWindow.getByLabel("float");

      if (existing && floatWindowRef.current) {
        // 正常 toggle：ref 有效，窗口存在
        if (floatVisible) {
          await existing.hide();
          setFloatVisible(false);
        } else {
          await existing.show();
          await positionFloatTopRight(existing);
          await existing.setFocus();
          setFloatVisible(true);
          await syncState(existing);
        }
        return;
      }

      if (existing && !floatWindowRef.current) {
        // Adopt：窗口存在但 ref 失效（关闭过渡期残留、部分创建失败残留）
        try {
          await adoptFloatWindow(existing);
          if (floatVisible) {
            await existing.hide();
            setFloatVisible(false);
          } else {
            await existing.show();
            await positionFloatTopRight(existing);
            await existing.setFocus();
            setFloatVisible(true);
            await syncState(existing);
          }
          return;
        } catch (err) {
          // adopt 失败，销毁残留窗口后走创建路径
          console.warn("Adopt float window failed, destroying and recreating:", err);
          try { await existing.destroy(); } catch { /* ignore */ }
          // 不 return，继续走创建路径
        }
      }

      // 窗口不存在，首次创建
      if (isCreatingRef.current) return;
      isCreatingRef.current = true;
      try {
        const floatWin = await createFloat();
        floatWindowRef.current = floatWin;
        setFloatVisible(true);
        await syncState(floatWin);
      } catch (err) {
        console.error("Failed to create float window:", err);
        toast.error("无法创建悬浮窗");
      } finally {
        isCreatingRef.current = false;
      }
    }, [floatVisible, createFloat, syncState]);
    ```

    注意：`adoptFloatWindow` 需要访问 `floatWindowRef`、`unlistenersRef`、`setFloatVisible`、`cleanupListeners`、`onExecuteRef`、`syncState`。由于它是在 hook 内部定义的函数（不是 useCallback），直接闭包引用这些即可。但因为它被 `toggleFloat` 引用，需确保定义在 `toggleFloat` 之前。

    **3. 给 createFloat 添加失败回滚**

    在 createFloat 的 `await new Promise(...)` 之后（第 134 行 `await positionFloatTopRight(floatWin)` 之后），如果后续的 `listen("float:execute", ...)` 或 `floatWin.onCloseRequested(...)` 抛出异常，需要销毁已创建的窗口。

    将 createFloat 中第 136-156 行的事件监听器注册包裹在 try-catch 中：

    ```typescript
    // 监听悬浮窗发来的执行请求 + 关闭事件
    try {
      const unlistenExecute = await listen<{ command: string }>(
        "float:execute",
        (event) => {
          const { command } = event.payload;
          if (typeof command === "string" && command.length > 0) {
            onExecuteRef.current(command);
          }
        }
      );
      unlistenersRef.current.push(unlistenExecute);

      const unlistenClose = await floatWin.onCloseRequested(() => {
        floatWindowRef.current = null;
        setFloatVisible(false);
        cleanupListeners();
      });
      unlistenersRef.current.push(unlistenClose);
    } catch (err) {
      // 监听器注册失败 -- 回滚：销毁已创建的窗口
      try { await floatWin.destroy(); } catch { /* ignore */ }
      throw err; // 重新抛出让调用方处理
    }
    ```

    **4. 更新窗口创建尺寸（Gap 3 的一部分）**

    将 createFloat 中的 WebviewWindow 创建参数从：
    ```
    width: 280, minWidth: 280, maxWidth: 280, height: 400,
    ```
    改为：
    ```
    width: 220, minWidth: 220, maxWidth: 220, height: 300,
    ```

    **5. 更新 positionFloatTopRight 中的宽度偏移计算**

    将 `positionFloatTopRight` 函数第 37 行的 `const posX = logicalRight - 280 - 16;` 改为 `const posX = logicalRight - 220 - 16;`
  </action>

  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - 文件中包含 `adoptFloatWindow` 函数定义
    - `positionFloatTopRight` 中使用 `220` 而非 `280` 计算 posX
    - createFloat 中 WebviewWindow 参数 `width: 220`, `height: 300`
    - createFloat 中包含 try-catch 包裹事件监听器注册，catch 中有 `floatWin.destroy()`
    - toggleFloat 中包含 `existing && !floatWindowRef.current` 分支（adopt 路径）
    - TypeScript 编译零错误
  </acceptance_criteria>

  <done>
    - toggleFloat 在窗口存在但 ref 失效时能 adopt 窗口而非尝试创建新窗口
    - createFloat 在监听器注册失败时回滚销毁已创建的窗口
    - 窗口默认尺寸缩小为 220x300
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 缩小 FloatApp 组件尺寸</name>
  <files>src/components/FloatApp.tsx</files>

  <read_first>
    - src/components/FloatApp.tsx (当前完整文件，重点：第 62 行 className、第 111 行 IconComponent size)
  </read_first>

  <action>
    修改 `src/components/FloatApp.tsx` 中的尺寸样式，与 Task 1 的窗口尺寸变更匹配：

    **1. 修改根容器 className（第 62 行）**

    将 `w-[280px] h-auto max-h-[600px]` 改为 `w-[220px] h-auto max-h-[400px]`

    **2. 修改项目名 span 的 max-width（第 71 行）**

    将 `max-w-[200px]` 改为 `max-w-[160px]`（因为窗口变窄了 60px，项目名截断宽度相应缩小）

    **3. (可选微调) 修改指令行高度**

    将第 102 行的 `h-8` 保持不变（32px 行高在 220px 宽度下仍然合适）
  </action>

  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit 2>&1 | head -30 && npx vite build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - FloatApp.tsx 根容器 className 包含 `w-[220px]`（不包含 `w-[280px]`）
    - FloatApp.tsx 根容器 className 包含 `max-h-[400px]`（不包含 `max-h-[600px]`）
    - 项目名 span 的 max-w 为 `max-w-[160px]`
    - TypeScript 编译零错误
    - Vite 构建成功，输出包含 dist/float.html
  </acceptance_criteria>

  <done>
    - 悬浮窗 UI 宽度从 280px 缩小至 220px，最大高度从 600px 缩小至 400px
    - 项目名截断宽度相应调整
    - 构建通过
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` 零错误
2. `npx vite build` 成功，输出包含 `dist/float.html`
3. grep 验证：
   - `grep -n "adoptFloatWindow" src/hooks/useFloatWindow.ts` 存在
   - `grep -n "width: 220" src/hooks/useFloatWindow.ts` 存在
   - `grep -n "w-\[220px\]" src/components/FloatApp.tsx` 存在
   - `grep -c "280" src/hooks/useFloatWindow.ts` 返回 0（旧值已全部替换）
   - `grep -c "w-\[280px\]" src/components/FloatApp.tsx` 返回 0
</verification>

<success_criteria>
- 托盘菜单点击"打开悬浮窗/关闭悬浮窗"可正常切换，不再报错"无法创建悬浮窗"
- 悬浮窗 X 按钮可关闭，关闭后可从 TitleBar 按钮或托盘菜单重新打开
- createFloat 失败时不会残留孤儿窗口
- 悬浮窗宽度约 220px、高度约 300px，比之前更紧凑
- TypeScript 编译和 Vite 构建均通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-迷你悬浮窗/13-03-SUMMARY.md`
</output>
