---
phase: 23-env-tabs-management
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - src/components/MainArea.tsx
  - src/App.tsx
  - src/components/EnvTabBar.tsx (new)
  - src/components/EnvSwitchBar.tsx (new)
  - src/components/ManageEnvDialog.tsx (new)
autonomous: false
requirements:
  - ENV-01
  - ENV-02
  - ENV-08

must_haves:
  truths:
    - "项目环境内页顶部横向标签页可左右滚动切换浏览不同环境的文件"
    - "点击标签页切换当前浏览的环境，下方文件列表随之更新（指令卡片不变）"
    - "零环境时显示引导空状态（「暂无环境」+ 管理环境按钮）"
    - "已应用环境在对应标签页右上角显示绿色圆点"
    - "「管理环境」按钮固定在标签页行右侧，始终可见"
    - "管理环境模态窗支持新增环境（名称输入），列表显示名称/创建时间/修改时间/操作"
    - "重命名环境功能正常（弹出输入框）"
    - "删除环境有二次确认弹窗（AlertDialog），禁止删除已应用的环境"
    - "环境切换栏：下拉框选择目标环境 + 启用按钮"
    - "选择的环境与已应用环境相同时，启用按钮禁用"
    - "点击启用将所选环境的配置文件内容写入项目根目录，toast 反馈结果"
    - "文件列表区域为占位区域（提示文件管理功能将在后续版本中提供）"
  artifacts:
    - path: "src/components/EnvTabBar.tsx"
      provides: "环境标签页栏 + 管理环境按钮 + 引导空状态 + 文件占位区"
      exports: ["EnvTabBar"]
    - path: "src/components/EnvSwitchBar.tsx"
      provides: "环境切换下拉框 + 启用按钮"
      exports: ["EnvSwitchBar"]
    - path: "src/components/ManageEnvDialog.tsx"
      provides: "管理环境模态窗 — 新增/列表/重命名/删除"
      exports: ["ManageEnvDialog"]
    - path: "src/components/MainArea.tsx"
      provides: "环境组件集成到 MainArea 布局"
    - path: "src/App.tsx"
      provides: "env 状态推导和包装处理器传递给 MainArea"
  key_links:
    - from: "MainArea.tsx"
      to: "EnvTabBar.tsx"
      via: "import + JSX usage"
      pattern: "import.*EnvTabBar"
    - from: "MainArea.tsx"
      to: "EnvSwitchBar.tsx"
      via: "import + JSX usage"
      pattern: "import.*EnvSwitchBar"
    - from: "MainArea.tsx"
      to: "ManageEnvDialog.tsx"
      via: "import + JSX usage"
      pattern: "import.*ManageEnvDialog"
    - from: "App.tsx"
      to: "useProject"
      via: "env state destructuring + wrapper handlers"
      pattern: "projectEnvsMap|createEnv|renameEnv|deleteEnv|setActiveEnv|applyEnv"
---
<objective>
创建环境标签页、管理环境模态窗和环境切换栏 UI 组件并集成到应用。

**Purpose:** 实现 ENV-01（标签页切换浏览）、ENV-02（管理环境模态窗 CRUD）和 ENV-08（环境切换栏）。这是 Phase 23 的核心 UI 交付，用户可以通过标签页浏览不同环境的文件，管理环境的增删改名，以及通过切换栏将环境配置写入项目目录。

**Output:**
1. `src/components/EnvTabBar.tsx` — 标签页栏 + 管理环境按钮 + 引导空状态 + 文件占位区
2. `src/components/EnvSwitchBar.tsx` — 下拉框 + 启用按钮
3. `src/components/ManageEnvDialog.tsx` — 管理环境模态窗
4. `src/components/MainArea.tsx` — 环境组件集成到 MainArea 布局
5. `src/App.tsx` — env 状态提取、推导和包装处理器
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-env-tabs-management/23-CONTEXT.md (D-14~D-30, 布局顺序 D-23，标签页交互, 管理模态窗, 切换栏)
@.planning/phases/23-env-tabs-management/23-01-PLAN.md (Environment 和 ManagedFile 类型，用于组件 import)
@.planning/phases/23-env-tabs-management/23-02-PLAN.md (useProject 中 env 方法的签名和返回)

<interfaces>
From src/components/MainArea.tsx (existing — line 11-32):
```typescript
interface MainAreaProps {
  currentProject: ProjectItem | null;
  onExecute: (command: string, cmd?: CommandItem) => void;
  commands: CommandItem[];
  editMode: boolean;
  setEditMode: (editMode: boolean) => void;
  addCommand: (name: string, command: string, icon?: string, extra?: {...}) => Promise<void>;
  updateCommand: (id: string, data: {...}) => Promise<void>;
  deleteCommand: (id: string) => Promise<void>;
  enableProjectCommands: () => Promise<void>;
  activeZone: "sidebar" | "main";
  onZoneSwitch: () => void;
  projectInfo: { size: string; branch: string | null } | null;
  projectInfoLoading: boolean;
  projectInfoError: boolean;
  onOpenFolder: () => void;
}
```

From src/App.tsx (existing — uses useProject() and destructures, passes to MainArea):
```typescript
const {
  projects, selectedId, currentProject, selectFolder, selectProject,
  removeProject, executeCommand, executeScriptCommand,
  commands, editMode, setEditMode, addCommand, updateCommand, deleteCommand,
  enableProjectCommands, updateProjectStyle, reorderProjects,
  projectInfo, projectInfoLoading, projectInfoError, openFolder,
  shortcutBindings, setShortcutBinding, clearShortcutBinding, resetAllShortcuts,
  store, profileMetas, activeProfileId, profileSwitching,
  switchProfile, createProfile, deleteProfile, renameProfile,
  importProfile, exportProfile, mainStore,
} = useProject();
```

From useProject.ts (new env return values — added by Plan 02):
```typescript
projectEnvsMap,           // Record<string, Environment[]>
projectActiveEnvMap,      // Record<string, string>
createEnv,                // (projectId: string, name: string) => Promise<string | null>
renameEnv,                // (projectId: string, envId: string, newName: string) => Promise<void>
deleteEnv,                // (projectId: string, envId: string) => Promise<void>
setActiveEnv,             // (projectId: string, envId: string | null) => Promise<void>
applyEnv,                 // (projectId: string, envId: string) => Promise<boolean>
getProjectEnvs,           // (projectId: string) => Environment[]
getProjectActiveEnv,      // (projectId: string) => string | null
```

From types.ts (added by Plan 01):
```typescript
export interface Environment {
  id: string;
  name: string;
  createdAt: number;
  updatedAt: number;
  files: ManagedFile[];
}
export interface ManagedFile {
  name: string;
  content: string;
  addedAt: number;
}
```
</interfaces>

<dependency_context>
Plan 01 在 src/lib/types.ts 中定义了 Environment 和 ManagedFile 类型。
Plan 02 在 src/hooks/useProject.ts 中实现了 env 状态管理、CRUD、持久化和生命周期集成。
Plan 03 负责纯 UI 层：创建 3 个新组件并将它们集成到 MainArea 和 App.tsx。
</dependency_context>
</context>

<tasks>

<task type="auto" checkpoint_type="human-verify">
  <name>Task 1: Create EnvTabBar and EnvSwitchBar components</name>
  <files>src/components/EnvTabBar.tsx (new), src/components/EnvSwitchBar.tsx (new)</files>
  <read_first>src/components/MainArea.tsx (line 162-229 — 现有布局结构，了解 empty state 风格、项目信息区域样式、flex/spacing 模式)</read_first>
  <action>
    Create two new components for the environment UI.

    ---

    **Component 1: `src/components/EnvTabBar.tsx`**

    Props interface:
    ```typescript
    interface EnvTabBarProps {
      envs: Environment[];
      selectedEnvId: string | null;      // current browsing tab
      activeEnvId: string | null;         // applied env (green dot)
      onSelectEnv: (envId: string) => void;
      onManageEnv: () => void;
    }
    ```

    Implementation:
    - Flex row container with `overflow-x: auto` + `scrollbar-none` class (per D-15, discretion on scrollbar hiding)
    - Map over `envs` to render tab buttons:
      - Each tab: `shrink-0` inline-flex button with relative positioning
      - Tab text = env name
      - Selected tab: visual highlight (bg + text color, matches existing shadcn tab style)
      - If `env.id === activeEnvId` (NOT selectedEnvId — these are independent per D-14): show a green dot (absolute positioned `size-2 rounded-full bg-green-500` in top-right corner, per D-14/discretion)
      - onClick calls `onSelectEnv(env.id)`
    - "管理环境" button (per D-16): `shrink-0` + `sticky right-0`, positioned after tabs, calls `onManageEnv` on click. Use shadcn Button variant="outline" size="sm" with text "管理环境"
    - **Zero env state** (per D-17): When `envs.length === 0`, render a centered empty state:
      - Text: "暂无环境"
      - A "管理环境" button that calls `onManageEnv`
      - Follow the same centered layout pattern as MainArea's empty state (line 164-172: `flex flex-col items-center justify-center`, muted text, etc.)
    - **File placeholder area** (per D-19): Below the tab row (or within the same component), render a placeholder block:
      - A bordered dashed area with centered text: "文件管理功能将在后续版本中提供"
      - Match the style of the "添加指令" placeholder card (dashed border, muted colors)
      - This will be replaced by the real file list in Phase 24
    - Use shadcn Button component (already imported in MainArea)

    **Important:** When envs.length === 0, the zero-env empty state replaces the entire tab bar area. When envs.length > 0, show the tab bar + file placeholder. The file placeholder is always visible when envs.length > 0 (even if no env is selected, it just shows the placeholder).

    ---

    **Component 2: `src/components/EnvSwitchBar.tsx`**

    Props interface:
    ```typescript
    interface EnvSwitchBarProps {
      envs: Environment[];
      activeEnvId: string | null;   // currently applied env
      onApply: (envId: string) => void;
      applying: boolean;             // loading state during apply
    }
    ```

    Implementation:
    - A horizontal flex row with items-center gap-2
    - shadcn Select component (per D-24):
      - Options: each env's name, value = env.id
      - Default value: `activeEnvId` (shows applied env name) (per D-24)
      - If activeEnvId is null: show placeholder text "选择环境" (per D-24)
      - onValueChange: store selected env id in local state
    - shadcn Button component:
      - Text: "启用"
      - Disabled when: (per D-25) selected env === activeEnvId, OR no env selected, OR `envs.length === 0`
      - onClick: calls `onApply(selectedEnvId)`
      - Show loading state when `applying === true`
    - When envs.length === 0: the entire bar is still visible but both Select and Button are disabled, showing "暂无可用环境"

    Use local state `const [selectedId, setSelectedId] = useState<string | null>(null)` for the dropdown selection (independent of applied env).
    Reset `selectedId` when `envs` array identity changes (useEffect watching envs).
  </action>
  <verify>
    手动检查（创建后在浏览器/Build 中验证）:
    1. 标签页可横向滚动，管理按钮 sticky 右侧
    2. 零环境时显示引导空状态
    3. 已应用环境标签页右上角有绿色小圆点
    4. 切换栏下拉框可选环境，选择已应用环境时按钮禁用
  </verify>
  <acceptance_criteria>
    - EnvTabBar 组件导出，支持 envs/selectedEnvId/activeEnvId/onSelectEnv/onManageEnv props
    - 标签页横向 flex + overflow-x: auto 滚动
    - 管理环境按钮 sticky right-0
    - 零环境时显示引导空状态
    - 绿色圆点出现在 activeEnvId 对应标签页（独立于 selectedEnvId）
    - 文件占位区显示提示文字
    - EnvSwitchBar 组件导出，支持 envs/activeEnvId/onApply/applying props
    - Select 组件默认显示 activeEnvId 或占位文字
    - 选择相同环境时启用按钮 disabled
    - 正在 apply 时显示 loading 状态
  </acceptance_criteria>
</task>

<task type="auto" checkpoint_type="human-verify">
  <name>Task 2: Create ManageEnvDialog component</name>
  <files>src/components/ManageEnvDialog.tsx (new)</files>
  <read_first>src/hooks/useProject.ts (createEnv, renameEnv, deleteEnv signatures — 了解方法参数和返回)</read_first>
  <action>
    Create ManageEnvDialog component — a modal dialog for managing environments (per ENV-02).

    Props interface:
    ```typescript
    interface ManageEnvDialogProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      envs: Environment[];
      activeEnvId: string | null;
      onCreateEnv: (name: string) => Promise<string | null>;
      onRenameEnv: (envId: string, newName: string) => Promise<void>;
      onDeleteEnv: (envId: string) => Promise<void>;
    }
    ```

    Implementation:

    **Dialog structure:**
    - Use shadcn/ui Dialog (already installed)
    - Title: "管理环境"
    - Body: vertically stacked layout

    **Top section — Create environment:**
    - A horizontal row with Input + Button
    - Input placeholder: "环境名称"
    - Button text: "新增"
    - On click: validate name not empty, check uniqueness against existing envs (per D-20), call `onCreateEnv(name)`, clear input on success
    - If name already exists: toast.error("环境名称已存在") (D-20)
    - If user input is empty/whitespace: toast.error("请输入环境名称")

    **Bottom section — Environment list:**
    - shadcn/ui Table (already installed)
    - Four columns (per D-02 spec):
      1. 名称 — env.name + if env.id === activeEnvId, show a small green dot badge "已应用"
      2. 创建时间 — format `env.createdAt` timestamp to locale string (`new Date(env.createdAt).toLocaleString()`)
      3. 修改时间 — format `env.updatedAt` timestamp to locale string
      4. 操作 — Two buttons:
        - 重命名 button: opens rename UI (inline input or prompt dialog)
        - 删除 button: opens AlertDialog for confirmation (D-21, D-22)

    **Rename flow:**
    - Click "重命名": show an inline edit or a small Dialog/AlertDialog with Input
    - Pre-fill with current env name
    - On confirm: validate new name (not empty, unique), call `onRenameEnv(envId, newName)`
    - On success: toast.success("已重命名为: [name]"), close rename UI

    **Delete flow** (per D-21, D-22):
    - Click "删除": open shadcn AlertDialog
    - AlertDialog title: "确认删除环境?"
    - AlertDialog description: "环境「[name]」将被永久删除，其管理的文件副本将一并移除。此操作不可撤销。"
    - If `env.id === activeEnvId` (per D-21): show additional warning text in AlertDialog description: "该环境当前已应用，请先切换到其他环境后再删除。" and disable the confirm button (or add a separate note explaining why it can't be deleted).
    - Confirm button text: "删除", variant: "destructive"
    - Cancel button text: "取消"
    - On confirm: call `onDeleteEnv(envId)`, toast.success("已删除环境: [name]"), close AlertDialog

    **Dialog sizing:**
    - Use shadcn DialogContent with className that fits a table: `sm:max-w-[600px]`
    - Scroll area inside dialog if table is tall

    Use local state for:
    - Create env input text useState
    - Rename state: which env is being renamed + new name text
    - Delete state: Dialog and AlertDialog open/close
  </action>
  <verify>
    手动检查:
    1. 打开管理环境模态窗，显示正确列表
    2. 新建环境：填入名称点击新增，列表更新
    3. 重命名环境：点击重命名，输入新名称，确认后列表更新
    4. 删除环境（非已应用）：弹出 AlertDialog，确认后删除
    5. 删除环境（已应用）：弹出 AlertDialog，显示额外警告，确认按钮禁用
    6. 重名检测：新建/重命名时输入已存在的名称，toast 提示
  </verify>
  <acceptance_criteria>
    - ManageEnvDialog 组件导出
    - Dialog 内顶部 Input + 新增按钮
    - Table 四列：名称、创建时间、修改时间、操作
    - 重命名功能（点击弹出输入框，保存后更新）
    - 删除使用 shadcn AlertDialog 二次确认
    - 已应用环境不可删除（AlertDialog 中禁用确认按钮 + 显示额外警告）
    - 新建/重命名时检测重名
    - 已应用环境在名称列显示绿色小圆点标记
    - 使用 sonner toast 反馈操作结果
  </acceptance_criteria>
</task>

<task type="auto" checkpoint_type="human-verify">
  <name>Task 3: Wire env components into MainArea and App.tsx</name>
  <files>src/components/MainArea.tsx, src/App.tsx</files>
  <read_first>src/components/MainArea.tsx (full file, focus on layout structure line 175-303 and props interface line 11-32), src/App.tsx (full file, focus on useProject destructuring line 33-78 and MainArea JSX line 501-517)</read_first>
  <action>
    Wire the three new env components into MainArea and App.tsx.

    **Part A: MainAreaProps — Add env prop types**

    In `src/components/MainArea.tsx`, add to the `MainAreaProps` interface (after line 31):
    ```typescript
    // Phase 23: Environment management
    envs: Environment[];
    activeEnvId: string | null;
    onCreateEnv: (name: string) => Promise<string | null>;
    onRenameEnv: (envId: string, newName: string) => Promise<void>;
    onDeleteEnv: (envId: string) => Promise<void>;
    onApplyEnv: (envId: string) => Promise<boolean>;
    ```

    Add `Environment` to the type import line:
    ```typescript
    import type { CommandItem, Environment } from "@/lib/types";
    ```

    **Part B: MainArea layout — Integrate env components**

    In the return JSX, replace the "项目环境" span section (currently lines 216-228) with the new env layout (per D-23):

    Current code (lines 215-229):
    ```tsx
    {/* 项目环境标签 + 打开文件夹 button row */}
    <div className="flex items-center justify-between mt-2">
      <span className="text-xs text-muted-foreground">项目环境</span>
      <Button ...>
        <FolderOpen className="size-3.5" />
        打开文件夹
      </Button>
    </div>
    ```

    New code — replace with:
    ```tsx
    {/* Phase 23: 环境切换栏（ENV-08）- 项目信息下方，标签页上方 */}
    {currentProject && (
      <EnvSwitchBar
        envs={envs}
        activeEnvId={activeEnvId}
        onApply={onApplyEnv}
        applying={applyingEnv}
      />
    )}

    {/* Phase 23: 环境标签页 + 管理按钮（ENV-01/ENV-02） */}
    {currentProject && (
      <EnvTabBar
        envs={envs}
        selectedEnvId={selectedEnvId}
        activeEnvId={activeEnvId}
        onSelectEnv={setSelectedEnvId}
        onManageEnv={() => setManageEnvOpen(true)}
      />
    )}

    {/* 打开文件夹 button row (moved from old "项目环境" position) */}
    <div className="flex items-center justify-between mt-2">
      <span className="text-xs text-muted-foreground">项目环境</span>
      <Button ...>
        <FolderOpen className="size-3.5" />
        打开文件夹
      </Button>
    </div>
    ```

    Wait — the "项目环境" label + "打开文件夹" button should remain but the env components are their own rows. Let me reconsider the layout exactly per D-23:

    The layout per D-23 is:
    ```
    项目信息（title, path, size, branch）→
    环境切换栏（ENV-08）→
    标签页行+管理按钮（ENV-01/ENV-02）→
    文件占位区 →
    指令卡片
    ```

    But the "项目环境" label + "打开文件夹" button row was at line 215-228 — this was from Phase 22 when "项目环境" was just a label replacing the old toggle group. Now with Phase 23, the env tabs and switch bar replace the label.

    So the new MainArea layout section should be:

    ```tsx
    {/* Phase 23: 环境切换栏 — 项目信息下方 */}
    {currentProject && (
      <EnvSwitchBar
        envs={envs}
        activeEnvId={activeEnvId}
        onApply={onApplyEnv}
        applying={applyingEnv}
      />
    )}

    {/* Phase 23: 环境标签页行 + 管理按钮 */}
    {currentProject && (
      <EnvTabBar
        envs={envs}
        selectedEnvId={selectedEnvId}
        activeEnvId={activeEnvId}
        onSelectEnv={setSelectedEnvId}
        onManageEnv={() => setManageEnvOpen(true)}
      />
    )}

    {/* 打开文件夹 button — 保持原有位置 */}
    <div className="flex items-center justify-between mt-2">
      <span className="text-xs text-muted-foreground">项目环境</span>
      <Button
        variant="outline"
        size="sm"
        onClick={onOpenFolder}
        disabled={!currentProject}
        aria-label="打开项目文件夹"
      >
        <FolderOpen className="size-3.5" />
        打开文件夹
      </Button>
    </div>
    ```

    Change the `mb-6` on the project info container to `mb-4` to tighten spacing with the new env rows.

    **Part C: MainArea internal state — Add env state + ManageEnvDialog**

    Add internal state declarations (after existing state at line 54-58):
    ```typescript
    // Phase 23: Selected env for browsing (independent from active/applied env per D-14)
    const [selectedEnvId, setSelectedEnvId] = useState<string | null>(null);
    const [manageEnvOpen, setManageEnvOpen] = useState(false);
    const [applyingEnv, setApplyingEnv] = useState(false);
    ```

    Add "管理环境" before the command cards section (before line 231), render ManageEnvDialog:
    ```tsx
    {/* Phase 23: 管理环境模态窗 */}
    {currentProject && (
      <ManageEnvDialog
        open={manageEnvOpen}
        onOpenChange={setManageEnvOpen}
        envs={envs}
        activeEnvId={activeEnvId}
        onCreateEnv={onCreateEnv}
        onRenameEnv={onRenameEnv}
        onDeleteEnv={async (envId) => {
          await onDeleteEnv(envId);
          // If deleted env was the selected browsing tab, auto-switch (per D-18)
          setSelectedEnvId((prev) => prev === envId ? null : prev);
        }}
      />
    )}
    ```

    Wait, D-18 says "删除当前浏览的环境后，自动切换到最近邻环境（右侧优先，无右侧则左侧）。无剩余环境则回到引导空状态". This is better handled as an effect or in the delete handler. Let me wrap the onDeleteEnv to handle tab switching:

    ```tsx
    const handleDeleteEnv = useCallback(async (envId: string) => {
      await onDeleteEnv(envId);
      // D-18: auto-switch selected tab
      setSelectedEnvId((prev) => {
        if (prev !== envId) return prev; // wasn't selected, no change
        const remaining = envs.filter((e) => e.id !== envId);
        if (remaining.length === 0) return null;
        const deletedIdx = envs.findIndex((e) => e.id === envId);
        // right neighbor first, then left neighbor
        const nextIdx = Math.min(deletedIdx, remaining.length - 1);
        return remaining[nextIdx].id;
      });
    }, [envs, onDeleteEnv]);
    ```

    **Part D: App.tsx — Extract env state and create wrapper handlers**

    Add to destructuring from useProject() (after existing destructuring):
    ```typescript
    // Phase 23: Environment management
    projectEnvsMap,
    projectActiveEnvMap,
    createEnv,
    renameEnv,
    deleteEnv,
    setActiveEnv,
    applyEnv,
    getProjectEnvs,
    getProjectActiveEnv,
    ```

    Add derived values (after existing useMemo for `commands`):
    ```typescript
    // Phase 23: Derived env state for current project
    const envList = useMemo(
      () => selectedId ? getProjectEnvs(selectedId) : [],
      [selectedId, getProjectEnvs, projectEnvsMap]
    );
    const activeEnvId = useMemo(
      () => selectedId ? getProjectActiveEnv(selectedId) : null,
      [selectedId, getProjectActiveEnv, projectActiveEnvMap]
    );
    ```

    Add wrapper handlers (before the return statement, after handleOpenFolder):
    ```typescript
    // Phase 23: Environment wrapper handlers
    const handleCreateEnv = useCallback(
      async (name: string) => selectedId ? createEnv(selectedId, name) : null,
      [selectedId, createEnv]
    );
    const handleRenameEnv = useCallback(
      async (envId: string, newName: string) => {
        if (selectedId) await renameEnv(selectedId, envId, newName);
      },
      [selectedId, renameEnv]
    );
    const handleDeleteEnv = useCallback(
      async (envId: string) => {
        if (selectedId) await deleteEnv(selectedId, envId);
      },
      [selectedId, deleteEnv]
    );
    const handleApplyEnv = useCallback(
      async (envId: string) => selectedId ? applyEnv(selectedId, envId) : false,
      [selectedId, applyEnv]
    );
    ```

    Pass env props to MainArea:
    ```tsx
    <MainArea
      ...existing props...
      // Phase 23: Environment
      envs={envList}
      activeEnvId={activeEnvId}
      onCreateEnv={handleCreateEnv}
      onRenameEnv={handleRenameEnv}
      onDeleteEnv={handleDeleteEnv}
      onApplyEnv={handleApplyEnv}
    />
    ```

    **Part E: Default selected env tab** (per D-14: first time load, auto-select the first env):
    In MainArea, add useEffect:
    ```typescript
    // Phase 23: Auto-select first env when envs change and no env is selected
    useEffect(() => {
      if (envs.length > 0 && selectedEnvId === null) {
        setSelectedEnvId(envs[0].id);
      }
      // When envs becomes empty (e.g., last env deleted), clear selection
      if (envs.length === 0) {
        setSelectedEnvId(null);
      }
    }, [envs, selectedEnvId]);
    ```
  </action>
  <verify>
    手动检查和构建验证:
    1. `npx tsc --noEmit` — TypeScript 编译通过
    2. 启动应用，选中项目后查看 env 各组件是否正确显示
    3. 布局顺序：项目信息 → 环境切换栏 → 标签页行 → 打开文件夹 → 指令卡片
    4. 标签页切换改变 selectedEnvId，文件占位区保持不变
    5. 管理环境按钮打开 ManageEnvDialog
    6. 环境切换栏 Select 和启用按钮正常工作
  </verify>
  <acceptance_criteria>
    - MainAreaProps 包含 5 个新的 env prop
    - 布局顺序符合 D-23：项目信息 → 环境切换栏 → 标签页 → 打开文件夹 → 卡片
    - selectedEnvId 独立于 activeEnvId（标签页切换不影响已应用状态）
    - 管理环境对话框通过"管理环境"按钮打开
    - EnvSwitchBar 和 EnvTabBar 在无项目时隐藏
    - 默认选中第一个 env 的标签页
    - 删除环境后自动切换到最近邻环境
    - App.tsx 中正确推导 envList 和 activeEnvId
    - App.tsx 中正确创建 4 个包装处理器
    - TypeScript 编译通过
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI components → useProject methods | 用户操作 UI 触发 env CRUD 和 apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | D | ManageEnvDialog delete | mitigate | 已应用环境禁止删除（D-21）— AlertDialog 中禁用确认按钮并显示额外警告。仅在 env.id !== activeEnvId 时允许确认操作 |
| T-23-08 | D | EnvSwitchBar apply | mitigate | 选择与已应用环境相同时禁用启用按钮（D-25），防止无操作触发 apply |
| T-23-09 | S | EnvSwitchBar Select placeholder | accept | 占位文字"选择环境"无安全风险 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 编译无错误
2. 应用启动后选中项目 — env 各组件正确渲染
3. 布局顺序：项目信息 → 环境切换栏 → 标签页行+管理按钮 → 打开文件夹行 → 指令卡片网格
4. 管理环境模态窗：创建/重命名/删除/重名检测功能正常
5. 删除已应用环境被阻止（AlertDialog 中禁用）
6. 环境切换栏：选择环境 → 点击启用 → toast 反馈
7. 标签页选中态 + 绿色圆点独立显示
</verification>

<success_criteria>
- [ ] EnvTabBar: 横向滚动标签页、管理按钮 sticky、零环境空状态、绿色圆点、文件占位区
- [ ] EnvSwitchBar: Select 下拉框 + 启用按钮、相同环境禁用、loading 状态
- [ ] ManageEnvDialog: 新增/重命名/删除环境、重名检测、AlertDialog 二次确认、已应用环境保护
- [ ] MainArea 布局按 D-23 顺序排列
- [ ] App.tsx 推导 envList/activeEnvId 并传递包装处理器
- [ ] TypeScript 编译通过
- [ ] 向后兼容：旧项目（无环境数据）在 env section 显示零环境空状态
</success_criteria>

<output>
After completion, create `.planning/phases/23-env-tabs-management/23-03-SUMMARY.md`
</output>
