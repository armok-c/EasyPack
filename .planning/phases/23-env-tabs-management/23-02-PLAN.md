---
phase: 23-env-tabs-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useProject.ts
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-08

must_haves:
  truths:
    - "环境数据存储在 profileStore 中，切换 profile 时环境数据随之切换"
    - "环境数据在应用启动时通过 loadProfileDataIntoState 一次性预加载"
    - "删除项目时同步清理对应的环境存储键和内存 state"
    - "Profile 导出时包含 projectEnvs 和 projectActiveEnvs 数据"
    - "Profile 导入时正确恢复 projectEnvs 和 projectActiveEnvs 数据"
    - "创建/重命名/删除环境的方法暴露给 UI 层使用"
    - "应用环境（写入文件到项目目录）的方法暴露给 UI 层使用"
    - "向后兼容：现有用户无 env 数据时静默过渡，环境 state 为空对象"
  artifacts:
    - path: "src/hooks/useProject.ts"
      provides: "环境 CRUD + 应用 + 持久化 + 加载 + 导出/导入 + 清理"
      contains: "projectEnvsMap, projectActiveEnvMap, env persistence keys, env CRUD methods"
  key_links:
    - from: "useProject.ts state"
      to: "profileStore"
      via: "profileStore.set + profileStore.save"
      pattern: "profileStore\\.set\\(\"projectEnvs:"
    - from: "loadProfileDataIntoState"
      to: "profileStore keys()"
      via: "keys().filter(k.startsWith('projectEnvs:'))"
      pattern: "projectEnvs:"
    - from: "removeProject"
      to: "profileStore delete"
      via: "profileStore.delete(envStorageKey)"
      pattern: "projectEnvs:"
---
<objective>
在 useProject hook 中添加环境状态管理、CRUD 方法和持久化集成。

**Purpose:** 提供 UI 层所需的所有环境数据操作能力。遵循 projectCommands 的现有模式（per-project key、不可变 state、profileStore 持久化），确保环境数据在启动加载、profile 切换、导出导入和项目删除中的正确性。

**Output:**
1. `src/hooks/useProject.ts` — 新增 state、CRUD 方法、持久化键、启动加载集成、删除清理、导出导入字段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-env-tabs-management/23-CONTEXT.md (D-01~D-06, D-11, D-14, D-18, D-20~D-30)
@.planning/phases/22-global-command-removal/22-02-SUMMARY.md (useProject 重构模式 — 命令 CRUD 可参考作为 env CRUD 的实现模板)

<interfaces>
在 `src/hooks/useProject.ts` 中，现有 projectCommands 管理的模式：

持久化键模式:
```typescript
function projectCommandsKey(projectId: string): string {
  return `projectCommands:${projectId}`;
}
```

state 声明模式:
```typescript
const [projectCommandsMap, setProjectCommandsMap] = useState<Record<string, CommandItem[]>>({});
```

CRUD 调用模式（addCommand 为例）:
```typescript
const updated = [...current, newItem];
setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: updated }));
await profileStore?.set(projectCommandsKey(selectedId), updated);
await profileStore?.save();
```

数据加载模式（loadProfileDataIntoState 中）:
```typescript
const allKeys = await (s as unknown as { keys: () => Promise<string[]> }).keys();
const projectCmdEntries = await Promise.all(
  allKeys.filter((k) => k.startsWith("projectCommands:"))
    .map(async (k) => {
      const projectId = k.replace("projectCommands:", "");
      const cmds = await s.get<CommandItem[]>(k);
      return [projectId, cmds ?? []] as const;
    })
);
const map = Object.fromEntries(projectCmdEntries);
setProjectCommandsMap(map);
```

清理模式（removeProject 中）:
```typescript
await profileStore?.delete(projectCommandsKey(id));
setProjectCommandsMap((prev) => {
  const next = { ...prev };
  delete next[id];
  return next;
});
await profileStore?.save();
```

导出模式（exportProfile 中）:
```typescript
const projectCommands: Record<string, unknown> = {};
for (const key of allKeys) {
  if (key.startsWith("projectCommands:")) {
    const val = await profileStore.get(key);
    projectCommands[key.replace("projectCommands:", "")] = val;
  }
}
// ... then in exportData.data: projectCommands,
```

导入模式（importProfile 中）:
```typescript
if (data.projectCommands && typeof data.projectCommands === "object") {
  for (const [projectId, cmds] of Object.entries(data.projectCommands)) {
    await ps.set(projectCommandsKey(projectId), cmds);
  }
}
```

Env 类型（来自 types.ts — 由 Plan 01 创建）:
```typescript
export interface Environment {
  id: string;
  name: string;
  createdAt: number;
  updatedAt: number;
  files: ManagedFile[];
}
export interface ManagedFile {
  name: string;
  content: string;
  addedAt: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add env state declarations and persistence key helpers</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>src/hooks/useProject.ts (full file, focus on state declarations ~line 57-81, key functions ~line 41-55)</read_first>
  <action>
    Add environment-related state and key helpers to useProject.ts, following the exact same patterns as projectCommands.

    1. Add import for `Environment` from `@/lib/types` (add to existing import on line 7):
       ```typescript
       import type { CommandItem, Environment, ProfileMeta, ProfileExportData } from "@/lib/types";
       ```

    2. Add storage key functions (after `projectCommandsKey` around line 41-43):
       ```typescript
       function projectEnvsKey(projectId: string): string {
         return `projectEnvs:${projectId}`;
       }
       function projectActiveEnvKey(projectId: string): string {
         return `projectActiveEnv:${projectId}`;
       }
       ```

    3. Add state declarations (after `projectCommandsMap` state around line 72, before editMode):
       ```typescript
       // Phase 23: environment state per project
       const [projectEnvsMap, setProjectEnvsMap] = useState<Record<string, Environment[]>>({});
       const [projectActiveEnvMap, setProjectActiveEnvMap] = useState<Record<string, string>>({});
       ```
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 通过
    - `Environment` 类型从 `@/lib/types` 导入
    - `projectEnvsKey()` 和 `projectActiveEnvKey()` 函数定义正确
    - `projectEnvsMap` 和 `projectActiveEnvMap` state 声明正确
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add env CRUD and apply methods</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>src/hooks/useProject.ts (focus on CRUD methods ~line 487-578 to follow the exact pattern of addCommand/updateCommand/deleteCommand)</read_first>
  <action>
    Add env CRUD methods after `deleteCommand` (around line 578) and before the shortcut methods (line 580). Each method MUST follow the exact same pattern as the corresponding command CRUD method:
    - Immutable state update with spread operator
    - profileStore.set() for the project-specific key
    - profileStore.save() after each change
    - toast success/error feedback

    **Key: `createEnv`** (per D-20 unique name check):
    ```typescript
    async (projectId: string, name: string) => Promise<string | null>
    ```
    - Validate `selectedId` exists (use selectedId if projectId not provided)
    - Check name uniqueness: if existing envs have same name → toast.error("环境名称已存在") → return null
    - Generate UUID for id
    - Create `Environment` object with `{ id, name, createdAt: Date.now(), updatedAt: Date.now(), files: [] }`
    - Use same immutable update + profileStore.set/save pattern as `addCommand`
    - Return the new environment's id on success
    - Toast: `toast.success(\`已创建环境: ${name}\`)`

    **Key: `renameEnv`** (per D-20 unique name check):
    ```typescript
    async (projectId: string, envId: string, newName: string) => Promise<void>
    ```
    - Check name uniqueness (excluding the target env itself)
    - Map through envs, replace name and update `updatedAt`
    - Persist + toast

    **Key: `deleteEnv`** (per D-21, D-18):
    ```typescript
    async (projectId: string, envId: string) => Promise<void>
    ```
    - Filter out the target env
    - If deleted env was the active env, clear `projectActiveEnvMap` for this project AND delete the profileStore key `projectActiveEnvKey(projectId)`
    - Persist envs + optionally clear active env
    - Toast: `toast.success(\`已删除环境: ${target.name}\`)`
    - NOTE: The UI layer (ManageEnvDialog) handles the "cannot delete applied env" check before calling this method (D-21), so this method does not need to block deletion of applied envs.

    **Key: `setActiveEnv`** (per D-14):
    ```typescript
    async (projectId: string, envId: string | null) => Promise<void>
    ```
    - Set the active env id for this project in `projectActiveEnvMap`
    - Persist via `profileStore.set(projectActiveEnvKey(projectId), envId)` + save
    - Used by the apply action and by deleteEnv cleanup

    **Key: `applyEnv`** (per D-26, D-27, D-28, D-30):
    ```typescript
    async (projectId: string, envId: string) => Promise<boolean>
    ```
    - Get the env from projectEnvsMap[projectId]
    - Iterate over `env.files`:
      - For each file, call `invoke("write_file_content", { projectPath, fileName: file.name, content: file.content })`
      - If any file write fails:
        - Attempt to rollback: iterate over previously written files, write back their original content (not feasible without original content — best effort: attempt to delete the written file). Per D-28, if rollback fails, toast "启用失败，部分文件已写入：[文件名]" listing the specific files.
        - Return false
    - If all files succeed: call `setActiveEnv(projectId, envId)`, toast `toast.success(\`已启用环境: ${env.name}\`)`
    - Return true
    - IMPORTANT: This needs `currentProject` from the useProject scope. Since `applyEnv` is defined inside useProject, it can access `currentProject` directly.
    - Use try-catch around each invoke call. On catch, start rollback.

    **Key: `getProjectEnvs`** (for UI convenience):
    ```typescript
    (projectId: string): Environment[]
    ```
    - Returns `projectEnvsMap[projectId] ?? []`

    **Key: `getProjectActiveEnv`** (for UI convenience):
    ```typescript
    (projectId: string): string | null
    ```
    - Returns `projectActiveEnvMap[projectId] ?? null`
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 通过
    - `createEnv` 方法创建环境并持久化
    - `renameEnv` 方法重命名环境并更新 updatedAt，检测重名
    - `deleteEnv` 方法删除环境，若是 active env 则清除 active 记录
    - `setActiveEnv` 方法设置/清除 active env id
    - `applyEnv` 方法调用 invoke("write_file_content") 并处理回滚
    - `getProjectEnvs` 和 `getProjectActiveEnv` 返回正确数据
    - 所有方法遵循不可变 state 更新模式
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate env state into load/cleanup/export/import and return new values</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>src/hooks/useProject.ts (loadProfileDataIntoState ~line 132-175, removeProject ~line 372-400, exportProfile ~line 766-803, importProfile ~line 806-878, return statement ~line 880-947)</read_first>
  <action>
    Integrate env state management into existing lifecycle methods and return new values.

    **1. `loadProfileDataIntoState` (around line 132-175):**
    After the project commands loading block (~line 153, after `setProjectCommandsMap(map)`), add env data loading:
    ```typescript
    // Phase 23: Restore environment data
    const envKeys = allKeys.filter((k) => k.startsWith("projectEnvs:"));
    const envEntries = await Promise.all(
      envKeys.map(async (k) => {
        const projectId = k.replace("projectEnvs:", "");
        const envs = await s.get<Environment[]>(k);
        return [projectId, envs ?? []] as const;
      })
    );
    const envMap = Object.fromEntries(envEntries);
    setProjectEnvsMap(envMap);

    // Restore active env per project
    const activeEnvKeys = allKeys.filter((k) => k.startsWith("projectActiveEnv:"));
    const activeEnvEntries = await Promise.all(
      activeEnvKeys.map(async (k) => {
        const projectId = k.replace("projectActiveEnv:", "");
        const activeId = await s.get<string>(k);
        return [projectId, activeId ?? null] as const;
      })
    );
    const validActiveEntries = activeEnvEntries.filter(([, v]) => v !== null) as [string, string][];
    const activeEnvMap = Object.fromEntries(validActiveEntries);
    setProjectActiveEnvMap(activeEnvMap);
    ```

    **2. `removeProject` (around line 372-400):**
    After the projectCommands cleanup (line 391-396), add env cleanup:
    ```typescript
    // Phase 23: Clean up environment data
    await profileStore?.delete(projectEnvsKey(id));
    await profileStore?.delete(projectActiveEnvKey(id));
    setProjectEnvsMap((prev) => {
      const next = { ...prev };
      delete next[id];
      return next;
    });
    setProjectActiveEnvMap((prev) => {
      const next = { ...prev };
      delete next[id];
      return next;
    });
    ```

    **3. `exportProfile` (around line 766-803):**
    Within the exportData.data object (~line 785-793), add env fields AFTER `recentCommands`:
    ```typescript
    // Phase 23: Export environment data
    projectEnvs: Object.fromEntries(envEntries),
    projectActiveEnvs: Object.fromEntries(activeEnvEntries),
    ```
    This requires collecting env keys similarly to how projectCommands are collected. Add the collection code before building the exportData object:
    ```typescript
    // Phase 23: Collect environment data
    const envKeys = allKeys.filter((k) => k.startsWith("projectEnvs:"));
    const envEntries = await Promise.all(
      envKeys.map(async (k) => {
        const projectId = k.replace("projectEnvs:", "");
        const val = await profileStore.get(k);
        return [projectId, val] as const;
      })
    );
    const activeEnvKeys = allKeys.filter((k) => k.startsWith("projectActiveEnv:"));
    const activeEnvEntries = await Promise.all(
      activeEnvKeys.map(async (k) => {
        const projectId = k.replace("projectActiveEnv:", "");
        const val = await profileStore.get(k);
        return [projectId, val] as const;
      })
    );
    ```

    **4. `importProfile` (around line 806-878):**
    After the projectCommands import block (~line 857-862), add env import:
    ```typescript
    // Phase 23: Import environment data
    if (data.projectEnvs && typeof data.projectEnvs === "object") {
      for (const [projectId, envs] of Object.entries(data.projectEnvs as Record<string, unknown>)) {
        await ps.set(projectEnvsKey(projectId), envs);
      }
    }
    if (data.projectActiveEnvs && typeof data.projectActiveEnvs === "object") {
      for (const [projectId, activeId] of Object.entries(data.projectActiveEnvs as Record<string, unknown>)) {
        if (activeId !== null) {
          await ps.set(projectActiveEnvKey(projectId), activeId);
        }
      }
    }
    ```

    **5. Return statement (~line 880-947):**
    Add to the return object (after `projectCommandsMap` and `openFolder`):
    ```typescript
    // Phase 23: Environment management
    projectEnvsMap,           // Record<string, Environment[]>
    projectActiveEnvMap,      // Record<string, string>
    createEnv,                // (projectId: string, name: string) => Promise<string | null>
    renameEnv,                // (projectId: string, envId: string, newName: string) => Promise<void>
    deleteEnv,                // (projectId: string, envId: string) => Promise<void>
    setActiveEnv,             // (projectId: string, envId: string | null) => Promise<void>
    applyEnv,                 // (projectId: string, envId: string) => Promise<boolean>
    getProjectEnvs,           // (projectId: string) => Environment[]
    getProjectActiveEnv,      // (projectId: string) => string | null
    ```

    **Backward compatibility:** (per D-06) — If projectEnvsMap[projectId] is undefined or empty, all env methods work on empty arrays. No special handling needed beyond the null-safety in getProjectEnvs (which returns [] for missing keys). No toast, no migration.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 通过
    - `loadProfileDataIntoState` 中正确加载 projectEnvs:* 和 projectActiveEnv:* 键
    - `removeProject` 中正确清理 projectEnvs:* 和 projectActiveEnv:* 键和内存 state
    - `exportProfile` 的导出数据包含 projectEnvs 和 projectActiveEnvs
    - `importProfile` 的导入逻辑正确恢复 projectEnvs 和 projectActiveEnvs
    - 返回对象包含所有 7 个新的 env 属性和方法
    - 向后兼容：无 env 数据时静默工作（空对象 + 空数组）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → applyEnv | 用户点击"启用"按钮触发文件写入操作 |
| applyEnv → invoke("write_file_content") | 将环境文件内容写入项目目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | T | applyEnv rollback | accept | 回滚是 best-effort（D-28）。若部分文件写入后失败，尝试删除已写入文件，无法回滚时 toast 报告具体文件名。风险可控因用户在项目目录中已有 shell 执行权限 |
| T-23-05 | D | applyEnv | mitigate | 在调用 applyEnv 前确认 envId 存在（从 projectEnvsMap 查找），防止写入空/无效环境 |
| T-23-06 | I | deleteEnv applied env | mitigate | UI 层（ManageEnvDialog）阻止删除已应用环境（D-21）。后端 deleteEnv 不额外校验，因为 UI 层必须强制此约束 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 编译无错误
2. 手动检查 useProject.ts 中所有集成点：loadProfileDataIntoState、removeProject、exportProfile、importProfile、return 语句
3. 确认 projectEnvsKey/projectActiveEnvKey 不与现有 projectCommandsKey 冲突
</verification>

<success_criteria>
- [ ] projectEnvsMap 和 projectActiveEnvMap state 定义正确
- [ ] createEnv/renameEnv/deleteEnv CRUD 方法实现完整
- [ ] setActiveEnv 和 applyEnv 方法实现完整
- [ ] loadProfileDataIntoState 恢复环境数据
- [ ] removeProject 清理环境数据
- [ ] exportProfile/importProfile 包含环境字段
- [ ] 所有 env 方法从 useProject 返回对象中暴露
- [ ] TypeScript 编译通过
- [ ] 向后兼容：无 env 数据的旧用户数据不受影响
</success_criteria>

<output>
After completion, create `.planning/phases/23-env-tabs-management/23-02-SUMMARY.md`
</output>
