---
phase: 23-env-tabs-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src-tauri/src/commands/shell.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-08

must_haves:
  truths:
    - "Environment 和 ManagedFile 类型在 types.ts 中定义并可被其他模块导入"
    - "ProfileExportData 包含环境数据的序列化/反序列化字段"
    - "Rust 后端可通过 read_file_content 读取项目目录中的任意文本文件"
    - "Rust 后端可通过 write_file_content 向项目目录写入文件，嵌套路径自动创建目录"
    - "write_file_content 在写入失败时返回具体错误信息"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Environment, ManagedFile 接口定义 + ProfileExportData 环境字段"
      contains: "interface Environment, interface ManagedFile"
    - path: "src-tauri/src/commands/shell.rs"
      provides: "read_file_content, write_file_content 命令"
      exports: ["read_file_content", "write_file_content"]
    - path: "src-tauri/src/lib.rs"
      provides: "新命令注册"
      contains: "commands::shell::read_file_content, commands::shell::write_file_content"
  key_links:
    - from: "useProject.ts"
      to: "types.ts"
      via: "import"
      pattern: "import.*Environment|ManagedFile|ProfileExportData"
    - from: "App.tsx / MainArea.tsx"
      to: "types.ts"
      via: "import Environment / ManagedFile"
    - from: "shell.ts Rust"
      to: "lib.rs"
      via: "invoke_handler 注册"
      pattern: "commands::shell::(read_file_content|write_file_content)"
---
<objective>
创建环境数据模型类型和 Rust 文件读写命令。

**Purpose:** 为 Phase 23 的标签页、管理模态窗和切换栏提供数据类型基础和后端文件操作能力。环境数据模型是标签页和管理的核心数据结构；Rust 文件读写命令是「启用环境」将配置文件写入项目目录的关键能力。

**Output:**
1. `src/lib/types.ts` — 新增 `Environment`、`ManagedFile` 接口，更新 `ProfileExportData.data` 字段
2. `src-tauri/src/commands/shell.rs` — 新增 `read_file_content`、`write_file_content` 异步命令 + 单元测试
3. `src-tauri/src/lib.rs` — 注册新命令到 `invoke_handler`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-env-tabs-management/23-CONTEXT.md (D-07, D-08, D-03, D-12, D-13)

<interfaces>
From src/lib/types.ts (existing):
```typescript
export interface ProfileExportData {
  formatVersion: number;
  profileName: string;
  exportedAt: string;
  data: {
    projects: unknown;
    selectedProjectId: unknown;
    customCommands: unknown;
    projectCommands: Record<string, unknown>;
    shortcutBindings: unknown;
    presetShortcuts: unknown;
    recentCommands: unknown;
  };
}
```

From src-tauri/src/commands/shell.rs (existing pattern — double-quote validation):
```rust
#[tauri::command]
pub async fn execute_command(project_path: String, shell_command: String) -> Result<(), String> {
    if project_path.contains('"') { ... }
    if shell_command.contains('"') { ... }
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Environment, ManagedFile types and update ProfileExportData</name>
  <files>src/lib/types.ts</files>
  <read_first>src/lib/types.ts (full file — 55 lines)</read_first>
  <action>
    Add two new interfaces and update one existing interface in src/lib/types.ts:

    1. Add `Environment` interface (per D-07):
       - `id: string` — UUID
       - `name: string` — 环境名称，在同一项目内唯一
       - `createdAt: number` — Date.now() 时间戳
       - `updatedAt: number` — Date.now() 时间戳
       - `files: ManagedFile[]` — 此环境管理的配置文件列表

    2. Add `ManagedFile` interface (per D-08):
       - `name: string` — 相对路径（如 ".env"、"config/settings.json"），支持嵌套目录
       - `content: string` — 文件完整文本内容
       - `addedAt: number` — Date.now() 时间戳

    3. Update `ProfileExportData.data`:
       - Add `projectEnvs: Record<string, Environment[]>` — key=projectId, value=环境数组
       - Add `projectActiveEnvs: Record<string, string>` — key=projectId, value=activeEnvId

    Place new interfaces after the `ProfileMeta` interface (before `ProfileExportData`).
    Maintain existing JSDoc comment style.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 编译通过
    - `Environment` 接口可从 `@/lib/types` 导入
    - `ManagedFile` 接口可从 `@/lib/types` 导入
    - `ProfileExportData.data` 包含 `projectEnvs` 和 `projectActiveEnvs` 字段
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add read_file_content and write_file_content Rust commands with tests</name>
  <files>src-tauri/src/commands/shell.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/commands/shell.rs (full file), src-tauri/src/lib.rs (invoke_handler section ~line 24-33)</read_first>
  <action>
    Add two new Tauri commands to `src-tauri/src/commands/shell.rs`, following the existing pattern (double-quote validation, async, `Result<..., String>`). Then register them in `lib.rs`.

    **Command 1: `read_file_content`** (per D-09, D-12)
    - Signature: `pub async fn read_file_content(project_path: String, file_name: String) -> Result<String, String>`
    - Validate `project_path` and `file_name` do not contain `"` (double quote)
    - Validate `file_name` is not empty
    - Construct full path: `project_path / file_name` (use `std::path::PathBuf`)
    - Read file with `std::fs::read_to_string()`
    - On error, return the IO error message as the error string (DO NOT silently return empty string — the frontend handles the "file not found = empty content" case per D-09)
    - Return the file content string on success

    **Command 2: `write_file_content`** (per D-12, D-28)
    - Signature: `pub async fn write_file_content(project_path: String, file_name: String, content: String) -> Result<(), String>`
    - Validate `project_path` and `file_name` do not contain `"` (double quote)
    - Validate `file_name` is not empty
    - Construct full path: `project_path / file_name`
    - Create parent directories: `std::fs::create_dir_all(parent_dir)` (per D-12 "auto create_dir_all")
    - Write file with `std::fs::write(full_path, content.as_bytes())`
    - Return Ok(()) on success, Err with IO error message on failure
    
    Both commands follow the exact same pattern as `execute_command`:
    - `#[tauri::command]`
    - `pub async fn`
    - Validate double quotes
    - Return `Result<..., String>`

    **Registration in `src-tauri/src/lib.rs`:**
    - Add both commands to the `invoke_handler` array, maintaining alphabetical-ish order (add after `commands::shell::open_folder`)

    **Tests in `src-tauri/src/commands/shell.rs`:**
    - Add a test module with `#[cfg(test)]` following the existing test module pattern at the bottom of shell.rs
    - Test `read_file_content`:
      - Creates a temp dir, writes a test file, calls read_file_content, asserts content matches
      - Tests that non-existent file returns error
    - Test `write_file_content`:
      - Creates a temp dir with unique subfolder, calls write_file_content to create a file, asserts file exists with correct content
      - Tests nested path (e.g., "config/settings.json") — asserts create_dir_all works and file content is correct
      - Tests double-quote validation returns error
    - Use `tokio::runtime::Runtime::new().unwrap().block_on(...)` pattern (same as existing tests such as `test_execute_command_rejects_quoted_command`)
    - Clean up temp dirs after tests (use tempfile crate or manual cleanup)

    DO NOT change any existing commands or test code. Only append to the file.
  </action>
  <verify>cd src-tauri && cargo test -- read_file_content write_file_content 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - `cargo test` 中 read_file_content 和 write_file_content 的相关测试全部通过
    - `read_file_content` 能正确读取项目目录下的文本文件
    - `read_file_content` 对不存在的文件返回错误（非空字符串）
    - `write_file_content` 能正确写入文件并自动创建父级目录
    - `write_file_content` 拒绝含双引号的路径
    - 两个命令均在 lib.rs 的 invoke_handler 中注册
    - `npx tsc --noEmit` 前端编译通过（仅确认 types.ts 修改无误）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → Rust command | 前端通过 invoke 调用 Rust 命令 |
| Rust command → filesystem | 读取/写入项目目录下的文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | S | read_file_content | mitigate | 验证 project_path 和 file_name 不含双引号；file_name 非空，防止路径遍历 |
| T-23-02 | T | write_file_content | mitigate | 同样验证路径参数；仅写入 `project_path` 下的文件（通过 `project_path / file_name` 构造，无法逃逸到外部目录） |
| T-23-03 | I | read_file_content error | accept | 错误消息只包含 IO 错误描述，不包含系统敏感信息（文件内容本身通过正常路径返回） |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 编译无错误
2. `cd src-tauri && cargo test` — 所有 Rust 测试通过
3. `cd src-tauri && cargo test -- read_file_content write_file_content` — 新测试专项验证
</verification>

<success_criteria>
- [ ] `Environment` 和 `ManagedFile` 类型在 types.ts 中可用
- [ ] `ProfileExportData` 包含 `projectEnvs` 和 `projectActiveEnvs` 字段
- [ ] `read_file_content` Rust 命令能读取文件内容
- [ ] `write_file_content` Rust 命令能写入文件并自动创建父目录
- [ ] 两个命令都在 lib.rs 中注册
- [ ] 单元测试全部通过
- [ ] TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/23-env-tabs-management/23-01-SUMMARY.md`
</output>
