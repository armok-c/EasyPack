---
phase: 14-边缘抽屉
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/App.tsx
  - src/components/TitleBar.tsx
  - src/components/SettingsDialog.tsx
  - src/components/SnapIndicator.tsx
  - src/hooks/useEdgeDrawer.ts
  - src/hooks/useTray.ts
autonomous: false
requirements:
  - DRAWER-01
  - DRAWER-04
  - DRAWER-06
  - DRAWER-05

must_haves:
  truths:
    - "用户拖拽窗口到屏幕边缘松手后窗口自动吸附隐藏为 thin sliver"
    - "鼠标接触 sliver 后窗口完整滑出，显示吸附前的位置和尺寸"
    - "鼠标离开滑出的窗口后延迟收回"
    - "拖拽滑出的窗口超过 20px 自动取消吸附恢复原始位置"
    - "设置中可以开关边缘抽屉功能"
    - "拖拽接近边缘 10px 内时显示半透明吸附提示 (D-04)"
  artifacts:
    - path: "src/App.tsx"
      provides: "useEdgeDrawer 集成、drawerEnabled 状态管理、设置持久化、SnapIndicator 渲染、拖拽中实时位置检测"
      contains: "useEdgeDrawer"
    - path: "src/components/TitleBar.tsx"
      provides: "拖拽结束吸附检测 + 滑出状态拖拽取消吸附"
      contains: "handleDragEnd"
    - path: "src/components/SettingsDialog.tsx"
      provides: "边缘抽屉设置分区（启用/禁用开关）"
      contains: "drawerEnabled"
    - path: "src/components/SnapIndicator.tsx"
      provides: "拖拽接近边缘时的吸附预览覆盖层"
      exports: ["SnapIndicator"]
    - path: "src/hooks/useEdgeDrawer.ts"
      provides: "restoreFromDrawer 补充实现（如 Plan 14-02 未完成）"
      contains: "restoreFromDrawer"
    - path: "src/hooks/useTray.ts"
      provides: "DRAWER_HIDDEN 状态下托盘恢复窗口"
      contains: "onRestoreFromDrawer"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useEdgeDrawer.ts"
      via: "useEdgeDrawer hook 调用"
      pattern: "useEdgeDrawer"
    - from: "src/App.tsx"
      to: "src/components/SnapIndicator.tsx"
      via: "JSX 渲染 <SnapIndicator /> 并传入实时 edge/workArea"
      pattern: "<SnapIndicator"
    - from: "src/components/TitleBar.tsx"
      to: "src/hooks/useEdgeDrawer.ts"
      via: "handleDragEnd 回调 prop"
      pattern: "handleDragEnd"
    - from: "src/components/SettingsDialog.tsx"
      to: "src/App.tsx"
      via: "drawerEnabled prop"
      pattern: "drawerEnabled"
    - from: "src/components/SnapIndicator.tsx"
      to: "src/lib/drawer-geometry.ts"
      via: "import detectSnapEdge 计算预览位置"
      pattern: "SnapIndicator"
    - from: "src/hooks/useTray.ts"
      to: "src/hooks/useEdgeDrawer.ts"
      via: "restoreFromDrawer 回调"
      pattern: "restoreFromDrawer"
---

<objective>
将边缘抽屉功能集成到 UI 层。

Purpose: 连接 useEdgeDrawer hook 到实际 UI 组件 -- TitleBar 拖拽行为、App.tsx 状态管理、SettingsDialog 设置开关、SnapIndicator 吸附提示（含拖拽中实时窗口位置检测驱动 D-04）。实现用户可感知的完整交互体验。

Output: 更新后的 App.tsx、TitleBar.tsx、SettingsDialog.tsx、useTray.ts、useEdgeDrawer.ts，新增 SnapIndicator.tsx。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-边缘抽屉/14-CONTEXT.md
@.planning/phases/14-边缘抽屉/14-RESEARCH.md
@.planning/phases/14-边缘抽屉/14-01-SUMMARY.md
@.planning/phases/14-边缘抽屉/14-02-SUMMARY.md

@src/App.tsx
@src/components/TitleBar.tsx
@src/components/SettingsDialog.tsx
@src/hooks/useEdgeDrawer.ts
@src/hooks/useVisibilityState.ts
@src/hooks/useFloatWindow.ts
@src/hooks/useTray.ts
@src-tauri/src/lib.rs
</context>

<interfaces>
<!-- Wave 2 创建的 hook 接口 -->

From src/hooks/useEdgeDrawer.ts (Plan 14-02):
```typescript
interface UseEdgeDrawerOptions {
  visibility: VisibilityState;
  hideToDrawer: () => void;
  showFromDrawer: () => void;
  drawerEnabled: boolean;
}

interface UseEdgeDrawerReturn {
  handleDragEnd: () => Promise<void>;
  handleMouseEnter: () => void;
  handleMouseLeave: () => void;
  handleDragWhileSnapped: (deltaX: number, deltaY: number) => void;
  restoreFromDrawer: () => Promise<void>;    // 外部调用恢复窗口（托盘/快捷键）
  isDrawerAnimating: boolean;
  snapEdge: SnapEdge | null;
}

export function useEdgeDrawer(options: UseEdgeDrawerOptions): UseEdgeDrawerReturn;
```

<!-- 现有组件接口 -->

From src/components/TitleBar.tsx:
```typescript
interface TitleBarProps {
  onSettingsOpen: () => void;
  onFloatToggle: () => void;
  floatVisible: boolean;
}
```

From src/components/SettingsDialog.tsx:
```typescript
interface SettingsDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  trayEnabled: boolean;
  onTrayEnabledChange: (enabled: boolean) => void;
  closeToTray: boolean;
  onCloseToTrayChange: (enabled: boolean) => void;
}
```

From src/hooks/useVisibilityState.ts:
```typescript
export type VisibilityState = "VISIBLE" | "TRAY_HIDDEN" | "DRAWER_HIDDEN";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SnapIndicator 组件 + 修改 TitleBar + 修改 SettingsDialog</name>
  <files>src/components/SnapIndicator.tsx, src/components/TitleBar.tsx, src/components/SettingsDialog.tsx</files>
  <read_first>
    - src/components/TitleBar.tsx (reason: 需看到当前拖拽实现和 props 接口)
    - src/components/SettingsDialog.tsx (reason: 需看到当前设置分区模式)
    - src/hooks/useEdgeDrawer.ts (reason: 确认 UseEdgeDrawerReturn 接口)
    - src/lib/drawer-geometry.ts (reason: SnapIndicator 需要调用 detectSnapEdge)
    - src/App.tsx (reason: 了解 SettingsDialog 和 TitleBar 的 prop 传递模式)
  </read_first>
  <action>
    **创建 src/components/SnapIndicator.tsx**：

    吸附预览覆盖层组件，当窗口拖拽接近屏幕边缘时显示。实现 D-04。

    ```typescript
    interface SnapIndicatorProps {
      edge: SnapEdge | null;       // 当前接近的边，null 时不显示
      workArea: Rect | null;       // monitor workArea，用于定位指示器
    }
    ```

    实现：
    - 如果 edge 为 null，返回 null（不渲染）
    - 使用 fixed 定位覆盖层，位置根据 edge 计算：
      - left: `{ left: 0, top: workArea.y, width: 4px, height: workArea.h }`
      - right: `{ right: 0, top: workArea.y, width: 4px, height: workArea.h }`
      - top: `{ top: 0, left: workArea.x, height: 4px, width: workArea.w }`
      - bottom: `{ bottom: 0, left: workArea.x, height: 4px, width: workArea.w }`
    - 样式：半透明主题色条（`bg-blue-500/30`），配合 `transition-all duration-150` 平滑出现
    - D-04：半透明边框提示"即将吸附"

    **修改 src/components/TitleBar.tsx**：

    扩展 TitleBarProps 接口，添加：
    ```typescript
    interface TitleBarProps {
      onSettingsOpen: () => void;
      onFloatToggle: () => void;
      floatVisible: boolean;
      // Phase 14: 边缘抽屉
      onDragEnd: (() => Promise<void>) | null;  // 吸附检测回调，null 时跳过
      onDragWhileSnapped: ((deltaX: number, deltaY: number) => void) | null;
      drawerSnapEdge: string | null;  // 当前吸附边，非 null 时 TitleBar 处于"可取消吸附"模式
    }
    ```

    修改 handleDragStart 函数：
    - 现有的 `appWindow.startDragging()` 保持不变
    - 在 onMouseUp 事件中（需要在 mousedown 时添加 mouseup 监听器到 window）：
      - 调用 onDragEnd?.()（如果 prop 非空）
    - 当 drawerSnapEdge 非空时，在 mousemove 事件中：
      - 计算 deltaX/deltaY 累积位移
      - 如果累积位移 > 5px，调用 onDragWhileSnapped(deltaX, deltaY)

    具体实现方式：
    - 在 handleDragStart 中，除了调用 startDragging()，同时注册一个一次性 mouseup 监听器：
      ```typescript
      function handleDragStart(e: React.MouseEvent<HTMLDivElement>) {
        if (e.button !== 0) return;
        const target = e.target as HTMLElement;
        if (target.closest("button")) return;
        appWindow.startDragging();

        // Phase 14: 拖拽结束时检测吸附
        if (onDragEnd) {
          const handleUp = () => {
            onDragEnd();
            window.removeEventListener("mouseup", handleUp);
          };
          window.addEventListener("mouseup", handleUp);
        }
      }
      ```

    注意：startDragging() 在 Tauri v2 中会接管整个拖拽循环（包括 mouseup），所以 mouseup 监听器实际上可能不会被触发。如果遇到此问题，改用 Tauri 的 onMoved 事件来检测拖拽结束（当窗口位置不再变化时判定拖拽结束）。

    **修改 src/components/SettingsDialog.tsx**：

    扩展 SettingsDialogProps：
    ```typescript
    interface SettingsDialogProps {
      // 现有 props 保持不变
      open: boolean;
      onOpenChange: (open: boolean) => void;
      trayEnabled: boolean;
      onTrayEnabledChange: (enabled: boolean) => void;
      closeToTray: boolean;
      onCloseToTrayChange: (enabled: boolean) => void;
      // Phase 14: 边缘抽屉
      drawerEnabled: boolean;
      onDrawerEnabledChange: (enabled: boolean) => void;
    }
    ```

    在 `<div className="py-4 space-y-6">` 内，系统托盘分区之后添加边缘抽屉分区：

    ```jsx
    {/* Section: 边缘抽屉 */}
    <div>
      <div className="border-b border-white/10 pb-2 mb-4">
        <Label>边缘抽屉</Label>
      </div>
      <div className="space-y-4">
        <div className="flex items-center justify-between">
          <div>
            <p className="text-sm">启用边缘抽屉</p>
            <p className="text-xs text-muted-foreground">
              拖拽窗口到屏幕边缘自动隐藏，鼠标滑过边缘快速唤出
            </p>
          </div>
          <Switch
            checked={drawerEnabled}
            onCheckedChange={onDrawerEnabledChange}
          />
        </div>
      </div>
    </div>
    ```

    复用现有托盘分区的视觉样式（border-b border-white/10, space-y-4, flex items-center justify-between）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(error|TitleBar|SettingsDialog|SnapIndicator)" | head -20</automated>
  </verify>
  <done>
    - SnapIndicator.tsx 存在，导出 SnapIndicator 组件
    - TitleBar 接收 onDragEnd 和 onDragWhileSnapped 和 drawerSnapEdge props
    - TitleBar 在拖拽结束时调用 onDragEnd
    - SettingsDialog 包含"边缘抽屉"设置分区，有启用/禁用开关
    - SettingsDialogProps 包含 drawerEnabled 和 onDrawerEnabledChange
    - TypeScript 编译无新增错误
  </done>
</task>

<task type="auto">
  <name>Task 2: App.tsx 集成 useEdgeDrawer + SnapIndicator 渲染 + 拖拽中实时位置检测 + 设置持久化 + useTray 抽屉菜单项</name>
  <files>src/App.tsx, src/hooks/useTray.ts, src/hooks/useEdgeDrawer.ts</files>
  <read_first>
    - src/App.tsx (reason: 主集成文件，需看到完整状态管理和生命周期)
    - src/hooks/useEdgeDrawer.ts (reason: 确认 UseEdgeDrawerOptions 和 UseEdgeDrawerReturn 接口，特别是 restoreFromDrawer)
    - src/hooks/useTray.ts (reason: 需看到 buildMenu 和 tray action 回调模式)
    - src/hooks/useVisibilityState.ts (reason: 确认三态接口)
    - src/hooks/useFloatWindow.ts (reason: 理解 destroyFloat 在 onQuit 中的调用模式)
    - src/components/SnapIndicator.tsx (reason: 确认 SnapIndicatorProps 接口)
    - src/lib/drawer-geometry.ts (reason: 确认 detectSnapEdge、Rect、MonitorInfo 类型)
  </read_first>
  <action>
    **前置检查：restoreFromDrawer 方法**

    检查 src/hooks/useEdgeDrawer.ts 的 UseEdgeDrawerReturn 接口中是否包含 `restoreFromDrawer` 方法。如果 Plan 14-02 执行时遗漏了此方法，需要在此 Task 中先补充到 useEdgeDrawer.ts：
    - 在 UseEdgeDrawerReturn 接口中添加 `restoreFromDrawer: () => Promise<void>`
    - 实现逻辑：emit("drawer:stop-polling")，恢复窗口到 originalRectRef 位置（animateWindow），恢复 minWidth（setMinSize(600,400)），清除 snapEdgeRef 和 currentSnapEdge，不改变 visibility 状态

    **修改 src/App.tsx**：

    1. 导入 useEdgeDrawer 和 SnapIndicator：
       ```typescript
       import { useEdgeDrawer } from "@/hooks/useEdgeDrawer";
       import { SnapIndicator } from "@/components/SnapIndicator";
       ```

    2. 从 drawer-geometry 导入类型（用于 SnapIndicator 位置计算）：
       ```typescript
       import { detectSnapEdge, type Rect, type SnapEdge, type MonitorInfo, type WindowInfo } from "@/lib/drawer-geometry";
       ```

    3. 添加 drawerEnabled 状态（同 trayEnabled 模式）：
       ```typescript
       const [drawerEnabled, setDrawerEnabled] = useState(false);
       ```

    4. 添加拖拽中实时吸附预览状态（D-04）：
       ```typescript
       const [snapPreviewEdge, setSnapPreviewEdge] = useState<SnapEdge | null>(null);
       const [snapPreviewWorkArea, setSnapPreviewWorkArea] = useState<Rect | null>(null);
       ```

    5. 从 useVisibilityState 获取扩展的三态接口：
       ```typescript
       const { visibility, hideToTray, showFromTray, hideToDrawer, showFromDrawer, isVisible, isDrawerHidden } = useVisibilityState();
       ```

    6. 调用 useEdgeDrawer：
       ```typescript
       const { handleDragEnd, handleMouseEnter, handleMouseLeave, handleDragWhileSnapped, restoreFromDrawer, isDrawerAnimating, snapEdge } = useEdgeDrawer({
         visibility,
         hideToDrawer,
         showFromDrawer,
         drawerEnabled,
       });
       ```

    7. 在 loadTraySettings useEffect 中追加 drawerEnabled 加载：
       ```typescript
       const savedDrawer = await store.get<boolean>("drawerEnabled");
       if (mounted) {
         // ... existing tray settings ...
         setDrawerEnabled(savedDrawer !== undefined && savedDrawer !== null ? savedDrawer : false);
       }
       ```

    8. 添加 drawerEnabled 变更持久化：
       ```typescript
       const handleDrawerEnabledChange = useCallback(async (enabled: boolean) => {
         setDrawerEnabled(enabled);
         await store?.set("drawerEnabled", enabled);
       }, [store]);
       ```

    9. **D-04 实现：拖拽中实时窗口位置检测驱动 SnapIndicator**

       添加 useEffect 监听 Tauri 窗口 onMoved 事件，在拖拽过程中实时计算窗口是否接近边缘：
       ```typescript
       useEffect(() => {
         if (!drawerEnabled) return;
         let unlisten: (() => void) | null = null;

         async function setupMoveListener() {
           const currentWindow = getCurrentWindow();
           unlisten = await currentWindow.onMoved(async () => {
             // 只在 VISIBLE 状态且未在动画中时检测（即用户正在拖拽）
             if (snapEdge !== null || isDrawerAnimating) {
               setSnapPreviewEdge(null);
               return;
             }
             try {
               const pos = await currentWindow.outerPosition();
               const size = await currentWindow.innerSize();
               const monitor = await currentWindow.primaryMonitor();
               if (!monitor) return;

               const scaleFactor = monitor.scaleFactor;
               const workArea: Rect = {
                 x: monitor.workArea.x,
                 y: monitor.workArea.y,
                 w: monitor.workArea.width,
                 h: monitor.workArea.height,
               };
               const windowInfo: WindowInfo = {
                 x: pos.x / scaleFactor,
                 y: pos.y / scaleFactor,
                 w: size.width / scaleFactor,
                 h: size.height / scaleFactor,
                 isMaximized: false,
               };

               const snapResult = detectSnapEdge(windowInfo, { workArea, scaleFactor, isPrimary: true });
               if (snapResult) {
                 setSnapPreviewEdge(snapResult.edge);
                 setSnapPreviewWorkArea(workArea);
               } else {
                 setSnapPreviewEdge(null);
                 setSnapPreviewWorkArea(null);
               }
             } catch {
               // 窗口操作可能失败（窗口已销毁等），静默忽略
             }
           });
         }
         setupMoveListener();

         return () => {
           unlisten?.();
         };
       }, [drawerEnabled, snapEdge, isDrawerAnimating]);
       ```

       注意：onMoved 在拖拽过程中会高频触发（每次窗口位置变化一次）。detectSnapEdge 计算很轻量，无需节流。

       拖拽结束时清除预览：
       ```typescript
       const handleDragEndWithCleanup = useCallback(async () => {
         setSnapPreviewEdge(null);
         setSnapPreviewWorkArea(null);
         await handleDragEnd();
       }, [handleDragEnd]);
       ```

    10. 修改 onShow 回调（传给 useTray 的）：
        当 visibility 为 DRAWER_HIDDEN 时，先调用 restoreFromDrawer 恢复窗口位置：
        ```typescript
        onShow: () => {
          if (visibility === "DRAWER_HIDDEN" || isDrawerHidden) {
            restoreFromDrawer();
            showFromDrawer();
          } else {
            showFromTray();
          }
          appWindow.show().catch(console.error);
          appWindow.setFocus().catch(console.error);
        },
        ```

    11. 修改 onCloseRequested 处理器：
        - 当 DRAWER_HIDDEN 状态时关闭窗口应先恢复到 VISIBLE 再正常关闭
        - 在 event.preventDefault() 之后，如果 visibility === "DRAWER_HIDDEN"，先调用 restoreFromDrawer() + showFromDrawer() 恢复窗口

    12. **在 return JSX 中渲染 SnapIndicator 和传递新 props**：

        ```jsx
        return (
          <div
            className="flex flex-col h-screen w-screen overflow-hidden"
            onMouseEnter={snapEdge ? handleMouseEnter : undefined}
            onMouseLeave={snapEdge ? handleMouseLeave : undefined}
          >
            <TitleBar
              onSettingsOpen={() => setSettingsOpen(true)}
              onFloatToggle={toggleFloat}
              floatVisible={floatVisible}
              onDragEnd={drawerEnabled ? handleDragEndWithCleanup : null}
              onDragWhileSnapped={drawerEnabled ? handleDragWhileSnapped : null}
              drawerSnapEdge={snapEdge}
            />
            <div className="flex flex-1 overflow-hidden">
              {/* ... Sidebar 和 MainArea 保持不变 ... */}
            </div>
            <SettingsDialog
              open={settingsOpen}
              onOpenChange={setSettingsOpen}
              trayEnabled={trayEnabled}
              onTrayEnabledChange={handleTrayEnabledChange}
              closeToTray={closeToTray}
              onCloseToTrayChange={handleCloseToTrayChange}
              drawerEnabled={drawerEnabled}
              onDrawerEnabledChange={handleDrawerEnabledChange}
            />
            <SnapIndicator edge={snapPreviewEdge} workArea={snapPreviewWorkArea} />
            <Toaster richColors position="bottom-right" duration={1500} />
          </div>
        );
        ```

        关键点：
        - `<SnapIndicator>` 放在根 div 内部、`<Toaster>` 之前，使用 fixed 定位覆盖在所有内容之上
        - `onDragEnd` 使用 `handleDragEndWithCleanup`（拖拽结束时清除预览状态）
        - 根 div 添加 `onMouseEnter`/`onMouseLeave`（延迟收回，仅当 snapEdge 非空时激活）

    **修改 src/hooks/useTray.ts**：

    在 UseTrayOptions 接口中添加：
    ```typescript
    // Phase 14: 边缘抽屉
    onRestoreFromDrawer?: () => void;
    ```

    在 buildMenu 函数中，"显示窗口" toggle item 的 action 中，当 visibilityRef.current === "DRAWER_HIDDEN" 时，除了 show/focus，还需调用 onRestoreFromDrawer 回调（如果存在）。

    具体修改：
    ```typescript
    const toggleItem = await MenuItem.new({
      id: "toggle-window",
      text: toggleText,
      action: () => {
        if (visibilityRef.current === "VISIBLE") {
          onHideRef.current();
          appWindow.hide().catch(console.error);
        } else {
          // 如果从 DRAWER_HIDDEN 恢复，需要先恢复窗口位置
          if (visibilityRef.current === "DRAWER_HIDDEN" && onRestoreFromDrawerRef.current) {
            onRestoreFromDrawerRef.current();
          }
          onShowRef.current();
          appWindow.show().catch(console.error);
          appWindow.setFocus().catch(console.error);
        }
      },
    });
    ```

    添加 ref：
    ```typescript
    const onRestoreFromDrawerRef = useRef(onRestoreFromDrawer);
    onRestoreFromDrawerRef.current = onRestoreFromDrawer;
    ```

    在 App.tsx 中传递给 useTray：
    ```typescript
    useTray({
      // ... existing props
      onRestoreFromDrawer: restoreFromDrawer,
    });
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - App.tsx 导入并调用 useEdgeDrawer
    - App.tsx 导入并渲染 SnapIndicator 组件（D-04 吸附提示）
    - 拖拽过程中通过 appWindow.onMoved 实时检测窗口位置，更新 snapPreviewEdge/snapPreviewWorkArea 驱动 SnapIndicator
    - 拖拽结束时清除 SnapIndicator 预览状态
    - drawerEnabled 状态通过 tauri-plugin-store 持久化
    - TitleBar 接收 onDragEnd/onDragWhileSnapped/drawerSnapEdge props
    - SettingsDialog 接收 drawerEnabled/onDrawerEnabledChange props
    - 根 div 有 onMouseEnter/onMouseLeave 处理器（延迟收回）
    - useTray 在 DRAWER_HIDDEN 状态下显示窗口时调用 restoreFromDrawer
    - 关闭窗口时正确处理 DRAWER_HIDDEN 状态
    - D-16：悬浮窗不受抽屉隐藏影响（destroyFloat 在 onQuit 中仍然被调用）
    - TypeScript 编译无新增错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    边缘抽屉完整功能：窗口拖拽到屏幕边缘吸附隐藏、鼠标接触滑出、延迟收回、设置开关、拖拽中吸附提示（SnapIndicator）。
  </what-built>
  <how-to-verify>
    1. 运行 `npm run tauri dev` 启动应用
    2. 打开设置（齿轮按钮），确认"边缘抽屉"分区存在，开关默认关闭
    3. 开启"边缘抽屉"开关
    4. 拖拽主窗口到屏幕左边缘（接近 10px 内），观察：
       - 拖拽过程中出现半透明蓝色提示条（SnapIndicator，D-04）
       - 提示条出现在屏幕左边缘
    5. 松手，观察：
       - 窗口平滑滑入边缘，留下 1-2px 主题色细条
       - SnapIndicator 提示条消失
    6. 将鼠标移到左侧边缘的细条处，观察：
       - 窗口从左侧滑出到原始位置和尺寸
    7. 将鼠标移出窗口，等待约 400ms，观察：
       - 窗口自动收回为 thin sliver
    8. 再次触发滑出，然后拖拽窗口离开边缘超过 20px，观察：
       - 窗口取消吸附，恢复为正常窗口
    9. 测试右/上/下边缘吸附（同上步骤，确认 SnapIndicator 出现在对应边缘）
    10. 测试悬浮窗：主窗口吸附隐藏后，悬浮窗仍然独立存活
    11. 测试托盘：DRAWER_HIDDEN 状态下点击托盘图标，窗口正确恢复到原始位置
    12. 关闭应用重启，确认窗口不自动进入抽屉状态 (D-17)
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户拖拽输入 → 吸附检测 | 用户可通过拖拽触发窗口隐藏/显示 |
| 设置持久化 → store | drawerEnabled 通过 IPC 写入 tauri-plugin-store |
| 托盘菜单 → 窗口恢复 | 托盘操作触发 drawer 状态恢复 |
| onMoved 事件 → SnapIndicator | 高频窗口位置事件驱动 UI 预览 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Denial of Service | 吸附动画卡顿 | accept | 最坏情况用户等待 200-300ms 动画完成，可通过拖拽取消 |
| T-14-08 | Tampering | drawerEnabled store 值 | accept | 用户手动修改 store 文件最多导致设置不一致，无安全影响 |
| T-14-09 | Denial of Service | onMoved 高频事件 | accept | detectSnapEdge 计算极轻量（纯数值比较），不会造成性能问题 |
</threat_model>

<verification>
```bash
npx tsc --noEmit
```
TypeScript 编译无错误。
</verification>

<success_criteria>
- 用户可以将窗口拖到屏幕四边并吸附隐藏
- 拖拽接近边缘 10px 内时 SnapIndicator 半透明提示条出现 (D-04)
- 松手后 SnapIndicator 消失，窗口吸附隐藏
- 鼠标接触 sliver 触发窗口滑出
- 鼠标离开后延迟收回
- 拖拽超过 20px 取消吸附
- 设置中可开关边缘抽屉
- 悬浮窗不受抽屉隐藏影响 (D-16)
- 重启后不自动进入抽屉状态 (D-17)
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/14-边缘抽屉/14-03-SUMMARY.md`
</output>
