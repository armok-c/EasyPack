# Phase 25: 同步差异对比 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 25-同步差异对比
**Areas discussed:** 环境选择弹窗, 双向选择复制, 缺失文件处理, Tab 页面导航

---

## 环境选择弹窗

| Option | Description | Selected |
|--------|-------------|----------|
| 纯多选列表 | Dialog 内展示环境列表，每行复选框 + 环境名 + 文件数，确认按钮执行对比 | ✓ |
| 即时切换式 | 下拉多选，选中立即激活对比 | |
| 双列表对比式 | 左侧源环境 + 右侧目标环境列表 | |

### 弹窗内匹配信息展示
| Option | Description | Selected |
|--------|-------------|----------|
| 匹配数+缺失数 | 「环境名 · 3个匹配, 2个缺失」 | ✓ |
| 仅环境名+文件数 | 「环境名 · 5个文件」 | |
| 可展开详情 | 默认简要 + 可展开详情 | |

### 弹窗标题
**标题「同步差异」，展示源环境名称**

### 弹窗尺寸
中等 Dialog（~480px 宽）

### 批量操作
| Option | Description | Selected |
|--------|-------------|----------|
| 全选/取消全选 | 顶部全选复选框 + 底部确认/取消 | ✓ |
| 仅逐项选择 | 无全选 | |

### 确认按钮行为
| Option | Description | Selected |
|--------|-------------|----------|
| 按钮禁用 | 未选目标环境时确认按钮灰色禁用 | ✓ |
| toast 提示 | 点击确认后提示 | |

### 单环境场景
保留弹窗（不自动跳过），即使只有 1 个其他环境

### 列表排序
按名称字母排序

### 文本溢出处理
长环境名 ellipsis 截断 + hover tooltip

### 选择记忆
不记住上次选择（每次打开默认全未选）

### 对比维度
| Option | Description | Selected |
|--------|-------------|----------|
| 按文件分组 | 主 Tab 文件，子 Tab 目标环境 | ✓ |
| 按环境分组 | 主 Tab 环境，子 Tab 文件 | |

---

## 差异触发按钮

### 位置
FileList 工具栏左侧：「N个文件 | 同步差异 | 添加文件 | 删除」

### 样式
Primary 变体 + GitCompare 图标 + 文字「同步差异」

### 未勾选行为
| Option | Description | Selected |
|--------|-------------|----------|
| 禁用 | 始终显示但灰色禁用 + tooltip | ✓ |
| 隐藏/显示 | 勾选后出现 | |

---

## 双向选择复制

### 操作粒度
| Option | Description | Selected |
|--------|-------------|----------|
| 逐块选择 | 每个 diff hunk 有「← 使用源」「使用目标 →」按钮 | ✓ |
| 整文件级别 | 顶部整文件按钮 | |
| 两者都要 | 同时提供 | |

### 数据方向
| Option | Description | Selected |
|--------|-------------|----------|
| 仅目标变更 | 源只读，目标被覆盖 | ✓ |
| 双向均可 | 可双向覆盖 | |

### 持久化时机
| Option | Description | Selected |
|--------|-------------|----------|
| 立即持久化 | 选择后立即写入 profileStore | ✓ |
| 仅内存修改 | 关闭时保存 | |

### 二次确认
| Option | Description | Selected |
|--------|-------------|----------|
| 不需要确认 | toast 反馈即可 | ✓ |
| 需要确认 | 弹窗确认 | |

### 撤销操作
| Option | Description | Selected |
|--------|-------------|----------|
| 需要撤销 | 支持撤销已应用的 hunk 变更 | ✓ |
| 不需要 | 一旦应用即写入 | |

### 视觉区分
| Option | Description | Selected |
|--------|-------------|----------|
| 颜色标记 | 已解决：绿色背景；未解决：灰色背景 | ✓ |
| 文字标记 | 纯文本 | |

### 未解决关闭行为
| Option | Description | Selected |
|--------|-------------|----------|
| 静默关闭 | 直接关闭，已应用保留 | ✓ |
| 需要提示 | 弹出警告 | |

### 全部解决状态
| Option | Description | Selected |
|--------|-------------|----------|
| 仅显示状态 | 底部显示「全部已解决」 | ✓ |
| 自动关闭 | 完成即关闭 | |

### 环境名显示
标题栏下方小字显示当前对比的源/目标环境名

### 计数显示
底部状态栏显示已解决/未解决计数

---

## 缺失文件处理

### 创建方式
| Option | Description | Selected |
|--------|-------------|----------|
| 以源内容填充 | 创建后以源环境内容填充 | ✓ |
| 以空内容创建 | 创建后为空 | |
| 仅标注不创建 | 仅提示缺失 | |

### 批量创建
| Option | Description | Selected |
|--------|-------------|----------|
| 独立创建 | 每个目标环境单独操作 | ✓ |
| 批量创建 | 一键全创建 | |

### 创建前确认
| Option | Description | Selected |
|--------|-------------|----------|
| 不需要确认 | toast 反馈 | ✓ |
| 需要确认 | mini 确认弹出 | |

### 创建后视图
| Option | Description | Selected |
|--------|-------------|----------|
| 显示空对比 | 源 vs 空白差异 | ✓ |
| 关闭 Tab | 自动关闭 | |

### 撤销创建
| Option | Description | Selected |
|--------|-------------|----------|
| 可撤销 | 从目标环境删除 | ✓ |
| 不可撤销 | 创建即持久化 | |

### 视觉提示
图标+计数标记（FileX 图标）

### 创建持久化
立即持久化

---

## Tab 页面导航

### 布局
| Option | Description | Selected |
|--------|-------------|----------|
| 双层 Tab | 顶部文件 Tabs + 内容区环境子 Tabs | ✓ |
| 左侧列表+右侧 | 左侧文件列表 + 右侧视图 | |

### 默认显示
| Option | Description | Selected |
|--------|-------------|----------|
| 自动打开首个 | 第一个文件的第一个环境 | ✓ |
| 空白起始 | 用户手动点击 | |

### 单文件 Tab
始终显示文件 Tab 行

### 溢出处理
| Option | Description | Selected |
|--------|-------------|----------|
| 水平滚动 | overflow-x: auto | ✓ |
| 下拉选择 | Select 切换 | |

### 文件 Tab 排序
按用户勾选顺序

### 文件 Tab 徽标
不需要

### 环境子 Tab 进度
不需要

### 标题
动态标题：「差异对比 — 文件名 (源环境 → 目标环境)」

### 模态窗大小
80% 视口（与 FileEditorDialog 一致）

---

## Claude's Discretion

- `@git-diff-view/react` 具体配置（split/unified 默认、diff 算法参数）
- 差异块按钮的具体位置和样式
- 颜色方案在暗色主题下的具体色值
- Tab 组件的具体实现方式
- 环境弹窗匹配/缺失文件数的 UI 排列
- 底部状态栏布局
- 缺失文件创建按钮位置
- Tab 关闭行为和快捷键
- 撤销的状态管理方案
