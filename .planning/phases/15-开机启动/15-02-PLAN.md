---
phase: 15-开机启动
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/App.tsx
  - src/components/SettingsDialog.tsx
autonomous: true
requirements: [BOOT-01, BOOT-04]

must_haves:
  truths:
    - "用户可以在 SettingsDialog 托盘设置分区内看到并切换开机启动开关"
    - "开机启动开关在 closeToTray=false 时禁用且半透明"
    - "用户关闭 trayEnabled 时开机启动级联关闭 (per D-10)"
    - "开机启动开关状态持久化到 store，重启后保持"
    - "启用开机启动时调用 autostart.enable()，禁用时调用 autostart.disable()"
    - "前端收到 app:autostart-hidden 事件时调用 hideToTray() 同步状态"
  artifacts:
    - path: "src/components/SettingsDialog.tsx"
      provides: "开机启动 Switch 作为托盘设置分区第三个开关"
      contains: "autostartEnabled"
    - path: "src/App.tsx"
      provides: "autostartEnabled 状态管理、store 持久化、事件监听、级联逻辑"
      contains: "autostartEnabled"
  key_links:
    - from: "src/App.tsx"
      to: "@tauri-apps/plugin-autostart"
      via: "import enable/disable/isEnabled"
      pattern: "plugin-autostart"
    - from: "src/App.tsx"
      to: "app:autostart-hidden event"
      via: "listen callback -> hideToTray()"
      pattern: "app:autostart-hidden"
    - from: "src/components/SettingsDialog.tsx"
      to: "onAutostartEnabledChange prop"
      via: "Switch onCheckedChange"
      pattern: "onAutostartEnabledChange"
---

<objective>
前端 UI 集成——SettingsDialog 添加开机启动 Switch、App.tsx 添加状态管理/持久化/事件监听/级联关闭逻辑。

Purpose: 为用户提供开机启动的 UI 控制界面，并与已有的托盘设置形成正确的依赖关系（closeToTray 是 autostart 的前提，trayEnabled 关闭时级联关闭 autostart）。
Output: SettingsDialog 新增开机启动开关，App.tsx 完成状态管理集成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-开机启动/15-CONTEXT.md
@.planning/phases/15-开机启动/15-RESEARCH.md

<interfaces>
<!-- App.tsx 中与 autostart 集成相关的现有接口 -->

From src/App.tsx 现有状态管理模式:
- trayEnabled / setTrayEnabled (useState<boolean>)
- closeToTray / setCloseToTray (useState<boolean>)
- handleTrayEnabledChange: async (enabled: boolean) => void — 关闭时级联关闭 closeToTray
- handleCloseToTrayChange: async (enabled: boolean) => void
- loadTraySettings useEffect: 从 store 加载 trayEnabled, closeToTray, drawerEnabled

From src/components/SettingsDialog.tsx 现有接口:
- SettingsDialogProps: open, onOpenChange, trayEnabled, onTrayEnabledChange, closeToTray, onCloseToTrayChange, drawerEnabled, onDrawerEnabledChange
- 托盘设置分区使用 space-y-4 布局，Switch 依赖控制模式: cn("flex items-center justify-between", !trayEnabled && "opacity-50 pointer-events-none")

From @tauri-apps/plugin-autostart JS API:
- enable(): Promise<void> — 注册开机启动
- disable(): Promise<void> — 取消开机启动
- isEnabled(): Promise<boolean> — 检查是否已注册
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: App.tsx 添加 autostart 状态管理 + SettingsDialog 添加 Switch</name>
  <files>src/App.tsx, src/components/SettingsDialog.tsx</files>
  <read_first>
    - src/App.tsx (完整文件，理解状态管理、loadTraySettings、handleXxxChange 回调模式)
    - src/components/SettingsDialog.tsx (完整文件，理解 props 接口、Switch 依赖控制模式)
    - .planning/phases/15-开机启动/15-CONTEXT.md (D-09 ~ D-11: 前端相关决策)
    - .planning/phases/15-开机启动/15-RESEARCH.md (Pattern 4: 前端 JS API 调用模式)
  </read_first>
  <action>
    **前置: 安装前端 JS 绑定**
    在项目根目录执行 `pnpm add @tauri-apps/plugin-autostart`。

    **App.tsx 修改 (per D-09, D-10, D-11):**

    1. 添加 import: `import { enable as autostartEnable, disable as autostartDisable, isEnabled as autostartIsEnabled } from "@tauri-apps/plugin-autostart";`

    2. 添加状态: 在 `closeToTray` state 之后添加 `const [autostartEnabled, setAutostartEnabled] = useState(false);`

    3. 修改 loadTraySettings useEffect: 在加载 drawerEnabled 之后追加加载 autostartEnabled:
       - `const savedAutostart = await store.get<boolean>("autostartEnabled");`
       - `setAutostartEnabled(savedAutostart !== undefined && savedAutostart !== null ? savedAutostart : false);`
       - 注意: 不做前端自愈检查 (per D-08: 自愈逻辑全部在 Rust 端 setup 执行，前端不需要感知自愈逻辑)

    4. 添加 handleAutostartEnabledChange 回调:
       - `const handleAutostartEnabledChange = useCallback(async (enabled: boolean) => { ... }, [store]);`
       - 内部: setAutostartEnabled(enabled) -> await store?.set("autostartEnabled", enabled) -> if enabled: await autostartEnable() else: await autostartDisable()
       - enable/disable 调用包裹在 try-catch 中，catch 静默忽略 (不干扰用户)

    5. 修改 handleTrayEnabledChange 回联: 在已有的 `if (!enabled) { setCloseToTray(false); await store?.set("closeToTray", false); }` 块内追加:
       - `setAutostartEnabled(false);`
       - `await store?.set("autostartEnabled", false);`
       - `try { await autostartDisable(); } catch {}` (静默禁用注册表项)
       这实现了 D-10: 关闭 trayEnabled 时级联关闭 autostartEnabled。

    6. 添加 app:autostart-hidden 事件监听: 在已有的 `useEffect` (监听 main:shown-from-rust / main:hidden-from-rust) 中追加:
       - `const unlistenAutostartHidden = listen("app:autostart-hidden", () => { hideToTray(); });`
       - 在 return cleanup 中: `unlistenAutostartHidden.then((fn) => fn());`

    7. 传递 props 给 SettingsDialog: 追加 `autostartEnabled={autostartEnabled}` 和 `onAutostartEnabledChange={handleAutostartEnabledChange}` 和 `closeToTray={closeToTray}` (closeToTray 已传入，确认即可)。

    **SettingsDialog.tsx 修改 (per D-09, D-11):**

    1. 扩展 SettingsDialogProps 接口: 追加 `autostartEnabled: boolean;` 和 `onAutostartEnabledChange: (enabled: boolean) => void;`

    2. 解构 props 中追加 `autostartEnabled` 和 `onAutostartEnabledChange`。

    3. 在托盘设置分区的 space-y-4 div 中，在"关闭时隐藏到托盘"Switch 之后追加开机启动 Switch:
       - 外层 div 使用 cn("flex items-center justify-between", !closeToTray && "opacity-50 pointer-events-none") — 依赖 closeToTray 启用 (per D-11)
       - 标题: "开机启动"
       - 描述: "Windows 启动时自动运行 EasyPack 并最小化到系统托盘"
       - Switch: checked={autostartEnabled}, onCheckedChange={onAutostartEnabledChange}, disabled={!closeToTray}
  </action>
  <verify>
    <automated>cd "E:/git/EasyPack" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - App.tsx 包含 autostartEnabled 状态和 handleAutostartEnabledChange 回调
    - App.tsx loadTraySettings 中加载 autostartEnabled (自愈由 Rust 端处理，前端不感知 per D-08)
    - App.tsx handleTrayEnabledChange 在关闭 trayEnabled 时级联关闭 autostartEnabled + 调用 autostartDisable()
    - App.tsx 监听 app:autostart-hidden 事件并调用 hideToTray()
    - SettingsDialog.tsx 包含开机启动 Switch 作为托盘设置分区第三个开关
    - 开机启动 Switch 在 closeToTray=false 时禁用且半透明
    - TypeScript 编译通过，无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 -> autostart JS API | 前端调用 enable()/disable() 操作注册表 |
| store 持久化 | autostartEnabled 布尔值写入本地 JSON 文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | store JSON 文件 | accept | autostartEnabled 仅是布尔值，不含敏感信息，存储在用户 AppData 目录 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 编译通过
2. grep -c "autostartEnabled" src/App.tsx 不小于 5
3. grep -c "autostartEnabled" src/components/SettingsDialog.tsx 不小于 3
4. grep -c "app:autostart-hidden" src/App.tsx 等于 1 (事件监听)
5. grep -c "autostartDisable" src/App.tsx 不小于 2 (handleTrayEnabledChange 级联 + handleAutostartEnabledChange)
</verification>

<success_criteria>
- SettingsDialog 显示开机启动 Switch，位于"关闭时隐藏到托盘"之后
- 开机启动 Switch 依赖 closeToTray 状态（closeToTray=false 时禁用）
- 关闭 trayEnabled 时 autostartEnabled 级联关闭
- autostartEnabled 状态持久化到 store
- 收到 app:autostart-hidden 事件时前端同步 TRAY_HIDDEN 状态
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-开机启动/15-02-SUMMARY.md`
</output>
