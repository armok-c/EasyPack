---
phase: 12-系统托盘
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/TitleBar.tsx
  - src/components/SettingsDialog.tsx
  - src/components/ui/switch.tsx
  - src/App.tsx
  - src/hooks/useProject.ts
autonomous: false
requirements:
  - TRAY-02
  - TRAY-08

must_haves:
  truths:
    - "点击关闭按钮（TitleBar X）隐藏窗口到托盘，不退出程序"
    - "Alt+F4 也隐藏到托盘"
    - "最小化按钮正常最小化到任务栏"
    - "TitleBar 新增齿轮按钮，点击打开设置弹窗"
    - "设置弹窗包含两个开关：启用系统托盘 + 关闭时隐藏到托盘"
    - "关闭总开关立即消失托盘图标，关闭按钮恢复为退出"
    - "执行指令后自动记录到最近执行列表"
  artifacts:
    - path: "src/components/SettingsDialog.tsx"
      provides: "通用设置弹窗组件"
      min_lines: 60
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch 组件"
    - path: "src/components/TitleBar.tsx"
      provides: "新增齿轮按钮 + 关闭改为 hide"
    - path: "src/App.tsx"
      provides: "useTray + useVisibilityState + useRecentCommands 集成 + onCloseRequested 拦截"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useTray.ts"
      via: "useTray() hook 调用"
      pattern: "useTray"
    - from: "src/App.tsx"
      to: "src/hooks/useVisibilityState.ts"
      via: "useVisibilityState() hook 调用"
      pattern: "useVisibilityState"
    - from: "src/App.tsx"
      to: "src/hooks/useRecentCommands.ts"
      via: "useRecentCommands() hook 调用 + executeCommand 时 addRecentCommand"
      pattern: "useRecentCommands"
    - from: "src/App.tsx"
      to: "src/components/TitleBar.tsx"
      via: "onSettingsOpen prop + onCloseBehavior prop"
      pattern: "TitleBar"
    - from: "src/App.tsx"
      to: "src/components/SettingsDialog.tsx"
      via: "settingsOpen state + onOpenChange prop"
      pattern: "SettingsDialog"
    - from: "src/components/TitleBar.tsx"
      to: "@tauri-apps/api/window"
      via: "appWindow.hide() 替代 appWindow.close()"
      pattern: "appWindow\\.hide"
---

<objective>
UI 集成：TitleBar 改造 + SettingsDialog 设置弹窗 + App 集成全部托盘 Hook

Purpose: 将 Plan 01 创建的基础设施集成到 UI 中——修改 TitleBar 关闭行为、新增齿轮按钮和设置弹窗、在 App.tsx 中集成所有托盘 Hook 并设置 onCloseRequested 拦截。执行指令时自动记录到最近执行列表。

Output: 完整的系统托盘功能：托盘图标常驻、关闭/Alt+F4 隐藏到托盘、设置弹窗控制托盘行为、最近执行指令出现在托盘菜单。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-系统托盘/12-CONTEXT.md
@.planning/phases/12-系统托盘/12-UI-SPEC.md
@.planning/phases/12-系统托盘/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/hooks/useTray.ts:
```typescript
interface UseTrayOptions {
  currentProject: ProjectItem | null;
  commands: CommandItem[];
  recentCommands: RecentCommand[];
  visibility: VisibilityState;
  onExecute: (command: string) => void;
  onShow: () => void;
  onHide: () => void;
  onQuit: () => void;
  enabled: boolean;
}
export function useTray(options: UseTrayOptions): void;
```

From src/hooks/useVisibilityState.ts:
```typescript
type VisibilityState = "VISIBLE" | "TRAY_HIDDEN";
export function useVisibilityState(): {
  visibility: VisibilityState;
  hideToTray: () => void;
  showFromTray: () => void;
  setVisibility: (state: VisibilityState) => void;
  isVisible: boolean;
};
```

From src/hooks/useRecentCommands.ts:
```typescript
interface RecentCommand = { name: string; command: string };
export function useRecentCommands(options: { store: Store | null }): {
  recentCommands: RecentCommand[];
  addRecentCommand: (name: string, command: string) => Promise<void>;
};
```

<!-- Current TitleBar exports -->
From src/components/TitleBar.tsx:
```typescript
export function TitleBar() {
  // Currently no props — will need onSettingsOpen prop added
  // handleClose currently calls appWindow.close()
  // Window control buttons: minimize, maximize, close
}
```

<!-- Current App.tsx hooks usage -->
From src/App.tsx:
```typescript
function App() {
  const { ..., executeCommand, commands, currentProject, store } = useProject();
  useGlobalShortcuts({ commands, onExecute: executeCommand, enabled: !!currentProject, recording: isRecording });
  // TitleBar currently: <TitleBar />
  // Will become: <TitleBar onSettingsOpen={...} onCloseBehavior={...} />
}
```

<!-- Dialog pattern to follow -->
From src/components/CommandDialog.tsx:
```typescript
// Uses: Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter
// Content inside DialogContent with className="sm:max-w-[480px]"
// Body: <div className="py-4 space-y-4">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 shadcn Switch + 创建 SettingsDialog 组件 + 修改 TitleBar</name>
  <files>
    src/components/ui/switch.tsx,
    src/components/SettingsDialog.tsx,
    src/components/TitleBar.tsx
  </files>
  <read_first>
    src/components/TitleBar.tsx
    src/components/CommandDialog.tsx
    src/components/ui/dialog.tsx
    src/components/ui/button.tsx
    .planning/phases/12-系统托盘/12-UI-SPEC.md
    .planning/phases/12-系统托盘/12-CONTEXT.md
  </read_first>
  <action>
    **1. 安装 shadcn Switch 组件:**
    ```bash
    cd /e/git/EasyPack && npx shadcn@latest add switch
    ```
    这会创建 `src/components/ui/switch.tsx`。

    **2. 创建 src/components/SettingsDialog.tsx** — 通用设置弹窗（per D-14, D-15, UI-SPEC）:

    Props 接口:
    ```typescript
    interface SettingsDialogProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      trayEnabled: boolean;
      onTrayEnabledChange: (enabled: boolean) => void;
      closeToTray: boolean;
      onCloseToTrayChange: (enabled: boolean) => void;
    }
    ```

    组件结构（per UI-SPEC Component Inventory）:
    - Root: `<Dialog open={open} onOpenChange={onOpenChange}>`
    - `<DialogContent className="sm:max-w-[380px]">`
    - `<DialogHeader>` + `<DialogTitle className="text-lg font-semibold">` "设置"
    - Body: `<div className="py-4 space-y-6">`
    - Section "系统托盘":
      - `<Label>` "系统托盘" 作为 section 标题，底部有 `border-b border-white/10 pb-2 mb-4` 分隔
      - Switch row 1 — "启用系统托盘":
        ```
        <div className="flex items-center justify-between">
          <div>
            <p className="text-sm">启用系统托盘</p>
            <p className="text-xs text-muted-foreground">在系统托盘显示应用图标，关闭窗口后保持运行</p>
          </div>
          <Switch checked={trayEnabled} onCheckedChange={onTrayEnabledChange} />
        </div>
        ```
      - Switch row 2 — "关闭时隐藏到托盘"（依赖总开关）:
        ```
        <div className={cn("flex items-center justify-between", !trayEnabled && "opacity-50 pointer-events-none")}>
          <div>
            <p className="text-sm">关闭时隐藏到托盘</p>
            <p className="text-xs text-muted-foreground">点击关闭按钮时隐藏到托盘而不是退出程序</p>
          </div>
          <Switch checked={closeToTray} onCheckedChange={onCloseToTrayChange} disabled={!trayEnabled} />
        </div>
        ```
    - 无 DialogFooter（settings apply immediately via onChange）

    **3. 修改 src/components/TitleBar.tsx**:

    变更 1 — 添加 Props 接口:
    ```typescript
    interface TitleBarProps {
      onSettingsOpen: () => void;
      onCloseBehavior: "hide" | "close";  // "hide" = hide to tray, "close" = exit app
    }
    ```

    变更 2 — 组件签名改为接收 props:
    ```typescript
    export function TitleBar({ onSettingsOpen, onCloseBehavior }: TitleBarProps) {
    ```

    变更 3 — 修改 handleClose:
    ```typescript
    async function handleClose() {
      if (onCloseBehavior === "hide") {
        await appWindow.hide();
      } else {
        await appWindow.close();
      }
    }
    ```
    注意：handleClose 仍在组件内部定义，使用 props.onCloseBehavior 判断。

    变更 4 — 在窗口控制按钮组内、最小化按钮之前，添加齿轮按钮:
    ```typescript
    import { Settings } from "lucide-react";  // 新增导入
    ```
    在 `<div className="flex items-center h-full">` 内，minimize button 之前添加:
    ```tsx
    <button
      className="titlebar-button"
      onClick={onSettingsOpen}
      aria-label="设置"
    >
      <Settings className="w-[14px] h-[14px]" />
    </button>
    ```

    布局顺序: `[spacer] [Settings gear] [minimize] [maximize] [close]`
  </action>
  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit</automated>
  </verify>
  <done>
    - src/components/ui/switch.tsx 存在（shadcn Switch 组件）
    - SettingsDialog.tsx 包含 Dialog 结构，DialogTitle 为 "设置"
    - SettingsDialog.tsx 包含两个 Switch 行："启用系统托盘" 和 "关闭时隐藏到托盘"
    - "关闭时隐藏到托盘" 在总开关关闭时 disabled + opacity-50 + pointer-events-none
    - SettingsDialog 无 DialogFooter（即时生效）
    - TitleBar.tsx 新增 onSettingsOpen 和 onCloseBehavior props
    - TitleBar.tsx 包含 Settings 齿轮按钮（w-[14px] h-[14px]，aria-label="设置"）
    - TitleBar.tsx handleClose 根据 onCloseBehavior 调用 hide() 或 close()
    - TypeScript 编译通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SettingsDialog 设置弹窗 + TitleBar 齿轮按钮 + 关闭行为改造。需要人工验证 UI 渲染和交互是否正确。</what_building>
  <how-to-verify>
    1. 运行 `cd /e/git/EasyPack && pnpm tauri dev` 启动开发模式
    2. 检查 TitleBar 右侧按钮顺序：[齿轮] [最小化] [最大化] [关闭]
    3. 点击齿轮按钮，确认弹出设置弹窗
    4. 设置弹窗标题为 "设置"，包含 "系统托盘" section
    5. 两个 Switch 开关： "启用系统托盘" 和 "关闭时隐藏到托盘"
    6. 关闭 "启用系统托盘" 时，"关闭时隐藏到托盘" 变灰且不可点击
    7. 点击关闭按钮（X），确认窗口隐藏（如果 closeToTray=true）或退出（如果 closeToTray=false）
    8. 最小化按钮行为不变（最小化到任务栏）
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

<task type="auto">
  <name>Task 2: App.tsx 集成 — useTray + useVisibilityState + useRecentCommands + onCloseRequested + useProject 扩展</name>
  <files>
    src/App.tsx,
    src/hooks/useProject.ts
  </files>
  <read_first>
    src/App.tsx
    src/hooks/useProject.ts
    src/hooks/useTray.ts
    src/hooks/useVisibilityState.ts
    src/hooks/useRecentCommands.ts
    src/components/TitleBar.tsx
    src/components/SettingsDialog.tsx
    .planning/phases/12-系统托盘/12-CONTEXT.md
  </read_first>
  <action>
    **1. 修改 src/hooks/useProject.ts** — 扩展 executeCommand 以支持最近指令记录:

    在 return 语句之前添加 `store` 到返回值中（如果尚未导出）:
    ```typescript
    // 确保 store 被导出，供 useRecentCommands 使用
    store,  // Store | null
    ```

    **2. 修改 src/App.tsx** — 集成所有托盘 Hook:

    新增导入:
    ```typescript
    import { useState, useCallback } from "react";  // useState 已有，确认即可
    import { useVisibilityState } from "@/hooks/useVisibilityState";
    import { useRecentCommands } from "@/hooks/useRecentCommands";
    import { useTray } from "@/hooks/useTray";
    import { SettingsDialog } from "@/components/SettingsDialog";
    import { getCurrentWindow } from "@tauri-apps/api/window";
    ```

    新增 state:
    ```typescript
    // Phase 12: tray settings
    const [settingsOpen, setSettingsOpen] = useState(false);
    const [trayEnabled, setTrayEnabled] = useState(true);  // 默认启用
    const [closeToTray, setCloseToTray] = useState(true);   // 默认启用
    ```

    新增 Hook 调用（在 useGlobalShortcuts 之后）:
    ```typescript
    // Phase 12: window visibility state machine
    const { visibility, hideToTray, showFromTray } = useVisibilityState();

    // Phase 12: recent commands tracking
    const { recentCommands, addRecentCommand } = useRecentCommands({ store });

    // Phase 12: execute with recent command tracking
    const handleExecuteWithRecent = useCallback(async (shellCommand: string) => {
      await executeCommand(shellCommand);
      // 查找 command name 从 commands 列表
      const cmd = commands.find((c) => c.command === shellCommand);
      if (cmd) {
        await addRecentCommand(cmd.name, cmd.command);
      } else {
        await addRecentCommand(shellCommand, shellCommand);
      }
    }, [executeCommand, commands, addRecentCommand]);
    ```

    useTray 调用:
    ```typescript
    // Phase 12: system tray
    useTray({
      currentProject,
      commands,
      recentCommands,
      visibility,
      onExecute: handleExecuteWithRecent,
      onShow: () => { showFromTray(); appWindow.show(); appWindow.setFocus(); },
      onHide: () => { hideToTray(); appWindow.hide(); },
      onQuit: async () => { await appWindow.close(); },  // 真正退出
      enabled: trayEnabled,
    });
    ```

    onCloseRequested 拦截（per D-07）:
    ```typescript
    useEffect(() => {
      if (!closeToTray) return;
      const unlisten = appWindow.onCloseRequested(async (event) => {
        event.preventDefault();
        hideToTray();
        await appWindow.hide();
      });
      return () => { unlisten.then((fn) => fn()); };
    }, [closeToTray, hideToTray]);
    ```

    设置持久化（将 trayEnabled 和 closeToTray 保存到 store）:
    ```typescript
    // 初始化时从 store 恢复 tray 设置
    useEffect(() => {
      let mounted = true;
      async function loadTraySettings() {
        // store 来自 useProject，需要在 useProject 初始化后才能使用
        // 方案：在 useProject 返回的 store 上读取
        if (!store) return;
        const saved = await store.get<boolean>("trayEnabled");
        const savedCTT = await store.get<boolean>("closeToTray");
        if (mounted) {
          if (saved !== undefined && saved !== null) setTrayEnabled(saved);
          if (savedCTT !== undefined && savedCTT !== null) setCloseToTray(savedCTT);
        }
      }
      loadTraySettings();
      return () => { mounted = false; };
    }, [store]);

    // 设置变更时持久化
    const handleTrayEnabledChange = useCallback(async (enabled: boolean) => {
      setTrayEnabled(enabled);
      if (!enabled) {
        setCloseToTray(false);  // D-16: 关闭总开关时联动关闭 closeToTray
      }
      await store?.set("trayEnabled", enabled);
    }, [store]);

    const handleCloseToTrayChange = useCallback(async (enabled: boolean) => {
      setCloseToTray(enabled);
      await store?.set("closeToTray", enabled);
    }, [store]);
    ```

    TitleBar 渲染修改:
    ```tsx
    <TitleBar
      onSettingsOpen={() => setSettingsOpen(true)}
      onCloseBehavior={closeToTray ? "hide" : "close"}
    />
    ```

    SettingsDialog 渲染（在 Toaster 之前）:
    ```tsx
    <SettingsDialog
      open={settingsOpen}
      onOpenChange={setSettingsOpen}
      trayEnabled={trayEnabled}
      onTrayEnabledChange={handleTrayEnabledChange}
      closeToTray={closeToTray}
      onCloseToTrayChange={handleCloseToTrayChange}
    />
    ```

    **重要 — useGlobalShortcuts 的 onExecute 更新:**
    将 useGlobalShortcuts 的 `onExecute: executeCommand` 改为 `onExecute: handleExecuteWithRecent`，使全局快捷键执行的指令也记录到最近列表。

    **onCloseBehavior 逻辑（per D-05, D-07, D-08）:**
    - `closeToTray=true` 且 `trayEnabled=true` 时，TitleBar X 和 Alt+F4 都隐藏到托盘
    - `closeToTray=false` 或 `trayEnabled=false` 时，TitleBar X 直接调用 appWindow.close()（退出）
    - Alt+F4 被 onCloseRequested 拦截（仅 closeToTray=true 时拦截）
    - 真正退出仅通过托盘 "退出" 菜单项
  </action>
  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - App.tsx 导入并调用 useVisibilityState, useRecentCommands, useTray
    - App.tsx 包含 onCloseRequested 拦截（closeToTray=true 时拦截 Alt+F4）
    - App.tsx 包含 tray 设置持久化（store get/set trayEnabled + closeToTray）
    - TitleBar 接收 onSettingsOpen 和 onCloseBehavior props
    - SettingsDialog 渲染在 App 组件中
    - 全局快捷键 onExecute 使用 handleExecuteWithRecent 记录最近指令
    - handleTrayEnabledChange 在关闭总开关时联动 setCloseToTray(false)
    - useProject 导出 store
    - TypeScript 编译通过，所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings Dialog → store | 设置值写入 tauri-plugin-store，无敏感数据 |
| onCloseRequested → appWindow.hide() | 系统级窗口事件拦截，阻止默认关闭行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | I (Info Disclosure) | trayEnabled/closeToTray store values | accept | 仅布尔值，无敏感信息 |
| T-12-07 | T (Tampering) | onCloseRequested interception | accept | 拦截关闭是预期行为（D-05, D-07），用户可通过托盘菜单退出或关闭设置中关闭托盘 |
| T-12-08 | D (DoS) | Settings switch rapid toggle | accept | Store autoSave 已配置，频繁写入不会导致问题 |
</threat_model>

<verification>
1. `cd /e/git/EasyPack && npx tsc --noEmit` — TypeScript 编译无错误
2. `cd /e/git/EasyPack && npx vitest run` — 所有测试通过（含已有 118 个不回归）
3. 检查 App.tsx 包含 useTray, useVisibilityState, useRecentConnections 调用
4. 检查 App.tsx 包含 onCloseRequested useEffect
5. 检查 TitleBar 接收 onSettingsOpen prop
6. 检查 SettingsDialog 包含两个 Switch 行
</verification>

<success_criteria>
- TitleBar 关闭按钮根据 closeToTray 设置调用 hide() 或 close()
- Alt+F4 被 onCloseRequested 拦截（closeToTray=true 时）
- 齿轮按钮点击打开设置弹窗
- 设置弹窗两个开关控制托盘行为，关闭总开关联动关闭 closeToTray
- 设置持久化到 store，重启后恢复
- 托盘菜单执行指令自动记录到最近执行列表
- 全局快捷键执行也记录到最近列表
- 所有测试通过，TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/12-系统托盘/12-02-SUMMARY.md`
</output>
