---
phase: 24-file-management-editor
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01-PLAN.md
  - 24-02-PLAN.md
files_modified:
  - src/components/FileList.tsx
  - src/components/AddFileDialog.tsx
  - src/components/FileEditorDialog.tsx
  - src/components/MainArea.tsx
autonomous: true
requirements:
  - ENV-03
  - ENV-04
  - ENV-06
  - ENV-07
user_setup: []

must_haves:
  truths:
    - "文件列表显示勾选框、文件名+后缀、修改时间、查看按钮五列，顶部工具栏含文件计数+'添加文件'+'删除'按钮"
    - "点击'添加文件'打开系统文件选择对话框（含手动输入框、文件类型过滤下拉），添加后 toast 反馈"
    - "勾选文件后可点击'删除'，AlertDialog 二次确认后从环境移除"
    - "点击'查看'打开 80% 视口编辑器 Dialog，支持语法高亮、语法错误检测、底部状态栏"
    - "编辑器保存按钮将内容保存到环境副本（profileStore），toast '已保存'"
    - "MainArea 中 FileList 替换原'项目环境'+'打开文件夹'行，指令卡片布局不受影响"
  artifacts:
    - path: "src/components/AddFileDialog.tsx"
      provides: "添加文件对话框组件"
      uses: ["tauri-plugin-dialog.open()"]
    - path: "src/components/FileEditorDialog.tsx"
      provides: "文件编辑器模态窗"
      uses: ["useCodeMirror hook", "file-lang.ts"]
    - path: "src/components/FileList.tsx"
      provides: "文件列表组件（工具栏 + Table + 删除确认）"
      uses: ["shadcn/ui Table", "shadcn/ui AlertDialog"]
    - path: "src/components/MainArea.tsx"
      provides: "主区域集成"
      changes: "替换第 299-312 行的'项目环境'+'打开文件夹'行为 FileList 组件"
  key_links:
    - from: "AddFileDialog"
      to: "invoke('read_file_content')"
      via: "用户选择文件 → invoke('read_file_content', { projectPath, fileName }) → ManagedFile"
    - from: "FileEditorDialog"
      to: "useCodeMirror hook"
      via: "const { viewRef } = useCodeMirror(parentRef, { value, onChange, extensions })"
    - from: "FileEditorDialog"
      to: "file-lang.ts"
      via: "getLanguageExtension(fileName), getLinterExtensions(fileName)"
    - from: "FileList"
      to: "addFiles / deleteFiles / updateFileContent"
      via: "props.onAddFiles(envId, files), onDeleteFiles(envId, fileNames)"
    - from: "MainArea"
      to: "FileList"
      via: "<FileList files={currentEnv.files} envId={currentEnv.id} ... />"
---

<objective>
实现文件管理 UI 组件：FileList（列表）、AddFileDialog（添加对话框）、FileEditorDialog（编辑器模态窗），并集成到 MainArea。

Purpose: 完成 ENV-03/ENV-04/ENV-06/ENV-07 的全部功能——从文件添加到删除、列表查看、语法高亮编辑的完整流程。

Output:
- src/components/AddFileDialog.tsx — 文件选择对话框（tauri-plugin-dialog + 手动输入 + 类型过滤）
- src/components/FileEditorDialog.tsx — CodeMirror 6 编辑器模态窗（语法高亮 + lint + 保存）
- src/components/FileList.tsx — 文件列表组件（Table + 工具栏 + 删除确认）
- src/components/MainArea.tsx — 集成 FileList 到主区域，新增 props
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 24 所有成功标准
@.planning/REQUIREMENTS.md — ENV-03（添加文件）、ENV-04（删除文件）、ENV-06（文件列表）、ENV-07（文件查看编辑）
@.planning/phases/24-file-management-editor/24-CONTEXT.md — D-01~D-31（全部决策）
@.planning/phases/24-file-management-editor/24-RESEARCH.md — 数据流架构、文件添加流程、文件编辑流程、代码示例
@.planning/phases/24-file-management-editor/24-UI-SPEC.md — 完整组件设计、Copywriting Contract、交互模式、布局、间距、颜色

<interfaces>

From src/hooks/useProject.ts（Phase 24 新增，Plan 24-02）：
```typescript
addFiles: (projectId: string, envId: string, files: ManagedFile[]) => Promise<void>;
deleteFiles: (projectId: string, envId: string, fileNames: string[]) => Promise<void>;
updateFileContent: (projectId: string, envId: string, fileName: string, content: string) => Promise<void>;
```

From src/hooks/useCodeMirror.ts（Phase 24 新增，Plan 24-01）：
```typescript
interface UseCodeMirrorOptions {
  value: string;
  onChange: (value: string) => void;
  darkMode?: boolean;
  height?: string;
  extensions?: Extension[];
}
function useCodeMirror(
  parentRef: React.RefObject<HTMLDivElement | null>,
  options: UseCodeMirrorOptions,
): { viewRef: React.MutableRefObject<EditorView | null> };
```

From src/lib/file-lang.ts（Phase 24 新增，Plan 24-01）：
```typescript
function getLanguageExtension(fileName: string): Extension[];
function getLinterExtensions(fileName: string): Extension[];
function formatRelativeTime(timestamp: number): string;
```

From src/lib/types.ts（Phase 23）：
```typescript
interface ManagedFile {
  name: string;    // 相对路径
  content: string;
  addedAt: number;
}
```

From src/components/ManageEnvDialog.tsx — shadcn/ui Dialog + Table + AlertDialog 使用模式参考。
From src/components/MainArea.tsx — 第 299-312 行待替换区域。

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 AddFileDialog 组件</name>
  <files>
    - src/components/AddFileDialog.tsx
  </files>
  <read_first>
    - src/components/ManageEnvDialog.tsx（shadcn Dialog 模式参考）
    - src/hooks/useProject.ts（invoke import 模式参考）
    - src/lib/types.ts（ManagedFile 接口）
  </read_first>
  <action>
    创建 `src/components/AddFileDialog.tsx`：

    Props 接口 `AddFileDialogProps`：
    - `open: boolean`、`onOpenChange: (open: boolean) => void`
    - `projectPath: string` — 项目根目录路径，用于剥离绝对路径前缀（per D-06）
    - `existingFileNames: string[]` — 当前环境已有文件名列表，用于去重检查
    - `onConfirm: (files: ManagedFile[]) => Promise<void>` — 确认添加回调

    组件内部状态：
    - `loading: boolean` — 读取文件时的 loading
    - `selectedFilter: string` — 下拉过滤选中项（per D-09）

    UI 布局（per D-04, D-09, Claude's Discretion 按此排列）：

    1. Dialog header: 标题「添加配置文件」
    2. 文件类型过滤下拉（shadcn/ui Select）：
       - 选项：「所有支持的格式」「JSON (.json)」「YAML (.yaml/.yml)」「TOML (.toml)」「XML (.xml)」「INI (.conf/.ini/.cfg)」「环境文件 (.env)」「文本文件 (.txt/.md)」
       - 选择后更新 `selectedFilter` 状态
       - 仅影响文件选择对话框的 filters 参数（per D-09）
    3. 「选择文件」按钮（shadcn/ui Button），点击时调用 `tauri-plugin-dialog` 的 `open()` API：
       - `multiple: true`（per D-02）
       - `filters` 根据 `selectedFilter` 动态构建（per D-09, D-03）
       - `title: "选择配置文件"`
       - 返回路径数组 `string[] | null`
    4. 手动输入框（per D-04）：
       - shadcn/ui Input，placeholder：「输入文件相对路径」
       - 支持单条路径输入（如 `config/settings.json`）
       - 输入后按 Enter 或点击「添加」按钮确认
    5. 确认按钮：调用 `onConfirm` 前做以下处理：
       - 合并对话框选择路径 + 手动输入路径（去重）
       - 将绝对路径转换为相对路径（per D-06）：从 `projectPath` 中剥离前缀，保留子路径
         - 例如 `C:\project\config\settings.json` + projectPath=`C:\project` → `config\settings.json`
         - 使用 `path.replace(projectPath, '').replace(/^[\\/]+/, '')` 或类似逻辑
         - 注意正反斜杠处理（Windows 环境）
       - 对每个文件调用 `invoke<string>("read_file_content", { projectPath, fileName: relativePath })` 读取内容（per D-05）
       - 读取成功后构建 `ManagedFile` 对象：`{ name: relativePath, content, addedAt: Date.now() }`
       - 调用 `onConfirm(managedFiles)` 传回上层处理去重和持久化
       - 部分文件读取失败时（per D-08）：继续处理成功文件，loading 完成后调用 `onConfirm` 携带部分成功文件

    6. Loading 态：读取文件时按钮显示「读取中...」并 disabled

    使用 `sonner toast` 在组件内或由调用方反馈结果（per D-05: 「已添加 N 个文件」等在调用方处理更合理——FileList 或 MainArea 统一处理反馈）。
  </action>
  <verify>
    <automated>tsc --noEmit 无类型错误</automated>
  </verify>
  <done>
    - 文件存在，导出 `AddFileDialog` 组件和 `AddFileDialogProps` 接口
    - 有文件类型下拉过滤 Select
    - 有「选择文件」按钮调用 `open()` API（可验证引用）
    - 有手动输入框 Input
    - 确认时剥离 projectPath 前缀为相对路径
    - 确认时通过 `invoke("read_file_content")` 读取内容
    - 确认时调用 `onConfirm(files: ManagedFile[])` 回调
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 FileEditorDialog 组件</name>
  <files>
    - src/components/FileEditorDialog.tsx
  </files>
  <read_first>
    - src/hooks/useCodeMirror.ts（通用 hook，Plan 24-01）
    - src/lib/file-lang.ts（语言检测 + lint 工厂，Plan 24-01）
    - src/components/ScriptEditor.tsx（现有 CodeMirror 编辑器模式参考）
    - src/components/ManageEnvDialog.tsx（Dialog 模式参考）
    - .planning/phases/24-file-management-editor/24-UI-SPEC.md（编辑器交互模式完整描述）
  </read_first>
  <action>
    创建 `src/components/FileEditorDialog.tsx`：

    Props 接口 `FileEditorDialogProps`：
    - `open: boolean`、`onOpenChange: (open: boolean) => void`
    - `file: ManagedFile | null` — 当前编辑的文件（null = 关闭状态）
    - `onSave: (fileName: string, content: string) => Promise<void>` — 保存回调（调用 updateFileContent）

    组件内部状态：
    - `editingContent: string` — 编辑器当前内容（来自 file.content 初始值）
    - `isDirty: boolean` — 是否有未保存修改（per D-19）
    - `pendingSwitchFile: ManagedFile | null` — 有未保存修改时打开新文件，暂存目标文件（per D-20）

    UI 结构和交互模式（严格按 UI-SPEC 实现）：

    1. Dialog 尺寸：`DialogContent` 使用 `className="sm:max-w-[80vw] max-h-[80vh] h-[80vh] flex flex-col"`（per D-15）
       - 内部 flex 布局：标题栏 + 编辑器区域 + 底部状态栏 + 保存按钮

    2. 标题栏（per D-19, D-13）：
       - 左：未保存圆点指示器（`className="size-2 rounded-full bg-orange-400"`，仅 `isDirty` 时显示）
       - 文件名（`file.name` 提取基本名称和斜杠后的最后一段）
       - 分隔符「·」
       - 相对时间（调用 `formatRelativeTime(file.addedAt)`）
       - 分隔符「·」
       - 错误计数（如「3 个错误」），从 lint 状态获取 —— 通过传递 lint 诊断信息或使用状态管理
       - 右：Dialog 自带关闭按钮（X）

    3. 编辑器区域（per D-14, D-17）：
       - `div ref={parentRef}` 作为 useCodeMirror 的容器，flex-1 填充剩余高度
       - 调用 `useCodeMirror(parentRef, { value: editingContent, onChange: handleChange, darkMode: true, extensions })`
       - `extensions` 由 `getLanguageExtension(file.name)` + `getLinterExtensions(file.name)` 合并（per D-10, D-11）
       - 初始 `editingContent` 从 `file.content` 设置
       - 当 `file` prop 变化（打开新文件）时，重置 `editingContent` 和 `isDirty`

    4. 底部状态栏（per D-13）：
       - 左对齐：文件名 + 修改时间 + 错误计数（从 lint 诊断获取）
       - 右对齐：保存按钮（shadcn/ui Button, variant="default"）
       - 状态栏高度约 36px（`h-9`），使用 `border-t border-border` 分割

    5. 保存逻辑（per D-16）：
       - 点击保存按钮或按 Ctrl+S（per D-18）：调用 `onSave(file.name, editingContent)`
       - 保存后设置 `isDirty = false`
       - toast「已保存」（由 onSave 调用方或本组件触发）

    6. 未保存修改提示（per D-18, D-19, D-20）：
       - Escape 键关闭时若 `isDirty`，弹出提示 Dialog（保存 / 放弃 / 取消）
       - 使用三层 Dialog（shadcn/ui AlertDialog 或嵌套 Dialog）：
         - 标题：「文件未保存」
         - 描述：「当前文件有未保存的修改，是否先保存？」
         - 按钮：「保存」（AlertDialogAction, 先保存再关闭）、「放弃」（variant="destructive", 直接关闭）、「取消」（AlertDialogCancel, 不做操作）
       - 保存后 `onSave` 回调完成再关闭

    7. Ctrl+S 快捷键（per D-18）：
       - 在 Dialog 上监听 `onKeyDown` 事件：`e.key === "s" && (e.ctrlKey || e.metaKey)` 时触发保存
       - `e.preventDefault()` 阻止浏览器默认保存

    8. 同步外部 file 变化（per D-38）：
       - `useEffect` 监听 `file` prop 变化
       - 如果当前 `isDirty`（有未保存修改）+ 新 `file` 不同名 → 弹出保存提示（per D-20 单实例编辑器）
         - 「保存」：先执行 `onSave` 再更新 `editingContent` 为新文件内容
         - 「放弃」：直接更新为新文件内容，`isDirty` 重置
         - 「取消」：不切换文件
       - 清空状态：关闭 Dialog 时重置所有内部状态

    9. 监听 Dialog 关闭请求（per D-18 Escape）：
       - `onOpenChange` 中判断：若 `!open`（关闭请求）+ `isDirty` → 弹出保存提示
       - 阻止直接关闭，通过提示中的「放弃」或「保存」后真正关闭

    使用 `useCallback` 包裹事件处理函数。所有文字严格按 UI-SPEC Copywriting Contract。
  </action>
  <verify>
    <automated>tsc --noEmit 无类型错误</automated>
  </verify>
  <done>
    - 文件存在，导出 `FileEditorDialog` 组件和 `FileEditorDialogProps` 接口
    - Dialog 使用 `sm:max-w-[80vw]` 尺寸
    - 编辑器通过 `useCodeMirror` hook 实现
    - 扩展来自 `getLanguageExtension() + getLinterExtensions()`
    - Ctrl+S 触发保存逻辑
    - Escape + dirty → 保存/放弃/取消 提示 Dialog
    - 保存后 toast「已保存」
    - 未保存指示圆点仅在 dirty 时显示
    - 底部状态栏含文件名 + 相对时间 + 保存按钮
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 FileList 组件</name>
  <files>
    - src/components/FileList.tsx
  </files>
  <read_first>
    - src/components/ManageEnvDialog.tsx（Table 模式 + AlertDialog 模式参考）
    - src/lib/file-lang.ts（formatRelativeTime 工具）
    - .planning/phases/24-file-management-editor/24-UI-SPEC.md（交互模式完整描述）
  </read_first>
  <action>
    创建 `src/components/FileList.tsx`：

    Props 接口 `FileListProps`：
    - `envId: string` — 当前环境 ID
    - `files: ManagedFile[]` — 当前环境的文件列表
    - `projectPath: string` — 项目根路径（传递给 AddFileDialog 用于路径剥离）
    - `onAddFiles: (envId: string, files: ManagedFile[]) => Promise<void>`
    - `onDeleteFiles: (envId: string, fileNames: string[]) => Promise<void>`
    - `onUpdateFile: (envId: string, fileName: string, content: string) => Promise<void>`

    组件内部状态：
    - `selectedNames: Set<string>` — 勾选的文件名集合（per D-31 行点击切换勾选）
    - `addDialogOpen: boolean`
    - `editorDialogOpen: boolean`
    - `editingFile: ManagedFile | null` — 当前正在编辑的文件
    - `deleteDialogOpen: boolean`

    UI 结构（严格按 UI-SPEC）：

    1. 外部容器：`rounded-lg border` 包装，内部 flex flex-col

    2. 顶部工具栏（per D-25）：
       - Flex justify-between, items-center
       - 左：文件计数文字 `{files.length} 个文件`（text-xs text-muted-foreground）
       - 右：`添加文件` 按钮（Button variant="default" size="sm"）+ `删除` 按钮（Button variant="outline" size="sm"）
       - 删除按钮在 `selectedNames.size === 0` 时 `disabled` + `opacity-50 cursor-not-allowed`（per D-25）
       - 删除按钮 tooltip「请先勾选要删除的文件」（当 disabled 时）

    3. 文件列表 Table（per D-22, D-23, D-28, D-29, D-30, D-31）：
       - 使用 shadcn/ui Table 组件
       - 容器 `max-h-[300px] overflow-y-auto`（per D-28）
       - 五列：
         - 勾选框列（width `w-12`）：`<input type="checkbox">` 原生 checkbox，per UI-SPEC checkbox 推荐方案
         - 文件名列：`truncate` 截断 + `title` tooltip 显示完整相对路径（per D-30），显示基本名称（路径的最后一段）
         - 后缀列：从 `file.name` 提取扩展名（如 `.json`），使用 `text-xs text-muted-foreground`
         - 修改时间列：`formatRelativeTime(file.addedAt)`，`title` 属性显示绝对时间 `new Date(timestamp).toLocaleString()`（per D-27）
         - 操作列：`查看` 按钮（Button variant="outline" size="sm"）
       - TableHeader：列标题「文件名」「后缀」「修改时间」「操作」
       - 排序（per D-29）：`useMemo` 按 `file.name` 字母排序
       - 行交互（per D-31）：
         - 点击行（任意列，操作列除外）切换该行 checkbox
         - `hover:bg-accent`（CSS 类）
         - 操作列点击「查看」打开编辑器（不切换 checkbox）

    4. 空状态（per D-24）：
       - `files.length === 0` 时：显示居中文字「暂无文件，点击添加按钮添加配置文件」
       - `text-sm text-muted-foreground text-center`，py-8 垂直内边距

    5. 删除确认 AlertDialog（per D-26）：
       - 标题：「确认删除文件？」
       - 描述：「以下 {N} 个文件将被从当前环境移除，源文件不受影响。」
       - 文件列表：显示 `selectedNames` 中的文件名（逗号/项目符号分隔）
       - Footer：「取消」（AlertDialogCancel）+「删除」（AlertDialogAction, className `bg-destructive text-destructive-foreground`）
       - 确认后调用 `onDeleteFiles(envId, Array.from(selectedNames))`
       - 成功后清除 `selectedNames`

    6. 集成 AddFileDialog 和 FileEditorDialog：
       - 点击「添加文件」按钮 → 打开 AddFileDialog（`addDialogOpen = true`）
       - AddFileDialog 的 `onConfirm` 回调内调用 `onAddFiles(envId, files)`
         - 去重逻辑在 useProject.addFiles 内处理（per D-07）
         - 添加完成后 toast「已添加 N 个文件」
       - AddFileDialog 关闭后更新 `selectedNames`（不需要特别处理）
       - 点击「查看」按钮 → 设置 `editingFile = file; editorDialogOpen = true`
       - FileEditorDialog 的 `onSave` 回调内调用 `onUpdateFile(envId, fileName, content)`
         - 保存后 toast「已保存」

    7. 文件添加后的去重和部分失败 toast 处理（per D-07, D-08）：
       - AddFileDialog 已处理好读取和路径转换
       - 在 `onAddFiles` 调用时，useProject.addFiles 处理去重
       - 本组件中显示 toast 反馈：「已添加 N 个文件」或「N 个文件已存在，已跳过」

    使用不可变更新处理 `selectedNames`（创建新 Set）。
  </action>
  <verify>
    <automated>tsc --noEmit 无类型错误</automated>
  </verify>
  <done>
    - 文件存在，导出 `FileList` 组件和 `FileListProps` 接口
    - 工具栏含文件计数 +「添加文件」+「删除」按钮
    - 删除按钮在未勾选时 disabled
    - Table 五列：勾选框/文件名/后缀/修改时间/查看
    - 行点击切换勾选（查看按钮除外）
    - max-h-[300px] 滚动容器
    - 空状态居中引导文字
    - 删除使用 AlertDialog 确认
    - 集成 AddFileDialog 和 FileEditorDialog
    - 按文件名排序（useMemo）
  </done>
</task>

<task type="auto">
  <name>Task 4: 集成 FileList 到 MainArea + 更新 Props</name>
  <files>
    - src/components/MainArea.tsx
  </files>
  <read_first>
    - src/components/MainArea.tsx（当前完整文件，了解 Props 接口和 JSX 结构）
    - src/components/FileList.tsx（已创建的 FileList 组件）
    - .planning/phases/24-file-management-editor/24-UI-SPEC.md（Layout Placement 章节）
  </read_first>
  <action>
    修改 `src/components/MainArea.tsx`：

    1. 添加 import：
       ```
       import { FileList } from "@/components/FileList";
       import type { ManagedFile } from "@/lib/types";
       ```
       （`invoke` 不在本文件中直接使用，不需要导入）

    2. 扩展 `MainAreaProps` 接口（第 14-41 行），新增三个 props：
       ```
       // Phase 24: File management
       onAddFiles: (projectId: string, envId: string, files: ManagedFile[]) => Promise<void>;
       onDeleteFiles: (projectId: string, envId: string, fileNames: string[]) => Promise<void>;
       onUpdateFile: (projectId: string, envId: string, fileName: string, content: string) => Promise<void>;
       ```

    3. 更新函数解构参数（第 47-70 行），新增三个 props 解构（可放在 Phase 23 参数后面）：
       ```
       onAddFiles, onDeleteFiles, onUpdateFile,
       ```

    4. 替换第 299-312 行（`{/* 打开文件夹 button */}` 块）：
       - 原始代码块（div.flex.items-center.justify-between.mt-2）整体删除
       - 替换为：
         ```
         {/* Phase 24: 文件管理列表 */}
         {currentProject && selectedEnvId && (() => {
           const currentEnv = envs.find((e) => e.id === selectedEnvId);
           if (!currentEnv) return null;
           return (
             <FileList
               envId={currentEnv.id}
               files={currentEnv.files}
               projectPath={currentProject.path}
               onAddFiles={onAddFiles}
               onDeleteFiles={onDeleteFiles}
               onUpdateFile={onUpdateFile}
             />
           );
         })()}
         ```
       - 在 FileList 上方添加 `mt-2` 间距类使与 EnvTabBar 有适当间距

    5. 「打开文件夹」按钮的处理：
       - 原按钮从第 299-312 行移除
       - 在项目信息区域的路径显示位置（第 262 行 `currentProject.path` 附近）添加一个小尺寸「打开文件夹」按钮/链接
       - 推荐：在 `currentProject.path` 行末尾使用 `text-xs text-muted-foreground hover:text-foreground cursor-pointer` 样式的链接按钮
       - 图标使用 `FolderOpen size-3`，文字「打开文件夹」
       - 点击调用 `onOpenFolder` prop

    6. 注意：
       - `FolderOpen` 已导入（第 2 行），无需重复
       - `Button` 已导入（第 3 行），无需重复
       - `FileList` 组件从 `@/components/FileList` 导入
       - `ManagedFile` 类型从 `@/lib/types` 导入

    不做其他修改。EnvSwitchBar（279-286 行）、EnvTabBar（289-297 行）、ManageEnvDialog（316-326 行）、指令卡片网格（329-390 行）保持原样。
  </action>
  <verify>
    <automated>tsc --noEmit 无类型错误</automated>
  </verify>
  <done>
    - MainAreaProps 包含 `onAddFiles`, `onDeleteFiles`, `onUpdateFile` 三个新 props
    - 第 299-312 行的「项目环境」+「打开文件夹」按钮行已替换为 `<FileList>` 组件
    - FileList 接收正确的 props：`envId`, `files`, `projectPath`, 三个回调
    - 「打开文件夹」按钮已移至项目信息区域
    - EnvTabBar 和指令卡片网格之间（FileList 所在位置）布局正确
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddFileDialog dialog → file name | System file dialog returns user-selected file paths (absolute). Must convert to relative paths. |
| FileEditorDialog CodeMirror → rendered content | File content rendered in CodeMirror editor. Text rendering via CodeMirror DOM, no HTML evaluation. |
| FileList table → file name display | File names from profileStore displayed in Table cells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | T (Tampering) | AddFileDialog 路径转换 | mitigate | 使用 `projectPath` 剥离绝对路径前缀转换为相对路径。验证返回的路径字符串不为空，处理后不包含 `..` 路径遍历片段。 |
| T-24-07 | T (Tampering) | FileEditorDialog 内容存储 | mitigate | 编辑器内容直接保存到 profileStore，不写磁盘。不经 eval/execScript 处理文件内容——纯文本编辑。 |
| T-24-08 | S (Spoofing) | Dialog 返回值 | mitigate | `tauri-plugin-dialog.open()` 返回 `string[] | null`，处理 null（用户取消），验证返回值为字符串数组。 |
| T-24-09 | T (Tampering) | XSS via 文件名 | mitigate | React 默认对 JSX 文本内容进行 HTML 编码，不使用 `dangerouslySetInnerHTML`。文件名仅作为纯文本渲染。 |
| T-24-10 | I (Information Disclosure) | 文件内容泄露 | accept | 文件内容仅在前端内存中编辑和显示，不写入日志或发送网络请求。Tauri WebView 沙箱阻止跨域访问。 |
</threat_model>

<verification>
1. `tsc --noEmit` 通过
2. MainArea 渲染无报错
3. 文件列表显示在 EnvTabBar 下方、指令卡片上方
4. 添加文件对话框正常打开
5. 删除确认弹窗正常显示
6. 编辑器模态窗正常打开，语法高亮和 lint 生效
</verification>

<success_criteria>
- 「添加文件」按钮打开系统文件选择对话框，支持多选 + 手动输入
- 文件列表正确显示五列，hover 高亮，行点击切换勾选
- 勾选文件后「删除」按钮点亮，点击弹出确认弹窗
- 点击「查看」打开 CodeMirror 6 编辑器，语法高亮 + 错误检测
- Ctrl+S 保存到环境副本
- 有未保存修改时关闭或切换文件有提示
- MainArea 中布局正确：标签页 → 文件列表 → 指令卡片
</success_criteria>

<output>
After completion, create `.planning/phases/24-file-management-editor/24-03-SUMMARY.md`
</output>
