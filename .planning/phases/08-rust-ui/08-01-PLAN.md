---
phase: 08-rust-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/project_info.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - PROJ-07
  - PROJ-09
  - PROJ-10

must_haves:
  truths:
    - "Rust scan_project_icons 命令能扫描项目目录返回图标候选列表（package.json/Cargo.toml/常见图标文件名）"
    - "Rust get_project_info 命令能返回文件夹大小（排除大目录）和 Git 分支名"
    - ".git/HEAD 为 detached HEAD 时返回 None 分支"
    - "assetProtocol 在 tauri.conf.json 中启用，前端 convertFileSrc 能正常工作"
    - "两个新 Tauri 命令注册到 invoke_handler"
  artifacts:
    - path: "src-tauri/src/commands/project_info.rs"
      provides: "scan_project_icons + get_project_info 命令实现"
      exports: ["scan_project_icons", "get_project_info"]
    - path: "src-tauri/src/commands/mod.rs"
      provides: "project_info 模块注册"
      contains: "pub mod project_info"
    - path: "src-tauri/tauri.conf.json"
      provides: "assetProtocol 配置"
      contains: "assetProtocol"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "commands::project_info::scan_project_icons"
      via: "invoke_handler generate_handler!"
      pattern: "commands::project_info::"
    - from: "src-tauri/tauri.conf.json"
      to: "assetProtocol.enable"
      via: "JSON configuration"
      pattern: "assetProtocol"
---

<objective>
创建 Rust 后端的两个新 Tauri 命令（scan_project_icons 和 get_project_info），配置 assetProtocol 使前端能渲染本地文件图标，注册所有命令到 invoke_handler。

Purpose: 为前端提供项目信息检测的后端能力，这是 Phase 8 所有前端功能的数据基础。
Output: 编译通过的 Rust 后端，新增 2 个 Tauri 命令，assetProtocol 配置就绪。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rust-ui/08-CONTEXT.md
@.planning/phases/08-rust-ui/08-RESEARCH.md
@.planning/phases/08-rust-ui/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src-tauri/src/commands/mod.rs (current):
```rust
pub mod shell;
```

From src-tauri/src/lib.rs (current invoke_handler):
```rust
.invoke_handler(tauri::generate_handler![
    commands::shell::execute_command,
])
```

From src-tauri/Cargo.toml (current dependencies):
```toml
[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-store = "2.4.2"
```

From src-tauri/tauri.conf.json (current security config):
```json
"security": {
  "csp": null
}
```

From src/hooks/useProject.ts (ProjectItem interface):
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;    // lucide icon name or "file:/path/to/icon.png"
  color?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 scan_project_icons 和 get_project_info Rust 命令</name>
  <files>src-tauri/src/commands/project_info.rs</files>
  <read_first>
    - src-tauri/src/commands/shell.rs (现有 Tauri command 实现模式参考)
    - src-tauri/Cargo.toml (确认现有依赖)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Code Examples 部分有详细参考代码)
    - .planning/phases/08-rust-ui/08-CONTEXT.md (D-01 到 D-08 的锁定决策)
  </read_first>
  <behavior>
    - Test 1: scan_project_icons 对非空项目目录返回 Vec<IconCandidate>，candidates 中 path 为绝对路径
    - Test 2: scan_project_icons 对不存在的目录返回 Err
    - Test 3: get_project_info 返回人类可读的文件夹大小（如 "12.3 MB"）和 Git 分支名
    - Test 4: get_project_info 对非 Git 项目返回 branch: None
    - Test 5: get_project_info 对 detached HEAD (.git/HEAD 内容为 commit hash) 返回 branch: None
    - Test 6: calculate_dir_size 排除 EXCLUDED_DIRS 中的目录名
    - Test 7: format_size 正确格式化各种字节数（B, KB, MB, GB）
    - Test 8: read_git_branch 正确解析 "ref: refs/heads/main\n" 格式
  </behavior>
  <action>
    创建 `src-tauri/src/commands/project_info.rs`，包含以下内容（per D-01 到 D-08）：

    **1. IconCandidate 结构体 (per D-01):**
    ```rust
    #[derive(Serialize)]
    pub struct IconCandidate {
        pub path: String,       // 文件绝对路径
        pub name: String,       // 文件名（用于 UI 显示）
        pub source: String,     // 来源: "package.json" | "file"
    }
    ```

    **2. ProjectInfo 结构体 (per D-08):**
    ```rust
    #[derive(Serialize)]
    pub struct ProjectInfo {
        pub size: String,           // 人类可读格式，如 "12.3 MB"
        pub branch: Option<String>, // None = 非 Git 或 detached HEAD
    }
    ```

    **3. scan_project_icons 命令 (per D-01):**
    - 参数: `project_path: String`
    - 返回: `Result<Vec<IconCandidate>, String>`
    - 逻辑:
      a. 检查目录存在性，不存在返回 Err("项目目录不存在")
      b. 读取 package.json（用 serde_json 解析），检查 "icon" 和 "icons" 字段对应的文件是否存在
      c. 扫描项目根目录（不递归子目录，per CONTEXT specifics）中的常见图标文件名:
         favicon.ico, icon.ico, app-icon.ico, icon.png, logo.png, app-icon.png, icon.svg, logo.svg
      d. 去重：package.json 已指向的文件不再重复添加
      e. 返回候选列表

    **4. get_project_info 命令 (per D-04, D-05, D-07, D-08):**
    - 参数: `project_path: String`
    - 返回: `Result<ProjectInfo, String>`
    - 逻辑:
      a. calculate_dir_size: 手动递归（std::fs::read_dir），跳过 EXCLUDED_DIRS:
         node_modules, .git, target, dist, .next, .cache, build, __pycache__, .venv, .env, .tox, coverage, .terraform, vendor
         (per D-05 和 CONTEXT specifics)
      b. format_size: < 1KB → "xxx B", < 1MB → "xxx KB" (1位小数), < 1GB → "xxx MB" (1位小数), >= 1GB → "xxx GB" (1位小数)
      c. read_git_branch: 读取 `.git/HEAD` 文件，检查是否以 "ref: refs/heads/" 开头，
         是则提取分支名，否则返回 None（detached HEAD 或非 Git 仓库）
      d. 返回 ProjectInfo { size, branch }

    **5. 测试（#[cfg(test)] mod tests）:**
    在文件底部创建测试模块，使用 `tempfile` 或手动创建临时目录进行测试。
    由于不引入 tempfile crate，使用 std::fs 在 target/ 下创建临时测试目录，测试后清理。

    先写测试（RED），再实现函数体（GREEN）。
  </action>
  <verify>
    <automated>cd E:/git/EasyPack/src-tauri && cargo test -p easypack_lib -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `cargo test -p easypack_lib` 所有测试通过
    - `project_info.rs` 包含 `scan_project_icons` 和 `get_project_info` 两个 pub async fn
    - `IconCandidate` 结构体有 path, name, source 三个字段
    - `ProjectInfo` 结构体有 size (String) 和 branch (Option<String>) 两个字段
    - EXCLUDED_DIRS 包含 node_modules, .git, target, dist, .next, .cache, build, __pycache__, .venv, .env, .tox, coverage, .terraform, vendor
    - format_size 正确处理 B/KB/MB/GB 边界
    - read_git_branch 对 "ref: refs/heads/main\n" 返回 Some("main")
    - read_git_branch 对 commit hash 返回 None
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册命令模块并配置 assetProtocol</name>
  <files>src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs, src-tauri/tauri.conf.json, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/src/commands/mod.rs (当前模块注册)
    - src-tauri/src/lib.rs (当前 invoke_handler 注册)
    - src-tauri/tauri.conf.json (当前安全配置)
    - src-tauri/capabilities/default.json (当前权限列表)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Pitfall 1: assetProtocol 配置要求)
  </read_first>
  <action>
    **1. 修改 `src-tauri/src/commands/mod.rs`:**
    添加 `pub mod project_info;` 在 `pub mod shell;` 之后。

    **2. 修改 `src-tauri/src/lib.rs`:**
    在 `invoke_handler` 的 `generate_handler![]` 数组中添加两个新命令:
    ```rust
    commands::project_info::scan_project_icons,
    commands::project_info::get_project_info,
    ```

    **3. 修改 `src-tauri/tauri.conf.json`:**
    在 `app.security` 下添加 `assetProtocol` 配置（per RESEARCH Pitfall 1 和 Pattern 2）:
    ```json
    "security": {
      "csp": null,
      "assetProtocol": {
        "enable": true,
        "scope": {
          "allow": ["**"],
          "deny": []
        }
      }
    }
    ```
    scope 使用 `["**"]` 因为用户可选择任意路径的图标文件（个人工具，安全风险低，per RESEARCH Open Question 1）。

    **4. 修改 `src-tauri/capabilities/default.json`:**
    在 `permissions` 数组中添加 asset protocol 相关权限（如果 Tauri v2 需要的话）。
    注意：assetProtocol 在 tauri.conf.json 中启用即可，capabilities 权限主要用于 shell/dialog 等插件。
    检查是否需要添加 `core:asset:default` 或类似权限。如不需要则不添加。

    **5. 编译验证:**
    运行 `cargo build` 确认编译通过。
  </action>
  <verify>
    <automated>cd E:/git/EasyPack/src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src-tauri/src/commands/mod.rs` 包含 `pub mod project_info;`
    - `src-tauri/src/lib.rs` 的 invoke_handler 包含 `commands::project_info::scan_project_icons` 和 `commands::project_info::get_project_info`
    - `src-tauri/tauri.conf.json` 的 `app.security` 包含 `assetProtocol.enable: true`
    - `src-tauri/tauri.conf.json` 的 `assetProtocol.scope.allow` 为 `["**"]`
    - `cargo build` 编译成功无错误
    - `cargo test -p easypack_lib` 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust scan_project_icons | 用户传入的项目路径可能包含特殊字符或路径遍历 |
| Frontend -> Rust get_project_info | 用户传入的项目路径同上 |
| assetProtocol -> WebView | 本地文件系统通过 asset protocol 暴露给 WebView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | scan_project_icons project_path 参数 | accept | 路径通过 Tauri invoke 传入（非外部网络输入），用户操作自己的项目目录，攻击面极低 |
| T-08-02 | Information Disclosure | assetProtocol scope: ** | accept | 个人桌面工具，用户选择自己的图标文件。scope 限制为项目相关文件会增加使用复杂度，收益低 |
| T-08-03 | Tampering | get_project_info project_path 参数 | accept | 同 T-08-01，本地工具，非网络暴露接口 |
</threat_model>

<verification>
1. `cargo test -p easypack_lib` 所有测试通过
2. `cargo build` 编译成功
3. `grep "pub mod project_info" src-tauri/src/commands/mod.rs` 输出匹配
4. `grep "scan_project_icons" src-tauri/src/lib.rs` 输出匹配
5. `grep "assetProtocol" src-tauri/tauri.conf.json` 输出匹配
</verification>

<success_criteria>
- Rust 后端两个新 Tauri 命令可编译、测试通过、注册到 invoke_handler
- assetProtocol 在 tauri.conf.json 中启用
- 所有 cargo test 通过，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/08-rust-ui/08-01-SUMMARY.md`
</output>
