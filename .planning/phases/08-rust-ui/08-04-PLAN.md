---
phase: 08-rust-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/components/ProjectSettingsDialog.tsx
autonomous: false
requirements:
  - PROJ-07
  - PROJ-08

must_haves:
  truths:
    - "用户点击'从项目导入'按钮后，系统从项目目录扫描图标候选并展示可选列表"
    - "用户点击'选择文件'按钮后，文件选择对话框弹出并支持 .ico/.png/.svg 过滤"
    - "候选图标缩略图正确渲染（通过 convertFileSrc）"
    - "扫描中显示 skeleton pulse 加载动画"
    - "扫描失败显示错误提示"
    - "无候选显示空状态文本"
    - "选中文件图标后预览区域更新显示"
  artifacts:
    - path: "src/components/ProjectSettingsDialog.tsx"
      provides: "自定义图标导入和文件选择功能"
      contains: "scan_project_icons"
      contains: "从项目导入"
      contains: "选择文件"
  key_links:
    - from: "src/components/ProjectSettingsDialog.tsx"
      to: "scan_project_icons Tauri command"
      via: "invoke('scan_project_icons', { projectPath })"
      pattern: "invoke.*scan_project_icons"
    - from: "src/components/ProjectSettingsDialog.tsx"
      to: "tauri-plugin-dialog open()"
      via: "文件选择对话框"
      pattern: "open\\(.*filters"
    - from: "src/components/ProjectSettingsDialog.tsx"
      to: "src/lib/icons.ts"
      via: "import isFileIcon, getFilePath"
      pattern: "isFileIcon"
---

<objective>
扩展 ProjectSettingsDialog 支持自定义图标功能：从项目导入（扫描图标候选）+ 选择文件（系统文件选择器）。包含候选图标网格 UI、加载/错误/空状态处理（per D-01, D-02, D-03, PROJ-07, PROJ-08）。

Purpose: 用户可以为项目设置自定义图标，增强项目辨识度。这是 Phase 8 图标功能的对话框 UI 部分。
Output: 完整的自定义图标导入 UI 流程。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rust-ui/08-CONTEXT.md
@.planning/phases/08-rust-ui/08-UI-SPEC.md
@.planning/phases/08-rust-ui/08-01-SUMMARY.md
@.planning/phases/08-rust-ui/08-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (Rust backend commands) -->

Rust Tauri commands registered in invoke_handler:
```rust
// scan_project_icons: String -> Result<Vec<IconCandidate>, String>
// IconCandidate { path: String, name: String, source: String }
```

From src/lib/icons.ts (Plan 03 outputs):
```typescript
export function isFileIcon(icon: string): boolean;
export function getFilePath(icon: string): string;
```

From src/components/ProjectSettingsDialog.tsx (current icon picker, lines 86-113):
- Uses ICON_OPTIONS entries to render icon grid
- selectedIcon state tracks current selection
- Selection highlights with bg-white/15 border-primary ring-1 ring-primary/50

Frontend APIs:
```typescript
import { convertFileSrc } from "@tauri-apps/api/core";
import { open } from "@tauri-apps/plugin-dialog";
import { invoke } from "@tauri-apps/api/core";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ProjectSettingsDialog 支持自定义图标导入和文件选择</name>
  <files>src/components/ProjectSettingsDialog.tsx</files>
  <read_first>
    - src/components/ProjectSettingsDialog.tsx (当前完整实现)
    - .planning/phases/08-rust-ui/08-UI-SPEC.md (Interaction Specifications section 2: Icon Auto-Detection, Copywriting Contract)
    - .planning/phases/08-rust-ui/08-CONTEXT.md (D-01, D-02, D-03 锁定决策)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Pattern 2: convertFileSrc, Pattern 3: 图标类型判别)
    - src/lib/icons.ts (Plan 03 新增的 isFileIcon, getFilePath 函数)
  </read_first>
  <action>
    修改 `src/components/ProjectSettingsDialog.tsx`，在现有 lucide 图标选择器下方添加自定义图标区域。

    **1. 添加 import:**
    ```typescript
    import { invoke } from "@tauri-apps/api/core";
    import { convertFileSrc } from "@tauri-apps/api/core";
    import { open } from "@tauri-apps/plugin-dialog";
    import { FolderOpen, FileImage, Loader2 } from "lucide-react";
    import { isFileIcon, getFilePath } from "@/lib/icons";
    ```

    **2. 新增 IconCandidate 接口（匹配 Rust 返回类型）:**
    ```typescript
    interface IconCandidate {
      path: string;
      name: string;
      source: string;
    }
    ```

    **3. 新增状态变量（在组件内部，现有 useState 之后）:**
    ```typescript
    const [candidates, setCandidates] = useState<IconCandidate[]>([]);
    const [scanning, setScanning] = useState(false);
    const [scanError, setScanError] = useState<string | null>(null);
    ```

    **4. 新增扫描函数:**
    ```typescript
    const handleScanIcons = useCallback(async () => {
      if (!project) return;
      setScanning(true);
      setScanError(null);
      try {
        const result = await invoke<IconCandidate[]>("scan_project_icons", {
          projectPath: project.path,
        });
        setCandidates(result);
      } catch {
        setScanError("图标扫描失败，请重试");
      } finally {
        setScanning(false);
      }
    }, [project]);
    ```

    **5. 新增文件选择函数:**
    ```typescript
    const handleSelectFile = useCallback(async () => {
      try {
        const selected = await open({
          multiple: false,
          directory: false,
          title: "选择图标文件",
          filters: [{
            name: "图标文件",
            extensions: ["ico", "png", "svg"],
          }],
        });
        if (typeof selected === "string") {
          setSelectedIcon(`file:${selected}`);
        }
      } catch {
        // 用户取消选择，不做任何处理
      }
    }, []);
    ```

    **6. 在 icon picker div (`<div className="space-y-2">` 包含的图标网格) 之后、color picker 之前，添加自定义图标区域:**

    ```tsx
    {/* Custom icon section (per D-02, D-03) */}
    <div className="space-y-2">
      <Label>自定义图标</Label>
      <div className="flex gap-2">
        <Button
          variant="outline"
          size="sm"
          onClick={handleScanIcons}
          disabled={scanning}
          aria-label="从项目目录导入图标"
        >
          {scanning ? (
            <Loader2 className="size-3.5 mr-1 animate-spin" />
          ) : (
            <FileImage className="size-3.5 mr-1" />
          )}
          从项目导入
        </Button>
        <Button
          variant="outline"
          size="sm"
          onClick={handleSelectFile}
          aria-label="选择图标文件"
        >
          <FolderOpen className="size-3.5 mr-1" />
          选择文件
        </Button>
      </div>

      {/* Candidate icons from project scan */}
      {scanning && (
        <div className="flex gap-2">
          {[1, 2, 3].map((i) => (
            <div key={i} className="size-8 rounded-md bg-white/5 animate-pulse" />
          ))}
        </div>
      )}
      {!scanning && candidates.length > 0 && (
        <div
          className="flex flex-wrap gap-2"
          role="radiogroup"
          aria-label="扫描到的图标"
        >
          {candidates.map((candidate) => {
            const iconValue = `file:${candidate.path}`;
            const isSelected = selectedIcon === iconValue;
            return (
              <button
                key={candidate.path}
                type="button"
                role="radio"
                aria-checked={isSelected}
                aria-label={candidate.name}
                onClick={() => setSelectedIcon(iconValue)}
                className={cn(
                  "flex flex-col items-center gap-1 p-1.5 rounded-md",
                  "bg-white/5 border border-white/10",
                  "transition-all duration-150 ease-out",
                  "cursor-pointer outline-none",
                  "hover:bg-white/10 hover:border-white/20",
                  "focus-visible:ring-2 focus-visible:ring-ring",
                  isSelected && "bg-white/15 border-primary ring-1 ring-primary/50"
                )}
              >
                <img
                  src={convertFileSrc(candidate.path)}
                  alt=""
                  className="size-8 rounded-md object-cover"
                />
                <span className="text-xs text-muted-foreground truncate max-w-[32px]">
                  {candidate.name}
                </span>
              </button>
            );
          })}
        </div>
      )}
      {!scanning && scanError && (
        <p className="text-xs text-destructive">{scanError}</p>
      )}
      {!scanning && candidates.length === 0 && scanError === null && (
        <p className="text-xs text-muted-foreground">
          未找到可用图标
        </p>
      )}
    </div>
    ```

    **7. 修改预览部分（Preview section）的图标渲染:**
    当前代码（约 line 186）:
    ```tsx
    <PreviewIcon className="size-3.5 text-muted-foreground flex-shrink-0" />
    ```
    替换为:
    ```tsx
    {isFileIcon(selectedIcon) ? (
      <img
        src={convertFileSrc(getFilePath(selectedIcon))}
        alt=""
        className="size-3.5 flex-shrink-0 rounded-sm object-cover"
        onError={(e) => { e.currentTarget.style.display = "none"; }}
      />
    ) : (
      <PreviewIcon className="size-3.5 text-muted-foreground flex-shrink-0" />
    )}
    ```

    **8. 修改 reset 逻辑（useEffect 中）:**
    当对话框打开时，除了重置 selectedIcon 和 selectedColor，还需重置扫描状态:
    ```typescript
    setCandidates([]);
    setScanError(null);
    ```

    **注意事项:**
    - 按钮 copy per UI-SPEC Copywriting Contract: "从项目导入", "选择文件"
    - 候选图标缩略图 per UI-SPEC: 32px (size-8), rounded-md, border border-white/10, bg-white/5
    - 候选图标文件名 per UI-SPEC: text-xs truncate max-w-[32px], text-muted-foreground
    - 选中状态与 lucide 图标保持一致: bg-white/15 border-primary ring-1 ring-primary/50
    - Toast per UI-SPEC Copywriting: 成功时不特别 toast（保存按钮已给出反馈）
    - hasChanges 判断无需修改，selectedIcon 变化已经涵盖 "file:" 前缀变化
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - ProjectSettingsDialog 包含 "自定义图标" 区域
    - "从项目导入" 按钮调用 `invoke("scan_project_icons", ...)`
    - "选择文件" 按钮调用 `open()` 文件选择器，filters 包含 ico/png/svg
    - 候选图标网格使用 `convertFileSrc` 渲染缩略图
    - 选中文件图标后 selectedIcon 设为 "file:{path}" 格式
    - 预览区域根据 isFileIcon 判断渲染文件图标或 lucide 图标
    - 扫描中状态显示 skeleton pulse 动画
    - 扫描失败显示 "图标扫描失败，请重试"
    - 无候选显示 "未找到可用图标"
    - 所有 vitest 测试通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证自定义图标功能</name>
  <files>N/A (verification only)</files>
  <action>
    人工验证完整的自定义图标功能流程，包括图标扫描、文件选择、侧边栏渲染、DialogContent 滚动。等待用户确认通过或描述问题。
  </action>
  <read_first>
    - src/components/ProjectSettingsDialog.tsx (验证修改后的实现)
    - src/components/Sidebar.tsx (验证文件图标渲染)
    - src/components/ui/dialog.tsx (验证 DialogContent 滚动)
  </read_first>
  <what-built>
    完整的自定义图标功能:
    1. ProjectSettingsDialog 新增 "自定义图标" 区域，包含 "从项目导入" 和 "选择文件" 按钮
    2. "从项目导入" 扫描项目目录图标候选并展示缩略图网格
    3. "选择文件" 弹出系统文件选择器（.ico/.png/.svg）
    4. Sidebar 项目列表正确渲染文件图标（通过 convertFileSrc，Plan 03 实现）
    5. DialogContent 小窗口下内容可滚动（Plan 02 的修改）
  </what-built>
  <how-to-verify>
    1. 运行 `pnpm tauri dev` 启动应用
    2. 添加一个有 package.json 或图标文件的项目（如 EasyPack 自身）
    3. 右键项目 -> "设置图标和颜色" -> 打开设置对话框
    4. 验证对话框在窗口缩小时不截断内容（拖拽窗口底边缩小高度，检查内容是否可滚动）
    5. 在 "自定义图标" 区域点击 "从项目导入"
    6. 验证扫描结果展示缩略图网格（如果有图标文件）
    7. 点击一个候选图标 -> 验证预览更新
    8. 点击 "选择文件" -> 验证文件选择器弹出，过滤器为 .ico/.png/.svg
    9. 选择一个图标文件 -> 验证预览更新
    10. 点击 "保存设置" -> 验证侧边栏项目显示文件图标
    11. 关闭重新打开应用 -> 验证文件图标持久化并正确渲染
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust scan_project_icons | 通过 invoke 调用，参数为项目路径 |
| File picker -> Frontend | 系统文件选择器返回本地文件路径 |
| assetProtocol -> WebView | convertFileSrc 将文件路径转为 WebView 可加载 URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | 文件图标路径存储 | accept | "file:" 前缀图标路径存储在本地 JSON store 中，用户自己选择。不存在网络输入场景 |
| T-08-08 | Information Disclosure | convertFileSrc 暴露本地文件 | accept | 只暴露用户主动选择的图标文件，且 assetProtocol 已在 Plan 01 中配置 scope |
| T-08-09 | Spoofing | 恶意 SVG 文件 | accept | img 标签不执行脚本。convertFileSrc 返回的 URL 作为 img src 不会导致 XSS |

</threat_model>

<verification>
1. `npx vitest run` 所有测试通过
2. `grep "scan_project_icons" src/components/ProjectSettingsDialog.tsx` 输出匹配
3. `grep "从项目导入" src/components/ProjectSettingsDialog.tsx` 输出匹配
4. `grep "选择文件" src/components/ProjectSettingsDialog.tsx` 输出匹配
5. `grep "isFileIcon" src/components/ProjectSettingsDialog.tsx` 输出匹配
</verification>

<success_criteria>
- ProjectSettingsDialog 完整的自定义图标 UI（扫描 + 文件选择）
- Sidebar 正确渲染文件图标
- DialogContent 小窗口可滚动
- 所有测试通过，人工验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-rust-ui/08-04-SUMMARY.md`
</output>
