---
phase: 08-rust-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/lib/icons.ts
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - PROJ-08

must_haves:
  truths:
    - "icons.ts 提供 isFileIcon 和 getFilePath 工具函数，能正确区分文件图标和 lucide 图标"
    - "Sidebar 项目列表能根据图标类型分别渲染文件图标和 lucide 图标"
    - "文件图标加载失败时隐藏图片元素（fallback 行为）"
  artifacts:
    - path: "src/lib/icons.ts"
      provides: "isFileIcon / getFilePath 图标类型判别函数"
      contains: "isFileIcon"
      contains: "getFilePath"
    - path: "src/components/Sidebar.tsx"
      provides: "文件图标渲染支持"
      contains: "convertFileSrc"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/lib/icons.ts"
      via: "import isFileIcon, getFilePath"
      pattern: "isFileIcon.*getFilePath"
    - from: "src/components/Sidebar.tsx"
      to: "convertFileSrc"
      via: "asset protocol URL 转换"
      pattern: "convertFileSrc"
---

<objective>
在 icons.ts 中添加图标类型判别工具函数（isFileIcon / getFilePath），修改 Sidebar.tsx 的图标渲染逻辑支持文件图标显示（per D-02, PROJ-08）。

Purpose: 建立图标类型判别基础能力，使侧边栏能正确渲染自定义文件图标。这是图标功能的前端基础设施。
Output: icons.ts 新增工具函数 + Sidebar 文件图标渲染。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rust-ui/08-CONTEXT.md
@.planning/phases/08-rust-ui/08-UI-SPEC.md
@.planning/phases/08-rust-ui/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (Rust backend commands) -->

Rust Tauri commands registered in invoke_handler:
```rust
// scan_project_icons: String -> Result<Vec<IconCandidate>, String>
// IconCandidate { path: String, name: String, source: String }

// get_project_info: String -> Result<ProjectInfo, String>
// ProjectInfo { size: String, branch: Option<String> }
```

From src/hooks/useProject.ts (ProjectItem.icon type):
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;    // lucide icon name OR "file:/path/to/icon.png"
  color?: string;
}
```

From src/lib/icons.ts (current):
```typescript
export const ICON_OPTIONS: Record<string, LucideIcon> = {
  Terminal, Code, Server, Zap, GitBranch, Package, Globe, Wrench, Rocket, Play,
};
export const DEFAULT_ICON = "Terminal";
export function getIconByName(name: string): LucideIcon {
  return ICON_OPTIONS[name] ?? Terminal;
}
```

From src/components/Sidebar.tsx (current icon rendering, lines 91-94):
```tsx
{project.icon && (() => {
  const ProjectIcon = getIconByName(project.icon);
  return <ProjectIcon className="size-3.5 mr-1.5 flex-shrink-0 text-muted-foreground" />;
})()}
```

Frontend API for file icon:
```typescript
import { convertFileSrc } from "@tauri-apps/api/core";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 icons.ts 添加图标类型判别工具函数并编写测试</name>
  <files>src/lib/icons.ts, src/lib/__tests__/icons.test.ts</files>
  <read_first>
    - src/lib/icons.ts (当前完整实现)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Pattern 3: 图标类型判别)
    - .planning/phases/08-rust-ui/08-UI-SPEC.md (Icon Type Discrimination section)
  </read_first>
  <behavior>
    - Test 1: isFileIcon("file:C:/Projects/app/icon.png") 返回 true
    - Test 2: isFileIcon("Terminal") 返回 false
    - Test 3: isFileIcon("file:") 返回 true（空路径仍为 file 类型）
    - Test 4: isFileIcon("") 返回 false
    - Test 5: getFilePath("file:C:/Projects/app/icon.png") 返回 "C:/Projects/app/icon.png"
    - Test 6: getFilePath("file:") 返回 ""（空路径）
    - Test 7: getIconByName("file:C:/test.png") 返回 Terminal（fallback，不将文件路径当作 lucide name 查询）
    - Test 8: getIconByName("Terminal") 仍返回 Terminal 组件（回归）
    - Test 9: getIconByName("Code") 仍返回 Code 组件（回归）
  </behavior>
  <action>
    先创建测试文件 `src/lib/__tests__/icons.test.ts`，编写上述 9 个测试用例（RED）。

    然后修改 `src/lib/icons.ts` 实现功能使测试通过（GREEN）:

    在文件末尾添加两个导出函数:

    ```typescript
    /**
     * 判断图标值是否为文件路径类型（"file:" 前缀）
     * per UI-SPEC Icon Type Discrimination
     */
    export function isFileIcon(icon: string): boolean {
      return icon.startsWith("file:");
    }

    /**
     * 从文件图标值中提取文件路径（去掉 "file:" 前缀）
     * per UI-SPEC Icon Type Discrimination
     */
    export function getFilePath(icon: string): string {
      return icon.slice(5);
    }
    ```

    同时修改 `getIconByName` 函数，如果传入的是 "file:" 前缀的图标，直接返回 Terminal（fallback），防止将文件路径当作 lucide name 查询:
    ```typescript
    export function getIconByName(name: string): LucideIcon {
      if (isFileIcon(name)) return Terminal; // fallback for file icons
      return ICON_OPTIONS[name] ?? Terminal;
    }
    ```
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run src/lib/__tests__/icons.test --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/lib/icons.ts` 导出 `isFileIcon` 函数
    - `src/lib/icons.ts` 导出 `getFilePath` 函数
    - `getIconByName` 对 "file:" 前缀输入返回 Terminal（fallback）
    - `src/lib/__tests__/icons.test.ts` 包含 9 个测试用例且全部通过
    - 所有现有测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 Sidebar.tsx 支持文件图标渲染</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (当前 SortableProjectItem 中的图标渲染逻辑，lines 91-94)
    - src/lib/icons.ts (新增的 isFileIcon, getFilePath 函数)
    - .planning/phases/08-rust-ui/08-UI-SPEC.md (Section 3: Sidebar File Icon Rendering)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Pattern 2: convertFileSrc)
  </read_first>
  <action>
    修改 `src/components/Sidebar.tsx` 的 SortableProjectItem 组件中的图标渲染逻辑。

    **添加 import:**
    在文件顶部添加:
    ```typescript
    import { convertFileSrc } from "@tauri-apps/api/core";
    import { isFileIcon, getFilePath } from "@/lib/icons";
    ```

    **替换图标渲染部分（当前 lines 91-94）:**
    当前代码:
    ```tsx
    {project.icon && (() => {
      const ProjectIcon = getIconByName(project.icon);
      return <ProjectIcon className="size-3.5 mr-1.5 flex-shrink-0 text-muted-foreground" />;
    })()}
    ```

    替换为:
    ```tsx
    {project.icon && (isFileIcon(project.icon) ? (
      <img
        src={convertFileSrc(getFilePath(project.icon))}
        alt=""
        className="size-3.5 mr-1.5 flex-shrink-0 rounded-sm object-cover"
        onError={(e) => {
          e.currentTarget.style.display = "none";
        }}
      />
    ) : (() => {
      const ProjectIcon = getIconByName(project.icon);
      return <ProjectIcon className="size-3.5 mr-1.5 flex-shrink-0 text-muted-foreground" />;
    })())}
    ```

    渲染规格 (per UI-SPEC Section 3):
    - 文件图标: `<img>` 标签，size-3.5 (14px)，rounded-sm，object-cover，flex-shrink-0
    - 使用 `convertFileSrc` 转换本地文件路径为 asset protocol URL
    - onError fallback: 隐藏图片元素（不显示占位，per UI-SPEC Fallback Chain）
    - lucide 图标保持现有渲染逻辑不变

    **不要修改其他部分的逻辑。** 只修改图标渲染这一处。
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/components/Sidebar.tsx` 导入 `convertFileSrc`, `isFileIcon`, `getFilePath`
    - 图标渲染逻辑根据 `isFileIcon` 分别处理文件图标和 lucide 图标
    - 文件图标使用 `<img>` + `convertFileSrc` 渲染，尺寸 size-3.5
    - 文件图标有 onError 处理隐藏失败图片
    - lucide 图标保持原有渲染方式不变
    - 所有 vitest 测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assetProtocol -> WebView | convertFileSrc 将文件路径转为 WebView 可加载 URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | convertFileSrc 暴露本地文件 | accept | 只暴露用户主动选择的图标文件，且 assetProtocol 已在 Plan 01 中配置 scope |
| T-08-06 | Spoofing | 恶意 SVG 文件 | accept | img 标签不执行脚本。convertFileSrc 返回的 URL 作为 img src 不会导致 XSS |

</threat_model>

<verification>
1. `npx vitest run` 所有测试通过
2. `grep "isFileIcon" src/lib/icons.ts` 输出匹配
3. `grep "getFilePath" src/lib/icons.ts` 输出匹配
4. `grep "convertFileSrc" src/components/Sidebar.tsx` 输出匹配
5. `grep "isFileIcon" src/components/Sidebar.tsx` 输出匹配
6. `ls src/lib/__tests__/icons.test.ts` 测试文件存在
</verification>

<success_criteria>
- icons.ts 提供 isFileIcon 和 getFilePath 工具函数，测试覆盖
- Sidebar 正确渲染文件图标（通过 convertFileSrc）
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-rust-ui/08-03-SUMMARY.md`
</output>
