---
phase: 03-command-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/components/__tests__/CommandCard.test.tsx
  - src/components/__tests__/MainArea.test.tsx
  - src/index.css
  - src/components/CommandCard.tsx
  - src/components/MainArea.tsx
autonomous: false
requirements: [CMD-01, CMD-02, CMD-03, CMD-08, UI-01, UI-05]

must_haves:
  truths:
    - "选中项目后，右侧主区域展示 4 个预设指令卡片（打包项目、启动项目、Git Pull、启动 Claude）"
    - "点击指令卡片后，卡片有 400ms 闪光动效（边框高亮 + 缩放回弹），同时图标旋转"
    - "未选中项目时，主区域显示空状态引导页，不渲染任何指令卡片"
    - "卡片网格使用 CSS Grid auto-fill + minmax(140px, 1fr) 自适应列数"
    - "600px 最小窗口宽度时至少显示 2 列卡片"
  artifacts:
    - path: "src/index.css"
      provides: "card-flash @keyframes 动画定义 + --animate-card-flash 主题变量"
      contains: "@keyframes card-flash"
    - path: "src/components/CommandCard.tsx"
      provides: "带执行反馈动效的指令卡片组件"
      contains: "flashing"
    - path: "src/components/MainArea.tsx"
      provides: "自适应网格主区域布局"
      contains: "auto-fill"
    - path: "vitest.config.ts"
      provides: "Vitest 测试配置"
      contains: "jsdom"
  key_links:
    - from: "src/components/MainArea.tsx"
      to: "src/components/CommandCard.tsx"
      via: "PRESET_COMMANDS.map -> <CommandCard>"
      pattern: "PRESET_COMMANDS\\.map"
    - from: "src/components/CommandCard.tsx"
      to: "src/index.css"
      via: "animate-card-flash CSS class"
      pattern: "animate-card-flash"
    - from: "src/components/CommandCard.tsx"
      to: "src/hooks/useProject.ts"
      via: "onClick -> onExecute -> executeCommand"
      pattern: "onExecute"
---

<objective>
Phase 3 核心目标：完成"选中项目 -> 点击卡片 -> 终端执行"核心循环的 UI 交互增强。

Purpose: 将 Phase 1 的基础骨架（固定 2 列网格 + 简单 hover/active）升级为完整的交互体验——自适应网格布局、执行反馈闪光动效、完整的交互状态层次。所有改动限于 3 个文件 + 测试基础设施，不引入新依赖。

Output:
  - src/index.css: 新增 card-flash @keyframes 和 --animate-card-flash 主题变量
  - src/components/CommandCard.tsx: 新增 flashing 状态驱动的执行反馈动效
  - src/components/MainArea.tsx: 网格从 grid-cols-2 升级为 auto-fill + minmax 自适应
  - vitest.config.ts + 测试文件: Wave 0 测试基础设施
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-command-cards/03-CONTEXT.md
@.planning/phases/03-command-cards/03-RESEARCH.md
@.planning/phases/03-command-cards/03-UI-SPEC.md
@.planning/phases/03-command-cards/03-VALIDATION.md
@.planning/phases/02-sidebar-persistence/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/presets.ts:
```typescript
export interface PresetCommand {
  name: string;
  command: string;
  icon: LucideIcon;
}

export const PRESET_COMMANDS: PresetCommand[] = [
  { name: "打包项目", command: "npm run build", icon: Package },
  { name: "启动项目", command: "npm run dev", icon: Play },
  { name: "Git Pull", command: "git pull", icon: GitPullRequest },
  { name: "启动 Claude", command: "claude", icon: Sparkles },
];
```

From src/hooks/useProject.ts:
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
}

// executeCommand signature:
const executeCommand = useCallback(
  async (shellCommand: string) => { ... },
  [currentProject]
);
```

From src/components/MainArea.tsx (current):
```typescript
interface MainAreaProps {
  currentProject: ProjectItem | null;
  onExecute: (command: string) => void;
}
// Grid: grid-cols-2 gap-3 (TO BE CHANGED to auto-fill)
```

From src/components/CommandCard.tsx (current):
```typescript
interface CommandCardProps {
  name: string;
  icon: LucideIcon;
  disabled?: boolean;
  onClick?: () => void;
}
// No flashing state yet, no command prop for tooltip
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 测试基础设施 + CSS 动画定义 + 组件改造</name>
  <files>
    vitest.config.ts,
    src/components/__tests__/CommandCard.test.tsx,
    src/components/__tests__/MainArea.test.tsx,
    src/index.css,
    src/components/CommandCard.tsx,
    src/components/MainArea.tsx
  </files>
  <read_first>
    src/index.css
    src/components/CommandCard.tsx
    src/components/MainArea.tsx
    src/lib/presets.ts
    src/hooks/useProject.ts
    .planning/phases/03-command-cards/03-UI-SPEC.md
    .planning/phases/03-command-cards/03-RESEARCH.md
  </read_first>
  <action>
    **Part A: 测试基础设施 (Wave 0)**

    1. 安装测试依赖:
       ```
       npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
       ```

    2. 创建 `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import path from 'path';

       export default defineConfig({
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: [],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```

    3. 创建 `src/components/__tests__/CommandCard.test.tsx` -- 测试用例:
       - 渲染卡片显示 name 和 icon (使用 Package from lucide-react)
       - 点击未禁用卡片触发 onClick callback
       - disabled=true 时点击不触发 onClick
       - 点击后卡片进入 flashing 状态 (按钮元素上有 animate-card-flash class)
       - flashing 为 true 时再次点击不重复触发 onClick (防止连点)
       - flashing 为 true 时图标有 animate-spin class
       - disabled 时按钮有 opacity-40 和 cursor-not-allowed class

    4. 创建 `src/components/__tests__/MainArea.test.tsx` -- 测试用例:
       - currentProject=null 时渲染空状态引导页 ("选择一个项目开始" 文字)
       - currentProject=null 时不渲染任何 CommandCard
       - currentProject 有值时渲染 4 个 CommandCard (PRESET_COMMANDS.length === 4)
       - currentProject 有值时显示项目名称和路径
       - 网格容器使用 auto-fill + minmax 样式 (检查 className 包含 "auto-fill" 和 "minmax")

    注意: CommandCard 测试中，因为组件使用 useState 管理内部 flashing 状态，测试需要通过 act() + setTimeout mock 来验证。使用 vi.useFakeTimers() 控制时间推进。

    **Part B: CSS @keyframes 动画 (per D-05, D-06, D-07)**

    5. 修改 `src/index.css`:
       - 在 `@theme inline { }` 块内（最后一个变量之后）添加:
         ```css
         --animate-card-flash: card-flash 400ms ease-out;
         ```
       - 在 `@theme inline { }` 块外部（同一文件中 body 规则之后）添加:
         ```css
         @keyframes card-flash {
           0% {
             border-color: rgba(255, 255, 255, 0.3);
             box-shadow: 0 0 12px rgba(255, 255, 255, 0.1);
             transform: scale(0.97);
           }
           50% {
             border-color: rgba(255, 255, 255, 0.5);
             box-shadow: 0 0 20px rgba(255, 255, 255, 0.15);
           }
           100% {
             border-color: rgba(255, 255, 255, 0.1);
             box-shadow: none;
             transform: scale(1);
           }
         }
         ```
       - 注意: `--animate-card-flash` 必须在 `@theme inline { }` 内部, `@keyframes card-flash` 必须在该块外部。顺序不能颠倒。

    **Part C: CommandCard 执行反馈动效 (per D-05, D-06, D-07)**

    6. 修改 `src/components/CommandCard.tsx`:
       - 添加 import: `import { useState, useCallback } from "react";` (替换现有 import)
       - 在 CommandCardProps interface 中新增 `command?: string;` 属性 (用于 title tooltip)
       - 在组件函数内新增 state: `const [flashing, setFlashing] = useState(false);`
       - 添加 handleClick 函数:
         ```typescript
         const handleClick = useCallback(() => {
           if (disabled || flashing) return; // 防止禁用和连点
           setFlashing(true);
           onClick?.();
           setTimeout(() => setFlashing(false), 420); // 400ms 动画 + 20ms buffer
         }, [disabled, flashing, onClick]);
         ```
       - 修改 `<button>` 属性:
         - onClick 从 `onClick` 改为 `handleClick`
         - 添加 title 属性: `title={disabled ? undefined : command}` (per UI-SPEC Tooltip Strategy)
         - 添加 `motion-reduce:animate-none` class (per UI-SPEC Accessibility reduced-motion support)
       - 修改 className 逻辑:
         - 基础 transition 仅在非 flashing 时生效: `!flashing && "transition-all duration-150 ease-out"`
         - flashing 时添加 animate-card-flash: `flashing && "animate-card-flash"`
         - hover/active 仅在非禁用且非 flashing 时生效: `!disabled && !flashing && [...]` (保持现有 hover/active 样式不变)
         - disabled 状态保持不变
       - 修改 `<Icon>` className: `<Icon className={cn("size-6", flashing && "animate-spin")} />`
       - 解构 props 时添加 command: `{ name, icon: Icon, disabled = false, onClick, command }`
       - 完整组件结构参考 RESEARCH.md Pattern 2 代码示例

    **Part D: MainArea 自适应网格 (per D-08, D-09, D-10)**

    7. 修改 `src/components/MainArea.tsx`:
       - 将网格 className 从 `"grid grid-cols-2 gap-3"` 改为:
         `"grid grid-cols-[repeat(auto-fill,_minmax(140px,_1fr))] gap-3"`
       - 在 `<CommandCard>` 调用中添加 `command={cmd.command}` prop (用于 tooltip)
       - 空状态和项目信息区域保持不变
       - 注意: Tailwind v4 arbitrary value 中逗号后空格必须用下划线 `_` 替代
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "jsdom" E:/git/EasyPack/vitest.config.ts` 返回至少 1
    - `grep -c "@keyframes card-flash" E:/git/EasyPack/src/index.css` 返回 1
    - `grep -c "animate-card-flash" E:/git/EasyPack/src/index.css` 返回至少 1
    - `grep -c "flashing" E:/git/EasyPack/src/components/CommandCard.tsx` 返回至少 3
    - `grep "auto-fill" E:/git/EasyPack/src/components/MainArea.tsx` 有匹配
    - `grep "minmax" E:/git/EasyPack/src/components/MainArea.tsx` 有匹配
    - `grep "command=" E:/git/EasyPack/src/components/MainArea.tsx` 有匹配
    - `npx vitest run` exit code 0
  </acceptance_criteria>
  <done>
    - vitest.config.ts 存在，包含 environment: 'jsdom' 和 @ alias
    - src/components/__tests__/CommandCard.test.tsx 包含至少 5 个测试用例
    - src/components/__tests__/MainArea.test.tsx 包含至少 4 个测试用例
    - src/index.css 包含 `--animate-card-flash` 在 @theme inline 块内
    - src/index.css 包含 `@keyframes card-flash` 在 @theme 块外部
    - src/components/CommandCard.tsx 包含 `useState` 和 `flashing` 状态
    - src/components/CommandCard.tsx 包含 `animate-card-flash` class
    - src/components/CommandCard.tsx 包含 `handleClick` 函数，在 flashing||disabled 时 return
    - src/components/CommandCard.tsx 的 Icon 有条件 animate-spin
    - src/components/CommandCard.tsx 接受 command prop 并设置 title 属性
    - src/components/MainArea.tsx 网格使用 auto-fill + minmax(140px, 1fr)
    - src/components/MainArea.tsx 传递 command prop 给 CommandCard
    - 所有测试通过: `npx vitest run` exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 视觉和交互人工验证</name>
  <files>none</files>
  <action>
    启动 Tauri 开发服务器 (`npm run tauri dev`)，人工验证以下交互行为:

    1. 空状态: 确认未选中项目时右侧显示 FolderOpen 图标 + "选择一个项目开始" 引导文字，无指令卡片
    2. 添加项目: 点击"添加项目"选择文件夹，确认 4 个预设指令卡片出现
    3. 自适应网格: 拖拽窗口宽度，确认 600px 时 2 列、变宽时列数增加
    4. 执行反馈动效: 点击卡片，确认边框闪光(400ms) + 图标旋转 + Toast 提示
    5. 悬停 tooltip: 鼠标悬停卡片，确认原生 tooltip 显示 Shell 命令
    6. hover/active 状态: 确认 hover scale(1.02) + active scale(0.98) 微动效
    7. 终端弹出: 点击"打包项目"，确认终端弹出执行 npm run build
  </action>
  <verify>
    用户输入 "approved" 确认所有验证点通过
  </verify>
  <done>
    用户确认所有 7 个验证点通过，Phase 3 交互体验符合预期
  </done>
  <what-built>
    Phase 3 完整交互体验:
    1. 自适应网格布局（窗口缩放时列数自动调整）
    2. 卡片点击执行反馈动效（400ms 边框闪光 + 缩放回弹 + 图标旋转）
    3. 卡片 hover/active 状态保持正常
    4. 未选中项目时空状态引导页显示
    5. 卡片悬停时原生 tooltip 显示 Shell 命令
  </what-built>
  <how-to-verify>
    启动应用: `cd E:/git/EasyPack && npm run tauri dev`

    验证步骤:
    1. **空状态**: 应用启动后（无项目选中），确认右侧显示 FolderOpen 图标 + "选择一个项目开始" + 引导文字，无指令卡片可见
    2. **添加项目**: 点击侧边栏"添加项目"，选择一个本地文件夹
    3. **卡片网格**: 确认右侧显示 4 个预设指令卡片（打包项目、启动项目、Git Pull、启动 Claude），布局紧凑美观
    4. **自适应网格**: 拖拽窗口边缘调整宽度，确认:
       - 600px 窗口宽度时至少 2 列
       - 窗口变宽时列数自动增加
       - 卡片宽度随列数自适应
    5. **执行反馈动效**: 点击任意指令卡片，确认:
       - 卡片有短暂边框闪光效果（白色高亮）
       - 卡片有微弱缩放回弹（按下再恢复）
       - 图标在动效期间旋转
       - 整体效果约 400ms 后消失
       - Toast 提示"已执行: {命令}"
    6. **悬停 tooltip**: 鼠标悬停在卡片上，确认浏览器原生 tooltip 显示 Shell 命令文本（如 "npm run build"）
    7. **hover/active**: 快速 hover 卡片确认 scale(1.02) 放大效果，按下确认 scale(0.98) 缩小效果
    8. **终端弹出**: 点击"打包项目"卡片，确认 Windows Terminal 弹出并执行 npm run build
  </how-to-verify>
  <resume-signal>输入 "approved" 通过验证，或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Rust backend | 卡片点击 -> executeCommand -> invoke -> Rust execute_command。此边界在 Phase 1 已建立，Phase 3 不改变数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CommandCard handleClick | mitigate | flashing 状态防止快速连点导致重复执行: `if (disabled \|\| flashing) return` |
| T-03-02 | Tampering | MainArea -> CommandCard -> onExecute | accept | 命令执行路径已在 Phase 1 通过 Rust 后端参数化执行保护，UI 层无新增攻击面 |
| T-03-03 | Elevation of Privilege | disabled state bypass | accept | disabled 卡片的点击保护通过 HTML disabled 属性 + React handler 双重守卫，桌面应用无远程攻击向量 |

Phase 3 不引入新的安全面——所有数据流（卡片点击 -> onExecute -> invoke -> Rust 命令执行）在 Phase 1 已建立并经过安全审计。Phase 3 仅增强 UI 层交互（动画、布局），不改变功能逻辑。
</threat_model>

<verification>
1. 单元测试全部通过: `cd E:/git/EasyPack && npx vitest run`
2. TypeScript 编译无错误: `cd E:/git/EasyPack && npx tsc --noEmit`
3. Vite 前端构建成功: `cd E:/git/EasyPack && npx vite build`
4. 人工验证通过（Task 2 checkpoint）
</verification>

<success_criteria>
- 选中项目后 4 个预设指令卡片以自适应网格排列
- 点击卡片有 400ms 执行反馈动效（边框闪光 + 缩放回弹 + 图标旋转）
- 未选中项目时主区域显示空状态引导页
- 窗口宽度 600px 时至少 2 列，窗口变宽时列数自动增加
- 卡片 hover/active 微动效正常工作
- 所有单元测试通过
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/03-command-cards/03-01-SUMMARY.md`
</output>
