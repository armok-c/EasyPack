---
phase: 10-预设指令系统
plan: 03
type: execute
wave: 1
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/lib/icons.ts
  - src/lib/presets.ts
  - src/components/CommandDialog.tsx
  - src/components/MainArea.tsx
  - src/hooks/useProject.ts
autonomous: true
gap_closure: true
requirements:
  - PRE-04

must_haves:
  truths:
    - "Ship 图标可从 ICON_OPTIONS 获取且在运行时为有效的 LucideIcon 组件"
    - "Rust/Cargo 分类的预设命令和分类图标使用 Ship 字符串，getIconByName 返回有效组件"
    - "用户通过预设添加指令时，可在弹窗中选择添加为全局指令或当前项目指令"
    - "scope 选择控件在有项目选中时默认跟随当前 commandMode，但允许用户手动切换"
    - "scope 选择控件在无项目选中时仅显示全局选项"
  artifacts:
    - path: "src/lib/icons.ts"
      provides: "ICON_OPTIONS 包含 Ship（替换 CargoShip）"
      contains: "Ship"
    - path: "src/lib/presets.ts"
      provides: "PRESET_CATEGORIES 和 ALL_PRESETS 中 CargoShip 字符串替换为 Ship"
      contains: "icon: \"Ship\""
    - path: "src/components/CommandDialog.tsx"
      provides: "scope 选择控件 + onSubmit 包含 scope 参数"
      contains: "scope"
  key_links:
    - from: "src/components/CommandDialog.tsx"
      to: "src/components/MainArea.tsx"
      via: "onSubmit 回调签名扩展 (name, command, icon, scope)"
      pattern: "scope.*global.*project"
    - from: "src/components/MainArea.tsx"
      to: "src/hooks/useProject.ts"
      via: "addCommand 调用传入显式 scope 参数"
      pattern: "addCommand.*scope"
---

<objective>
修复 Phase 10 验证中发现的两个阻塞性差距：CargoShip 图标不存在 + PRE-04 scope 选择 UI 缺失

Purpose: VERIFICATION.md 识别出两个 BLOCKER：lucide-react 不导出 CargoShip 导致运行时崩溃，以及用户无法选择指令添加的 scope（全局/项目）。本计划一次性修复这两个问题，使 Phase 10 所有 must-haves 达标。

Output: icons.ts 中 CargoShip 替换为 Ship；presets.ts 中所有 CargoShip 引用替换为 Ship；CommandDialog 包含 scope 选择控件；addCommand 接受显式 scope 参数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-预设指令系统/10-VERIFICATION.md
@.planning/phases/10-预设指令系统/10-CONTEXT.md

<interfaces>
<!-- Executor MUST use these contracts -- no codebase exploration needed -->

From src/components/CommandDialog.tsx (CURRENT — lines to modify):
```typescript
// Line 28: current onSubmit signature — MUST add scope parameter
onSubmit: (data: { name: string; command: string; icon: string }) => void;

// Line 101-105: handleSubmit — MUST pass scope in onSubmit call
onSubmit({
  name: name.trim(),
  command: command.trim(),
  icon: selectedIcon,
});

// Lines 32-37: CommandDialog receives no commandMode or project info — MUST add props for scope logic
```

From src/components/MainArea.tsx (CURRENT — lines to modify):
```typescript
// Line 19: addCommand signature — currently no scope parameter
addCommand: (name: string, command: string, icon?: string) => Promise<void>;

// Lines 72-78: handleDialogSubmit — calls addCommand without scope
async (data: { name: string; command: string; icon: string }) => {
  if (editingCommand) {
    await updateCommand(editingCommand.id, data);
  } else {
    await addCommand(data.name, data.command, data.icon);
  }
};
```

From src/hooks/useProject.ts (CURRENT — lines to modify):
```typescript
// Line 263-298: addCommand — currently auto-detects scope from commandMode
const addCommand = useCallback(
  async (name: string, command: string, icon?: string) => {
    if (commandMode === "project" && selectedId) {
      // ... scope: "project"
    } else {
      // ... scope: "global"
    }
  },
  [commandMode, selectedId, projectCommandsMap, customCommands, store]
);

// Line 445: return type signature
addCommand, // (name: string, command: string, icon?: string) => Promise<void>
```

From src/lib/icons.ts (CURRENT — line to modify):
```typescript
// Line 13: invalid import
import { ..., CargoShip, ... } from "lucide-react";
// Line 33: invalid key
CargoShip,  // runtime value = undefined
```

From src/lib/presets.ts (CURRENT — lines to modify):
```typescript
// Line 21: category icon references CargoShip
{ id: "rust", label: "Rust/Cargo", icon: "CargoShip" },
// Lines 51-56: 6 preset commands reference CargoShip
icon: "CargoShip", category: "rust"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 替换 CargoShip 为 Ship 图标</name>
  <files>src/lib/icons.ts, src/lib/presets.ts</files>
  <read_first>
    src/lib/icons.ts
    src/lib/presets.ts
  </read_first>
  <action>
**Gap 1 修复：将 CargoShip 替换为 lucide-react 中实际存在的 Ship 图标。**

1. 修改 `src/lib/icons.ts`：
   - 第 13 行：将 `CargoShip` 替换为 `Ship`。import 语句变为：
     ```typescript
     import {
       Terminal, Code, Server, Zap, GitBranch, Package, Globe, Wrench, Rocket, Play, Sparkles, Ship,
       type LucideIcon,
     } from "lucide-react";
     ```
   - 第 33 行：将 `ICON_OPTIONS` 中的 `CargoShip,` 替换为 `Ship,`
   - 不修改其他任何行

2. 修改 `src/lib/presets.ts`：
   - 第 21 行：将 `icon: "CargoShip"` 替换为 `icon: "Ship"`
   - 第 51-56 行：将所有 6 处 `icon: "CargoShip"` 替换为 `icon: "Ship"`
   - 不修改其他任何行。最终 presets.ts 中不应存在任何 "CargoShip" 字符串

共需替换：icons.ts 2 处（import + ICON_OPTIONS），presets.ts 7 处（1 个分类 + 6 个命令）
  </action>
  <verify>
    <automated>! grep -q "CargoShip" src/lib/icons.ts && ! grep -q "CargoShip" src/lib/presets.ts && grep -q "Ship" src/lib/icons.ts && grep -q '"Ship"' src/lib/presets.ts && node -e "const m = require('lucide-react'); console.log(typeof m.Ship)" 2>&1 | grep -q "object" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/icons.ts` 中不存在 "CargoShip" 字符串
    - `src/lib/presets.ts` 中不存在 "CargoShip" 字符串
    - `src/lib/icons.ts` 的 import 包含 `Ship`（非 `CargoShip`）
    - `src/lib/icons.ts` 的 ICON_OPTIONS 包含 `Ship` 键（非 `CargoShip`）
    - `src/lib/presets.ts` 中 rust 分类的 icon 值为 `"Ship"`
    - `src/lib/presets.ts` 中 6 个 rust 预设命令的 icon 值均为 `"Ship"`
    - `node -e "const m = require('lucide-react'); console.log(typeof m.Ship)"` 输出 `object`
    - `npx tsc --noEmit` 零错误
  </acceptance_criteria>
  <done>icons.ts 中 CargoShip 替换为 Ship，presets.ts 中所有 7 处 CargoShip 字符串替换为 Ship，lucide-react Ship 导出验证通过，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 添加 scope 选择 UI 并贯通 scope 参数传递链</name>
  <files>src/components/CommandDialog.tsx, src/components/MainArea.tsx, src/hooks/useProject.ts</files>
  <read_first>
    src/components/CommandDialog.tsx
    src/components/MainArea.tsx
    src/hooks/useProject.ts
  </read_first>
  <action>
**Gap 2 修复：添加 scope 选择控件，贯通从 UI 到数据层的 scope 参数传递链。**

PRE-04 要求"用户通过预设添加指令时，可选择添加为全局指令或当前项目指令"。当前 D-08 的"自动跟随 commandMode"设计不满足此需求。ROADMAP 需求优先于 CONTEXT.md 决策。

**Step 1: 修改 `src/hooks/useProject.ts` — addCommand 接受显式 scope 参数**

修改 addCommand 函数签名，新增第 4 个参数 `scope`：
```typescript
// 修改前（第 263 行）:
async (name: string, command: string, icon?: string) => {

// 修改后:
async (name: string, command: string, icon?: string, scope?: "global" | "project") => {
```

修改 addCommand 内部逻辑，使用显式 scope 参数而非 commandMode：
```typescript
const addCommand = useCallback(
  async (name: string, command: string, icon?: string, scope?: "global" | "project") => {
    // 使用显式 scope 参数，fallback 到当前 commandMode
    const effectiveScope = scope ?? (commandMode === "project" && selectedId ? "project" : "global");

    if (effectiveScope === "project" && selectedId) {
      // Project-level: add to projectCommandsMap
      const newItem: CommandItem = {
        id: crypto.randomUUID(),
        name,
        command,
        icon: icon ?? DEFAULT_ICON,
        type: "custom",
        scope: "project",
        addedAt: Date.now(),
      };
      const current = projectCommandsMap[selectedId] ?? [];
      const updated = [...current, newItem];
      setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: updated }));
      await store?.set(projectCommandsKey(selectedId), updated);
      toast.success(`已添加指令: ${name}`);
    } else {
      // Global mode: add to customCommands
      const newItem: CommandItem = {
        id: crypto.randomUUID(),
        name,
        command,
        icon: icon ?? DEFAULT_ICON,
        type: "custom",
        scope: "global",
        addedAt: Date.now(),
      };
      const updated = [...customCommands, newItem];
      setCustomCommands(updated);
      await store?.set(CUSTOM_COMMANDS_KEY, updated);
      toast.success(`已添加指令: ${name}`);
    }
  },
  [commandMode, selectedId, projectCommandsMap, customCommands, store]
);
```

更新返回类型注释（第 445 行附近）：
```typescript
addCommand, // (name: string, command: string, icon?: string, scope?: "global" | "project") => Promise<void>
```

**Step 2: 修改 `src/components/MainArea.tsx` — 传递 scope 参数**

修改 MainAreaProps 中 addCommand 的类型签名（第 19 行）：
```typescript
addCommand: (name: string, command: string, icon?: string, scope?: "global" | "project") => Promise<void>;
```

修改 handleDialogSubmit（第 71-82 行），接收 scope 并传递给 addCommand：
```typescript
const handleDialogSubmit = useCallback(
  async (data: { name: string; command: string; icon: string; scope?: "global" | "project" }) => {
    if (editingCommand) {
      await updateCommand(editingCommand.id, data);
    } else {
      await addCommand(data.name, data.command, data.icon, data.scope);
    }
    setDialogOpen(false);
    setEditingCommand(null);
  },
  [editingCommand, addCommand, updateCommand]
);
```

**Step 3: 修改 `src/components/CommandDialog.tsx` — 添加 scope 选择 UI**

3a. 扩展 CommandDialogProps（第 25-30 行），添加 commandMode 和 hasProject props：
```typescript
interface CommandDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSubmit: (data: { name: string; command: string; icon: string; scope?: "global" | "project" }) => void;
  initialData?: CommandItem | null;
  commandMode: "global" | "project";
  hasProject: boolean;
}
```

3b. 在组件参数解构中添加新 props（第 33-37 行）：
```typescript
export function CommandDialog({
  open,
  onOpenChange,
  onSubmit,
  initialData = null,
  commandMode,
  hasProject,
}: CommandDialogProps) {
```

3c. 添加 scope state（在 selectedPresetId 之后，约第 48 行后）：
```typescript
const [selectedScope, setSelectedScope] = useState<"global" | "project">(
  () => commandMode
);
```

3d. 在 handleOpenChange（第 108-123 行）的 reset 块中添加 scope 重置：
```typescript
setSelectedScope(commandMode);
```
添加到 `setSelectedPresetId("");` 之后。

3e. 修改 handleSubmit（第 99-106 行），传递 scope：
```typescript
const handleSubmit = useCallback(() => {
  if (!isValid) return;
  onSubmit({
    name: name.trim(),
    command: command.trim(),
    icon: selectedIcon,
    scope: selectedScope,
  });
}, [isValid, name, command, selectedIcon, selectedScope, onSubmit]);
```

3f. 在 JSX 中，在预设选择区域（border-b border-white/10 的 div 之后，名称字段之前）添加 scope 选择控件。位置：约第 196 行（`</div>` 关闭预设区域后，Name field 的 `<div className="space-y-2">` 之前）。

插入以下 JSX：
```tsx
{/* Scope selector per PRE-04 */}
<div className="pb-4 mb-4 border-b border-white/10">
  <Label className="mb-2 block">添加到</Label>
  <div
    className="inline-flex rounded-md overflow-hidden border border-white/10"
    role="radiogroup"
    aria-label="指令作用域"
  >
    <button
      type="button"
      role="radio"
      aria-checked={selectedScope === "global"}
      aria-label="全局指令"
      onClick={() => setSelectedScope("global")}
      className={cn(
        "px-3 py-1.5 text-xs transition-all duration-150 ease-out",
        "focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none",
        selectedScope === "global"
          ? "bg-white/15 text-foreground border-r border-white/10"
          : "bg-transparent text-muted-foreground hover:bg-white/5"
      )}
    >
      全局指令
    </button>
    <button
      type="button"
      role="radio"
      aria-checked={selectedScope === "project"}
      aria-label="当前项目指令"
      disabled={!hasProject}
      onClick={hasProject ? () => setSelectedScope("project") : undefined}
      className={cn(
        "px-3 py-1.5 text-xs transition-all duration-150 ease-out",
        "focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none",
        selectedScope === "project"
          ? "bg-white/15 text-foreground"
          : "bg-transparent text-muted-foreground hover:bg-white/5",
        !hasProject && "opacity-40 cursor-not-allowed"
      )}
    >
      当前项目指令
    </button>
  </div>
</div>
```

3g. **重要：同步更新 App.tsx 或调用 CommandDialog 的地方，传入新 props。**

检查 `MainArea.tsx` 中 CommandDialog 的调用（第 317-322 行），添加新 props：
```tsx
<CommandDialog
  open={dialogOpen}
  onOpenChange={handleDialogOpenChange}
  onSubmit={handleDialogSubmit}
  initialData={editingCommand}
  commandMode={commandMode}
  hasProject={!!currentProject}
/>
```

  </action>
  <verify>
    <automated>grep -q "scope" src/components/CommandDialog.tsx && grep -q "selectedScope" src/components/CommandDialog.tsx && grep -q "scope.*global.*project" src/hooks/useProject.ts && grep -q "commandMode" src/components/CommandDialog.tsx && grep -q "hasProject" src/components/CommandDialog.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/CommandDialog.tsx` 的 CommandDialogProps 包含 `commandMode: "global" | "project"` 和 `hasProject: boolean`
    - `src/components/CommandDialog.tsx` 的 onSubmit 回调签名包含 `scope?: "global" | "project"`
    - `src/components/CommandDialog.tsx` 包含 `selectedScope` state 和 scope 选择 UI（radiogroup 含"全局指令"和"当前项目指令"两个按钮）
    - `src/components/CommandDialog.tsx` 中 scope 选择控件的"当前项目指令"按钮在 `!hasProject` 时 disabled
    - `src/components/CommandDialog.tsx` 的 handleSubmit 传递 `scope: selectedScope` 给 onSubmit
    - `src/components/MainArea.tsx` 的 handleDialogSubmit 接收 `scope` 并传递给 addCommand
    - `src/components/MainArea.tsx` 传递 `commandMode` 和 `hasProject` 给 CommandDialog
    - `src/hooks/useProject.ts` 的 addCommand 接受第 4 个参数 `scope?: "global" | "project"`
    - `src/hooks/useProject.ts` 的 addCommand 使用 `effectiveScope` 而非直接读取 `commandMode`
    - `npx tsc --noEmit` 零错误
    - `npm test` 通过
  </acceptance_criteria>
  <done>CommandDialog 包含 scope 选择控件（全局/项目切换），用户可在添加指令时主动选择 scope；scope 参数从 UI 经 MainArea 传递到 useProject.addCommand；无项目时"当前项目指令"选项不可选；TypeScript 编译和测试通过</done>
</task>

</tasks>

<verification>
1. `! grep "CargoShip" src/lib/icons.ts && ! grep "CargoShip" src/lib/presets.ts` -- CargoShip 已完全替换
2. `grep "Ship" src/lib/icons.ts` -- Ship 图标已添加
3. `grep "selectedScope" src/components/CommandDialog.tsx` -- scope state 已添加
4. `grep "radiogroup" src/components/CommandDialog.tsx` -- scope 选择 UI 存在
5. `grep "effectiveScope" src/hooks/useProject.ts` -- addCommand 使用显式 scope
6. `grep "commandMode.*hasProject" src/components/CommandDialog.tsx || grep -c "commandMode" src/components/CommandDialog.tsx` -- 新 props 已传入
7. `npx tsc --noEmit` -- TypeScript 编译通过
8. `npm test` -- 测试通过
</verification>

<success_criteria>
- lucide-react 的 Ship 图标在运行时有效（typeof === "object"），icons.ts 和 presets.ts 中无 CargoShip 残留
- CommandDialog 包含 scope 选择控件，用户可选择"全局指令"或"当前项目指令"
- scope 参数从 CommandDialog.onSubmit -> MainArea.handleDialogSubmit -> useProject.addCommand 完整传递
- addCommand 使用用户选择的 scope 而非仅依赖 commandMode 自动决定
- 无项目选中时"当前项目指令"选项为 disabled 状态
- TypeScript 编译零错误，所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-预设指令系统/10-03-SUMMARY.md`
</output>
