---
phase: 10-预设指令系统
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/icons.ts
  - src/lib/presets.ts
  - src/components/ui/select.tsx
autonomous: true
requirements:
  - PRE-02
  - PRE-03

must_haves:
  truths:
    - "预设命令库包含 Git、NPM/Node、Python/Pip、Rust/Cargo 四个分类共 25 个预设命令"
    - "默认卡片函数仅返回 git pull 和启动 Claude 两个指令"
    - "Sparkles 和 CargoShip 图标可从 ICON_OPTIONS 获取"
    - "shadcn Select 组件已安装到 src/components/ui/select.tsx"
  artifacts:
    - path: "src/lib/presets.ts"
      provides: "PRESET_CATEGORIES, ALL_PRESETS, getDefaultsAsCommandItems, PresetCategory, PresetCommand 类型"
      exports: ["PRESET_CATEGORIES", "ALL_PRESETS", "getDefaultsAsCommandItems", "PresetCategory", "PresetCommand"]
    - path: "src/lib/icons.ts"
      provides: "扩展 ICON_OPTIONS 包含 Sparkles 和 CargoShip"
      contains: "Sparkles"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select 组件"
      min_lines: 20
  key_links:
    - from: "src/lib/presets.ts"
      to: "src/lib/types.ts"
      via: "import CommandItem"
      pattern: "import.*CommandItem.*from.*types"
    - from: "src/lib/presets.ts"
      to: "src/lib/icons.ts"
      via: "图标名引用"
      pattern: "icon:.*\"(GitBranch|Package|Terminal|CargoShip|Sparkles)\""
---

<objective>
扩展预设库数据层和安装 Select 组件依赖

Purpose: 为 CommandDialog 双下拉框 UI 提供完整的数据层（分类预设库 + 精简默认卡片）和组件依赖（shadcn Select），使 Plan 02 能专注于 UI 集成。
Output: 重写的 presets.ts（分类预设库 25 命令）、扩展的 icons.ts（新增 Sparkles/CargoShip）、安装的 Select 组件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-预设指令系统/10-CONTEXT.md
@.planning/phases/10-预设指令系统/10-UI-SPEC.md

<interfaces>
<!-- Executor MUST use these contracts — no codebase exploration needed -->

From src/lib/types.ts:
```typescript
export interface CommandItem {
  id: string;
  name: string;
  command: string;
  icon: string;
  type: "preset" | "custom";
  scope: "global" | "project";
  addedAt: number;
}
```

From src/lib/icons.ts (CURRENT — 10 icons, missing Sparkles and CargoShip):
```typescript
export const ICON_OPTIONS: Record<string, LucideIcon> = {
  Terminal, Code, Server, Zap, GitBranch, Package, Globe, Wrench, Rocket, Play,
};
export const DEFAULT_ICON = "Terminal";
export function getIconByName(name: string): LucideIcon;
```

From src/hooks/useProject.ts (consumer of presets):
```typescript
// Line 78: used in commands useMemo for global mode
return [...getPresetAsCommandItems(), ...customCommands].sort(...)
// Line 377: used in enableProjectCommands to seed project command set
const presets = getPresetAsCommandItems();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 shadcn Select 组件 + 扩展 icons.ts</name>
  <files>src/components/ui/select.tsx, src/lib/icons.ts</files>
  <read_first>
    src/lib/icons.ts
    components.json
  </read_first>
  <action>
1. 运行 `npx shadcn@latest add select` 安装 Select 组件到 `src/components/ui/select.tsx`。

2. 修改 `src/lib/icons.ts`：
   - 在 lucide-react import 中添加 `Sparkles` 和 `CargoShip`
   - 在 `ICON_OPTIONS` 对象中添加两个新条目：
     ```typescript
     Sparkles,
     CargoShip,
     ```
   这两个图标是 UI-SPEC 中预设分类和默认卡片使用的图标（Sparkles 用于 "启动 Claude"，CargoShip 用于 Rust/Cargo 分类）。

注意：不要改变现有的 10 个图标或 DEFAULT_ICON 值，只追加新图标。
  </action>
  <verify>
    <automated>test -f src/components/ui/select.tsx && grep -q "Sparkles" src/lib/icons.ts && grep -q "CargoShip" src/lib/icons.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/select.tsx` 文件存在
    - `src/lib/icons.ts` 中 ICON_OPTIONS 包含 Sparkles 和 CargoShip 键
    - `src/lib/icons.ts` 中原有 10 个图标条目未改变
    - `npx tsc --noEmit` 无新增错误
  </acceptance_criteria>
  <done>Select 组件已安装，icons.ts 包含 12 个图标（原 10 + Sparkles + CargoShip），TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 重写 presets.ts 为分类预设库</name>
  <files>src/lib/presets.ts</files>
  <read_first>
    src/lib/presets.ts
    src/lib/types.ts
    src/lib/icons.ts
    .planning/phases/10-预设指令系统/10-UI-SPEC.md
  </read_first>
  <action>
完全重写 `src/lib/presets.ts`，使用 UI-SPEC.md 中定义的精确数据结构。内容如下：

```typescript
import type { CommandItem } from "./types";

export interface PresetCategory {
  id: string;
  label: string;
  icon: string;
}

export interface PresetCommand {
  id: string;
  name: string;
  command: string;
  icon: string;
  category: string;
}

export const PRESET_CATEGORIES: PresetCategory[] = [
  { id: "git",    label: "Git",         icon: "GitBranch" },
  { id: "npm",    label: "NPM/Node",    icon: "Package" },
  { id: "python", label: "Python/Pip",  icon: "Terminal" },
  { id: "rust",   label: "Rust/Cargo",  icon: "CargoShip" },
];

export const ALL_PRESETS: PresetCommand[] = [
  // Git (8 commands)
  { id: "preset-git-pull",     name: "Git Pull",           command: "git pull",                  icon: "GitBranch", category: "git" },
  { id: "preset-git-push",     name: "Git Push",           command: "git push",                  icon: "GitBranch", category: "git" },
  { id: "preset-git-status",   name: "Git Status",         command: "git status",                icon: "GitBranch", category: "git" },
  { id: "preset-git-log",      name: "Git Log",            command: "git log --oneline -10",     icon: "GitBranch", category: "git" },
  { id: "preset-git-fetch",    name: "Git Fetch",          command: "git fetch",                 icon: "GitBranch", category: "git" },
  { id: "preset-git-checkout", name: "Git Checkout Main",  command: "git checkout main",         icon: "GitBranch", category: "git" },
  { id: "preset-git-add",      name: "Git Add All",        command: "git add .",                 icon: "GitBranch", category: "git" },
  { id: "preset-git-commit",   name: "Git Commit",         command: "git commit",                icon: "GitBranch", category: "git" },

  // NPM/Node (6 commands) — per D-02: includes "打包项目" and "启动项目" for user manual selection
  { id: "preset-npm-install",  name: "Install Dependencies", command: "npm install",             icon: "Package",   category: "npm" },
  { id: "preset-npm-build",    name: "Build Project",        command: "npm run build",           icon: "Package",   category: "npm" },
  { id: "preset-npm-dev",      name: "Dev Server",           command: "npm run dev",             icon: "Package",   category: "npm" },
  { id: "preset-npm-test",     name: "Run Tests",            command: "npm test",                icon: "Package",   category: "npm" },
  { id: "preset-npm-lint",     name: "Lint",                 command: "npm run lint",            icon: "Package",   category: "npm" },
  { id: "preset-npm-start",    name: "Start",                command: "npm start",               icon: "Package",   category: "npm" },

  // Python/Pip (5 commands)
  { id: "preset-py-run",       name: "Run Python",           command: "python",                   icon: "Terminal",  category: "python" },
  { id: "preset-py-install",   name: "Pip Install",          command: "pip install",              icon: "Terminal",  category: "python" },
  { id: "preset-py-reqs",      name: "Install Requirements", command: "pip install -r requirements.txt", icon: "Terminal", category: "python" },
  { id: "preset-py-venv",      name: "Create Venv",          command: "python -m venv venv",      icon: "Terminal",  category: "python" },
  { id: "preset-py-test",      name: "Pytest",               command: "pytest",                   icon: "Terminal",  category: "python" },

  // Rust/Cargo (6 commands)
  { id: "preset-rs-build",     name: "Cargo Build",          command: "cargo build",              icon: "CargoShip", category: "rust" },
  { id: "preset-rs-run",       name: "Cargo Run",            command: "cargo run",                icon: "CargoShip", category: "rust" },
  { id: "preset-rs-test",      name: "Cargo Test",           command: "cargo test",               icon: "CargoShip", category: "rust" },
  { id: "preset-rs-clippy",    name: "Clippy",               command: "cargo clippy",             icon: "CargoShip", category: "rust" },
  { id: "preset-rs-fmt",       name: "Cargo Fmt",            command: "cargo fmt",                icon: "CargoShip", category: "rust" },
  { id: "preset-rs-check",     name: "Cargo Check",          command: "cargo check",              icon: "CargoShip", category: "rust" },
];

/**
 * Returns only the 2 default command items for new projects.
 * Per D-06: only git pull + claude retained as defaults.
 * Replaces the old getPresetAsCommandItems() which returned 4 items.
 */
export function getDefaultsAsCommandItems(): CommandItem[] {
  return [
    {
      id: "preset-git-pull",
      name: "Git Pull",
      command: "git pull",
      icon: "GitBranch",
      type: "preset" as const,
      scope: "global" as const,
      addedAt: 0,
    },
    {
      id: "preset-claude",
      name: "启动 Claude",
      command: "claude",
      icon: "Sparkles",
      type: "preset" as const,
      scope: "global" as const,
      addedAt: 1,
    },
  ];
}
```

重要说明：
- 删除旧的 `PRESET_COMMANDS`、`PresetCommand`（旧版含 LucideIcon）、`PRESET_ICON_NAMES` 和 `getPresetAsCommandItems()`
- 不再从 lucide-react 直接导入图标组件，新设计使用字符串名引用
- 新 `PresetCommand` 接口使用 `icon: string`（不是 `icon: LucideIcon`）
- `getDefaultsAsCommandItems()` 返回 2 个默认项（per D-06），替代旧 `getPresetAsCommandItems()` 返回 4 项
- `preset-claude` ID 不在 ALL_PRESETS 中（claude 不是预设库命令），只在 defaults 中使用

然后更新 `src/hooks/useProject.ts` 中所有对旧函数的引用：
- 第 78 行：将 `getPresetAsCommandItems()` 替换为 `getDefaultsAsCommandItems()`
- 第 377 行：将 `getPresetAsCommandItems()` 替换为 `getDefaultsAsCommandItems()`
- 第 7 行：将 import 从 `getPresetAsCommandItems` 改为 `getDefaultsAsCommandItems`
  </action>
  <verify>
    <automated>grep -q "PRESET_CATEGORIES" src/lib/presets.ts && grep -q "ALL_PRESETS" src/lib/presets.ts && grep -q "getDefaultsAsCommandItems" src/lib/presets.ts && grep -q "getDefaultsAsCommandItems" src/hooks/useProject.ts && ! grep -q "getPresetAsCommandItems" src/hooks/useProject.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - presets.ts 导出 PRESET_CATEGORIES（4 项）、ALL_PRESETS（25 项）、getDefaultsAsCommandItems()、PresetCategory 和 PresetCommand 类型
    - presets.ts 不再导出旧的 PRESET_COMMANDS、PRESET_ICON_NAMES、getPresetAsCommandItems
    - presets.ts 不再从 lucide-react import 任何图标组件
    - ALL_PRESETS 包含恰好 25 个条目（git: 8, npm: 6, python: 5, rust: 6）
    - getDefaultsAsCommandItems() 返回恰好 2 个 CommandItem
    - useProject.ts 中所有对旧函数的引用已更新为 getDefaultsAsCommandItems
    - `npx tsc --noEmit` 无错误
    - `npm test` 通过
  </acceptance_criteria>
  <done>presets.ts 重写为分类预设库（4 分类 25 命令），默认卡片精简为 2 个（git pull + claude），useProject.ts 引用已更新，TypeScript 编译和测试通过</done>
</task>

</tasks>

<verification>
1. `test -f src/components/ui/select.tsx` — Select 组件已安装
2. `grep -c "Sparkles\|CargoShip" src/lib/icons.ts` — 新图标已添加（至少 2 个匹配）
3. `grep -c "ALL_PRESETS" src/lib/presets.ts` — 预设库数据存在
4. `grep "getDefaultsAsCommandItems" src/lib/presets.ts src/hooks/useProject.ts` — 新函数已导出并被使用
5. `npx tsc --noEmit` — TypeScript 编译通过
6. `npm test` — 测试通过
</verification>

<success_criteria>
- shadcn Select 组件文件存在且可导入
- icons.ts 包含 12 个图标（原 10 + Sparkles + CargoShip）
- presets.ts 导出 PRESET_CATEGORIES（4 分类）、ALL_PRESETS（25 命令）、getDefaultsAsCommandItems()（2 默认）
- useProject.ts 使用 getDefaultsAsCommandItems 替代旧函数
- TypeScript 编译零错误，所有现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-预设指令系统/10-01-SUMMARY.md`
</output>
