---
phase: 10-预设指令系统
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/CommandDialog.tsx
autonomous: true
requirements:
  - PRE-01
  - PRE-04

must_haves:
  truths:
    - "添加指令弹窗顶部包含双下拉框（分类 + 命令），选择后自动填充名称、命令、图标字段"
    - "分类下拉框包含 Git、NPM/Node、Python/Pip、Rust/Cargo 四个选项"
    - "选择分类后命令下拉框变为可用，显示该分类下的预设命令列表"
    - "选择预设命令后名称和命令字段自动填充，用户仍可手动修改"
    - "编辑已有指令时双下拉框不显示选中状态（编辑模式不触发预设选择）"
  artifacts:
    - path: "src/components/CommandDialog.tsx"
      provides: "双 Select 下拉框预设选择器 + 自动填充逻辑"
      exports: ["CommandDialog"]
  key_links:
    - from: "src/components/CommandDialog.tsx"
      to: "src/lib/presets.ts"
      via: "import PRESET_CATEGORIES, ALL_PRESETS"
      pattern: "import.*PRESET_CATEGORIES.*ALL_PRESETS.*from.*presets"
    - from: "src/components/CommandDialog.tsx"
      to: "src/components/ui/select.tsx"
      via: "import Select components"
      pattern: "import.*Select.*from.*@/components/ui/select"
    - from: "CommandDialog preset selection"
      to: "name/command/icon fields"
      via: "handlePresetChange callback auto-fills useState setters"
      pattern: "setName\(preset\.name\)"
---

<objective>
改造 CommandDialog 为双下拉框预设选择 + 可编辑填充模式

Purpose: 实现 PRE-01 核心交互——用户通过分类+命令双下拉框快速选择预设指令，选择后自动填充表单字段。PRE-04（scope 自动跟随）已在现有 addCommand 中实现（D-08），无需额外代码。
Output: 改造后的 CommandDialog.tsx，包含预设选择区域（分类 Select + 命令 Select）和自动填充逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-预设指令系统/10-CONTEXT.md
@.planning/phases/10-预设指令系统/10-UI-SPEC.md
@.planning/phases/10-预设指令系统/10-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 — executor MUST use these directly -->

From src/lib/presets.ts (Plan 01 creates these):
```typescript
export interface PresetCategory {
  id: string;       // "git" | "npm" | "python" | "rust"
  label: string;    // "Git" | "NPM/Node" | "Python/Pip" | "Rust/Cargo"
  icon: string;     // "GitBranch" | "Package" | "Terminal" | "CargoShip"
}

export interface PresetCommand {
  id: string;       // "preset-git-pull", "preset-npm-build", etc.
  name: string;     // "Git Pull", "Build Project", etc.
  command: string;  // "git pull", "npm run build", etc.
  icon: string;     // "GitBranch", "Package", etc. (inherits from category)
  category: string; // "git" | "npm" | "python" | "rust"
}

export const PRESET_CATEGORIES: PresetCategory[];
export const ALL_PRESETS: PresetCommand[];
export function getDefaultsAsCommandItems(): CommandItem[];
```

From src/components/ui/select.tsx (Plan 01 installs this):
```typescript
// shadcn Select component exports (standard):
export { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue };
```

From src/lib/icons.ts:
```typescript
export function getIconByName(name: string): LucideIcon;
export const ICON_OPTIONS: Record<string, LucideIcon>;
// Now includes: Sparkles, CargoShip (added by Plan 01)
```

From src/components/CommandDialog.tsx (CURRENT — to be modified):
```typescript
// Existing props interface (unchanged):
interface CommandDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSubmit: (data: { name: string; command: string; icon: string }) => void;
  initialData?: CommandItem | null;
}
// Existing state: name, command, selectedIcon, nameDirty, commandDirty
// Existing handlers: handleNameChange, handleCommandChange, handleIconSelect, handleSubmit, handleOpenChange
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 CommandDialog 中添加双 Select 预设选择区域</name>
  <files>src/components/CommandDialog.tsx</files>
  <read_first>
    src/components/CommandDialog.tsx
    src/lib/presets.ts
    src/components/ui/select.tsx
    .planning/phases/10-预设指令系统/10-UI-SPEC.md
  </read_first>
  <action>
修改 `src/components/CommandDialog.tsx`，在现有表单字段上方添加双 Select 预设选择区域。按照 UI-SPEC.md 的精确布局和交互规范实现。

**新增 imports（添加到文件顶部）：**
```typescript
import { useMemo } from "react";  // 如果尚未导入
import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { PRESET_CATEGORIES, ALL_PRESETS } from "@/lib/presets";
import { getIconByName } from "@/lib/icons";
```

**新增 state（在现有 useState 声明之后添加）：**
```typescript
const [selectedCategory, setSelectedCategory] = useState<string>("");
const [selectedPresetId, setSelectedPresetId] = useState<string>("");
```

**新增派生数据（useMemo）：**
```typescript
const filteredPresets = useMemo(
  () => ALL_PRESETS.filter((p) => p.category === selectedCategory),
  [selectedCategory]
);
```

**新增事件处理函数：**

`handleCategoryChange` — 分类变更时重置命令选择和表单字段：
```typescript
const handleCategoryChange = useCallback((categoryId: string) => {
  setSelectedCategory(categoryId);
  setSelectedPresetId("");
  setName("");
  setCommand("");
  setSelectedIcon(DEFAULT_ICON);
  setNameDirty(false);
  setCommandDirty(false);
}, []);
```

`handlePresetChange` — 选择预设命令时自动填充表单字段：
```typescript
const handlePresetChange = useCallback((presetId: string) => {
  const preset = ALL_PRESETS.find((p) => p.id === presetId);
  if (preset) {
    setName(preset.name);
    setCommand(preset.command);
    setSelectedIcon(preset.icon);
    setNameDirty(false);
    setCommandDirty(false);
  }
  setSelectedPresetId(presetId);
}, []);
```

**修改 handleOpenChange** — 在弹窗关闭时重置预设选择状态：
在 `if (!newOpen)` 块中添加：
```typescript
setSelectedCategory("");
setSelectedPresetId("");
```

**JSX 布局修改：**

在 `<div className="py-4 space-y-4">` 的**开头**（在 Name field 之前），插入预设选择区域：

```tsx
{/* Preset selection area per D-03 */}
<div className="pb-4 mb-4 border-b border-white/10">
  <div className="grid grid-cols-2 gap-2">
    {/* Category Select */}
    <div className="space-y-2">
      <Label>分类</Label>
      <Select
        value={selectedCategory}
        onValueChange={handleCategoryChange}
      >
        <SelectTrigger aria-label="选择预设分类">
          <SelectValue placeholder="选择分类..." />
        </SelectTrigger>
        <SelectContent>
          <SelectGroup>
            {PRESET_CATEGORIES.map((cat) => {
              const CategoryIcon = getIconByName(cat.icon);
              return (
                <SelectItem key={cat.id} value={cat.id}>
                  <div className="flex items-center gap-2">
                    <CategoryIcon className="size-4 text-muted-foreground" aria-hidden="true" />
                    {cat.label}
                  </div>
                </SelectItem>
              );
            })}
          </SelectGroup>
        </SelectContent>
      </Select>
    </div>

    {/* Command Select */}
    <div className="space-y-2">
      <Label>命令</Label>
      <Select
        value={selectedPresetId}
        onValueChange={handlePresetChange}
        disabled={!selectedCategory}
      >
        <SelectTrigger aria-label="选择预设命令">
          <SelectValue placeholder={selectedCategory ? "选择命令..." : "先选择分类"} />
        </SelectTrigger>
        <SelectContent>
          <SelectGroup>
            {filteredPresets.map((preset) => (
              <SelectItem key={preset.id} value={preset.id}>
                {preset.name}
              </SelectItem>
            ))}
          </SelectGroup>
        </SelectContent>
      </Select>
    </div>
  </div>
</div>
```

**重要实现细节：**
1. 预设选择区域与表单字段之间用 `border-b border-white/10` 分隔，加上 `pb-4 mb-4` 间距
2. 命令 Select 在未选择分类时为 `disabled` 状态
3. 编辑模式（isEditing=true）时，预设 Select 不需要特殊处理——state 初始化为空字符串，用户可以选择新预设覆盖编辑内容
4. `useMemo` 需要确保已导入（现有代码已导入）
5. 不需要修改 CommandDialogProps 接口——scope 自动跟随由 useProject.ts 的 addCommand 处理（D-08）
  </action>
  <verify>
    <automated>grep -q "PRESET_CATEGORIES" src/components/CommandDialog.tsx && grep -q "ALL_PRESETS" src/components/CommandDialog.tsx && grep -q "SelectTrigger" src/components/CommandDialog.tsx && grep -q "handleCategoryChange" src/components/CommandDialog.tsx && grep -q "handlePresetChange" src/components/CommandDialog.tsx && grep -q "border-b border-white/10" src/components/CommandDialog.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CommandDialog.tsx 导入了 Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue
    - CommandDialog.tsx 导入了 PRESET_CATEGORIES 和 ALL_PRESETS
    - 包含 selectedCategory 和 selectedPresetId 两个 state
    - 包含 filteredPresets useMemo 派生值
    - 包含 handleCategoryChange 和 handlePresetChange 回调
    - handleOpenChange 中重置 selectedCategory 和 selectedPresetId
    - JSX 中在表单字段上方存在预设选择区域，包含分类和命令两个 Select
    - 命令 Select 有 disabled 属性绑定 `!selectedCategory`
    - 预设区域有 `border-b border-white/10` 分隔线
    - `npx tsc --noEmit` 零错误
    - `npm test` 通过
  </acceptance_criteria>
  <done>CommandDialog 包含双 Select 预设选择区域，选择分类后命令下拉框激活，选择命令后自动填充名称/命令/图标字段，弹窗关闭时重置预设选择状态，TypeScript 编译和测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 构建验证 + 交互逻辑测试</name>
  <files>src/components/CommandDialog.tsx</files>
  <read_first>
    src/components/CommandDialog.tsx
    src/lib/presets.ts
  </read_first>
  <action>
执行完整构建验证，确保所有改动在生产构建中正常工作。

1. 运行 `npm run build` 验证 Vite 生产构建通过
2. 确认 `src/components/CommandDialog.tsx` 中：
   - 分类 Select 显示 4 个分类选项（Git, NPM/Node, Python/Pip, Rust/Cargo）
   - 命令 Select 在无分类时 disabled
   - 选择预设后 name/command/icon 字段自动填充
   - 关闭弹窗后预设选择状态重置
3. 检查现有 CommandDialog 测试是否需要更新（如果有引用旧的 PRESET_COMMANDS 或 getPresetAsCommandItems 的测试，更新为新导入）
4. 运行 `npm test` 确认所有测试通过
5. 运行 `cargo check` 确认 Rust 后端无影响
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` 退出码为 0
    - `npm test` 所有测试通过
    - 无新增 TypeScript 错误
    - 无新增 Rust 编译警告
  </acceptance_criteria>
  <done>构建通过，测试通过，Phase 10 所有需求（PRE-01/02/03/04）实现完成</done>
</task>

</tasks>

<verification>
1. `grep "PRESET_CATEGORIES" src/components/CommandDialog.tsx` — 预设数据已导入
2. `grep "SelectTrigger" src/components/CommandDialog.tsx` — Select 组件已使用
3. `grep "handlePresetChange" src/components/CommandDialog.tsx` — 预设选择逻辑已实现
4. `grep "disabled.*selectedCategory" src/components/CommandDialog.tsx` — 命令 Select 禁用逻辑正确
5. `npm run build` — 生产构建通过
6. `npm test` — 所有测试通过
</verification>

<success_criteria>
- CommandDialog 顶部包含分类+命令双 Select 下拉框
- 选择分类后命令 Select 激活，过滤显示该分类的预设命令
- 选择命令后 name、command、icon 字段自动填充
- 用户可手动修改自动填充的内容
- 编辑模式不触发预设选择
- 弹窗关闭时所有状态正确重置
- 生产构建和测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-预设指令系统/10-02-SUMMARY.md`
</output>
