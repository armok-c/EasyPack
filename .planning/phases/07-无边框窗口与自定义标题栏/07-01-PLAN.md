---
phase: 07-无边框窗口与自定义标题栏
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TitleBar.tsx
  - src/components/__tests__/TitleBar.test.tsx
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src/App.tsx
  - src/index.css
autonomous: false
requirements: [WIN-01, WIN-02, WIN-03]

must_haves:
  truths:
    - "用户看到应用顶部有自定义标题栏，包含 EasyPack 图标和名称"
    - "用户可以通过拖拽标题栏移动窗口"
    - "最小化/最大化/关闭三个按钮功能正常"
    - "窗口边缘可以拖拽调整大小"
    - "窗口在 Windows 11 下有圆角阴影效果"
    - "高 DPI 显示器下标题栏文字和按钮清晰无模糊"
  artifacts:
    - path: "src/components/TitleBar.tsx"
      provides: "自定义标题栏 React 组件"
      exports: ["TitleBar"]
    - path: "src/components/__tests__/TitleBar.test.tsx"
      provides: "标题栏组件单元测试"
    - path: "src-tauri/tauri.conf.json"
      provides: "无边框窗口配置"
      contains: '"decorations": false'
    - path: "src-tauri/capabilities/default.json"
      provides: "窗口操作权限声明"
      contains: "core:window:allow-minimize"
    - path: "src/App.tsx"
      provides: "根布局包含 TitleBar"
      contains: "TitleBar"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/TitleBar.tsx"
      via: "import + JSX"
      pattern: "import.*TitleBar.*from"
    - from: "src/components/TitleBar.tsx"
      to: "@tauri-apps/api/window"
      via: "getCurrentWindow().minimize/toggleMaximize/close"
      pattern: "getCurrentWindow"
    - from: "src/components/TitleBar.tsx"
      to: "data-tauri-drag-region"
      via: "HTML attribute on container + spacer + left section"
      pattern: "data-tauri-drag-region"
---

<objective>
实现无边框窗口和自定义标题栏

Purpose: 将 EasyPack 的窗口从系统原生标题栏切换为自定义 React 标题栏，实现现代化的 Raycast 风格无边框融合外观。所有窗口控制（最小化/最大化/关闭）通过 Tauri Window API 实现，拖拽通过 data-tauri-drag-region 属性实现。

Output: TitleBar 组件、更新的 Tauri 窗口配置、capabilities 权限、根布局调整、窗口控制按钮样式。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-无边框窗口与自定义标题栏/07-CONTEXT.md
@.planning/phases/07-无边框窗口与自定义标题栏/07-RESEARCH.md
@.planning/phases/07-无边框窗口与自定义标题栏/07-UI-SPEC.md

<interfaces>
<!-- Executor 需要了解的现有接口 -->

From src/App.tsx (当前根布局):
```tsx
// 第 49 行 — 当前布局结构
<div className="flex h-screen w-screen overflow-hidden">
  <Sidebar ... />
  <MainArea ... />
  <Toaster ... />
</div>
```

From src/index.css (颜色变量):
```css
--color-background: oklch(0.145 0.014 285.823);
--color-foreground: oklch(0.985 0.002 247.858);
--color-muted-foreground: oklch(0.556 0.019 285.938);
--color-accent: oklch(0.269 0.008 285.877);
```

From src-tauri/tauri.conf.json (当前窗口配置):
```json
{
  "app": {
    "windows": [{
      "label": "main",
      "title": "EasyPack",
      "width": 720,
      "height": 480,
      "minWidth": 600,
      "minHeight": 400,
      "resizable": true,
      "center": true
    }]
  }
}
```

From src-tauri/capabilities/default.json (当前权限):
```json
{
  "permissions": ["core:default", "dialog:default", "store:default"]
}
```

From src/lib/icons.ts (图标导出):
```typescript
import { Package, ... } from "lucide-react";
export const ICON_OPTIONS: Record<string, LucideIcon> = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 TitleBar 组件及测试</name>
  <files>src/components/TitleBar.tsx, src/components/__tests__/TitleBar.test.tsx, src/index.css</files>
  <read_first>
    - src/components/__tests__/MainArea.test.tsx (测试模式和 mock 风格参考)
    - src/index.css (颜色变量体系，确保标题栏样式使用相同变量)
    - src/lib/icons.ts (图标导入模式参考)
  </read_first>
  <behavior>
    - Test "renders EasyPack title": TitleBar 组件渲染后，页面包含文本 "EasyPack"
    - Test "renders window control buttons": TitleBar 组件渲染后，存在 aria-label 为 "最小化"、"最大化"、"关闭" 的三个 button 元素
    - Test "window control buttons call correct APIs": 点击最小化按钮时 mock 的 getCurrentWindow().minimize() 被调用；点击最大化按钮时 toggleMaximize() 被调用；点击关闭按钮时 close() 被调用
    - Test "drag region attributes": 标题栏容器 div 上存在 data-tauri-drag-region 属性；左侧图标+名称区域 div 上存在 data-tauri-drag-region 属性；弹性间隔 div 上存在 data-tauri-drag-region 属性；按钮区域的 div 上不存在 data-tauri-drag-region 属性
    - Test "title bar height": 标题栏容器 div 的 className 包含 "h-[28px]" 和 "shrink-0"
    - Test "double click toggles maximize": 在拖拽区域上触发 doubleClick 事件时，mock 的 toggleMaximize() 被调用
  </behavior>
  <action>
    **RED 阶段 — 先写测试:**

    创建 `src/components/__tests__/TitleBar.test.tsx`:

    1. Mock `@tauri-apps/api/window` 模块:
       ```typescript
       const mockMinimize = vi.fn().mockResolvedValue(undefined);
       const mockToggleMaximize = vi.fn().mockResolvedValue(undefined);
       const mockClose = vi.fn().mockResolvedValue(undefined);
       vi.mock("@tauri-apps/api/window", () => ({
         getCurrentWindow: () => ({
           minimize: mockMinimize,
           toggleMaximize: mockToggleMaximize,
           close: mockClose,
         }),
       }));
       ```

    2. 编写 6 个测试用例（如 <behavior> 中所列），使用 `render`、`screen`、`fireEvent`、`container.querySelector` 等方法

    3. 运行测试确认全部 RED: `npx vitest run src/components/__tests__/TitleBar.test.tsx`

    **GREEN 阶段 — 实现组件:**

    创建 `src/components/TitleBar.tsx`:

    1. 导入: `getCurrentWindow` from `@tauri-apps/api/window`; `Minus`, `Square`, `X`, `Package` from `lucide-react`

    2. 在模块顶层调用 `const appWindow = getCurrentWindow();`

    3. 导出 `TitleBar` 函数组件（无 props），包含:
       - `handleMinimize`: async () => await appWindow.minimize()
       - `handleMaximize`: async () => await appWindow.toggleMaximize()
       - `handleClose`: async () => await appWindow.close()

    4. JSX 结构（per D-01, D-02, D-03, D-04, D-05）:
       ```tsx
       <div data-tauri-drag-region className="flex items-center h-[28px] select-none shrink-0" onDoubleClick={handleMaximize}>
         <div data-tauri-drag-region className="flex items-center gap-[6px] pl-[10px]">
           <Package className="w-[14px] h-[14px] text-foreground/80" />
           <span className="text-[13px] font-medium text-foreground">EasyPack</span>
         </div>
         <div data-tauri-drag-region className="flex-1" />
         <div className="flex items-center h-full">
           <button className="titlebar-button" onClick={handleMinimize} aria-label="最小化">
             <Minus className="w-[12px] h-[12px]" />
           </button>
           <button className="titlebar-button" onClick={handleMaximize} aria-label="最大化">
             <Square className="w-[12px] h-[12px]" />
           </button>
           <button className="titlebar-button close-button" onClick={handleClose} aria-label="关闭">
             <X className="w-[12px] h-[12px]" />
           </button>
         </div>
       </div>
       ```

    **注意:** 按钮区域的容器 div (`className="flex items-center h-full"`) 上不添加 `data-tauri-drag-region`，这是关键细节。每个可拖拽子元素（左侧区域、spacer）都需要独立添加 `data-tauri-drag-region`。

    运行测试确认全部 GREEN: `npx vitest run src/components/__tests__/TitleBar.test.tsx`

    **CSS 阶段 — 添加窗口控制按钮样式:**

    在 `src/index.css` 文件末尾追加以下样式（使用自定义 class 而非 Tailwind，确保精确控制，per RESEARCH anti-pattern 建议）:

    ```css
    /* Window control buttons — custom classes for precise control */
    .titlebar-button {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 28px;
      height: 100%;
      border: none;
      background: transparent;
      color: var(--color-muted-foreground);
      cursor: default;
      transition: background-color 100ms ease;
    }

    .titlebar-button:hover {
      background: var(--color-accent);
      color: var(--color-foreground);
    }

    .titlebar-button.close-button:hover {
      background: oklch(0.5 0.15 25);
      color: white;
    }
    ```
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TitleBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/TitleBar.tsx` 文件存在且包含 `export function TitleBar` 或 `export const TitleBar`
    - TitleBar.tsx 包含 `import { getCurrentWindow } from "@tauri-apps/api/window"`
    - TitleBar.tsx 包含 `data-tauri-drag-region` 属性（至少 3 处：容器、左侧区域、spacer）
    - TitleBar.tsx 包含 `h-[28px]` 类名（per D-01）
    - TitleBar.tsx 包含 `shrink-0` 类名
    - TitleBar.tsx 包含 `aria-label="最小化"`、`aria-label="最大化"`、`aria-label="关闭"` 三个 button
    - TitleBar.tsx 包含 `onDoubleClick={handleMaximize}` 或等效双击处理
    - TitleBar.tsx 包含 `Package` 图标导入和使用
    - TitleBar.tsx 包含文本 "EasyPack"
    - `src/components/__tests__/TitleBar.test.tsx` 文件存在
    - `npx vitest run src/components/__tests__/TitleBar.test.tsx` 全部通过
    - `src/index.css` 包含 `.titlebar-button` 样式规则
    - `src/index.css` 包含 `.titlebar-button.close-button:hover` 样式规则
  </acceptance_criteria>
  <done>TitleBar 组件已创建并通过全部 6 个测试，窗口控制按钮样式已添加到 index.css</done>
</task>

<task type="auto">
  <name>Task 2: 配置 Tauri 无边框窗口并集成 TitleBar 到根布局</name>
  <files>src-tauri/tauri.conf.json, src-tauri/capabilities/default.json, src/App.tsx</files>
  <read_first>
    - src-tauri/tauri.conf.json (当前窗口配置，需要在 windows[0] 中添加 decorations 和 shadow)
    - src-tauri/capabilities/default.json (当前权限配置，需要添加窗口操作权限)
    - src/App.tsx (当前根布局，需要改为 flex-col 并插入 TitleBar)
    - src/components/TitleBar.tsx (Task 1 创建的组件，确认导出名称)
  </read_first>
  <action>
    **步骤 1 — 更新 tauri.conf.json (per D-06, D-07, WIN-01, WIN-02):**

    在 `src-tauri/tauri.conf.json` 的 `app.windows[0]` 对象中:
    - 在 `"center": true` 之后添加: `"decorations": false,`
    - 在 `"decorations": false,` 之后添加: `"shadow": true`

    修改后的窗口配置应为:
    ```json
    {
      "label": "main",
      "title": "EasyPack",
      "width": 720,
      "height": 480,
      "minWidth": 600,
      "minHeight": 400,
      "resizable": true,
      "center": true,
      "decorations": false,
      "shadow": true
    }
    ```

    **步骤 2 — 更新 capabilities/default.json (per RESEARCH Pitfall 4, WIN-01):**

    在 `src-tauri/capabilities/default.json` 的 `permissions` 数组中，在 `"core:default"` 之后添加三个窗口权限:
    ```json
    "permissions": [
      "core:default",
      "core:window:allow-minimize",
      "core:window:allow-toggle-maximize",
      "core:window:allow-close",
      "dialog:default",
      "store:default"
    ]
    ```

    **步骤 3 — 更新 App.tsx 根布局 (per RESEARCH Pattern 5, D-02):**

    1. 在 `App.tsx` 文件顶部的 import 区域添加:
       ```typescript
       import { TitleBar } from "@/components/TitleBar";
       ```

    2. 修改根容器 div (第 49 行附近):
       - 将 `className="flex h-screen w-screen overflow-hidden"` 改为 `className="flex flex-col h-screen w-screen overflow-hidden"`
       - 在根 div 开标签之后、`<Sidebar` 之前插入 `<TitleBar />`
       - 将 `<Sidebar>` 和 `<MainArea>` 包裹在一个新的 `<div className="flex flex-1 overflow-hidden">` 中

    3. 修改后的 JSX 结构:
       ```tsx
       <div className="flex flex-col h-screen w-screen overflow-hidden">
         <TitleBar />
         <div className="flex flex-1 overflow-hidden">
           <Sidebar ... />
           <MainArea ... />
         </div>
         <Toaster richColors position="bottom-right" duration={1500} />
       </div>
       ```

    **步骤 4 — 验证构建:**

    运行 `npx vitest run` 确保所有现有测试仍然通过（TitleBar 测试 + MainArea 测试 + CommandCard 测试 + CommandDialog 测试）。
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/tauri.conf.json` 包含 `"decorations": false`
    - `src-tauri/tauri.conf.json` 包含 `"shadow": true`
    - `src-tauri/capabilities/default.json` 包含 `"core:window:allow-minimize"`
    - `src-tauri/capabilities/default.json` 包含 `"core:window:allow-toggle-maximize"`
    - `src-tauri/capabilities/default.json` 包含 `"core:window:allow-close"`
    - `src/App.tsx` 包含 `import { TitleBar } from "@/components/TitleBar"`
    - `src/App.tsx` 包含 `<TitleBar />`
    - `src/App.tsx` 根容器 className 包含 `flex-col`
    - `src/App.tsx` 包含 `flex-1 overflow-hidden` 的包裹 div（包裹 Sidebar 和 MainArea）
    - `npx vitest run` 全部测试通过（无新增失败）
  </acceptance_criteria>
  <done>Tauri 窗口配置已更新为无边框模式，窗口操作权限已声明，TitleBar 已集成到根布局，所有测试通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 验证无边框窗口视觉效果和交互</name>
  <files>无文件修改</files>
  <read_first>
    - src/components/TitleBar.tsx (确认实现细节)
  </read_first>
  <action>
    此任务为人工验证 checkpoint。Claude 在 Task 1 和 Task 2 完成后，需要用户启动应用验证以下行为。
  </action>
  <what-built>
    无边框窗口 + 自定义标题栏的完整实现:
    - TitleBar React 组件（图标 + EasyPack 名称 + 拖拽区域 + 三个窗口控制按钮）
    - tauri.conf.json 无边框配置（decorations: false, shadow: true）
    - capabilities 窗口操作权限
    - App.tsx 根布局调整（flex-col + TitleBar 在顶部）
    - 窗口控制按钮 CSS 样式（半透明默认 + hover 高亮 + 关闭按钮红色 hover）
  </what-built>
  <how-to-verify>
    **启动应用:**
    ```bash
    cd E:/git/EasyPack && npm run tauri dev
    ```

    **检查项 1 — 标题栏外观 (WIN-01, D-01, D-02, D-03, D-04):**
    - [ ] 窗口顶部显示 28px 高度的自定义标题栏
    - [ ] 标题栏左侧显示 Package 图标和 "EasyPack" 文字
    - [ ] 标题栏与内容区域无分割线，背景色融为一体（融合风格）
    - [ ] 标题栏右侧显示三个窗口控制按钮（─ / □ / ×），默认半透明几乎不可见
    - [ ] 鼠标 hover 到按钮时显示高亮背景，关闭按钮 hover 显示红色背景

    **检查项 2 — 拖拽和窗口控制 (WIN-01, D-05):**
    - [ ] 拖拽标题栏（图标区域或空白区域）可以移动窗口
    - [ ] 点击最小化按钮（─）窗口最小化到任务栏
    - [ ] 点击最大化按钮（□）窗口最大化/还原切换
    - [ ] 点击关闭按钮（×）应用退出
    - [ ] 双击标题栏拖拽区域切换最大化/还原

    **检查项 3 — Resize 和阴影 (WIN-02, D-06, D-07):**
    - [ ] 拖拽窗口四边和四角可以调整窗口大小
    - [ ] Windows 11 下窗口有圆角阴影效果
    - [ ] 最小窗口尺寸限制（600x400）仍然生效

    **检查项 4 — 内容区域正常:**
    - [ ] 侧边栏正常显示和交互（添加/选择/删除项目）
    - [ ] 指令卡片正常显示和交互（点击执行命令）
    - [ ] 模态窗正常弹出和关闭
    - [ ] 无布局错乱或内容溢出

    **可选 — 高 DPI 验证 (WIN-03):**
    如果有条件，在 1.5x 或 2x DPI 显示器上验证标题栏文字和按钮清晰正常。
  </how-to-verify>
  <resume-signal>
    验证完成后请回复:
    - "approved" — 全部通过，继续提交
    - 描述具体问题 — 我将修复后重新验证
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri capabilities | 窗口 API 调用受 capabilities 权限系统控制，仅允许声明的窗口操作 |
| TitleBar UI | 硬编码内容（"EasyPack"），无动态输入或外部内容注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Elevation of Privilege | capabilities/default.json | mitigate | 仅声明三个必需权限（allow-minimize, allow-toggle-maximize, allow-close），不使用通配符 `core:window:allow-all` |
| T-7-02 | Tampering | TitleBar.tsx | accept | 标题栏内容硬编码为 "EasyPack"，无用户输入、无动态内容注入点，无 XSS 风险 |
| T-7-03 | Information Disclosure | tauri.conf.json | accept | 窗口配置（尺寸、无边框标志）不包含敏感信息，不影响安全态势 |
</threat_model>

<verification>
1. `npx vitest run` — 全部测试通过（包括 TitleBar 6 个新测试 + 现有测试）
2. `npm run tauri build` — 前端构建成功（验证 TypeScript 编译无错误）
3. checkpoint:human-verify — 人工验证窗口外观和交互
</verification>

<success_criteria>
- TitleBar 组件渲染 EasyPack 图标和名称，三个窗口控制按钮功能正常
- 窗口可通过拖拽标题栏移动，双击标题栏切换最大化
- 窗口边缘可拖拽调整大小（Tauri 内置 WM_NCHITTEST）
- Windows 11 下窗口有圆角阴影
- 所有测试通过（TitleBar 6 个 + 现有测试无回归）
- Tauri capabilities 仅包含三个必需的窗口权限（最小权限原则）
</success_criteria>

<output>
After completion, create `.planning/phases/07-无边框窗口与自定义标题栏/07-01-SUMMARY.md`
</output>
