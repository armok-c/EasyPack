---
phase: 22-global-command-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/hooks/useProject.ts
  - src/hooks/__tests__/useProject.test.tsx
autonomous: true
requirements:
  - CMD-09
  - CMD-10

must_haves:
  truths:
    - "useProject 不再返回 commandMode state"
    - "useProject 不再返回 customCommands state"
    - "commands 派生逻辑仅包含项目指令（projectCommandsMap）"
    - "addCommand/updateCommand/deleteCommand 无 global 分支"
    - "enableProjectCommands 简化为项目指令初始化（不调用 getDefaultsAsCommandItems）"
    - "disableProjectCommands 已移除（无模式切换概念）"
    - "FloatApp 和 useTray 的 commands 输入自动跟随派生变更（D-09）"
  artifacts:
    - path: src/hooks/useProject.ts
      provides: "无 commandMode/customCommands 的纯 project-scope 钩子"
      min_lines: 950
    - path: src/hooks/__tests__/useProject.test.tsx
      provides: "更新后的测试，覆盖简化后的 CRUD 逻辑"
      min_lines: 350
  key_links:
    - from: useProject.ts commands useMemo
      to: projectCommandsMap[selectedId]
      via: "projectCmds.map(injectBinding).sort(...)"
---

<objective>
Phase 22 核心逻辑重构

从 useProject.ts 中完全移除 `commandMode` state（D-10）、`customCommands` state（D-11）、以及所有与全局指令相关的 CRUD 分支（D-14）。
简化 `commands` 派生逻辑（仅读取 projectCommandsMap），并将 `enableProjectCommands` 退化为纯项目指令初始化（D-15）。
同时更新 useProject 测试用例以匹配新接口。

Purpose: 消除全局指令系统的全部逻辑痕迹。FloatApp/useTray 自动跟随 `commands` 派生变更（D-09），无需单独适配。
Output: 重构后的 useProject.ts（约 950 行，缩减 ~100 行）和对应测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-global-command-removal/22-01-PLAN.md
@.planning/phases/22-global-command-removal/22-CONTEXT.md

<interfaces>
变更前的 useProject 接口相关部分：
```typescript
return {
  // ...
  commandMode, // 'global' | 'project' ← 移除
  editMode, // boolean ← 保留
  setEditMode, // (editMode: boolean) => void ← 保留
  enableProjectCommands, // () => Promise<void> ← 简化
  disableProjectCommands, // () => Promise<void> ← 移除
  commands, // CommandItem[] ← 派生逻辑简化
  addCommand, // 移除 scope 参数 ← 简化
  updateCommand, // 移除 global 分支 ← 简化
  deleteCommand, // 移除 global 分支 ← 简化
  customCommands, // ← 移除
  // ...
};
```

设计决策参考（CONTEXT.md）：
- D-09: 悬浮窗/托盘自动跟随 commands 派生逻辑变更
- D-10: commandMode state 完全移除
- D-11: customCommands state 完全移除
- D-14: addCommand/updateCommand/deleteCommand 中所有 global 分支移除
- D-15: enableProjectCommands/disableProjectCommands 简化
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useProject.ts — 核心逻辑重构</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>src/hooks/useProject.ts</read_first>

  <behavior>
    - 行为 1: useProject 不再返回 commandMode 属性
    - 行为 2: useProject 不再返回 customCommands 属性
    - 行为 3: useProject 不再返回 disableProjectCommands 属性
    - 行为 4: commands 只从 projectCommandsMap[selectedId] 派生，不包含全局元素
    - 行为 5: addCommand 无 scope 参数，始终写入 projectCommandsMap
    - 行为 6: updateCommand 只操作 projectCommandsMap
    - 行为 7: deleteCommand 只操作 projectCommandsMap
    - 行为 8: enableProjectCommands 不调用 getDefaultsAsCommandItems()
  </behavior>

  <action>
    对 useProject.ts 进行以下精确修改（按顺序）：

    **1. 移除 state 声明**
    - 删除第 63 行：`const [customCommands, setCustomCommands] = useState<CommandItem[]>([]);`
    - 删除第 74 行：`const [commandMode, setCommandMode] = useState<"global" | "project">("global");`
    - 保留 `projectCommandsMap`、`editMode`、`presetShortcutsMap`

    **2. 简化 commands 派生（第 99-128 行的 useMemo）**
    替换为：
    ```typescript
    const commands = useMemo(() => {
      if (!selectedId) return [];
      const projectCmds = projectCommandsMap[selectedId];
      if (!projectCmds || projectCmds.length === 0) return [];
      return [...projectCmds]
        .map((cmd) => {
          const bindingKey = `command.${cmd.id}`;
          if (shortcutBindings[bindingKey]) {
            return { ...cmd, shortcut: shortcutBindings[bindingKey] };
          }
          return cmd;
        })
        .sort((a, b) => a.addedAt - b.addedAt);
    }, [selectedId, projectCommandsMap, shortcutBindings]);
    ```
    注意：不再引用 `customCommands`、`commandMode`、`presetShortcutsMap`。不再调用 `getDefaultsAsCommandItems()`。

    **3. 移除 auto-detect commandMode effect（第 131-139 行）**
    删除整个 useEffect 块。

    **4. 移除 loadProfileDataIntoState 中的 CUSTOM_COMMANDS_KEY 读取（第 166、171-172 行）**
    - 删除 `const savedCommands = await s.get<CommandItem[]>(CUSTOM_COMMANDS_KEY);`
    - 删除 `if (savedCommands) setCustomCommands(savedCommands); else setCustomCommands([]);`
    - 确保不在任何地方调用 setCustomCommands

    **5. 移除 migrateToProfiles 中的 CUSTOM_COMMANDS_KEY 引用**
    - 第 232 行：从 `keysToMigrate` 数组中移除 `CUSTOM_COMMANDS_KEY`
    - 第 259 行：删除 `const savedCommands = await ms.get<CommandItem[]>(CUSTOM_COMMANDS_KEY);`
    - 第 260-266 行：删除使用 `savedCommands` 的所有代码（第 259-266 行的 if 块，包含 shortcut 迁移逻辑）

    **6. 重构 addCommand（第 524-586 行）**
    简化签名和实现：
    - 参数从 `(name, command, icon?, scope?, extra?)` → `(name, command, icon?, extra?)`
    - 移除 effectiveScope 计算逻辑
    - 移除 global 分支（第 567-583 行 else 块）
    - 移除 `getDefaultsAsCommandItems()` 初始化逻辑（第 551-559 行）
    - 直接向 projectCommandsMap[selectedId] 追加新命令
    - 当 `!selectedId` 时直接 return 或 toast 提示

    **7. 重构 updateCommand（第 589-638 行）**
    - 移除 `commandMode` 判断，始终操作 projectCommandsMap
    - 移除 global 分支（第 618-634 行 else 块）
    - 签名简化：`async (id, data)`

    **8. 重构 deleteCommand（第 641-678 行）**
    - 移除 `commandMode` 判断，始终操作 projectCommandsMap
    - 移除 global 分支（第 667-674 行 else 块）
    - 当删除后项目指令为空时，不再 setCommandMode（命令模式已移除），直接删除 projectCommandsMap 条目

    **9. 简化 enableProjectCommands（第 788-807 行）**
    - 移除 `setCommandMode("project")` 调用
    - 移除 `getDefaultsAsCommandItems()` 调用
    - 简化为：如果已有项目指令则直接返回，否则创建空指令数组并进入编辑模式
    ```typescript
    const enableProjectCommands = useCallback(async () => {
      if (!selectedId) return;
      const existing = projectCommandsMap[selectedId];
      if (existing && existing.length > 0) {
        // Already has commands, just enter edit mode
        setEditMode(true);
        return;
      }
      // Initialize empty project command set
      setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: [] }));
      await profileStore?.set(projectCommandsKey(selectedId), []);
      setEditMode(true);
      toast.success("请添加项目指令");
    }, [selectedId, profileStore, projectCommandsMap]);
    ```

    **10. 移除 disableProjectCommands（第 810-812 行）**
    完全删除此函数。

    **11. 简化 selectProject（第 436-451 行）**
    移除第 441-442 行的 `setCommandMode` 调用。

    **12. 简化 switchProfile（第 315-348 行）**
    移除第 332 行的 `setCommandMode("global")`

    **13. 简化 assignShortcut（第 722-757 行）**
    移除所有 `commandMode === "project"` 和 `customCommands.some(...)` 的判断分支。
    对所有指令均统一操作 projectCommandsMap。

    **14. 简化 clearShortcut（第 760-783 行）**
    同 assignShortcut，统一操作 projectCommandsMap。

    **15. 更新 return 语句（第 1016 行以后）**
    - 移除 `commandMode`、`customCommands`、`disableProjectCommands` 条目
    - updateCommand 签名参数简化
    - addCommand 签名参数简化

    **16. 清理导入**
    - 检查 `getDefaultsAsCommandItems` 导入是否还有引用——如果没有则移除导入（第 8 行）
    - 检查 `toast` 导入是否仍被使用（会在各处 toast 调用中使用，应保留）
  </action>

  <verify>
    <automated>npx vitest run src/hooks/__tests__/useProject.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <done>
    - useProject 不再导出 commandMode、customCommands、disableProjectCommands
    - addCommand 签名：`(name, command, icon?, extra?)`，无 scope 参数
    - commands 派生只包含项目指令
    - 编译通过（`npx tsc --noEmit`）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useProject.test.tsx — 更新测试用例</name>
  <files>src/hooks/__tests__/useProject.test.tsx</files>
  <read_first>src/hooks/__tests__/useProject.test.tsx</read_first>

  <behavior>
    - 行为 1: 所有测试中 CommandItem.scope 字段应为 "project" 而非 "global"
    - 行为 2: addCommand 测试不传 scope 参数
    - 行为 3: 无 commandMode 相关断言
    - 行为 4: 无 customCommands 相关测试
    - 行为 5: disableProjectCommands 相关测试移除
    - 行为 6: enableProjectCommands 测试不验证 getDefaultsAsCommandItems() 初始化
  </behavior>

  <action>
    根据 useProject.ts 的重构结果，对标更新测试文件：

    **1. 数据更新**
    - 所有 `scope: "global"` 改为 `scope: "project"`（共约 6 处）

    **2. 移除的测试**
    - "addCommand updates customCommands state with new command" —— 原第 76-88 行，customCommands 已移除，删除
    - "initializes customCommands from persisted store data" —— 原第 182-205 行，customCommands 已移除，删除
    - "disableProjectCommands switches to global mode without deleting data" —— 原第 321-342 行，函数已移除，删除
    - 其他引用 customCommands 或 commandMode 的具体断言需逐个清理

    **3. 需要调整的测试**
    - "addCommand creates command with type=custom, scope=project"（原第 90-106 行）：保留，scope 断言改为 `"project"`，addCommand 调用不加 scope 参数
    - "commands returns merged presets + custom sorted by addedAt"（原第 149-163 行）：现在 addCommand 默认写入 projectCommandsMap，所以 commands 只包含 project 指令。更新断言，移除对全局预设的检查
    - "additionalPersists to projectCommands store key"（原第 165-180 行）：调整存储键验证，因为现在 addCommand 没有 scope 参数
    - "enableProjectCommands replaces commands with 2 project-level default copies"（原第 280-293 行）：enableProjectCommands 不再初始化默认指令，因此 commands 应为空数组。更新断言
    - "enableProjectCommands creates commands with type=preset scope=project"（原第 295-306 行）：同上，不再初始化预设指令
    - "deleting last project-level command auto-reverts to global mode"（原第 400-423 行）：不反回到 global 模式（模式已移除），仅验证指令为空

    **4. 部分保留的测试**
    - updateCommand / deleteCommand 核心逻辑测试保留，仅调整引用方式
    - removeProject cleans up project-level store key 保留
    - commands update when selected project changes 保留，调整断言
    - addCommand in project mode appends to project-level store key 保留

    **5. 新测试建议（不是必须）**
    - 验证 enableProjectCommands 在不初始化默认指令的情况下直接进入编辑模式
    - 验证 deleteCommand 删除最后一条指令后 projectCommandsMap 条目被移除

    注意：确保每次 `mockStore.get` 的模拟返回值中不包含 "customCommands" key（移除相关 mock 实现），
    因为 loadProfileDataIntoState 不再读取该 key。
  </action>

  <verify>
    <automated>npx vitest run src/hooks/__tests__/useProject.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <done>
    - 所有 useProject 测试通过
    - 无 "global" scope 引用
    - 无 commandMode/customCommands/disableProjectCommands 相关测试
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| useProject.ts → profileStore | 本地持久化存储，无外部输入 |
| commands 派生 → FloatApp/useTray | 通过事件/ref 传递，内部受控 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Data Loss (Tampering) | useProject.ts CRUD 重写 | mitigate | 所有非预期的 commandMode/customCommands 引用在编译时被 TypeScript 捕获；测试用例覆盖 CRUD 核心路径 |
| T-22-04 | DoS | enableProjectCommands 空初始化 | accept | 创建空指令数组后进入编辑模式，用户可自行添加指令，无功能损失 |
</threat_model>

<verification>
- `npx tsc --noEmit` 通过
- `npx vitest run src/hooks/__tests__/useProject.test.tsx` 通过
</verification>

<success_criteria>
- [ ] useProject 返回中无 commandMode、customCommands、disableProjectCommands
- [ ] addCommand 签名无 scope 参数
- [ ] commands 派生仅包含 projectCommandsMap 中数据
- [ ] 所有 useProject 测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/22-global-command-removal/22-02-SUMMARY.md`
</output>
