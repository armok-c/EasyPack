---
phase: 22-global-command-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/presets.ts
  - src/hooks/useProject.ts
autonomous: true
requirements:
  - CMD-09
  - CMD-10

must_haves:
  truths:
    - "CommandItem.scope 类型不再包含 'global' 值"
    - "预设库（ALL_PRESETS）中所有指令的 scope 为 'project'"
    - "getDefaultsAsCommandItems() 不再预设 scope 为 global"
    - "应用启动时，旧 CUSTOM_COMMANDS_KEY 数据被检测并自动清除，用户看到一次性 toast 通知"
  artifacts:
    - path: src/lib/types.ts
      provides: "CommandItem 接口定义，scope 类型仅含 'project'"
      contains: "scope: \"project\""
    - path: src/lib/presets.ts
      provides: "预设指令库，所有预设 scope 为 project"
      min_lines: 85
    - path: src/hooks/useProject.ts
      provides: "启动时检测并清除旧 CUSTOM_COMMANDS_KEY 数据"
  key_links:
    - from: src/lib/presets.ts
      to: src/lib/types.ts
      via: "CommandItem import"
    - from: src/hooks/useProject.ts
      to: profileStore
      via: "profileStore.get(CUSTOM_COMMANDS_KEY) → profileStore.delete()"
---

<objective>
Phase 22 数据模型基础 + 启动清理

移除 `CommandItem.scope` 中的 `"global"` 值（D-12），更新预设库 scope 为 `"project"`（D-07），废弃 `getDefaultsAsCommandItems()` 的项目初始化用途（D-08），并在应用启动时检测清理旧的全局指令存储数据（D-01/D-02）。

Purpose: 为后续全局指令系统移除打下数据层基础，确保启动时旧数据被彻底清理，用户获得一次性通知。
Output: 类型定义更新、预设库更新、useProject.ts 启动清理逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-global-command-removal/22-CONTEXT.md

<interfaces>
现状：CommandItem.scope 类型为 `"global" | "project"`，ALL_PRESETS 所有项目的 scope 为 "global"，
getDefaultsAsCommandItems() 的返回值 scope 为 "global"。profileStore 中可能存储着旧 CUSTOM_COMMANDS_KEY 数据。

设计原则（摘自 CONTEXT.md）：
- D-01: 现有全局自定义指令直接丢弃，toast 通知
- D-02: 启动时检测 CUSTOM_COMMANDS_KEY → 删除键 → toast
- D-07: 预设 scope 从 "global" 改为 "project"
- D-08: getDefaultsAsCommandItems() 不再用于初始化项目指令集
- D-12: CommandItem.scope 移除 "global" 值
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — 移除 CommandItem.scope 中的 "global" 值</name>
  <files>src/lib/types.ts</files>
  <read_first>src/lib/types.ts</read_first>
  <action>
    在 CommandItem 接口（第 9-20 行）中，将 `scope: "global" | "project"`（第 15 行）改为 `scope: "project"`。

    注意：只改这一行，不改动其他字段。保持所有注释不变。
  </action>
  <verify>
    <automated>
      grep -n 'scope:' src/lib/types.ts | grep -v '^\s*//'
    </automated>
    输出包含 `scope: "project"`，不包含 `"global" | "project"`
  </verify>
  <done>
    CommandItem.scope 类型变为仅 "project" 一个字符串字面量类型。文件编译通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: presets.ts — 更新预设 scope + 废弃 getDefaultsAsCommandItems</name>
  <files>src/lib/presets.ts</files>
  <read_first>src/lib/presets.ts</read_first>
  <action>
    做两处修改：

    1. getDefaultsAsCommandItems() 函数（第 64-85 行）中两处 `scope: "global" as const`（第 72、81 行）改为 `scope: "project" as const`。

    2. 在函数注释的第一行后增加说明：`@deprecated Phase 22 — 不再用于初始化项目指令集（D-08），仅保留供外部兼容引用。`

    注意：不删除该函数，不改变其他任何内容（ALL_PRESETS 中预设的 scope 不在该文件中——ALL_PRESETS 是 PresetCommand 类型，其定义中没有 scope 字段，所以无需修改）。
  </action>
  <verify>
    <automated>
      grep -c "scope.*project" src/lib/presets.ts
    </automated>
    输出不为 0（表示 scope 引用已改为 project）。文件编译通过。
  </verify>
  <done>
    presets.ts 中所有 scope 引用为 "project"，getDefaultsAsCommandItems() 标记为 deprecated。
  </done>
</task>

<task type="auto">
  <name>Task 3: useProject.ts — 启动时检测并清理旧 CUSTOM_COMMANDS_KEY 数据</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>src/hooks/useProject.ts</read_first>
  <action>
    在 useProject.ts 的 init useEffect（第 351-385 行的 `init()` 函数）中，在 `loadProfileDataIntoState(ps)` 调用之后、`setStore(ps)` 之前（约第 370 行），插入 CUSTOM_COMMANDS_KEY 数据清理。

    添加以下逻辑（参照 D-01/D-02）：

    ```typescript
    // Phase 22: 检测并清理旧全局指令数据（CUSTOM_COMMANDS_KEY）
    // D-02: 启动时一次性检测并删除旧数据，toast 通知用户
    const oldCustomCmds = await ps.get<CommandItem[]>(CUSTOM_COMMANDS_KEY);
    if (oldCustomCmds && oldCustomCmds.length > 0) {
      await ps.delete(CUSTOM_COMMANDS_KEY);
      await ps.save();
      toast.info("全局指令已移除，请使用项目环境添加指令");
    }
    ```

    导入 `CommandItem` 类型（检查是否已导入——在第 7 行 `import type { CommandItem ... }` 中已存在，无需新增）。

    注意：`toast.info` 方法——检查 sonner toast 是否支持 `.info()`。如果不支持，用 `toast("全局指令已移除，请使用项目环境添加指令")` 代替。
    用实际存在的 sonner API（参考现有 `toast.success` / `toast.error` 用法）。
  </action>
  <verify>
    <automated>
      grep -c "CUSTOM_COMMANDS_KEY" src/hooks/useProject.ts && grep -c "全局指令已移除" src/hooks/useProject.ts
    </automated>
    两行输出均不为 0。文件编译通过。
  </verify>
  <done>
    启动时自动检测旧全局指令数据，并 toast 通知用户。旧 key 被删除。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
无外部输入边界——本 Plan 仅涉及本地类型定义和启动时内部数据清理。

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Data Loss | useProject.ts CUSTOM_COMMANDS_KEY 清理 | accept | 用户已通过 toast 知悉全局指令被丢弃（D-01 明确允许） |
| T-22-02 | Compatibility | types.ts scope 类型变更 | mitigate | TypeScript 编译检查会捕获所有旧的 "global" 引用，后续 Plan 按依赖顺序修复 |
</threat_model>

<verification>
- `npx tsc --noEmit` 通过（types.ts + presets.ts 变更可编译）
- All existing tests pass: `npx vitest run` 通过
</verification>

<success_criteria>
- [ ] CommandItem.scope 类型为 `"project"` 单值
- [ ] presets.ts 中 scope 引用皆为 "project"
- [ ] CUSTOM_COMMANDS_KEY 启动清理逻辑就位
</success_criteria>

<output>
完成后创建 `.planning/phases/22-global-command-removal/22-01-SUMMARY.md`
</output>
