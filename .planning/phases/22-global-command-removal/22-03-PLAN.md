---
phase: 22-global-command-removal
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - src/App.tsx
  - src/components/MainArea.tsx
  - src/components/CommandDialog.tsx
  - src/components/__tests__/MainArea.test.tsx
  - src/components/__tests__/CommandDialog.test.tsx
autonomous: true
requirements:
  - CMD-09
  - CMD-10

must_haves:
  truths:
    - "MainArea 中没有全局/项目的 Toggle Group 切换按钮组"
    - "MainArea 中标题标签从 '项目指令' 改为 '项目环境'"
    - "有项目时，指令网格第一个卡片是内置 '终端' 卡片，不可编辑/删除"
    - "终端卡片点击后在项目目录打开 cmd.exe"
    - "CommandDialog 中没有 scope 选择器（无全局/项目切换）"
    - "App.tsx 不再传递 commandMode prop 到 MainArea"
  artifacts:
    - path: src/components/MainArea.tsx
      provides: "无 Toggle Group，含内置终端卡片的项目环境视图"
      min_lines: 300
    - path: src/components/CommandDialog.tsx
      provides: "无 scope 选择器的纯项目指令弹窗"
      min_lines: 440
    - path: src/App.tsx
      provides: "不再传递 commandMode/disableProjectCommands/isProjectToggleDisabled"
      min_lines: 550
  key_links:
    - from: MainArea.tsx
      to: CommandCard 组件
      via: "内置终端卡片 + 指令映射"
    - from: MainArea.tsx 终端卡片 onClick
      to: project executeCommand
      via: "调用 onExecute('cmd.exe') 或 Rust invoke execute_command"
---

<objective>
Phase 22 UI 重构

修改 MainArea：移除全局/项目切换 Toggle Group（D-13），添加内置"终端"卡片（D-03/D-04/D-05），重命名区域标签为"项目环境"（CMD-10）。
修改 CommandDialog：移除 scope 选择器（仅保留 project scope）。
修改 App.tsx：适配 useProject 的新接口，停止传递 commandMode 等已移除的 props。

Purpose: 完成全局指令移除的 UI 层，让用户可以直观看到重构后的"项目环境"界面。
Output: 更新后的 MainArea、CommandDialog、App.tsx 及对应测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-global-command-removal/22-01-PLAN.md
@.planning/phases/22-global-command-removal/22-02-PLAN.md
@.planning/phases/22-global-command-removal/22-CONTEXT.md

<interfaces>
useProject 新接口（Plan 02 后）中与 UI 相关的部分：
- `commands: CommandItem[]` — 仅包含项目指令
- `editMode: boolean`
- `setEditMode: (v: boolean) => void`
- `addCommand: (name, command, icon?, extra?) => Promise<void>` — 无 scope 参数
- `updateCommand: (id, data) => Promise<void>`
- `deleteCommand: (id) => Promise<void>`
- `enableProjectCommands: () => Promise<void>` — 简化版
- 不再有: `commandMode`, `disableProjectCommands`

App.tsx 中当前 MainArea props（第 506-525 行）需移除：
- `commandMode`（行 510）
- `disableProjectCommands`（行 517）
- `isProjectToggleDisabled`（行 524）

MainAreaProps interface（第 11-34 行）需移除：
- `commandMode`（行 16）
- `disableProjectCommands`（行 23）
- `isProjectToggleDisabled`（行 33）
- `enableProjectCommands` 保留但接口简化（行 22）

CommandDialogProps interface（第 29-43 行）需移除：
- `commandMode`（行 41）
- `scope?: "global" | "project"`（行 36）

设计决策参考：
- D-03: 始终内置"终端"卡片，在所有项目指令之前显示
- D-04: 终端在项目目录打开 cmd.exe，不对应 CommandItem 数据模型
- D-05: 终端不属于预设库，不可编辑/删除
- D-10: commandMode 移除（CommandDialog scope 选择器相应移除）
- D-13: Toggle Group 完全移除
- CMD-10: 原"项目指令"栏目改名为"项目环境"

"终端"卡片规格：
- 图标：Terminal（lucide-react），名称："终端"
- 始终在指令网格首位（commands[0] 之前独立渲染）
- 编辑模式下不显示编辑/删除按钮（canEdit=false）
- 点击行为：对应当前项目路径，打开系统默认终端窗口（cmd.exe）
- 键盘导航：作为网格中第一个可聚焦元素
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx — 适配 useProject 新接口</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    做以下精确修改：

    1. 从 useProject() 解构中移除（第 33-80 行）：
       - `commandMode`（第 44 行）
       - `disableProjectCommands`（第 51 行）
       - `enableProjectCommands` 保留（第 50 行——仍需要）

    2. 从 `MainArea` JSX props 中移除（第 506-525 行）：
       - 第 510 行：`commandMode={commandMode}`
       - 第 517 行：`disableProjectCommands={disableProjectCommands}`
       - 第 524 行：`isProjectToggleDisabled={isProjectToggleDisabled}`

    3. 检查 `isProjectToggleDisabled` 的声明（第 89-90 行）：
       - 如果变量 `isProjectToggleDisabled` 不再被任何 props 使用，则删除其声明

    注意：这三处移除是 App.tsx 中所有需要的变更。不添加新代码。
  </action>
  <verify>
    <automated>grep -c "commandMode" src/App.tsx</automated>
    输出应为 0。`npx tsc --noEmit` 通过。
  </verify>
  <done>
    App.tsx 不再引用 commandMode，不再传递已移除的 props。
  </done>
</task>

<task type="auto">
  <name>Task 2: MainArea.tsx — 移除 Toggle Group，添加终端卡片，重命名区域</name>
  <files>src/components/MainArea.tsx</files>
  <read_first>src/components/MainArea.tsx</read_first>
  <action>
    进行以下精确修改：

    **1. Prop 接口变更（MainAreaProps，第 11-34 行）**
    删除：
    - `commandMode: "global" | "project"`（第 16 行）
    - `disableProjectCommands: () => Promise<void>`（第 23 行）
    - `isProjectToggleDisabled: boolean`（第 33 行）
    - addCommand 的 `scope?: "global" | "project"` 参数（第 19 行）
    - updateCommand 中 scope 相关（不变——updateCommand 无 scope 参数，保持原样）
    更新：
    - `commands` 注释去掉 "Phase 4: command list" 中的 mode 相关说明
    - 保持 `enableProjectCommands: () => Promise<void>`（第 22 行，保留但重构后仍接受）

    **2. 组件签名变更（第 40-59 行）**
    - 删除 `commandMode`、`disableProjectCommands`、`isProjectToggleDisabled` 的解构
    - 保留 `enableProjectCommands`

    **3. handleDialogSubmit 变更（第 77-91 行）**
    - `data` 类型中移除 `scope?: "global" | "project"`
    - addCommand 调用中移除 `data.scope` 参数，改为 `addCommand(data.name, data.command, data.icon, { scriptLines: data.scriptLines, executionMode: data.executionMode })`

    **4. 移除 Toggle Group UI（第 222-258 行）**
    - 完全删除从 `<div className="flex items-center justify-between mt-2">` 开始到 `</div>` 结束的整个 Toggle Group 代码块（约第 222-269 行）。
    - 注意：保留 `onOpenFolder` 按钮！它现在应该直接放在项目标题区域下方或右侧。

    替换为简化版行（在原 Toggle Group 位置）：
    ```tsx
    <div className="flex items-center justify-between mt-2">
      <span className="text-xs text-muted-foreground">项目环境</span>
      <Button
        variant="outline"
        size="sm"
        onClick={onOpenFolder}
        disabled={!currentProject}
        aria-label="打开项目文件夹"
      >
        <FolderOpen className="size-3.5" />
        打开文件夹
      </Button>
    </div>
    ```

    **5. 添加内置"终端"卡片（在指令网格 renders 中，第 278 行之前）**

    在 `{commands.map((cmd, index)` 之前添加：
    ```tsx
    {/* D-03/D-04/D-05: 内置终端卡片 — 始终显示，不对应 CommandItem 数据模型 */}
    <CommandCard
      name="终端"
      icon={getIconByName("Terminal")}
      command="cmd.exe"
      isCustom={false}
      editMode={false}
      onClick={() => onExecute("cmd.exe")}
    />
    ```

    这需要导入 `CommandCard`（已导入第 4 行）。`getIconByName` 也已导入（第 6 行）。

    **6. 键盘导航调整（约第 64 行 focusedCardIndex）**
    由于终端卡片是额外元素（不是 commands 数组的一部分），键盘导航的 focusedCardIndex 仍指向 commands 数组索引。
    终端卡片作为网格的第一个元素，但不参与上下左右快捷键导航（保持 focusedCardIndex 范围 0..commands.length-1，
    聚焦第一个 commands 元素时跳过终端卡片）。

    具体调整：
    - 保持 `focusedCardIndex` 不变指向 `commands` 数组索引
    - 在 `handleGridKeyDown` 的 `Enter` 处理中（第 150-157 行），不处理终端（因为它不在 commands 中）
    - 在 DOM focus effect（第 111-118 行）中，`querySelectorAll` 会选中所有 button 元素（包括终端卡片），
      所以 `buttons[focusedCardIndex]` 需偏移 +1 来跳过终端卡片（网格位置 0 是终端，位置 1 是 commands[0]）。
      将 `buttons[focusedCardIndex]` 改为 `buttons[focusedCardIndex + 1]`（因为终端卡在索引 0）。

    **7. 删除不再使用的 prop 类型关联**
    在 MainArea 中搜索 `isProjectToggleDisabled` 和 `commandMode`，确保所有引用已移除。

    **8. 导入确认**
    确认 `Terminal` 从 lucide-react 的导入——检查是否有直接引用。CommandDialog 和 presets 已有 Terminal 引用，
    但 MainArea 通过 `getIconByName("Terminal")` 使用图标，不需要额外导入 lucide-react。
  </action>
  <verify>
    <automated>
      grep -c "Toggle Group\|commandMode\|commandMode ===\|disableProjectCommands\|isProjectToggleDisabled\|全局指令\|项目指令" src/components/MainArea.tsx
    </automated>
    输出为 0（无遗留引用）。检查 Terminal 卡片存在：`grep -c "终端" src/components/MainArea.tsx` 输出 > 0。
    `npx tsc --noEmit` 通过。
  </verify>
  <done>
    - MainArea 中无 Toggle Group / commandMode 引用
    - 区域标签显示"项目环境"
    - 终端卡片始终显示在指令网格首位，不可编辑/删除
  </done>
</task>

<task type="auto">
  <name>Task 3: CommandDialog.tsx — 移除 scope 选择器</name>
  <files>src/components/CommandDialog.tsx</files>
  <read_first>src/components/CommandDialog.tsx</read_first>
  <action>
    进行以下精确修改：

    **1. Props 接口变更（第 29-43 行）**
    - 删除第 36 行：`scope?: "global" | "project";`
    - 删除第 41 行：`commandMode: "global" | "project";`
    - 更新第 37 行：`onSubmit` 类型中移除 `scope?: "global" | "project"`

    **2. 移除 scope state 及相关逻辑**
    - 删除第 76-78 行：`const [selectedScope, setSelectedScope] = ...` 声明
    - 删除第 176 行：`setSelectedScope(...)` 在 handleOpenChange 中的重置

    **3. 移除 scope selector UI（第 307-351 行）**
    - 删除整个 scope selector 代码块（从 `{!isEditing && (` 到 `)}` 的 if 块，约第 307-351 行）

    **4. 更新 handleSubmit（第 143-163 行）**
    - 删除 `scope: selectedScope` 字段（第 151 行/第 160 行中的 scope 字段）

    **5. 清理组件签名（第 45-52 行）**
    - 从参数解构中删除 `commandMode`、`hasProject`
    - 如果 `hasProject` 不再被使用（检查内部引用），也删除相关 prop 声明和类型

    注意：删除 scope selector 后，submit 数据中不再包含 scope 字段，因为 CommandItem 的 scope 现在是固定的 "project"。
  </action>
  <verify>
    <automated>
      grep -c "selectedScope\|commandMode\|scope.*global\|scope.*project" src/components/CommandDialog.tsx
    </automated>
    输出应为 0（无 scope 选择器痕迹）。`npx tsc --noEmit` 通过。
  </verify>
  <done>
    CommandDialog 中无 scope 选择器，提交数据不含 scope 字段。
  </done>
</task>

<task type="auto">
  <name>Task 4: MainArea.test.tsx — 更新测试</name>
  <files>src/components/__tests__/MainArea.test.tsx</files>
  <read_first>src/components/__tests__/MainArea.test.tsx</read_first>
  <action>
    根据 MainArea.tsx 的变更对标更新测试文件：

    **1. 默认数据更新（第 16-19 行）**
    - `scope: "global"` → `scope: "project"`（共 2 处）
    - `commandMode: "global" as const` → 移除（第 26 行）
    - 移除 `disableProjectCommands` 的 mock（第 33 行）
    - 移除 `isProjectToggleDisabled` 的 mock（第 40 行）

    **2. 移除的测试**
    - "shows '全局指令' button in global mode with secondary variant"（第 111-116 行）
    - "shows '项目指令' button in project mode with secondary variant"（第 118-123 行）
    - "disables '项目指令' button when isProjectToggleDisabled is true"（第 131-135 行）
    - "calls enableProjectCommands when clicking '项目指令' in global mode"（第 137-143 行）

    **3. 更新现有测试**
    - "renders 2 CommandCards when project is selected"（第 60-64 行）：保留，但如果测试数据变了可能需要调整
    - "renders commands array as CommandCard list"（第 157-165 行）：scope 更新为 "project"

    **4. 新增测试**
    - "renders Terminal card when project is selected"：断言 "终端" 文字存在于文档中
    - "Terminal card calls onExecute with 'cmd.exe' when clicked"：点击"终端"卡片，验证 onExecute 被调用
    - "does not show Toggle Group buttons"：断言无 radio group "指令模式切换" 文字
    - "shows '项目环境' label when project is selected"：断言 "项目环境" 文字存在
    - "original '项目指令' label not present"：断言无旧标签 "项目指令"

    **5. 确认 onSubmit 测试**
    - 测试中 `onSubmit` 调用不再包含 scope 参数
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/MainArea.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    所有 MainArea 测试通过。包含终端卡片验证、无 Toggle Group 验证、标签"项目环境"验证。
  </done>
</task>

<task type="auto">
  <name>Task 5: CommandDialog.test.tsx — 更新测试</name>
  <files>src/components/__tests__/CommandDialog.test.tsx</files>
  <read_first>src/components/__tests__/CommandDialog.test.tsx</read_first>
  <action>
    根据 CommandDialog.tsx 的变更对标更新测试文件：

    **1. 移除 CommandDialog 的 scope/commandMode props**
    - 所有 `renderDialog` 调用中不再传入 `commandMode` prop（如需要移除引用）
    - renderDialog 中的默认 props 不再包含 commandMode

    **2. 更新 scope 引用**
    - 测试数据中所有 `scope: "global" as const` 改为 `scope: "project" as const`（第 48、64、124 行）

    **3. 更新 onSubmit 测试**
    - "calls onSubmit with correct data when save is clicked"（第 168-181 行）：确认提交数据不含 scope，移除 scope 断言（第 178 行原来应无 scope 字段——当前测试中 onSubmit 调用也不传 scope，确认检查后保持不变）
    - "calls onSubmit with selected icon when icon changed"（第 184-199 行）：同上

    **4. 移除 scope selector 相关测试**（如果有——当前测试文件中无 scope selector 相关测试，只需确认）
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CommandDialog.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    所有 CommandDialog 测试通过。测试数据无 "global" scope 引用。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MainArea → onExecute | 终端卡片调用 onExecute("cmd.exe")，由 Rust 后端执行 |
| CommandDialog → addCommand | 始终以 project scope 提交，无外部输入影响 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | MainArea 终端卡片 | mitigate | 终端卡片是纯内置 UI，无数据模型对应，不存在数据篡改路径 |
| T-22-06 | Information Disclosure | CommandDialog 旧 scope 引用残留 | mitigate | TypeScript 类型系统和编译检查确保无 "global" 引用残留 |
| T-22-07 | Elevation of Privilege | 终端卡片命令执行 | accept | 终端执行 cmd.exe 与现有指令卡片执行同一 invoke 路径，权限无差异 |
</threat_model>

<verification>
- `npx tsc --noEmit` 通过
- `npx vitest run src/components/__tests__/MainArea.test.tsx` 通过
- `npx vitest run src/components/__tests__/CommandDialog.test.tsx` 通过
- `npx vitest run` 全量测试通过
</verification>

<success_criteria>
- [ ] MainArea 无 Toggle Group，标签为"项目环境"
- [ ] 内置终端卡片显示在指令网格首位，不可编辑/删除
- [ ] CommandDialog 无 scope 选择器
- [ ] App.tsx 不传递 commandMode prop
- [ ] 所有测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/22-global-command-removal/22-03-SUMMARY.md`
</output>
