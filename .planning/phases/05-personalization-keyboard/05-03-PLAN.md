---
phase: 05-personalization-keyboard
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/hooks/useKeyboard.ts
  - src/components/Sidebar.tsx
  - src/components/CommandCard.tsx
  - src/components/MainArea.tsx
  - src/App.tsx
autonomous: false
requirements:
  - UI-03

must_haves:
  truths:
    - "上下箭头可在侧边栏项目之间切换焦点"
    - "Enter 键可选中当前焦点的项目"
    - "Tab 键可在侧边栏和卡片区域之间切换焦点区域"
    - "方向键可在卡片网格中移动焦点"
    - "数字键 1-9 直接执行对应位置的指令卡片（需选中项目且不在编辑/弹窗/输入状态）"
    - "应用启动后焦点默认在侧边栏第一个项目"
    - "所有焦点元素显示 focus-visible:ring-2 样式"
  artifacts:
    - path: "src/hooks/useKeyboard.ts"
      provides: "全局数字键快捷触发 hook"
      exports: ["useKeyboard"]
    - path: "src/components/Sidebar.tsx"
      provides: "侧边栏 roving tabindex 键盘导航"
      contains: "tabIndex"
    - path: "src/components/CommandCard.tsx"
      provides: "卡片键盘焦点 + 数字快捷键角标"
      contains: "tabIndex"
    - path: "src/components/MainArea.tsx"
      provides: "卡片区域键盘导航和 Tab 区域切换"
      contains: "onKeyDown"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useKeyboard.ts"
      via: "全局 keydown 事件监听"
      pattern: "useKeyboard"
    - from: "src/components/Sidebar.tsx"
      to: "roving tabindex pattern"
      via: "ArrowUp/ArrowDown 移动 tabindex=0"
      pattern: "tabIndex"
    - from: "src/components/MainArea.tsx"
      to: "src/components/CommandCard.tsx"
      via: "焦点索引状态传递给卡片的 tabIndex"
      pattern: "focusedCardIndex"
---

<objective>
实现双区域键盘导航。侧边栏用上下箭头切换项目、Enter 选中；卡片区域用方向键移动焦点、Enter 执行；数字键 1-9 直接触发指令；Tab 在两个区域间切换。

Purpose: 满足 UI-03 需求，让用户完全通过键盘高效操作所有核心功能。
Output: useKeyboard.ts hook, 以及修改后的 Sidebar.tsx, CommandCard.tsx, MainArea.tsx, App.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-personalization-keyboard/05-CONTEXT.md
@.planning/phases/05-personalization-keyboard/05-RESEARCH.md
@.planning/phases/05-personalization-keyboard/05-UI-SPEC.md
@.planning/phases/05-personalization-keyboard/05-01-SUMMARY.md
@.planning/phases/05-personalization-keyboard/05-02-SUMMARY.md

<interfaces>
<!-- Plan 01 + 02 完成后的接口状态 -->

From src/hooks/useProject.ts (Plan 01 + 02 扩展后):
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;
  color?: string;
}

// Plan 01 新增:
updateProjectStyle: (projectId: string, style: { icon?: string; color?: string }) => Promise<void>;
// Plan 02 新增:
reorderProjects: (reordered: ProjectItem[]) => Promise<void>;
// 现有:
commands: CommandItem[];
editMode: boolean;
setEditMode: (editMode: boolean) => void;
currentProject: ProjectItem | null;
executeCommand: (shellCommand: string) => Promise<void>;
```

From src/components/Sidebar.tsx (Plan 01 + 02 修改后):
```typescript
interface SidebarProps {
  projects: ProjectItem[];
  selectedId: string | null;
  onAddProject: () => void;
  onSelectProject: (id: string) => void;
  onRemoveProject: (id: string) => void;
  onUpdateStyle: (projectId: string, style: { icon: string; color: string }) => void;
  onReorderProjects: (reordered: ProjectItem[]) => void;
}
```
Sidebar 现在包含: ContextMenu 右键菜单、图标显示、彩色左边框、ProjectSettingsDialog、DragDropProvider 拖拽排序。

From src/components/CommandCard.tsx (当前):
```typescript
interface CommandCardProps {
  name: string;
  icon: LucideIcon;
  command?: string;
  disabled?: boolean;
  onClick?: () => void;
  isCustom?: boolean;
  editMode?: boolean;
  onEdit?: () => void;
  onDelete?: () => void;
  commandId?: string;
}
```

From src/components/MainArea.tsx (当前):
```typescript
interface MainAreaProps {
  currentProject: ProjectItem | null;
  onExecute: (command: string) => void;
  commands: CommandItem[];
  commandMode: "global" | "project";
  editMode: boolean;
  setEditMode: (editMode: boolean) => void;
  addCommand: (name: string, command: string, icon?: string) => Promise<void>;
  updateCommand: (id: string, data: { name: string; command: string; icon: string }) => Promise<void>;
  deleteCommand: (id: string) => Promise<void>;
  enableProjectCommands: () => Promise<void>;
  disableProjectCommands: () => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useKeyboard hook + Sidebar roving tabindex + CommandCard 焦点 + MainArea 键盘导航 + App 集成</name>
  <files>src/hooks/useKeyboard.ts, src/components/Sidebar.tsx, src/components/CommandCard.tsx, src/components/MainArea.tsx, src/App.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (Plan 01 + 02 修改后的完整代码)
    - src/components/CommandCard.tsx (当前卡片组件)
    - src/components/MainArea.tsx (当前主区域组件)
    - src/App.tsx (当前顶层组件)
    - src/hooks/useProject.ts (当前 hook 接口)
    - src/lib/types.ts (CommandItem 类型)
    - .planning/phases/05-personalization-keyboard/05-UI-SPEC.md (键盘导航规范: 焦点区域模型、快捷键表、roving tabindex)
    - .planning/phases/05-personalization-keyboard/05-RESEARCH.md (roving tabindex 实现模式、全局 keydown 模式)
  </read_first>
  <action>
1. 创建 `src/hooks/useKeyboard.ts` -- 全局数字键快捷触发:
   ```typescript
   import { useEffect, useCallback } from 'react';
   import type { CommandItem } from '@/lib/types';
   import type { ProjectItem } from '@/hooks/useProject';

   interface UseKeyboardOptions {
     commands: CommandItem[];
     currentProject: ProjectItem | null;
     onExecute: (command: string) => void;
     editMode: boolean;
     anyDialogOpen: boolean;
   }

   export function useKeyboard({
     commands, currentProject, onExecute, editMode, anyDialogOpen
   }: UseKeyboardOptions) {
     const handleKeyDown = useCallback((e: KeyboardEvent) => {
       // 不在输入框/文本域中时才响应 (per RESEARCH.md Pitfall 2)
       if (e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement) return;
       // 编辑模式或弹窗打开时不触发快捷键 (per RESEARCH.md Pitfall 6)
       if (editMode || anyDialogOpen) return;
       // 需要选中项目才能执行
       if (!currentProject) return;

       const num = parseInt(e.key, 10);
       if (num >= 1 && num <= 9 && num <= commands.length) {
         const cmd = commands[num - 1];
         if (cmd) {
           e.preventDefault();
           onExecute(cmd.command);
         }
       }
     }, [commands, currentProject, onExecute, editMode, anyDialogOpen]);

     useEffect(() => {
       window.addEventListener('keydown', handleKeyDown);
       return () => window.removeEventListener('keydown', handleKeyDown);
     }, [handleKeyDown]);
   }
   ```

2. 修改 `src/components/Sidebar.tsx` -- 添加 roving tabindex 键盘导航:
   - 新增内部状态: `focusedIndex: number` (初始 0，即第一个项目)
   - 新增内部状态: `isActiveZone: boolean` (当前侧边栏是否为活跃焦点区域，初始 true per D-16)
   - 新增 prop: `activeZone: 'sidebar' | 'main'`, `onZoneSwitch: () => void`
   - 使用 `useRef` 引用项目按钮元素列表: `itemRefs = useRef<(HTMLDivElement | null)[]>([])`
   - 每个项目行 div 的 tabIndex:
     - 如果 `isActiveZone && index === focusedIndex` → `tabIndex={0}`
     - 否则 → `tabIndex={-1}`
   - 项目行 div 添加 `onKeyDown` 处理:
     ```
     ArrowDown → focusedIndex = (focusedIndex + 1) % projects.length, focus 对应元素
     ArrowUp → focusedIndex = (focusedIndex - 1 + projects.length) % projects.length, focus 对应元素
     Enter → onSelectProject(projects[focusedIndex].id)
     Tab → e.preventDefault(), onZoneSwitch()
     ```
   - 添加初始焦点 useEffect (per D-16):
     ```typescript
     useEffect(() => {
       if (!loading && projects.length > 0 && activeZone === 'sidebar') {
         itemRefs.current[0]?.focus();
       }
     }, [loading, projects.length, activeZone]);
     ```
     注意: loading 状态从 props 传入（或从 useProject 获取）。如果 Sidebar 是纯展示组件不直接访问 loading，则通过 App 层 useEffect 处理初始焦点。
   - 项目行 div 添加焦点样式: `focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none` (per D-14)
   - 注意: 保留 Plan 02 的 DragDropProvider 包裹和 ContextMenu 包裹

3. 修改 `src/components/CommandCard.tsx` -- 添加键盘焦点支持:
   - 新增 props: `tabIndex: number`, `shortcutNumber?: number` (数字快捷键角标，1-9)
   - 将底层 `<button>` 的 tabIndex 改为接收 prop 值（覆盖默认的 0）
   - 当 `shortcutNumber` 存在且 `!disabled && !editMode` 时，在按钮左上角显示数字角标:
     ```
     <span
       className="absolute top-1 left-1 text-[10px] font-semibold text-muted-foreground/40 pointer-events-none"
       aria-hidden="true"
     >
       {shortcutNumber}
     </span>
     ```
     per UI-SPEC: absolute top-1 left-1, text-[10px] font-semibold, text-muted-foreground/40, aria-hidden="true"
   - 确保 button 元素已有 `focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none`（Plan 01 已有，确认保留）

4. 修改 `src/components/MainArea.tsx` -- 卡片区域键盘导航:
   - 新增 props: `activeZone: 'sidebar' | 'main'`, `onZoneSwitch: () => void`
   - 新增内部状态: `focusedCardIndex: number` (初始 -1，表示无焦点)
   - 当 activeZone 变为 'main' 时，自动 focus 第一个卡片:
     ```typescript
     useEffect(() => {
       if (activeZone === 'main' && commands.length > 0 && focusedCardIndex === -1) {
         setFocusedCardIndex(0);
       }
     }, [activeZone, commands.length]);
     ```
   - 给卡片网格容器 div 添加 `onKeyDown` 处理:
     ```
     ArrowRight → focusedCardIndex = Math.min(focusedCardIndex + 1, commands.length - 1)
     ArrowLeft → focusedCardIndex = Math.max(focusedCardIndex - 1, 0)
     ArrowDown → 移动到下一行同列卡片（需要计算列数，通过 ref 获取 grid 容器计算）
     ArrowUp → 移动到上一行同列卡片
     Enter → 对应卡片的 onClick（执行命令）
     Tab → e.preventDefault(), onZoneSwitch()
     ```
     注: 上下方向键的列数计算可通过 `getComputedStyle(gridRef.current).gridTemplateColumns` 获取列数。如果实现复杂度太高，上下方向键可以简化为 +/-3 或 +/-4（假设固定列数）。推荐先实现简化版，只在 action 中说明。
   - 给每个 CommandCard 传递 tabIndex 和 shortcutNumber:
     ```typescript
     <CommandCard
       ...
       tabIndex={activeZone === 'main' && index === focusedCardIndex ? 0 : -1}
       shortcutNumber={!disabled && !editMode && index < 9 ? index + 1 : undefined}
     />
     ```
   - 使用 useEffect + ref 在 focusedCardIndex 变化时 focus 对应卡片 DOM 元素

5. 修改 `src/App.tsx`:
   - 新增状态: `activeZone: 'sidebar' | 'main'` (初始 'sidebar' per D-16)
   - 新增 `handleZoneSwitch` 回调: 切换 activeZone 在 'sidebar' 和 'main' 之间
   - 传递给 Sidebar: `activeZone={activeZone}`, `onZoneSwitch={handleZoneSwitch}`
   - 传递给 MainArea: `activeZone={activeZone}`, `onZoneSwitch={handleZoneSwitch}`
   - 集成 useKeyboard hook:
     ```typescript
     useKeyboard({
       commands,
       currentProject,
       onExecute: executeCommand,
       editMode,
       anyDialogOpen: /* 需要从 MainArea 和 Sidebar 的弹窗状态汇总 */,
     });
     ```
     anyDialogOpen 需要通过状态汇总（MainArea 的 dialogOpen + Sidebar 的 settingsProjectId !== null）。可在 App 层新增一个 `anyDialogOpen` 状态，或使用更简单的方式：在 useKeyboard 中检查 `document.querySelector('[data-state="open"]')` 是否存在（Radix Dialog/ContextMenu 打开时会添加此属性）。推荐使用 DOM 检查方式避免多层状态传递:
     ```typescript
     const anyDialogOpen = !!document.querySelector('[data-radix-dialog-content-open], [data-radix-context-menu-content-open]');
     ```
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useKeyboard.ts` 存在
    - `useKeyboard.ts` 包含 `export function useKeyboard`
    - `useKeyboard.ts` 包含 `HTMLInputElement` (输入框检查)
    - `useKeyboard.ts` 包含 `editMode` (编辑模式检查)
    - `useKeyboard.ts` 包含 `anyDialogOpen` (弹窗检查)
    - `useKeyboard.ts` 包含 `parseInt(e.key, 10)` (数字键解析)
    - `useKeyboard.ts` 包含 `num >= 1 && num <= 9` (数字键范围)
    - `Sidebar.tsx` 包含 `tabIndex` (roving tabindex)
    - `Sidebar.tsx` 包含 `ArrowDown` 或 `ArrowUp` (方向键处理)
    - `Sidebar.tsx` 包含 `focus-visible:ring-2` (焦点样式)
    - `Sidebar.tsx` 包含 `activeZone` (区域切换 prop)
    - `CommandCard.tsx` 包含 `tabIndex` prop
    - `CommandCard.tsx` 包含 `shortcutNumber` prop
    - `CommandCard.tsx` 包含 `aria-hidden="true"` (数字角标)
    - `CommandCard.tsx` 包含 `text-muted-foreground/40` (角标颜色)
    - `MainArea.tsx` 包含 `activeZone` prop
    - `MainArea.tsx` 包含 `focusedCardIndex` (卡片焦点状态)
    - `MainArea.tsx` 包含 `ArrowRight` 或 `ArrowLeft` (卡片方向键)
    - `MainArea.tsx` 包含 `onZoneSwitch` (Tab 切换)
    - `App.tsx` 包含 `useKeyboard` (hook 调用)
    - `App.tsx` 包含 `activeZone` (区域状态)
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>侧边栏上下箭头切换项目焦点，Enter 选中项目；卡片区域方向键移动焦点，Enter 执行；Tab 在两个区域间切换；数字键 1-9 直接触发指令卡片；启动后焦点默认在侧边栏；所有焦点元素显示 ring 样式；弹窗/编辑/输入状态中数字键不触发</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 Phase 5 全部功能</name>
  <files>无文件修改</files>
  <action>
  等待用户运行应用并验证以下功能:

  图标与颜色标记验证:
  1. 添加 2-3 个项目到侧边栏
  2. 右键点击某个项目，确认出现"设置图标和颜色"菜单项
  3. 点击菜单项，确认弹窗显示 10 个图标网格和 8 色预设盘
  4. 选择一个图标和一个颜色，观察预览区实时更新
  5. 点击"保存设置"，确认侧边栏项目名称左侧显示图标，左侧显示 3px 彩色边框
  6. 为另一个项目设置不同的图标和颜色，确认样式独立生效

  拖拽排序验证:
  7. 鼠标悬停在侧边栏项目行上，确认左侧出现 GripVertical 拖拽手柄（与 X 删除按钮同时显示）
  8. 按住拖拽手柄，将中间项目拖到顶部位置
  9. 松开后确认项目顺序已更新

  键盘导航验证:
  10. 按 Tab 键确认焦点在侧边栏和卡片区域之间切换
  11. 在侧边栏区域按上下箭头确认项目焦点切换
  12. 按 Enter 确认选中焦点的项目
  13. Tab 切换到卡片区域，按方向键确认卡片焦点移动
  14. 在卡片焦点上按 Enter 确认执行指令
  15. 确认卡片左上角显示数字 1-9 角标
  16. 按数字键 1 确认直接执行第一个指令（终端打开）

  持久化验证:
  17. 关闭应用后重新启动，确认图标、颜色、排序全部恢复
  </action>
  <verify>用户输入 "approved" 确认所有功能正常</verify>
  <done>用户确认 Phase 5 所有功能（图标颜色标记、拖拽排序、键盘导航）均正常工作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard events -> Command execution | 用户按键触发 Shell 命令执行 |
| Global keydown listener | window 级别事件监听，可能捕获所有按键 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | E (Elevation) | useKeyboard 数字键 | mitigate | 严格检查 guard conditions: 输入框中不触发、编辑模式不触发、弹窗打开时不触发。所有条件必须同时为 false 才执行 (per RESEARCH.md Pitfall 2 + 6) |
| T-05-10 | T (Tampering) | focus management | accept | 焦点管理仅影响 UI 显示，不涉及数据修改 |
| T-05-11 | I (Info Disclosure) | keyboard events | accept | 本地桌面应用，键盘事件不离开本地进程 |
| T-05-12 | D (DoS) | keyboard handler | accept | 键盘事件频率受限于用户输入速度，executeCommand 已有防重入保护 (flashing state) |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. 现有测试通过: `npx vitest run`
3. 人工验证键盘导航和所有交互功能
</verification>

<success_criteria>
- 上下箭头切换侧边栏项目焦点，Enter 选中
- 方向键在卡片网格中移动焦点
- Tab 在侧边栏和卡片区域间切换
- 数字键 1-9 直接触发对应指令
- 弹窗/编辑/输入状态中数字键不触发
- 启动后焦点默认在侧边栏
- 所有焦点元素显示 ring 样式
- TypeScript 编译无错误
- 人工验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-personalization-keyboard/05-03-SUMMARY.md`
</output>
