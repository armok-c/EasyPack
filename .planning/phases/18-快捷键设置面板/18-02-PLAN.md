---
phase: 18-快捷键设置面板
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/ShortcutPanel.tsx
  - src/components/SettingsDialog.tsx
  - src/App.tsx
autonomous: true
requirements:
  - KBD-01
  - KBD-02
  - KBD-03
  - KBD-04

must_haves:
  truths:
    - "用户可在面板中看到所有可绑定操作的列表（分组显示）"
    - "用户可点击操作后按键录制新快捷键组合"
    - "快捷键冲突时显示黄色警告提示"
    - "面板支持搜索和分类筛选"
    - "面板支持全部重置功能"
    - "面板入口位于 SettingsDialog 底部"
    - "录制状态下虚线边框闪烁 + Esc 取消 + Dialog 不关闭"
  artifacts:
    - path: "src/components/ShortcutPanel.tsx"
      provides: "快捷键设置面板主组件（独立 Dialog）"
      exports: ["ShortcutPanel"]
      min_lines: 100
    - path: "src/components/SettingsDialog.tsx"
      provides: "底部添加快捷键设置入口按钮"
      contains: "快捷键设置"
    - path: "src/App.tsx"
      provides: "ShortcutPanel 集成 + handler 传递"
      contains: "ShortcutPanel"
  key_links:
    - from: "src/components/ShortcutPanel.tsx"
      to: "src/hooks/useShortcutActions.ts"
      via: "useShortcutActions hook 获取操作列表"
      pattern: "useShortcutActions"
    - from: "src/components/ShortcutPanel.tsx"
      to: "src/hooks/useProject.ts"
      via: "shortcutBindings + setShortcutBinding + clearShortcutBinding + resetAllShortcuts"
      pattern: "setShortcutBinding"
    - from: "src/components/SettingsDialog.tsx"
      to: "ShortcutPanel"
      via: "onOpenShortcutPanel callback"
      pattern: "onOpenShortcutPanel"
    - from: "src/App.tsx"
      to: "ShortcutPanel"
      via: "传递 window/project 操作 handlers"
      pattern: "onToggleVisibility"
---

<objective>
前端 UI — ShortcutPanel 面板 — 创建 VS Code 风格的快捷键管理面板，包含搜索框、分组列表、按键录制、冲突检测、重置功能。在 SettingsDialog 底部添加入口按钮。在 App.tsx 中集成面板并传递窗口/项目操作回调。

Purpose: 这是用户直接交互的 UI 层，实现所有 6 个 KBD 需求的用户可见部分。
Output: ShortcutPanel.tsx 组件、SettingsDialog 入口按钮、App.tsx 集成代码
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-快捷键设置面板/18-CONTEXT.md
@.planning/phases/18-快捷键设置面板/18-RESEARCH.md
@.planning/phases/18-快捷键设置面板/18-01-SUMMARY.md
@src/components/SettingsDialog.tsx
@src/App.tsx
@src/lib/shortcutUtils.ts
@src/lib/types.ts

<interfaces>
<!-- Plan 01 产出，executor 必须使用这些接口 -->

From src/lib/types.ts (after Plan 01):
```typescript
export type ShortcutCategory = "command" | "window" | "project";

export interface ShortcutAction {
  id: string;
  label: string;
  category: ShortcutCategory;
  handler: () => void;
}
```

From src/hooks/useShortcutActions.ts (after Plan 01):
```typescript
interface UseShortcutActionsOptions {
  commands: CommandItem[];
  onExecute: (cmd: CommandItem) => void;
  onToggleVisibility: () => void;
  onToggleFloat: () => void;
  onPrevProject: () => void;
  onNextProject: () => void;
  onOpenFolder: () => void;
}

function useShortcutActions(options: UseShortcutActionsOptions): ShortcutAction[];
```

From src/hooks/useProject.ts (after Plan 01):
```typescript
// New exports:
shortcutBindings: Record<string, string>;
setShortcutBinding: (actionId: string, shortcut: string) => Promise<{ conflict: string | null }>;
clearShortcutBinding: (actionId: string) => Promise<void>;
resetAllShortcuts: () => Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShortcutPanel 组件 — 搜索、分组列表、录制、冲突检测、重置</name>
  <files>src/components/ShortcutPanel.tsx</files>
  <read_first>
    src/components/SettingsDialog.tsx
    src/lib/shortcutUtils.ts
    src/lib/types.ts
    .planning/phases/18-快捷键设置面板/18-CONTEXT.md (D-01 through D-18)
    .planning/phases/18-快捷键设置面板/18-RESEARCH.md (Pattern 4, Code Examples)
  </read_first>
  <acceptance_criteria>
    - ShortcutPanel 是独立 Dialog 组件，sm:max-w-[420px]
    - 面板顶部有搜索 Input，placeholder "搜索操作..."
    - 搜索过滤 actions 列表（匹配 label 或 category 名，中文匹配）
    - 面板内容区域按 3 个分类分组显示（per D-07）：每组有分类标题 + 操作行列表
    - 每个操作行：左侧操作名（text-sm），右侧快捷键标签区域（text-xs badge）
    - 快捷键标签有三种状态：已绑定（显示 shortcutToDisplay 结果）、未绑定（显示 "未设置" 灰色文字）、录制中（虚线边框闪烁 + "按下快捷键..." 文字，per D-13）
    - 点击快捷键标签进入录制状态：设置 recordingId = action.id，触发 onRecordingChange(true)
    - 录制状态下：Dialog 的 onEscapeKeyDown 阻止关闭并取消录制（per Pitfall 5 in RESEARCH）
    - 录制 keydown：调用 keyboardEventToShortcut(e)，有效则确认，Esc 取消
    - 确认绑定时调用 setShortcutBinding(actionId, shortcut)，检测冲突
    - 冲突时：在操作行下方显示黄色/琥珀色警告条，文字含冲突操作名（per D-14），显示"确认覆盖"和"取消"按钮
    - 确认覆盖：先 clearShortcutBinding(冲突actionId) 再 setShortcutBinding(当前actionId, shortcut)
    - 取消冲突：清除冲突状态
    - 每个已绑定的操作行显示 "x" 清除按钮（hover 时可见）
    - 面板底部有"重置所有快捷键"按钮（per D-17），点击后弹出确认 Dialog（per D-18）
    - 确认弹窗文字："确定要清除所有快捷键绑定吗？此操作不可撤销"，两个按钮"取消"和"确认重置"
    - 分组列表折叠/展开：每个分类标题可点击切换展开/折叠，默认全部展开
    - 面板内容区域可滚动（ScrollArea 组件）
    - 分类标题显示中文名："指令执行"、"窗口操作"、"项目操作"
  </acceptance_criteria>
  <action>
    新建 src/components/ShortcutPanel.tsx。组件 props interface 包含：open, onOpenChange, actions (ShortcutAction[]), bindings (Record<string, string>), onSetBinding, onClearBinding, onResetAll, onRecordingChange。

    内部状态：searchQuery (string), recordingId (string | null), conflictInfo ({ actionId: string; label: string; newShortcut: string } | null), expandedCategories (Set<ShortcutCategory>, 默认三个都展开), resetConfirmOpen (boolean)。

    搜索过滤：useMemo 计算 filteredActions，匹配 action.label.toLowerCase() 或 category 中文名。分组：将 filteredActions 按 category 分为三组。

    录制 keydown useEffect：当 recordingId !== null 时，window.addEventListener("keydown", handler)。handler 中：如果 target 是 input/textarea 则 return。Esc → setRecordingId(null) + onRecordingChange(false)。调用 keyboardEventToShortcut(e)，有效则调用 onSetBinding(recordingId, shortcut)，检查返回的 conflict。有冲突 → setConflictInfo。无冲突 → setRecordingId(null) + onRecordingChange(false)。

    Dialog onEscapeKeyDown：recordingId !== null 时 e.preventDefault() + setRecordingId(null)。

    录制中视觉：快捷键标签区域 className 用 animate-pulse + border-dashed border-accent，文字 "按下快捷键..."（per D-13）。

    冲突警告：当 conflictInfo 存在时，在当前操作行下方渲染一个 amber/yellow 背景条，文字 "此快捷键已分配给 {conflictLabel}，继续将覆盖"，按钮 "确认覆盖"（调用 onClearBinding(conflictActionId) + onSetBinding(currentActionId, shortcut)）和 "取消"（setConflictInfo(null)）。

    分组列表渲染：对每个 category（顺序：command → window → project），渲染分类标题（可点击切换展开/折叠），如果展开则渲染该分组下的 action 行。

    每行操作：div flex justify-between，左侧 span (action.label)，右侧快捷键区域。已绑定：span 显示 shortcutToDisplay(binding) + hover 时显示 "x" 清除按钮。未绑定：span 灰色 "未设置"。录制中：span 虚线边框闪烁 "按下快捷键..."。点击右侧区域触发录制（如果当前不在录制中）。

    重置：面板底部 border-t 分隔线后放置一个 "重置所有快捷键" 文字按钮。点击 → setResetConfirmOpen(true)。确认弹窗：复用 Dialog 组件，文字 "确定要清除所有快捷键绑定吗？此操作不可撤销"，按钮 "取消" 和 "确认重置"。确认后调用 onResetAll() + setResetConfirmOpen(false)。

    使用 shadcn/ui 组件：Dialog, DialogContent, DialogHeader, DialogTitle, Input (搜索), ScrollArea, Button。使用 lucide-react 图标：ChevronDown/ChevronRight (折叠), X (清除), RotateCcw (重置), Search (搜索)。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ShortcutPanel 组件创建完成
    - 搜索过滤正常工作
    - 分组列表按三类显示
    - 录制 UX 完整（虚线边框、按键捕获、Esc 取消、Dialog 不关闭）
    - 冲突检测和警告 UI 完整
    - 重置按钮和确认弹窗完整
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsDialog 入口按钮 + App.tsx 集成</name>
  <files>src/components/SettingsDialog.tsx, src/App.tsx</files>
  <read_first>
    src/components/SettingsDialog.tsx
    src/App.tsx
    src/components/ShortcutPanel.tsx (Task 1 产出)
    src/hooks/useShortcutActions.ts (Plan 01 产出)
    src/hooks/useProject.ts (shortcutBindings 相关导出)
  </read_first>
  <acceptance_criteria>
    - SettingsDialog props 新增 onOpenShortcutPanel: () => void
    - SettingsDialog 底部版本号区域上方添加"快捷键设置..."按钮（per D-02），样式与版本更新提示条类似（蓝色左边框 + 半透明背景）
    - 点击"快捷键设置..."按钮 → 调用 onOpenShortcutPanel() → 同时关闭设置弹窗（onOpenChange(false)）
    - App.tsx 新增 shortcutPanelOpen state (boolean)
    - App.tsx 新增 handleOpenShortcutPanel callback：setSettingsOpen(false) → setShortcutPanelOpen(true)
    - App.tsx 中调用 useShortcutActions hook，传入 commands 和各操作 handler
    - App.tsx 中 useGlobalShortcuts 的调用从 commands 改为 actions + shortcutBindings（per Plan 01 新接口）
    - App.tsx 中 ShortcutPanel 传入 actions、shortcutBindings、setShortcutBinding、clearShortcutBinding、resetAllShortcuts、onRecordingChange
    - ShortcutPanel 中 onRecordingChange 回调设置 App 的 isRecording state
    - 窗口操作 handler 实现：onToggleVisibility 切换 appWindow 显示/隐藏，onToggleFloat 调用 toggleFloat
    - 项目操作 handler 实现：onPrevProject/onNextProject 在 projects 数组中切换 selectProject，onOpenFolder 调用 handleOpenFolder
    - 应用运行正常，SettingsDialog 底部有"快捷键设置..."按钮，点击后关闭设置并打开快捷键面板
  </acceptance_criteria>
  <action>
    修改 src/components/SettingsDialog.tsx：在 props interface 新增 onOpenShortcutPanel: () => void。在版本号区域上方（"版本号 + 更新提示" div 之前）添加"快捷键设置..."按钮。按钮样式：w-full text-left px-3 py-2 rounded-md border-l-2 border-blue-400 bg-blue-400/10 text-sm text-blue-300 hover:bg-blue-400/20。点击 handler：onOpenShortcutPanel() + onOpenChange(false)（per D-02: 点击后关闭设置弹窗并打开快捷键面板）。解构 props 中新增 onOpenShortcutPanel。

    修改 src/App.tsx：

    1. 导入 ShortcutPanel 和 useShortcutActions。
    2. 新增 shortcutPanelOpen state。
    3. 新增 handleOpenShortcutPanel callback：setSettingsOpen(false) 后 setShortcutPanelOpen(true)。
    4. 从 useProject 解构 shortcutBindings, setShortcutBinding, clearShortcutBinding, resetAllShortcuts（Plan 01 新增导出）。
    5. 构建窗口操作 handler：
       - onToggleVisibility: 如果 visibility !== "VISIBLE" 则 showFromTray() + appWindow.show() + appWindow.setFocus()，否则 hideToTray() + appWindow.hide()
       - onToggleFloat: 调用 toggleFloat()
    6. 构建项目操作 handler（使用 useCallback）：
       - onPrevProject: 从 projects 数组找当前 selectedId 的 index，selectProject(projects[index - 1].id)（边界保护）
       - onNextProject: selectProject(projects[index + 1].id)（边界保护）
       - onOpenFolder: 调用 handleOpenFolder()
    7. 调用 useShortcutActions({ commands, onExecute: handleExecuteWithRecent, onToggleVisibility, onToggleFloat, onPrevProject, onNextProject, onOpenFolder })，得到 actions。
    8. 更新 useGlobalShortcuts 调用：从 { commands, onExecute, enabled, recording } 改为 { actions, bindings: shortcutBindings, enabled, recording }（per Plan 01 新接口）。
    9. 在 JSX 中 SettingsDialog 后添加 ShortcutPanel 组件：
       <ShortcutPanel open={shortcutPanelOpen} onOpenChange={setShortcutPanelOpen} actions={actions} bindings={shortcutBindings} onSetBinding={setShortcutBinding} onClearBinding={clearShortcutBinding} onResetAll={resetAllShortcuts} onRecordingChange={handleRecordingChange} />
    10. SettingsDialog 新增 onOpenShortcutPanel={handleOpenShortcutPanel} prop。
    11. handleRecordingChange 保留现有逻辑（设置 isRecording state 传给 useGlobalShortcuts 的 recording prop）。

    注意：旧的 assignShortcut 和 clearShortcut 不再从 App.tsx 传递到 MainArea。MainArea 的 props 中这些暂时保留但不再使用（Task 2 of Plan 01 已清理了 MainArea 内部对这些的使用）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - SettingsDialog 底部有"快捷键设置..."入口按钮
    - 点击入口按钮关闭设置弹窗并打开快捷键面板
    - 快捷键面板显示三类操作的分组列表
    - useGlobalShortcuts 从 ShortcutAction 驱动注册
    - 面板中录制、冲突检测、重置全部工作
    - TypeScript 编译零错误
    - 应用运行正常，无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户键盘输入 → 面板录制 | 录制中按键经 keyboardEventToShortcut 转换，需验证格式 |
| 面板 UI → 快捷键注册 | 面板操作触发 OS 级快捷键注册变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | 面板快捷键录制 | mitigate | keyboardEventToShortcut 已有 modifier + 3 键上限验证（D-15） |
| T-18-05 | Denial of Service | 录制状态 + Dialog 关闭冲突 | mitigate | onEscapeKeyDown 拦截 + recording 状态同步到 useGlobalShortcuts |
| T-18-06 | Information Disclosure | 冲突提示显示操作名 | accept | 面板本身是用户主动操作，显示操作名是功能需求 |
</threat_model>

<verification>
1. TypeScript 编译无错误：`npx tsc --noEmit`
2. 应用启动正常
3. SettingsDialog 底部可见"快捷键设置..."按钮
4. 点击按钮后设置弹窗关闭，快捷键面板打开
5. 面板显示三类操作的分组列表
6. 点击快捷键标签可进入录制状态
7. 录制中按 Esc 取消录制，面板不关闭
8. 冲突时显示黄色警告条
9. 重置按钮弹出确认弹窗
10. 全局快捷键仍可通过面板管理后正确注册
</verification>

<success_criteria>
- ShortcutPanel 组件功能完整（搜索、分组、录制、冲突、重置）
- SettingsDialog 底部入口按钮正常工作
- App.tsx 集成完整（handler 传递、useShortcutActions、useGlobalShortcuts 新接口）
- 所有 6 个 KBD 需求在 Plan 01 + Plan 02 联合覆盖下满足
- TypeScript 编译零错误
- 应用运行无回归
</success_criteria>

<output>
After completion, create `.planning/phases/18-快捷键设置面板/18-02-SUMMARY.md`
</output>
