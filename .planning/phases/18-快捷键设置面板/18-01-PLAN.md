---
phase: 18-快捷键设置面板
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/hooks/useGlobalShortcuts.ts
  - src/hooks/useShortcutActions.ts
  - src/components/CommandCard.tsx
  - src/components/MainArea.tsx
  - src/hooks/useProject.ts
autonomous: true
requirements:
  - KBD-03
  - KBD-05
  - KBD-06

must_haves:
  truths:
    - "ShortcutAction 统一类型覆盖 command/window/project 三类操作"
    - "所有快捷键持久化到 shortcutBindings store key"
    - "录制状态下全局快捷键临时禁用（已有 recording 机制）"
    - "冲突检测覆盖所有已注册操作"
    - "CommandCard 中完全移除快捷键录制 UI"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "ShortcutAction 类型 + ShortcutCategory 类型"
      contains: "ShortcutAction"
    - path: "src/hooks/useShortcutActions.ts"
      provides: "ShortcutAction 注册表 hook"
      exports: ["useShortcutActions"]
    - path: "src/hooks/useGlobalShortcuts.ts"
      provides: "扩展后的全局快捷键注册，支持 ShortcutAction"
      contains: "actions"
    - path: "src/hooks/useProject.ts"
      provides: "迁移逻辑 + assignShortcut 扩展"
      contains: "shortcutBindings"
  key_links:
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "ShortcutAction.bindings"
      via: "遍历 actions 注册 OS 快捷键"
      pattern: "actions\\.filter"
    - from: "src/hooks/useShortcutActions.ts"
      to: "src/hooks/useProject.ts"
      via: "读取 commands 列表"
      pattern: "options\\.commands"
    - from: "src/hooks/useProject.ts"
      to: "tauri-plugin-store"
      via: "shortcutBindings key 持久化"
      pattern: "store\\.set.*shortcutBindings"
---

<objective>
后端数据模型与快捷键注册扩展 — 为 Phase 18 建立 ShortcutAction 统一类型系统，扩展 useGlobalShortcuts 支持三类操作（指令/窗口/项目）的快捷键注册，实现独立 store key 持久化，迁移旧数据，并从 CommandCard 和 MainArea 移除旧录制 UI。

Purpose: 快捷键面板的前端 UI（Plan 02）需要一个统一的类型系统和注册机制来驱动。本计划建立这一基础，同时清理旧的散落式快捷键 UI，确保面板成为唯一的快捷键管理入口。
Output: ShortcutAction 类型、useShortcutActions hook、扩展后的 useGlobalShortcuts、迁移逻辑、简化的 CommandCard/MainArea
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-快捷键设置面板/18-CONTEXT.md
@.planning/phases/18-快捷键设置面板/18-RESEARCH.md
@src/lib/types.ts
@src/hooks/useGlobalShortcuts.ts
@src/hooks/useProject.ts
@src/components/CommandCard.tsx
@src/components/MainArea.tsx
@src/App.tsx
@src/lib/shortcutUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShortcutAction 类型 + useShortcutActions hook + 扩展 useGlobalShortcuts + 持久化迁移</name>
  <files>src/lib/types.ts, src/hooks/useShortcutActions.ts, src/hooks/useGlobalShortcuts.ts, src/hooks/useProject.ts</files>
  <read_first>
    src/lib/types.ts
    src/hooks/useGlobalShortcuts.ts
    src/hooks/useProject.ts (lines 50-92, 120-157, 474-536)
    src/lib/shortcutUtils.ts
    .planning/phases/18-快捷键设置面板/18-CONTEXT.md (D-07 through D-16)
    .planning/phases/18-快捷键设置面板/18-RESEARCH.md (Pattern 1, 2, 3)
  </read_first>
  <acceptance_criteria>
    - types.ts 导出 ShortcutCategory 类型（值为 "command" | "window" | "project"）
    - types.ts 导出 ShortcutAction interface（含 id: string, label: string, category: ShortcutCategory, handler: () => void）
    - useShortcutActions.ts 导出 useShortcutActions hook，接受 UseShortcutActionsOptions 参数，返回 ShortcutAction[]
    - useShortcutActions 构建的 actions 列表包含：动态指令操作（id 格式 "command.{commandId}"）、2 个固定窗口操作（id "window.toggle-visibility" + "window.toggle-visibility"）、3 个固定项目操作（id "project.prev" / "project.next" / "project.open-folder"）
    - useGlobalShortcuts 接口从 UseGlobalShortcutsOptions 中的 commands: CommandItem[] 改为 actions: ShortcutAction[] 和 bindings: Record<string, string>
    - useGlobalShortcuts 遍历 actions + bindings 来注册 OS 级快捷键（只注册当前 actions 中存在的 actionId）
    - useGlobalShortcuts 保持 version counter 防竞态和 recording 时 unregisterAll 的现有行为
    - useProject.ts 中新增 SHORTCUT_BINDINGS_KEY = "shortcutBindings" 常量
    - useProject.ts 中新增 loadShortcutBindings 函数：启动时从 store 读取 shortcutBindings，如不存在则从旧的 CommandItem.shortcut 和 presetShortcutsMap 迁移
    - 迁移逻辑：遍历所有 commands 中有 shortcut 字段的，写入 bindings["command.{cmd.id}"] = cmd.shortcut；遍历 presetShortcutsMap，写入 bindings["command.{presetId}"] = value；迁移后写入 store
    - useProject.ts 导出 shortcutBindings state 和 setShortcutBinding / clearShortcutBinding / resetAllShortcuts 函数
    - setShortcutBinding 执行全 ShortcutAction 范围的冲突检测（不仅是 commands 列表）
    - 所有 handler 使用 ref 模式避免闭包过期（Pitfall 4 in RESEARCH）
  </acceptance_criteria>
  <action>
    Per D-11: 在 src/lib/types.ts 中新增 ShortcutCategory type union 和 ShortcutAction interface。ShortcutAction 包含 id、label、category、handler 四个字段。handler 为 () => void 类型，不持久化。

    新建 src/hooks/useShortcutActions.ts。Per D-07/D-08/D-09/D-10: hook 接受 UseShortcutActionsOptions（commands, onExecute, onToggleVisibility, onToggleFloat, onPrevProject, onNextProject, onOpenFolder）。内部用 useMemo 构建 ShortcutAction[]：遍历 options.commands 生成 "command.{cmd.id}" 操作；硬编码 2 个窗口操作（"window.toggle-visibility": "显示/隐藏主窗口", "window.toggle-float": "切换悬浮窗"）；硬编码 3 个项目操作（"project.prev": "切换上一个项目", "project.next": "切换下一个项目", "project.open-folder": "打开当前项目文件夹"）。所有 handler 使用 ref 模式（useRef + 赋值更新），在 useMemo 的 handler 闭包中通过 ref.current 调用，避免闭包过期问题（RESEARCH Pitfall 4）。

    扩展 src/hooks/useGlobalShortcuts.ts：接口从 commands: CommandItem[] 改为 actions: ShortcutAction[] + bindings: Record<string, string>。useEffect 内：过滤 actions 中有对应 bindings[action.id] 的项，遍历调用 register(bindings[action.id], handler)。handler 通过 ref 模式调用 action.handler()。保留现有的 enabled、recording、version counter、unregisterAll 机制。onExecuteRef 改为 actionsRef。

    在 src/hooks/useProject.ts 中：新增 SHORTCUT_BINDINGS_KEY = "shortcutBindings" 常量和 shortcutBindings state（Record<string, string>）。在 init store 的 useEffect 中，读取 shortcutBindings key。如果不存在（首次迁移）：遍历当前 commands 列表中有 shortcut 字段的，写入 bindings["command.{cmd.id}"] = cmd.shortcut；遍历 presetShortcutsMap，写入 bindings["command.{presetId}"] = value。写入 store.set("shortcutBindings", migrated)。新增 setShortcutBinding(actionId: string, shortcut: string): 冲突检测遍历 shortcutBindings 所有 entry，如果某个 actionId 的 binding 等于新 shortcut 且不是当前 actionId，返回冲突信息。无冲突则更新 bindings + store。新增 clearShortcutBinding(actionId: string): 删除 bindings[actionId] + store。新增 resetAllShortcuts(): 清空 bindings + store。这些新函数在 return 对象中导出。注意：旧的 assignShortcut 和 clearShortcut 暂时保留以兼容过渡期 MainArea 的调用，但在后续清理任务中会被移除。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - types.ts 导出 ShortcutAction 和 ShortcutCategory
    - useShortcutActions hook 返回包含指令+窗口+项目三类操作的 ShortcutAction[]
    - useGlobalShortcuts 从 ShortcutAction[] + bindings 驱动注册
    - useProject 中有 shortcutBindings 持久化和迁移逻辑
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 移除 CommandCard 录制 UI + 简化 MainArea 快捷键管理</name>
  <files>src/components/CommandCard.tsx, src/components/MainArea.tsx</files>
  <read_first>
    src/components/CommandCard.tsx
    src/components/MainArea.tsx
    src/hooks/useProject.ts (exported interface)
  </read_first>
  <acceptance_criteria>
    - CommandCard 的 props 中移除 isRecording, onRecordingStart, onRecordingStop, onShortcutAssign, onShortcutClear, hasConflict 六个 prop
    - CommandCard 组件内部移除所有录制相关的 useEffect（keydown listener）和 JSX（录制状态 3 个分支 + "+" 按钮 + 清除按钮）
    - CommandCard 保留 shortcut prop 显示（badge），仅在 shortcut 有值时显示静态快捷键标签
    - CommandCard 在 editMode 下点击 shortcut badge 不再触发录制（无交互）
    - MainArea 移除 recordingCommandId 和 conflictCommandId 两个 state
    - MainArea 移除 handleRecordingStart, handleRecordingStop, handleShortcutAssign, handleShortcutClear 四个 callback
    - MainArea 移除 "Cancel recording when edit mode off" useEffect
    - MainArea 移除 "Sync recording state to parent" useEffect
    - MainArea 传递给 CommandCard 时不再传递 isRecording, hasConflict, onRecordingStart, onRecordingStop, onShortcutAssign, onShortcutClear
    - MainArea 的 props 中保留 assignShortcut, clearShortcut, onRecordingChange（Plan 02 面板会使用），但内部不再使用
    - App.tsx 中 isRecording state 和 handleRecordingChange callback 暂时保留（Plan 02 面板会使用）
  </acceptance_criteria>
  <action>
    Per D-05/D-06: 从 CommandCard 的 props interface 中移除 isRecording, onRecordingStart, onRecordingStop, onShortcutAssign, onShortcutClear, hasConflict。移除 shortcutUtils 的 import（如果不再需要 keyboardEventToShortcut）。移除 toast import（如果不再需要）。移除组件内部录制 keydown 的 useEffect。简化 shortcut badge 的 JSX：当 shortcut 有值时显示静态 badge（不可点击），无值时如果有 shortcutNumber 则显示数字。完全移除录制状态（"按下快捷键..."）、冲突状态（"冲突"）、空槽（"+"）三个分支。editMode 下也不再显示录制入口。

    在 MainArea 中：移除 recordingCommandId 和 conflictCommandId state。移除 handleRecordingStart、handleRecordingStop、handleShortcutAssign、handleShortcutClear 四个 useCallback。移除 "Cancel recording when edit mode off" useEffect（lines ~107-113）。移除 "Sync recording state to parent" useEffect（lines ~115-118）。在 CommandCard 渲染处：只传递 shortcut={cmd.shortcut} 和 shortcutNumber，不再传递 isRecording/hasConflict/onRecordingStart/onRecordingStop/onShortcutAssign/onShortcutClear。

    注意：MainArea 的 props interface 中 assignShortcut, clearShortcut, onRecordingChange 暂时保留（Plan 02 面板需要），但组件内部不再引用 assignShortcut 和 clearShortcut。App.tsx 中的 isRecording state 和 handleRecordingChange 暂时保留，传给 useGlobalShortcuts 的 recording prop 继续工作。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - CommandCard 只显示静态 shortcut badge，无录制交互
    - MainArea 无录制状态管理代码
    - TypeScript 编译无错误
    - 应用仍可正常运行（快捷键显示但不可从卡片编辑）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户键盘输入 → 快捷键注册 | 用户按键经 keyboardEventToShortcut 转换后注册到 OS，需验证输出格式 |
| store 读取 → 应用状态 | shortcutBindings 从 store 恢复，需容忍格式异常或缺失数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | keyboardEventToShortcut | mitigate | 已有输入验证：modifier 检查 + 3 键上限（D-15），无需额外处理 |
| T-18-02 | Tampering | shortcutBindings store 读取 | accept | 本地桌面应用，store 文件由应用独占读写，低风险 |
| T-18-03 | Denial of Service | 全局快捷键注册 | mitigate | version counter 防竞态 + recording 时 unregisterAll，已有机制 |
</threat_model>

<verification>
1. TypeScript 编译无错误：`npx tsc --noEmit`
2. 应用启动正常，现有功能不受影响
3. 快捷键 badge 在卡片上仍可显示（只读）
4. 全局快捷键仍可通过 useGlobalShortcuts 注册（从 ShortcutAction 驱动）
</verification>

<success_criteria>
- ShortcutAction 类型定义完成，覆盖三类操作
- useShortcutActions hook 构建正确的操作列表
- useGlobalShortcuts 从 ShortcutAction + bindings 驱动注册
- shortcutBindings 独立持久化，含旧数据迁移
- CommandCard 中录制 UI 完全移除
- MainArea 中录制状态管理代码完全移除
- TypeScript 编译零错误
</success_criteria>

<output>
After completion, create `.planning/phases/18-快捷键设置面板/18-01-SUMMARY.md`
</output>
