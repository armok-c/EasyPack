---
phase: 17-多行脚本指令
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - package.json
  - src/lib/batch-lang.ts
  - src/hooks/useBatchDetect.ts
  - src/components/ScriptEditor.tsx
  - src/components/CommandDialog.tsx
  - src/components/CommandCard.tsx
  - src/hooks/useProject.ts
autonomous: true
requirements:
  - SCRIPT-01
  - SCRIPT-03
  - SCRIPT-04

must_haves:
  truths:
    - "用户可以在 CommandDialog 中通过 Tab 切换单行/多行编辑模式"
    - "多行编辑器提供语法高亮和行号显示"
    - "从单行切到多行时已输入的内容自动保留"
    - "系统智能检测批处理语法关键字，简单命令显示严格/宽松开关"
    - "多行脚本在指令卡片上显示为截断的多行文本"
    - "点击多行指令卡片能调用 execute_script 执行脚本"
  artifacts:
    - path: "src/lib/batch-lang.ts"
      provides: "CM6 StreamLanguage batch tokenizer"
      exports: ["batchLanguage"]
    - path: "src/hooks/useBatchDetect.ts"
      provides: "批处理语法关键字检测 hook"
      exports: ["useBatchDetect"]
    - path: "src/components/ScriptEditor.tsx"
      provides: "CM6 编辑器 React 封装组件"
      exports: ["ScriptEditor"]
    - path: "src/components/CommandDialog.tsx"
      provides: "Tab 切换 + 多行编辑器集成"
      contains: "activeTab"
    - path: "src/components/CommandCard.tsx"
      provides: "多行内容截断显示"
      contains: "scriptLines"
    - path: "src/hooks/useProject.ts"
      provides: "executeScript 调用路径"
      contains: "execute_script"
  key_links:
    - from: "src/components/CommandDialog.tsx"
      to: "src/components/ScriptEditor.tsx"
      via: "Tab 切换条件渲染"
      pattern: "ScriptEditor"
    - from: "src/components/ScriptEditor.tsx"
      to: "src/lib/batch-lang.ts"
      via: "CM6 language extension import"
      pattern: "batchLanguage"
    - from: "src/hooks/useProject.ts"
      to: "execute_script (Rust)"
      via: "invoke call"
      pattern: "invoke.*execute_script"
    - from: "src/hooks/useBatchDetect.ts"
      to: "src/components/CommandDialog.tsx"
      via: "检测结果显示严格/宽松开关"
      pattern: "useBatchDetect"
---

<objective>
前端 UI 完整实现 -- Tab 切换、CodeMirror 编辑器、批处理检测、卡片多行显示、执行集成

Purpose: 将 Plan 01 的后端能力暴露给用户。用户可以在 CommandDialog 中编写多行批处理脚本，看到语法高亮和行号，智能检测内容类型，点击卡片执行脚本。

Output: ScriptEditor 组件、batch-lang 语法高亮、useBatchDetect hook、CommandDialog Tab 切换改造、CommandCard 多行显示、useProject executeScript 路径
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-多行脚本指令/17-CONTEXT.md
@.planning/phases/17-多行脚本指令/17-RESEARCH.md
@.planning/phases/17-多行脚本指令/17-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 创建的接口，Plan 02 executor 需要 -->

From src/lib/types.ts (Plan 01 扩展后):
```typescript
export interface CommandItem {
  id: string;
  name: string;
  command: string;
  icon: string;
  type: "preset" | "custom";
  scope: "global" | "project";
  addedAt: number;
  shortcut?: string;
  scriptLines?: string;   // Plan 01: \n 分隔多行脚本内容
  executionMode?: "strict" | "lenient" | "batch";  // Plan 01
}
```

From src-tauri/src/commands/shell.rs (Plan 01 新增):
```rust
#[tauri::command]
pub async fn execute_script(
    project_path: String,
    script_content: String,
    is_batch_script: bool,
    strict: bool,
) -> Result<String, String>
```

From src-tauri/tauri.conf.json line 28 (CSP 已兼容):
```
"csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'"
```

From src/main.tsx (StrictMode 启用 -- CM6 hook 需要处理):
```typescript
import { StrictMode } from "react";
// 渲染在 <StrictMode> 内
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 安装 CM6 依赖 + 创建 batch-lang.ts + useBatchDetect.ts + ScriptEditor.tsx</name>
  <files>package.json, src/lib/batch-lang.ts, src/hooks/useBatchDetect.ts, src/components/ScriptEditor.tsx</files>

  <read_first>
  - package.json (当前依赖列表)
  - src/lib/types.ts (确认 scriptLines/executionMode 字段已添加 -- Plan 01 产物)
  - src/hooks/useProject.ts (了解现有 hook 模式)
  - src/components/CommandDialog.tsx (了解 ScriptEditor 将被嵌入的位置)
  </read_first>

  <acceptance_criteria>
  - package.json dependencies 包含 codemirror, @codemirror/state, @codemirror/view, @codemirror/language, @codemirror/commands, @codemirror/theme-one-dark
  - src/lib/batch-lang.ts 导出 batchLanguage (StreamLanguage 实例)，支持 batch 关键字高亮 (if/for/goto/set/call/echo/rem/:label/%%/::comment/@prefix)
  - src/hooks/useBatchDetect.ts 导出 useBatchDetect(content: string): boolean，检测 if/for/goto/call/set/setlocal/endlocal/shift/:label/rem
  - src/components/ScriptEditor.tsx 导出 ScriptEditor 组件 (props: value, onChange, height, darkMode)
  - ScriptEditor 使用自定义 useCodeMirror hook (不用 @uiw/react-codemirror)
  - ScriptEditor 正确处理 StrictMode 双重渲染 (useEffect cleanup 中 view.destroy())
  - npx tsc --noEmit 零错误
  </acceptance_criteria>

  <action>
  安装 CM6 核心包:
  `pnpm add codemirror @codemirror/state @codemirror/view @codemirror/language @codemirror/commands @codemirror/theme-one-dark`

  创建 src/lib/batch-lang.ts:
  - 使用 StreamLanguage.define() 创建自定义 batch tokenizer (per RESEARCH Pattern 2)
  - 支持高亮: REM 注释 (行首 rem)、:: 注释、@ 前缀 (meta)、:label (labelName)、%% 变量、%var% 变量、"string"、batch 关键字 (if/else/for/do/goto/call/set/setlocal/endlocal/exit/echo/pause/shift/start/not/exist/defined/errorlevel/mkdir/rmdir/del/copy/move/ren/type/cls/chcp/cd/pushd/popd/title/color)
  - 导出 batchLanguage 常量和 batchSupport 函数 (LanguageSupport)

  创建 src/hooks/useBatchDetect.ts:
  - 导出 BATCH_KEYWORDS 正则 (/\b(if|for|goto|call|set|setlocal|endlocal|shift)\b/i)
  - 导出 BATCH_LABEL 正则 (/^:\w+/m)
  - 导出 BATCH_REM 正则 (/^rem\b/im)
  - 导出 useBatchDetect(content: string): boolean -- useMemo 包装，任一正则匹配则返回 true
  - 纯函数，无副作用

  创建 src/components/ScriptEditor.tsx:
  - Props: { value: string; onChange: (v: string) => void; height?: string; darkMode?: boolean }
  - 内部实现 useCodeMirror 自定义 hook:
    - 参数: (parentRef: RefObject<HTMLDivElement>, initialContent: string, onChange: (v: string) => void, darkMode: boolean)
    - useEffect 创建 EditorState + EditorView，extensions 包含: basicSetup (去掉 defaultKeymap 中的 indentLess/tab 相关), batchLanguage extension, lineNumbers, EditorView.updateListener 监听 docChanged 调用 onChange, darkMode ? oneDark : []
    - useEffect cleanup 调用 view.destroy() (处理 StrictMode 双重渲染)
    - 创建前检查 parentRef.current 是否已有子节点 (StrictMode 防护)
    - 返回 viewRef
  - JSX: div ref={parentRef} style={{ height }} className="cm-editor-wrapper"
  - 编辑器固定高度由父组件通过 height prop 控制 (per D-13 约 270px)
  - 当 value prop 变化且与编辑器当前内容不同时，通过 view.dispatch 更新编辑器内容（但不在自身 onChange 触发时更新，防止循环）
  </action>

  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | grep -c "error" | grep -q "^0$" && echo "TS OK" || echo "TS ERRORS"</automated>
  </verify>

  <done>
  - CM6 6 个核心包安装在 package.json
  - batch-lang.ts 导出 batchLanguage + batchSupport，提供 batch 关键字/注释/变量/字符串高亮
  - useBatchDetect.ts 导出 useBatchDetect hook，正确检测批处理关键字
  - ScriptEditor.tsx 导出 ScriptEditor React 组件，处理 StrictMode 双重渲染
  - tsc --noEmit 零错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 改造 CommandDialog Tab 切换 + CommandCard 多行显示 + useProject executeScript</name>
  <files>src/components/CommandDialog.tsx, src/components/CommandCard.tsx, src/hooks/useProject.ts</files>

  <read_first>
  - src/components/CommandDialog.tsx (完整文件 -- 需要添加 Tab 切换、集成 ScriptEditor、条件显示预设选择器)
  - src/components/CommandCard.tsx (完整文件 -- 需要添加多行内容截断显示)
  - src/hooks/useProject.ts (完整文件 -- 需要添加 executeScript 函数、修改 onSubmit 接口)
  - src/components/ScriptEditor.tsx (Plan 02 Task 1 产物)
  - src/hooks/useBatchDetect.ts (Plan 02 Task 1 产物)
  - src/lib/types.ts (确认 scriptLines/executionMode 字段)
  - src/main.tsx (确认 StrictMode 上下文)
  </read_first>

  <acceptance_criteria>
  - CommandDialog 顶部有 "单行" 和 "多行" 两个 Tab 按钮 (D-01)
  - 新建指令默认选中 "单行" Tab (D-02)
  - 从单行切到多行时，单行已输入内容自动填入编辑器 (D-03)
  - 预设指令选择器（双下拉框）仅在 "单行" Tab 时显示 (D-04)
  - 多行 Tab 显示 ScriptEditor 组件，替换 Input 位置 (D-12)
  - 多行 Tab 时，ScriptEditor 下方显示 useBatchDetect 检测结果：简单命令显示严格/宽松 Button pair，批处理脚本显示提示文本 (D-07)
  - 新建多行指令默认严格模式 (D-08)
  - 编辑含 scriptLines 的指令时自动选中 "多行" Tab 并加载内容
  - onSubmit 回调接收 scriptLines 和 executionMode 字段
  - CommandCard 接收 scriptLines prop 时显示多行截断文本（最多 3 行，超出用 "..."）(D-15)
  - CommandCard title 属性在有多行内容时显示完整脚本
  - useProject 导出 executeScript(projectPath, scriptContent, isBatchScript, strict) 函数
  - useProject 的 executeCommand 能判断指令是单行还是多行（scriptLines 存在时调用 executeScript）
  - npx tsc --noEmit 零错误
  </acceptance_criteria>

  <action>
  改造 CommandDialog.tsx:
  1. 添加状态: activeTab ("single" | "multi"), scriptContent (string), executionMode ("strict" | "lenient" | "batch")
  2. 初始化逻辑: 如果 initialData?.scriptLines 存在，activeTab 初始化为 "multi"，scriptContent 初始化为 initialData.scriptLines，executionMode 初始化为 initialData.executionMode ?? "strict"。否则 activeTab 为 "single" (per D-02)
  3. 添加 handleTabChange(tab) 回调: 如果从 "single" 切到 "multi" 且 command.trim() 非空，设置 scriptContent = command (per D-03)。清空另一方编辑器内容可选
  4. 在 DialogHeader 下方（DialogTitle 之后、预设选择器之前）渲染 Tab 按钮:
     - 两个 button 元素（参考现有 scope selector 的 inline-flex 按钮组样式）
     - "单行" 和 "多行" 文字标签
     - 选中态: bg-white/15 text-foreground，未选中态: bg-transparent text-muted-foreground
  5. 预设选择器区域 (div with border-b) 用 activeTab === "single" && 包裹 (per D-04)
  6. 命令输入区域: activeTab === "single" ? 现有 Input : ScriptEditor 组件 (per D-12)
     - ScriptEditor props: value={scriptContent}, onChange={setScriptContent}, height="270px" (per D-13 约 250-300px), darkMode={isDark}
     - isDark 从 useTheme() 或 document.documentElement.classList.contains("dark") 获取
  7. ScriptEditor 下方，activeTab === "multi" 时:
     - 调用 useBatchDetect(scriptContent) 获取 isBatch
     - isBatch=false: 显示严格/宽松 Button pair（两个按钮互斥切换，参考现有 scope selector 样式）(per D-07)，executionMode 在 "strict" 和 "lenient" 之间切换
     - isBatch=true: 显示提示文本 "已识别为批处理脚本，将原样执行"，executionMode 强制为 "batch"
  8. 修改 isValid: activeTab === "multi" ? name.trim().length > 0 && scriptContent.trim().length > 0 : 原有逻辑
  9. handleSubmit: activeTab === "multi" 时，onSubmit 传递 scriptLines: scriptContent, executionMode（command 字段传 scriptContent 的第一行或空字符串以保持兼容）
  10. handleOpenChange reset: 增加 setActiveTab("single"), setScriptContent(""), setExecutionMode("strict")
  11. 更新 CommandDialogProps.onSubmit 签名: data 增加 scriptLines?: string 和 executionMode?: "strict" | "lenient" | "batch"

  改造 CommandCard.tsx:
  1. Props 增加: scriptLines?: string
  2. 在 Icon 和 name 之间，如果 scriptLines 存在且非空:
     - 用 div 包裹，最多显示 3 行，line-clamp-3 样式 (per D-15)
     - 内容为 scriptLines，font-size 用 text-[10px]，text-muted-foreground/70 颜色
     - 超过 3 行时截断显示 "..."（CSS line-clamp 或 JS 截断）
  3. title 属性: 有 scriptLines 时显示完整脚本内容（用于 hover tooltip），否则显示 command

  改造 useProject.ts:
  1. 添加 executeScript 函数:
     - 参数: (scriptContent: string, isBatchScript: boolean, strict: boolean)
     - 调用 invoke("execute_script", { projectPath: currentProject.path, scriptContent, isBatchScript, strict })
     - 成功时 toast.success("已执行脚本")
     - 失败时 toast.error(执行失败信息)
  2. 修改 executeCommand 函数签名和逻辑:
     - 接收 CommandItem 而非纯 string（或者添加新的 executeCommandItem 函数）
     - 或者保持 executeCommand(shellCommand) 不变，添加新函数:
       executeScriptCommand(cmd: CommandItem): 当 cmd.scriptLines 存在时调用 executeScript，否则调用原 executeCommand
     - 推荐: 添加 executeScriptCommand 函数，不修改现有 executeCommand 签名（向后兼容）
     - executeScriptCommand 内部: cmd.scriptLines ? invoke("execute_script", { projectPath, scriptContent: cmd.scriptLines, isBatchScript: cmd.executionMode === "batch", strict: cmd.executionMode !== "lenient" }) : invoke("execute_command", { projectPath, shellCommand: cmd.command })
  3. 更新 updateCommand 和 addCommand 函数签名，支持 scriptLines 和 executionMode 字段:
     - addCommand: data 参数增加 scriptLines?, executionMode?
     - updateCommand: data 参数增加 scriptLines?, executionMode?
  4. 导出 executeScriptCommand

  关键注意事项:
  - StrictMode 已启用 (src/main.tsx)，ScriptEditor 的 useEffect 必须在 cleanup 中 destroy view
  - ScriptEditor 不使用 @uiw/react-codemirror (per RESEARCH Anti-Patterns)
  - batchLanguage 使用 StreamLanguage.define()，不是 @codemirror/lang-shell（不存在）
  - CommandDialog 的 sm:max-w-[480px] 在多行 Tab 时可能需要加宽到 sm:max-w-[560px] 以容纳编辑器
  - scope selector（全局指令/当前项目指令）在多行 Tab 时也保留显示
  - 图标选择器在两个 Tab 都显示
  </action>

  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | grep -c "error" | grep -q "^0$" && echo "TS OK" || echo "TS ERRORS"</automated>
  </verify>

  <done>
  - CommandDialog 支持 Tab 切换单行/多行模式
  - 多行模式使用 ScriptEditor (CM6) 替代 Input
  - 预设选择器仅在单行模式显示
  - Tab 切换时保留已输入内容
  - 多行模式显示智能检测 + 严格/宽松开关
  - CommandCard 支持多行内容截断显示（最多 3 行）
  - useProject 导出 executeScriptCommand 函数
  - addCommand / updateCommand 支持 scriptLines 和 executionMode 字段
  - tsc --noEmit 零错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 -> CM6 编辑器 | scriptContent 由用户完全控制 |
| 前端 -> Rust execute_script | IPC 调用传递脚本内容 |
| ScriptEditor DOM | CM6 动态创建 DOM 元素和 style 标签 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information Disclosure | CommandCard tooltip | accept | 多行脚本内容显示在 hover tooltip 中是预期行为（用户自己的脚本） |
| T-17-05 | Tampering | CM6 编辑器内容 | accept | 编辑器内容由用户控制，无外部输入来源 |
| T-17-06 | Denial of Service | 超大脚本内容 | accept | 本地桌面应用，用户自行决定脚本大小。CM6 可处理 MB 级文档 |
</threat_model>

<verification>
1. `cd E:/git/EasyPack && npx tsc --noEmit` -- TypeScript 零错误
2. `grep "activeTab" src/components/CommandDialog.tsx` -- Tab 切换状态存在
3. `grep "ScriptEditor" src/components/CommandDialog.tsx` -- 编辑器集成
4. `grep "useBatchDetect" src/components/CommandDialog.tsx` -- 检测 hook 使用
5. `grep "executeScriptCommand\|execute_script" src/hooks/useProject.ts` -- 执行路径
6. `grep "scriptLines" src/components/CommandCard.tsx` -- 多行显示
7. `grep "batchLanguage" src/lib/batch-lang.ts` -- 语法高亮
8. `pnpm ls codemirror` -- CM6 依赖已安装
</verification>

<success_criteria>
- 用户可以在 CommandDialog 中通过 "单行"/"多行" Tab 切换编辑模式
- 多行编辑器显示语法高亮（batch 关键字着色）和行号
- 从单行切到多行时已输入内容保留在编辑器中
- 预设选择器仅单行模式显示
- 智能检测批处理语法，简单命令显示严格/宽松开关
- 多行脚本在指令卡片上截断显示（最多 3 行）
- 点击含 scriptLines 的指令卡片调用 execute_script（非 execute_command）
- StrictMode 下 ScriptEditor 无双重渲染问题
- 深色/浅色主题切换时编辑器主题跟随
- tsc --noEmit 零错误
</success_criteria>

<output>
After completion, create `.planning/phases/17-多行脚本指令/17-02-SUMMARY.md`
</output>
