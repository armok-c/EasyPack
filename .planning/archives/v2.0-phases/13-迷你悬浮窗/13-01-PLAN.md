---
phase: 13-迷你悬浮窗
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - float.html
  - src/float-main.tsx
  - src/components/FloatApp.tsx
  - src/hooks/useFloatWindow.ts
  - src-tauri/capabilities/default.json
autonomous: true
requirements:
  - FLOAT-02
  - FLOAT-03
  - FLOAT-04
  - FLOAT-05
  - FLOAT-06
  - FLOAT-07

must_haves:
  truths:
    - "Vite 构建输出包含 float.html 入口，悬浮窗能加载独立页面"
    - "悬浮窗以 alwaysOnTop + skipTaskbar 创建，不出现在任务栏"
    - "悬浮窗显示当前项目的全部指令按钮（竖向列表）"
    - "点击指令按钮触发 200ms 绿色闪烁反馈"
    - "主窗口通过 Tauri 事件系统向悬浮窗推送项目/指令状态"
    - "悬浮窗独立关闭不影响主窗口"
    - "无项目时悬浮窗显示空状态提示"
  artifacts:
    - path: "float.html"
      provides: "悬浮窗独立 HTML 入口"
      contains: "float-main"
    - path: "src/float-main.tsx"
      provides: "悬浮窗 React 渲染入口"
      exports: ["default"]
    - path: "src/components/FloatApp.tsx"
      provides: "悬浮窗根组件（项目名 + 指令列表 + 关闭按钮）"
      contains: "FloatApp"
    - path: "src/hooks/useFloatWindow.ts"
      provides: "悬浮窗窗口创建/显示/隐藏/销毁管理 hook"
      contains: "useFloatWindow"
    - path: "src-tauri/capabilities/default.json"
      provides: "扩展多窗口权限"
      contains: "float"
  key_links:
    - from: "src/hooks/useFloatWindow.ts"
      to: "@tauri-apps/api/webviewWindow"
      via: "new WebviewWindow()"
      pattern: "WebviewWindow"
    - from: "src/components/FloatApp.tsx"
      to: "@tauri-apps/api/event"
      via: "listen('float:state-update')"
      pattern: "float:state-update"
    - from: "src/components/FloatApp.tsx"
      to: "@tauri-apps/api/event"
      via: "emit('float:execute')"
      pattern: "float:execute"
---

<objective>
搭建悬浮窗的基础设施层和独立 UI，实现窗口生命周期管理和跨窗口状态同步。

Purpose: 建立悬浮窗作为独立窗口的全部技术基础 -- Vite 多页构建、WebviewWindow 动态创建、Tauri 事件系统通信、React 独立入口和组件。
Output: 可独立运行的悬浮窗页面 + useFloatWindow hook + 完整的窗口创建/销毁/状态同步能力
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-迷你悬浮窗/13-CONTEXT.md
@.planning/phases/13-迷你悬浮窗/13-RESEARCH.md
@.planning/phases/13-迷你悬浮窗/13-UI-SPEC.md

<interfaces>
<!-- 现有类型和模式，执行者必须遵循 -->

From src/lib/types.ts:
```typescript
export interface CommandItem {
  id: string;
  name: string;
  command: string;
  icon: string;
  type: "preset" | "custom";
  scope: "global" | "project";
  addedAt: number;
  shortcut?: string;
}
```

From src/hooks/useProject.ts:
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;
  color?: string;
}
```

From src/components/TitleBar.tsx (拖拽模式):
```typescript
function handleDragStart(e: React.MouseEvent<HTMLDivElement>) {
  if (e.button !== 0) return;
  const target = e.target as HTMLElement;
  if (target.closest("button")) return;
  appWindow.startDragging();
}
```

From src/index.css (按钮样式):
```css
.titlebar-button {
  display: flex; align-items: center; justify-content: center;
  width: 28px; height: 100%; border: none;
  background: transparent; color: var(--color-muted-foreground);
  cursor: default; transition: background-color 100ms ease;
}
.titlebar-button:hover { background: var(--color-accent); color: var(--color-foreground); }
```

From src/hooks/useTray.ts (ref 回调模式):
```typescript
const onExecuteRef = useRef(onExecute);
onExecuteRef.current = onExecute;
```

From vite.config.ts (当前配置):
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";
// 需要添加 build.rollupOptions.input 配置
```

From src-tauri/capabilities/default.json (当前权限):
```json
{
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-start-dragging",
    // ... 更多权限
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 基础设施 -- Vite 多页构建 + capabilities + HTML 入口</name>
  <files>vite.config.ts, float.html, src/float-main.tsx, src-tauri/capabilities/default.json</files>
  <read_first>
    - vite.config.ts (当前配置，了解插件和路径别名)
    - index.html (主窗口入口模式，参照结构)
    - src/main.tsx (React 渲染入口模式)
    - src-tauri/capabilities/default.json (当前权限列表)
  </read_first>
  <behavior>
    - 验证 vite.config.ts 的 build.rollupOptions.input 包含 main 和 float 两个入口
    - 验证 float.html 存在且引用 float-main.tsx
    - 验证 src/float-main.tsx 挂载 FloatApp 到 #float-root
    - 验证 capabilities/default.json 的 windows 数组包含 "float"
    - 验证 capabilities/default.json 包含 "core:webview:allow-create-webview-window" 权限
  </behavior>
  <action>
1. **修改 vite.config.ts** — 添加 `build.rollupOptions.input` 配置实现多页构建：
   - 导入 `path` 已存在
   - 在 defineConfig 对象中添加 `build` 字段：
   ```typescript
   build: {
     rollupOptions: {
       input: {
         main: path.resolve(__dirname, "index.html"),
         float: path.resolve(__dirname, "float.html"),
       },
     },
   },
   ```
   这确保 Vite 同时输出 index.html 和 float.html

2. **创建 float.html** — 悬浮窗的 HTML 入口文件，放在项目根目录（与 index.html 同级）：
   ```html
   <!doctype html>
   <html lang="zh-CN">
     <head>
       <meta charset="UTF-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <title>EasyPack Floating</title>
     </head>
     <body>
       <div id="float-root"></div>
       <script type="module" src="/src/float-main.tsx"></script>
     </body>
   </html>
   ```
   注意：不引用 favicon，减少加载开销

3. **创建 src/float-main.tsx** — 悬浮窗的 React 渲染入口：
   ```typescript
   import { createRoot } from "react-dom/client";
   import FloatApp from "./components/FloatApp";
   import "./index.css";

   createRoot(document.getElementById("float-root")!).render(<FloatApp />);
   ```
   注意：不使用 StrictMode（悬浮窗是轻量级视图，不需要双重渲染调试）；引用同一个 index.css 确保 Tailwind 样式生效

4. **修改 src-tauri/capabilities/default.json** — 扩展多窗口权限：
   - `"windows"` 数组：`["main"]` 改为 `["main", "float"]`
   - `"permissions"` 数组追加以下权限：
     - `"core:webview:allow-create-webview-window"` — 允许前端创建 WebviewWindow
     - `"core:webview:allow-set-webview-size"` — 悬浮窗可能需要调整大小
     - `"core:window:allow-set-position"` — 设置悬浮窗初始位置（D-08 右上角）
     - `"core:window:allow-set-always-on-top"` — 运行时切换置顶状态
     - `"core:window:allow-set-skip-taskbar"` — 运行时切换任务栏显示
     - `"core:window:allow-is-maximized"` — 悬浮窗不需要但保持一致性
     - `"core:event:default"` — 事件系统默认权限（emit/listen/emitTo）
  </action>
  <acceptance_criteria>
    - vite.config.ts 包含 `build:` 和 `rollupOptions:` 字符串
    - vite.config.ts 包含 `float: path.resolve(__dirname, "float.html")` 字符串
    - float.html 文件存在，包含 `<div id="float-root">` 和 `src="/src/float-main.tsx"`
    - src/float-main.tsx 文件存在，导入 FloatApp 并挂载到 `float-root`
    - capabilities/default.json 的 `windows` 数组包含 `"float"`
    - capabilities/default.json 的 `permissions` 数组包含 `"core:webview:allow-create-webview-window"`
    - capabilities/default.json 的 `permissions` 数组包含 `"core:event:default"`
  </acceptance_criteria>
  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript check done"</automated>
  </verify>
  <done>
    - Vite 多页构建配置完成，build 时输出 index.html + float.html
    - 悬浮窗有独立的 HTML 入口和 React 入口
    - capabilities/default.json 包含 "float" 窗口和所有必要权限
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 悬浮窗 UI 组件 + 窗口生命周期 Hook</name>
  <files>src/components/FloatApp.tsx, src/hooks/useFloatWindow.ts</files>
  <read_first>
    - src/components/TitleBar.tsx (拖拽模式、按钮样式参考)
    - src/hooks/useTray.ts (ref 回调模式、事件监听模式)
    - src/hooks/useProject.ts (executeCommand、ProjectItem 类型)
    - src/lib/types.ts (CommandItem 类型)
    - src/lib/icons.ts (图标映射表)
    - src/index.css (titlebar-button 样式、OKLCH 色值)
    - .planning/phases/13-迷你悬浮窗/13-UI-SPEC.md (完整 UI 规范)
    - .planning/phases/13-迷你悬浮窗/13-RESEARCH.md (代码示例和反模式)
  </read_first>
  <behavior>
    - FloatApp 渲染项目名 header + 指令列表 + 关闭按钮
    - FloatApp 监听 "float:state-update" 事件更新项目/指令
    - FloatApp 点击指令行触发 emit "float:execute" 事件
    - FloatApp 点击指令行后该行显示 200ms 绿色闪烁 (bg-green-500/20)
    - FloatApp 无项目时显示 FolderOpen 图标 + "请先在主窗口选择一个项目"
    - FloatApp 关闭按钮调用 getCurrentWindow().close()
    - FloatApp header 区域 mousedown 触发 startDragging()
    - useFloatWindow 创建 WebviewWindow 并设置 alwaysOnTop, skipTaskbar, 右上角位置
    - useFloatWindow 维护 floatVisible 状态并暴露 toggle()
    - useFloatWindow 监听 "float:execute" 事件回调到 onExecute
    - useFloatWindow 在 currentProject/commands 变化时 emitTo("float", "float:state-update")
    - useFloatWindow 处理窗口关闭后的引用清理（悬挂引用防坑）
    - useFloatWindow 主窗口退出时 destroy 悬浮窗（D-12）
  </behavior>
  <action>
### 创建 src/components/FloatApp.tsx

悬浮窗根组件。这是一个独立运行的组件（不在主窗口 React 树中），通过 Tauri 事件系统接收数据。

**状态管理：**
- `project: ProjectItem | null` — 当前项目，从 "float:state-update" 事件接收
- `commands: CommandItem[]` — 当前指令列表，从 "float:state-update" 事件接收
- `flashIndex: number | null` — 正在闪烁的指令行索引（200ms 后清除）

**事件监听（useEffect）：**
```typescript
import { listen } from "@tauri-apps/api/event";
import { getCurrentWindow } from "@tauri-apps/api/window";

// 监听主窗口推送的状态
const unlisten = await listen<{
  project: ProjectItem | null;
  commands: CommandItem[];
}>("float:state-update", (event) => {
  setProject(event.payload.project);
  setCommands(event.payload.commands);
});
return () => { unlisten(); };
```

**UI 结构（按 13-UI-SPEC.md 规范）：**

```
根 div: w-[280px] h-auto max-h-[600px] flex flex-col bg-background border border-white/10 rounded-lg overflow-hidden

  Header (拖拽区): h-[28px] flex items-center justify-between px-2 border-b border-white/5
    左: 项目名 text-xs font-semibold text-foreground/80 truncate max-w-[200px]
    右: 关闭按钮 (X icon, 12px, hover:bg-red-500/80 hover:text-white rounded-sm)

  内容区: flex-1 overflow-y-auto p-2 (使用自定义滚动或 ScrollArea)
    有项目时: 指令行列表
    无项目时: 空状态居中（FolderOpen 图标 + 提示文字）
```

**指令行组件（内联，不单独文件）：**
```
行: h-8 flex items-center gap-2 px-2 rounded-md cursor-pointer select-none transition-all duration-150 ease-out

状态:
- Default: transparent bg, text-card-foreground
- Hover: bg-white/5
- Active press: bg-white/10
- Flash (D-17): bg-green-500/20 border border-green-500/40 (200ms)
- Disabled (无项目): text-muted-foreground/40 cursor-not-allowed

图标: w-[14px] h-[14px] (从 icons 映射表获取)
文字: text-xs font-normal
Tooltip: title={command.command} 显示 shell 命令
```

**图标解析：** 复用 src/lib/icons.ts 中的图标映射表（ICON_OPTIONS 或类似映射）。导入映射函数，将 CommandItem.icon 字符串解析为 Lucide 组件。

**拖拽处理：** 复用 TitleBar.tsx 的 handleDragStart 模式（检查 e.button === 0，检查 target.closest("button")），使用 `getCurrentWindow().startDragging()`。注意：悬浮窗中的 `getCurrentWindow()` 返回悬浮窗自身的 Window 实例，不是主窗口。

**闪烁反馈（D-17）：**
```typescript
const handleRowClick = (index: number, shellCommand: string) => {
  if (!project || flashIndex !== null) return;
  setFlashIndex(index);
  emit("float:execute", { command: shellCommand });
  setTimeout(() => setFlashIndex(null), 200);
};
```

**关闭按钮：** `onClick={() => getCurrentWindow().close()}` — 使用 close() 而非 destroy()，保留可预测的生命周期（D-07 独立关闭不影响主窗口）。

**空状态（D-13）：**
```tsx
{!project ? (
  <div className="flex flex-col items-center justify-center py-8 gap-2">
    <FolderOpen className="size-6 text-muted-foreground" />
    <span className="text-sm text-muted-foreground text-center">
      请先在主窗口选择一个项目
    </span>
  </div>
) : (
  // 指令列表
)}
```

**Accessibility：**
- 根 div: `role="dialog"` `aria-label="EasyPack 悬浮窗"`
- 项目名: `aria-live="polite"`
- 指令行: `<button>` 元素，`aria-label="{name}: {command}"`
- 关闭按钮: `aria-label="关闭悬浮窗"`
- 无项目时指令行: `aria-disabled="true"`

---

### 创建 src/hooks/useFloatWindow.ts

管理悬浮窗创建、显示、隐藏、销毁和状态同步的 hook。

**接口签名：**
```typescript
interface UseFloatWindowOptions {
  currentProject: ProjectItem | null;
  commands: CommandItem[];
  onExecute: (command: string) => void;
  appWindow: Window; // 主窗口实例
}

interface UseFloatWindowReturn {
  floatVisible: boolean;
  toggleFloat: () => void;
  destroyFloat: () => void; // 主窗口退出时调用 (D-12)
}
```

**内部状态：**
- `floatVisible: boolean` — 悬浮窗是否可见
- `floatWindowRef: useRef<WebviewWindow | null>` — 悬浮窗实例引用

**Ref 模式（防闭包过时，遵循 useTray.ts 已验证模式）：**
```typescript
const currentProjectRef = useRef(currentProject);
currentProjectRef.current = currentProject;
const commandsRef = useRef(commands);
commandsRef.current = commands;
const onExecuteRef = useRef(onExecute);
onExecuteRef.current = onExecute;
```

**创建悬浮窗函数（createFloat）：**
```typescript
async function createFloat(): Promise<WebviewWindow> {
  const url = import.meta.env.DEV
    ? "http://localhost:1420/float.html"
    : "float.html";

  const floatWin = new WebviewWindow("float", {
    url,
    width: 280,
    minWidth: 280,
    maxWidth: 280,
    height: 400,
    minHeight: 200,
    decorations: false,
    shadow: true,
    alwaysOnTop: true,     // FLOAT-03
    skipTaskbar: true,      // FLOAT-04
    resizable: false,       // D-05 固定宽度
    visible: true,          // 创建后立即可见
    title: "EasyPack Floating",
  });

  // 等待窗口创建完成
  await new Promise<void>((resolve, reject) => {
    floatWin.once("tauri://created", () => resolve());
    floatWin.once("tauri://error", (e) => reject(e));
  });

  // D-08: 设置初始位置为屏幕右上角（偏移 16px）
  try {
    const monitors = await availableMonitors();
    const primary = monitors[0];
    if (primary) {
      const { width, x } = primary.workArea.size;
      const { y } = primary.workArea.position;
      const scale = primary.scaleFactor;
      const logicalRight = (x + width) / scale;
      const posY = (y / scale) + 16;
      const posX = logicalRight - 280 - 16;
      await floatWin.setPosition(new LogicalPosition(posX, posY));
    }
  } catch (err) {
    console.error("Failed to set float window position:", err);
    // 非致命错误，继续运行
  }

  // 监听悬浮窗发来的执行请求
  await listen<{ command: string }>("float:execute", (event) => {
    onExecuteRef.current(event.payload.command);
  });

  // 监听悬浮窗关闭事件（清理引用）
  floatWin.onCloseRequested(() => {
    floatWindowRef.current = null;
    setFloatVisible(false);
  });

  return floatWin;
}
```

**toggle 逻辑：**
```typescript
const toggleFloat = useCallback(async () => {
  // 检查窗口是否存活
  const existing = WebviewWindow.getByLabel("float");
  if (existing && floatWindowRef.current) {
    // 窗口存在，toggle show/hide
    if (floatVisible) {
      await existing.hide();
      setFloatVisible(false);
    } else {
      await existing.show();
      await existing.setFocus();
      setFloatVisible(true);
      // 重新同步最新状态（窗口可能错过了 hide 期间的变化）
      await syncState(existing);
    }
    return;
  }

  // 窗口不存在（首次创建或已关闭后重新创建）
  try {
    const floatWin = await createFloat();
    floatWindowRef.current = floatWin;
    setFloatVisible(true);
    // 首次创建后同步当前状态
    await syncState(floatWin);
  } catch (err) {
    console.error("Failed to create float window:", err);
    toast.error("无法创建悬浮窗");
  }
}, [floatVisible]);
```

**状态同步（syncState）：**
```typescript
async function syncState(target?: WebviewWindow) {
  const win = target ?? floatWindowRef.current;
  if (!win) return;
  await emitTo("float", "float:state-update", {
    project: currentProjectRef.current,
    commands: commandsRef.current,
  });
}
```

**自动同步 effect（当项目/指令变化时推送）：**
```typescript
useEffect(() => {
  if (!floatVisible) return;
  syncState();
}, [currentProject, commands, floatVisible]);
```

**destroyFloat（D-12 主窗口退出时调用）：**
```typescript
const destroyFloat = useCallback(async () => {
  const existing = WebviewWindow.getByLabel("float");
  if (existing) {
    try {
      await existing.destroy();
    } catch {
      // 窗口可能已关闭，忽略错误
    }
  }
  floatWindowRef.current = null;
  setFloatVisible(false);
}, []);
```

**导入列表：**
```typescript
import { WebviewWindow } from "@tauri-apps/api/webviewWindow";
import { emitTo, listen } from "@tauri-apps/api/event";
import { availableMonitors } from "@tauri-apps/api/window";
import { LogicalPosition } from "@tauri-apps/api/dpi";
import { toast } from "sonner";
```

**清理 effect：** 组件卸载时清理事件监听器。在 createFloat 中 listen 返回的 UnlistenFn 需要在 cleanup 中调用。维护一个 `unlistenersRef: useRef<UnlistenFn[]>` 收集所有清理函数。
  </action>
  <acceptance_criteria>
    - src/components/FloatApp.tsx 存在，包含 `FloatApp` 导出
    - FloatApp.tsx 包含 `float:state-update` 事件监听字符串
    - FloatApp.tsx 包含 `float:execute` 事件发射字符串
    - FloatApp.tsx 包含 `FolderOpen` 图标导入
    - FloatApp.tsx 包含 `请先在主窗口选择一个项目` 空状态文字
    - FloatApp.tsx 包含 `bg-green-500/20` 闪烁反馈样式
    - FloatApp.tsx 包含 `startDragging` 拖拽调用
    - FloatApp.tsx 包含 `role="dialog"` 无障碍属性
    - src/hooks/useFloatWindow.ts 存在，包含 `useFloatWindow` 导出
    - useFloatWindow.ts 包含 `alwaysOnTop: true` 窗口属性
    - useFloatWindow.ts 包含 `skipTaskbar: true` 窗口属性
    - useFloatWindow.ts 包含 `new WebviewWindow("float"` 创建调用
    - useFloatWindow.ts 包含 `LogicalPosition` 位置计算
    - useFloatWindow.ts 包含 `floatVisible` 和 `toggleFloat` 返回值
    - useFloatWindow.ts 包含 `destroyFloat` 返回值（D-12）
    - useFloatWindow.ts 包含 `emitTo("float", "float:state-update"` 状态同步
    - useFloatWindow.ts 包含 `listen("float:execute"` 执行监听
    - TypeScript 编译无错误
  </acceptance_criteria>
  <verify>
    <automated>cd /e/git/EasyPack && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript check done"</automated>
  </verify>
  <done>
    - FloatApp 组件完整实现：项目名 header + 指令行列表 + 200ms 绿色闪烁 + 空状态 + 关闭按钮 + 拖拽移动
    - useFloatWindow hook 完整实现：WebviewWindow 动态创建 + alwaysOnTop/skipTaskbar + 右上角初始位置 + toggle show/hide + 状态同步 + 执行事件监听 + 悬挂引用清理 + destroyFloat
    - 悬浮窗窗口属性满足 FLOAT-03（置顶）、FLOAT-04（无任务栏）、D-05（280px 固定宽度）、D-07（无边框）、D-08（右上角初始位置）
    - 跨窗口通信使用 Tauri 事件系统（emitTo/listen），满足 FLOAT-05（点击执行）、FLOAT-06（实时同步）
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主窗口 → 悬浮窗 | 通过 Tauri 事件系统传递 project/commands 数据，仅限应用内窗口 |
| 悬浮窗 → 主窗口 | 通过 Tauri 事件系统发送 execute 请求，command 字符串经主窗口验证链 |
| 悬浮窗 → Tauri API | 需要正确配置 capabilities 权限，否则 API 调用被拒 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation | capabilities/default.json | mitigate | 精确授予悬浮窗所需权限，不使用 core:default 全部权限，仅添加 webview-window + set-position + event:default 等必要权限 |
| T-13-02 | Tampering | float:execute 事件 payload | accept | command 字符串经主窗口 executeCommand 路径处理（含 invoke 验证），与主窗口卡片同一条验证链，无额外风险 |
| T-13-03 | Tampering | WebviewWindow label | mitigate | 硬编码 label "float"，创建前检查 getByLabel 是否已存在，防止重复创建 |
| T-13-04 | Denial | 窗口创建失败 | accept | 非致命错误，toast 提示用户，主窗口不受影响，用户可重试 toggle |
</threat_model>

<verification>
1. TypeScript 编译无错误：`npx tsc --noEmit`
2. Vite 构建成功：`npx vite build` 输出包含 float.html
3. 文件结构完整：float.html, src/float-main.tsx, src/components/FloatApp.tsx, src/hooks/useFloatWindow.ts
4. capabilities 包含 "float" 窗口和必要权限
</verification>

<success_criteria>
- Vite 构建输出包含 float.html 入口
- capabilities/default.json 的 windows 数组包含 "float"，permissions 包含 webview 创建和事件系统权限
- FloatApp 组件可通过 Tauri 事件系统接收项目/指令数据并渲染
- useFloatWindow hook 可创建 WebviewWindow 并设置正确属性和位置
- 悬浮窗支持：拖拽移动、200ms 绿色闪烁反馈、独立关闭、空状态显示
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/13-迷你悬浮窗/13-01-SUMMARY.md`
</output>
