---
phase: 14-边缘抽屉
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/drawer-geometry.ts
  - src/lib/drawer-animation.ts
  - src/lib/__tests__/drawer-geometry.test.ts
  - src/lib/__tests__/drawer-animation.test.ts
  - src/hooks/useVisibilityState.ts
  - src/hooks/__tests__/useVisibilityState.test.ts
autonomous: true
requirements:
  - DRAWER-01
  - DRAWER-02
  - DRAWER-05

must_haves:
  truths:
    - "给定窗口位置和 monitor workArea，能正确判断窗口是否在主显示器四边 10px 内"
    - "给定吸附边和 monitor workArea，能计算正确的 thin sliver 位置和尺寸"
    - "easeInOut 缓动函数在 t=0 时返回 0，t=1 时返回 1，中间值平滑过渡"
    - "animateWindow 按帧插值，从 from 状态线性过渡到 to 状态"
    - "useVisibilityState 支持 VISIBLE / TRAY_HIDDEN / DRAWER_HIDDEN 三态，DRAWER_HIDDEN 和 TRAY_HIDDEN 互斥"
  artifacts:
    - path: "src/lib/drawer-geometry.ts"
      provides: "边缘检测坐标计算函数"
      exports: ["detectSnapEdge", "calculateSliverRect", "isPrimaryMonitor"]
    - path: "src/lib/drawer-animation.ts"
      provides: "缓动函数和帧驱动动画工具"
      exports: ["easeInOut", "animateWindow"]
    - path: "src/hooks/useVisibilityState.ts"
      provides: "三态可见性状态机"
      exports: ["useVisibilityState", "VisibilityState"]
  key_links:
    - from: "src/lib/drawer-geometry.ts"
      to: "monitor workArea / window position"
      via: "坐标比较函数"
      pattern: "detectSnapEdge"
---

<objective>
创建边缘抽屉的基础工具层和三态状态机扩展。

Purpose: 为 Wave 2 的 useEdgeDrawer hook 和 Wave 3 的 UI 集成提供纯函数工具和类型基础。几何工具负责边缘检测和 sliver 坐标计算，动画工具负责缓动和帧驱动，三态状态机为窗口可见性管理增加 DRAWER_HIDDEN 状态。

Output: drawer-geometry.ts, drawer-animation.ts, 更新后的 useVisibilityState.ts, 以及对应的单元测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-边缘抽屉/14-CONTEXT.md
@.planning/phases/14-边缘抽屉/14-RESEARCH.md

@src/hooks/useVisibilityState.ts
@src/lib/utils.ts
@vitest.config.ts
</context>

<interfaces>
<!-- 现有类型定义，executor 需要参考 -->

From src/hooks/useVisibilityState.ts:
```typescript
export type VisibilityState = "VISIBLE" | "TRAY_HIDDEN";

export function useVisibilityState() {
  // 返回 { visibility, hideToTray, showFromTray, setVisibility, isVisible }
}
```

From @tauri-apps/api/dpi (已安装):
```typescript
export class LogicalPosition { constructor(x: number, y: number); x: number; y: number; }
export class LogicalSize { constructor(width: number, height: number); width: number; height: number; }
```

From @tauri-apps/api/window (已安装):
```typescript
interface Monitor {
  workArea: { position: PhysicalPosition; size: PhysicalSize };
  scaleFactor: number;
  isPrimary: boolean;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 drawer-geometry.ts + 单元测试</name>
  <files>src/lib/drawer-geometry.ts, src/lib/__tests__/drawer-geometry.test.ts</files>
  <read_first>
    - src/lib/drawer-geometry.ts (reason: 新文件，无已有内容，但需了解目录结构)
    - src/lib/utils.ts (reason: 了解现有 lib 目录的导出风格和工具函数模式)
  </read_first>
  <behavior>
    - Test: detectSnapEdge 在窗口左边距 workArea 左边 5px 时返回 { edge: "left" }
    - Test: detectSnapEdge 在窗口右边距 workArea 右边 3px 时返回 { edge: "right" }
    - Test: detectSnapEdge 在窗口上边距 workArea 上边 8px 时返回 { edge: "top" }
    - Test: detectSnapEdge 在窗口下边距 workArea 下边 10px 时返回 { edge: "bottom" }
    - Test: detectSnapEdge 在窗口距所有边 > 10px 时返回 null
    - Test: detectSnapEdge 当 monitor 不是主显示器时返回 null (D-18)
    - Test: detectSnapEdge 当窗口最大化时返回 null (D-05)
    - Test: calculateSliverRect 为 left 边返回 { x: workArea.x, y: workArea.y, w: sliverLogical, h: workArea.h }
    - Test: calculateSliverRect 为 right 边返回 { x: workArea.x + workArea.w - sliverLogical, y: workArea.y, w: sliverLogical, h: workArea.h }
    - Test: calculateSliverRect 为 top 边返回 { x: workArea.x, y: workArea.y, w: workArea.w, h: sliverLogical }
    - Test: calculateSliverRect 为 bottom 边返回 { x: workArea.x, y: workArea.y + workArea.h - sliverLogical, w: workArea.w, h: sliverLogical }
    - Test: sliver 宽度在 150% DPI 下为 Math.ceil(2/1.5) = 2 逻辑像素 (D-08)
  </behavior>
  <action>
    先创建测试文件 src/lib/__tests__/drawer-geometry.test.ts，写上述测试用例。所有函数参数使用纯对象类型（不依赖 Tauri API 类型），便于测试。

    然后创建 src/lib/drawer-geometry.ts，导出以下函数和类型：

    ```typescript
    export type SnapEdge = "top" | "bottom" | "left" | "right";

    export interface SnapResult {
      edge: SnapEdge;
    }

    export interface Rect {
      x: number;
      y: number;
      w: number;
      h: number;
    }

    export interface MonitorInfo {
      workArea: Rect;
      scaleFactor: number;
      isPrimary: boolean;
    }

    export interface WindowInfo {
      x: number;
      y: number;
      w: number;
      h: number;
      isMaximized: boolean;
    }

    // SNAP_THRESHOLD = 10 (D-01)
    // detectSnapEdge(window: WindowInfo, monitor: MonitorInfo): SnapResult | null
    //   1. 如果 monitor.isPrimary === false，返回 null (D-18 仅主显示器)
    //   2. 如果 window.isMaximized === true，返回 null (D-05 最大化禁止吸附)
    //   3. 计算 window 到 workArea 四边的距离
    //   4. 如果最近边距离 <= 10，返回 { edge: 那个方向 }
    //   5. 否则返回 null

    // calculateSliverRect(edge: SnapEdge, workArea: Rect, scaleFactor: number): Rect
    //   1. sliverLogical = Math.ceil(2 / scaleFactor) (D-08)
    //   2. 根据边方向计算 sliver 的位置和尺寸
    //      left:   { x: wa.x, y: wa.y, w: sliverLogical, h: wa.h }
    //      right:  { x: wa.x + wa.w - sliverLogical, y: wa.y, w: sliverLogical, h: wa.h }
    //      top:    { x: wa.x, y: wa.y, w: wa.w, h: sliverLogical }
    //      bottom: { x: wa.x, y: wa.y + wa.h - sliverLogical, w: wa.w, h: sliverLogical }
    ```

    测试中 mock Tauri API 类型为纯对象。不安装任何新依赖。
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/drawer-geometry.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - drawer-geometry.ts 导出 detectSnapEdge, calculateSliverRect, SnapEdge, SnapResult, Rect, MonitorInfo, WindowInfo
    - 所有 12+ 个测试用例通过
    - detectSnapEdge 正确处理四边检测、最大化排除、非主显示器排除
    - calculateSliverRect 正确计算四边的 sliver 位置和 DPI 自适应宽度
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 drawer-animation.ts + useVisibilityState 三态扩展 + 单元测试</name>
  <files>src/lib/drawer-animation.ts, src/lib/__tests__/drawer-animation.test.ts, src/hooks/useVisibilityState.ts, src/hooks/__tests__/useVisibilityState.test.ts</files>
  <read_first>
    - src/hooks/useVisibilityState.ts (reason: 需要看到当前二态实现，扩展为三态)
    - src/lib/drawer-animation.ts (reason: 新文件)
    - src/hooks/useFloatWindow.ts (reason: 参考 operationLock mutex 模式)
  </read_first>
  <behavior>
    Animation tests:
    - Test: easeInOut(0) === 0
    - Test: easeInOut(1) === 1
    - Test: easeInOut(0.5) === 0.5
    - Test: easeInOut 在 (0,0.5) 区间为凸函数 (t^2 系数)
    - Test: animateWindow 调用 onUpdate 的次数 > 0 且最后一次调用的 t >= 1

    Visibility state tests:
    - Test: 初始状态为 VISIBLE
    - Test: hideToTray 将状态设为 TRAY_HIDDEN
    - Test: showFromTray 从 TRAY_HIDDEN 恢复为 VISIBLE
    - Test: hideToDrawer 将状态设为 DRAWER_HIDDEN
    - Test: showFromDrawer 从 DRAWER_HIDDEN 恢复为 VISIBLE
    - Test: hideToTray 在 DRAWER_HIDDEN 时直接覆盖为 TRAY_HIDDEN（互斥）
    - Test: hideToDrawer 在 TRAY_HIDDEN 时直接覆盖为 DRAWER_HIDDEN（互斥）
    - Test: isVisible 在 VISIBLE 时为 true，其他两态为 false
    - Test: isDrawerHidden 在 DRAWER_HIDDEN 时为 true，其他两态为 false
  </behavior>
  <action>
    先创建测试文件。

    **drawer-animation.test.ts**: 测试 easeInOut 和 animateWindow。animateWindow 接受一个 onUpdate 回调代替直接调用 Tauri API（解耦测试）。签名：

    ```typescript
    export function easeInOut(t: number): number {
      return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;
    }

    export interface AnimState {
      x: number; y: number; w: number; h: number;
    }

    export async function animateWindow(
      from: AnimState,
      to: AnimState,
      durationMs: number,
      onUpdate: (state: AnimState, t: number) => void
    ): Promise<void>
    ```

    animateWindow 使用 requestAnimationFrame（测试中 vitest jsdom 环境需 mock rAF）。每帧计算 t = elapsed/durationMs，clamp 到 [0,1]，通过 easeInOut 缓动后对 x/y/w/h 插值，调用 onUpdate。t >= 1 时 resolve。

    测试中 mock rAF：用 vi.stubGlobal 替换 requestAnimationFrame，手动触发回调来推进帧。

    **useVisibilityState.test.ts**: 使用 @testing-library/react 的 renderHook 测试。修改 src/hooks/useVisibilityState.ts：

    1. 扩展 VisibilityState 类型为 `"VISIBLE" | "TRAY_HIDDEN" | "DRAWER_HIDDEN"`
    2. 新增 hideToDrawer / showFromDrawer 回调
    3. 新增 isDrawerHidden 计算属性
    4. 返回值增加 hideToDrawer, showFromDrawer, isDrawerHidden

    不要破坏现有的 hideToTray / showFromTray / isVisible 接口。useTray.ts 导入的 VisibilityState 类型和 hideToTray/showFromTray 函数签名不变。

    返回对象完整列表：{ visibility, setVisibility, hideToTray, showFromTray, hideToDrawer, showFromDrawer, isVisible, isDrawerHidden }
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/drawer-animation.test.ts src/hooks/__tests__/useVisibilityState.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - drawer-animation.ts 导出 easeInOut, animateWindow, AnimState
    - easeInOut(0)=0, easeInOut(1)=1, easeInOut(0.5)=0.5，中间值平滑
    - animateWindow 调用 onUpdate 并在最后传入 t=1 和 to 状态
    - useVisibilityState 支持 VISIBLE/TRAY_HIDDEN/DRAWER_HIDDEN 三态
    - hideToDrawer 和 showFromDrawer 正确工作
    - DRAWER_HIDDEN 和 TRAY_HIDDEN 互斥（一个设置时覆盖另一个）
    - 现有 hideToTray/showFromTray/isVisible 接口不变，useTray.ts 不受影响
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 JS → Tauri API | 窗口操作通过 IPC 调用 Rust 后端，参数需验证 |
| monitor info 输入 | 外部 API 返回的 workArea 坐标可能为异常值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | detectSnapEdge 输入 | accept | 纯函数，输入由 Tauri API 提供（可信源）；异常值最多导致吸附判断错误，无安全影响 |
| T-14-02 | Tampering | animateWindow 帧计算 | accept | 窗口位置动画无安全敏感操作 |
</threat_model>

<verification>
```bash
npx vitest run src/lib/__tests__/drawer-geometry.test.ts src/lib/__tests__/drawer-animation.test.ts src/hooks/__tests__/useVisibilityState.test.ts --reporter=verbose
```
所有测试通过。
</verification>

<success_criteria>
- drawer-geometry.ts 导出 detectSnapEdge + calculateSliverRect，四边检测逻辑正确
- drawer-animation.ts 导出 easeInOut + animateWindow，缓动和帧驱动正确
- useVisibilityState.ts 扩展为三态，新增 hideToDrawer/showFromDrawer/isDrawerHidden
- 现有 useTray.ts 的导入兼容（VisibilityState 类型、hideToTray/showFromTray 签名不变）
- 全部单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/14-边缘抽屉/14-01-SUMMARY.md`
</output>
