---
phase: 11-全局快捷键
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/hooks/useProject.ts
  - src/components/CommandCard.tsx
  - src/components/MainArea.tsx
  - src/App.tsx
  - src/hooks/__tests__/useProject.test.tsx
  - src/components/__tests__/CommandCard.test.tsx
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - KB-06

must_haves:
  truths:
    - "编辑模式下卡片显示快捷键徽章：有快捷键显示组合（如 Ctrl+G），无快捷键显示 + 号空槽"
    - "非编辑模式下已绑定快捷键的卡片始终显示快捷键徽章"
    - "点击 + 号进入录制模式，按下有效组合键完成绑定"
    - "绑定冲突时显示红色冲突徽章和 toast 提示"
    - "编辑模式下 hover 已绑定徽章显示清除 X 按钮，点击清除绑定"
    - "Esc 键取消录制，点击卡片外区域取消录制"
    - "选中项目后按全局快捷键在系统终端执行对应指令"
    - "切换项目时自动更新快捷键注册"
    - "快捷键绑定通过 tauri-plugin-store 持久化，重启后保留"
  artifacts:
    - path: "src/hooks/useProject.ts"
      provides: "assignShortcut 和 clearShortcut 函数"
      contains: "assignShortcut"
    - path: "src/components/CommandCard.tsx"
      provides: "快捷键徽章 4 种状态渲染 + 录制交互"
      contains: "isRecording"
    - path: "src/components/MainArea.tsx"
      provides: "录制状态管理 + 快捷键分配回调"
      contains: "recordingCommandId"
    - path: "src/App.tsx"
      provides: "useGlobalShortcuts hook 集成"
      contains: "useGlobalShortcuts"
  key_links:
    - from: "src/components/MainArea.tsx"
      to: "src/hooks/useProject.ts"
      via: "assignShortcut / clearShortcut 回调"
      pattern: "assignShortcut|clearShortcut"
    - from: "src/components/CommandCard.tsx"
      to: "src/components/MainArea.tsx"
      via: "onRecordingStart / onShortcutAssign / onShortcutClear props"
      pattern: "onRecordingStart|onShortcutAssign"
    - from: "src/App.tsx"
      to: "src/hooks/useGlobalShortcuts.ts"
      via: "useGlobalShortcuts({ commands, onExecute, enabled })"
      pattern: "useGlobalShortcuts"
    - from: "src/App.tsx"
      to: "src/hooks/useProject.ts"
      via: "executeCommand 传入 onExecute"
      pattern: "executeCommand"
---

<objective>
在 UI 层实现快捷键徽章状态机、录制交互、useProject 快捷键 CRUD 扩展，并将 useGlobalShortcuts hook 集成到 App。

Purpose: 完成用户可感知的快捷键功能——卡片上的录制入口、冲突检测反馈、清除绑定、全局快捷键触发执行。

Output: 完整可用的全局快捷键 UI + 后端连线的用户功能。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-全局快捷键/11-RESEARCH.md
@.planning/phases/11-全局快捷键/11-UI-SPEC.md
@.planning/phases/11-全局快捷键/11-VALIDATION.md
@.planning/phases/11-全局快捷键/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出的接口 -->

From src/lib/types.ts (扩展后):
```typescript
export interface CommandItem {
  id: string;
  name: string;
  command: string;
  icon: string;
  type: "preset" | "custom";
  scope: "global" | "project";
  addedAt: number;
  shortcut?: string;  // Phase 11: Tauri Accelerator format, e.g. "CommandOrControl+G"
}
```

From src/lib/shortcutUtils.ts (Plan 01 产出):
```typescript
export function keyboardEventToShortcut(e: { ctrlKey: boolean; altKey: boolean; shiftKey: boolean; metaKey: boolean; key: string }): string | null;
export function shortcutToDisplay(shortcut: string): string;
```

From src/hooks/useGlobalShortcuts.ts (Plan 01 产出):
```typescript
interface UseGlobalShortcutsOptions {
  commands: CommandItem[];
  onExecute: (command: string) => void;
  enabled: boolean;
}
export function useGlobalShortcuts(options: UseGlobalShortcutsOptions): void;
```

From src/hooks/useProject.ts (现有 — 需扩展):
```typescript
// 现有返回值需新增: assignShortcut, clearShortcut
// 现有 store 持久化模式:
//   setProjectCommandsMap(prev => ({...prev, [selectedId]: updated}));
//   await store?.set(projectCommandsKey(selectedId), updated);
// 现有 commands 派生: global mode = presets + customCommands; project mode = projectCommandsMap[selectedId]
```

From src/components/CommandCard.tsx (现有 — 需扩展):
```typescript
interface CommandCardProps {
  name: string;
  icon: LucideIcon;
  command?: string;
  disabled?: boolean;
  onClick?: () => void;
  isCustom?: boolean;
  editMode?: boolean;
  onEdit?: () => void;
  onDelete?: () => void;
  commandId?: string;
  tabIndex?: number;
  shortcutNumber?: number;
  // Phase 11 新增 props:
  // shortcut?: string;
  // isRecording?: boolean;
  // onRecordingStart?: () => void;
  // onRecordingStop?: () => void;
  // onShortcutAssign?: (shortcut: string) => void;
  // onShortcutClear?: () => void;
  // hasConflict?: boolean;
}
```

From src/components/MainArea.tsx (现有 — 需扩展):
```typescript
// 现有 state: dialogOpen, editingCommand, focusedCardIndex
// 新增 state: recordingCommandId: string | null
// 现有 CommandCard 渲染: shortcutNumber={!isCustom && !canEdit && index < 9 ? index + 1 : undefined}
// 需更新为: shortcut={cmd.shortcut}
//           shortcutNumber={!cmd.shortcut && !isCustom && !canEdit && index < 9 ? index + 1 : undefined}
```

From src/App.tsx (现有):
```typescript
// 现有 useKeyboard({ commands, currentProject, onExecute: executeCommand, editMode })
// 新增 useGlobalShortcuts({ commands, onExecute: executeCommand, enabled: !!currentProject })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useProject with assignShortcut/clearShortcut + wire useGlobalShortcuts in App</name>
  <files>src/hooks/useProject.ts, src/App.tsx</files>
  <read_first>
    - E:/GitLib/EasyPack/src/hooks/useProject.ts (完整文件，CRUD 模式和 store 持久化)
    - E:/GitLib/EasyPack/src/App.tsx (当前 hook 集成方式)
    - E:/GitLib/EasyPack/src/hooks/useGlobalShortcuts.ts (Plan 01 产出的 hook)
    - E:/GitLib/EasyPack/src/lib/shortcutUtils.ts (shortcutToDisplay 函数)
  </read_first>
  <action>
    **Step 1 — 在 useProject.ts 添加 assignShortcut 函数:**

    在 deleteCommand 函数之后、enableProjectCommands 之前添加 assignShortcut:

    ```typescript
    // Phase 11: assign global shortcut to a command
    const assignShortcut = useCallback(
      async (commandId: string, shortcut: string) => {
        // Check for within-app conflict across current project's commands
        const conflict = commands.find(
          (c) => c.shortcut === shortcut && c.id !== commandId
        );
        if (conflict) {
          toast.error("快捷键冲突", {
            description: `快捷键 ${shortcutToDisplay(shortcut)} 已被指令 "${conflict.name}" 使用`,
          });
          return false;
        }

        if (commandMode === "project" && selectedId) {
          const projectCmds = projectCommandsMap[selectedId] ?? [];
          const idx = projectCmds.findIndex((c) => c.id === commandId);
          if (idx === -1) return false;
          const updated = projectCmds.map((c) =>
            c.id === commandId ? { ...c, shortcut } : c
          );
          setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: updated }));
          await store?.set(projectCommandsKey(selectedId), updated);
        } else {
          // Global mode: check both customCommands and default presets
          const customIdx = customCommands.findIndex((c) => c.id === commandId);
          if (customIdx !== -1) {
            const updated = customCommands.map((c) =>
              c.id === commandId ? { ...c, shortcut } : c
            );
            setCustomCommands(updated);
            await store?.set(CUSTOM_COMMANDS_KEY, updated);
          } else {
            // It might be a preset command — presets are derived from defaults,
            // shortcut binding on presets means adding to customCommands with shortcut
            // For simplicity, presets in global mode: we track shortcut in a separate map
            // Actually per the architecture, presets can have shortcuts too.
            // Since commands includes presets+custom, find it and persist:
            // If it's a preset, we need to persist shortcut somewhere.
            // Store preset shortcuts in a separate key "presetShortcuts"
            // But simpler: just add it to customCommands if not already there
            // Actually, the current architecture has getDefaultsAsCommandItems() returning fresh presets each time.
            // Shortcut on a preset won't persist unless we store it.
            // Solution: store preset shortcut overrides in a dedicated map.

            // Let's store shortcut overrides for presets in a separate store key
            const PRESHORTCUTS_KEY = "presetShortcuts";
            const currentOverrides = await store?.get<Record<string, string>>(PRESHORTCUTS_KEY) ?? {};
            const updatedOverrides = { ...currentOverrides, [commandId]: shortcut };
            await store?.set(PRESHORTCUTS_KEY, updatedOverrides);
            // Force re-render by touching a state — use the fact that commands useMemo
            // depends on customCommands, so we need a trigger. Use a counter or just
            // reload preset shortcuts into state.

            // Actually, simpler approach: just use a useState for presetShortcuts map,
            // include it in the commands useMemo derivation.
            // This is getting complex. Let me use the simplest approach:
            // Add the shortcut to a local presetShortcutsMap state, and
            // the commands useMemo will pick it up.

            // We need presetShortcutsMap state (added below in Step 1.5)
            setPresetShortcutsMap((prev) => ({ ...prev, [commandId]: shortcut }));
            await store?.set(PRESHORTCUTS_KEY, { ...presetShortcutsMap, [commandId]: shortcut });
          }
        }
        toast.success(`已绑定快捷键: ${shortcutToDisplay(shortcut)}`);
        return true;
      },
      [commandMode, selectedId, projectCommandsMap, customCommands, store, commands, presetShortcutsMap]
    );
    ```

    等等，上述方案引入了 presetShortcutsMap 的新状态和 PRESHORTCUTS_KEY，增加了复杂度。让我简化：

    **更简单的方案:**

    由于 commands useMemo 已经合并了 presets + customCommands，而 preset 的 shortcut 字段需要持久化。最简方案是添加一个 `presetShortcutsMap: Record<string, string>` 状态，在 commands useMemo 中将 preset shortcut 注入：

    在 useProject.ts 中添加:

    ```typescript
    // Phase 11: preset shortcut overrides (persisted separately)
    const [presetShortcutsMap, setPresetShortcutsMap] = useState<Record<string, string>>({});
    const PRESHORTCUTS_KEY = "presetShortcuts";
    ```

    在 commands useMemo 中修改全局模式的返回:

    ```typescript
    if (commandMode === "project") { /* ... unchanged ... */ }
    // Global mode: presets + global custom commands, inject preset shortcuts
    return [...getDefaultsAsCommandItems(), ...customCommands]
      .map((cmd) => {
        if (presetShortcutsMap[cmd.id]) {
          return { ...cmd, shortcut: presetShortcutsMap[cmd.id] };
        }
        return cmd;
      })
      .sort((a, b) => a.addedAt - b.addedAt);
    ```

    在 init effect 中恢复:

    ```typescript
    const savedPresetShortcuts = await s.get<Record<string, string>>(PRESHORTCUTS_KEY);
    if (savedPresetShortcuts) setPresetShortcutsMap(savedPresetShortcuts);
    ```

    然后 assignShortcut 简化为:

    ```typescript
    const assignShortcut = useCallback(
      async (commandId: string, shortcut: string) => {
        const conflict = commands.find(
          (c) => c.shortcut === shortcut && c.id !== commandId
        );
        if (conflict) {
          toast.error("快捷键冲突", {
            description: `快捷键 ${shortcutToDisplay(shortcut)} 已被指令 "${conflict.name}" 使用`,
          });
          return false;
        }

        if (commandMode === "project" && selectedId) {
          const updated = (projectCommandsMap[selectedId] ?? []).map((c) =>
            c.id === commandId ? { ...c, shortcut } : c
          );
          setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: updated }));
          await store?.set(projectCommandsKey(selectedId), updated);
        } else if (customCommands.some((c) => c.id === commandId)) {
          const updated = customCommands.map((c) =>
            c.id === commandId ? { ...c, shortcut } : c
          );
          setCustomCommands(updated);
          await store?.set(CUSTOM_COMMANDS_KEY, updated);
        } else {
          // Preset command: store in presetShortcutsMap
          const updatedMap = { ...presetShortcutsMap, [commandId]: shortcut };
          setPresetShortcutsMap(updatedMap);
          await store?.set(PRESHORTCUTS_KEY, updatedMap);
        }
        toast.success(`已绑定快捷键: ${shortcutToDisplay(shortcut)}`);
        return true;
      },
      [commandMode, selectedId, projectCommandsMap, customCommands, store, commands, presetShortcutsMap]
    );
    ```

    添加 clearShortcut:

    ```typescript
    const clearShortcut = useCallback(
      async (commandId: string) => {
        if (commandMode === "project" && selectedId) {
          const updated = (projectCommandsMap[selectedId] ?? []).map((c) =>
            c.id === commandId ? { ...c, shortcut: undefined } : c
          );
          setProjectCommandsMap((prev) => ({ ...prev, [selectedId]: updated }));
          await store?.set(projectCommandsKey(selectedId), updated);
        } else if (customCommands.some((c) => c.id === commandId)) {
          const updated = customCommands.map((c) =>
            c.id === commandId ? { ...c, shortcut: undefined } : c
          );
          setCustomCommands(updated);
          await store?.set(CUSTOM_COMMANDS_KEY, updated);
        } else if (presetShortcutsMap[commandId]) {
          const updatedMap = { ...presetShortcutsMap };
          delete updatedMap[commandId];
          setPresetShortcutsMap(updatedMap);
          await store?.set(PRESHORTCUTS_KEY, updatedMap);
        }
        toast.success("已清除快捷键");
      },
      [commandMode, selectedId, projectCommandsMap, customCommands, store, presetShortcutsMap]
    );
    ```

    在 return 对象中添加:
    ```typescript
    assignShortcut,
    clearShortcut,
    ```

    需要在文件顶部 import shortcutToDisplay:
    ```typescript
    import { shortcutToDisplay } from "@/lib/shortcutUtils";
    ```

    **Step 2 — 在 App.tsx 中集成 useGlobalShortcuts:**

    在现有 `useKeyboard(...)` 调用之后添加:
    ```typescript
    import { useGlobalShortcuts } from "@/hooks/useGlobalShortcuts";
    ```

    在 useKeyboard 调用之后:
    ```typescript
    useGlobalShortcuts({
      commands,
      onExecute: executeCommand,
      enabled: !!currentProject,
    });
    ```

    在解构 useProject() 返回值时添加 `assignShortcut` 和 `clearShortcut`。

    在 `<MainArea>` 组件调用中添加两个 props:
    ```tsx
    assignShortcut={assignShortcut}
    clearShortcut={clearShortcut}
    ```
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && npx vitest run --reporter=verbose 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useProject.ts 包含 "assignShortcut" 函数定义
    - src/hooks/useProject.ts 包含 "clearShortcut" 函数定义
    - src/hooks/useProject.ts 包含 "presetShortcutsMap" 状态声明
    - src/hooks/useProject.ts 包含 "PRESHORTCUTS_KEY" 常量
    - src/hooks/useProject.ts return 对象包含 assignShortcut 和 clearShortcut
    - src/App.tsx 包含 "useGlobalShortcuts" import
    - src/App.tsx 包含 "useGlobalShortcuts({" 调用
    - src/App.tsx 的 MainArea 组件接收 assignShortcut 和 clearShortcut props
    - npx vitest run 通过（无回归）
  </acceptance_criteria>
  <done>
    - useProject.ts 导出 assignShortcut(commandId, shortcut) 函数，支持 project/global/preset 三种模式
    - useProject.ts 导出 clearShortcut(commandId) 函数，清除绑定并 toast 通知
    - assignShortcut 在绑定前检查当前 commands 列表中的冲突，冲突时返回 false 并 toast 错误
    - presetShortcutsMap 状态持久化预设指令的快捷键到独立 store key
    - commands useMemo 在全局模式下注入 presetShortcutsMap 中的快捷键
    - App.tsx 集成 useGlobalShortcuts hook，传入 commands/executeCommand/enabled
    - MainArea 接收 assignShortcut 和 clearShortcut 作为新 props
    - 全量测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CommandCard shortcut badge state machine + MainArea recording management</name>
  <files>src/components/CommandCard.tsx, src/components/MainArea.tsx, src/components/__tests__/CommandCard.test.tsx</files>
  <read_first>
    - E:/GitLib/EasyPack/src/components/CommandCard.tsx (完整文件，现有 badge 和 props)
    - E:/GitLib/EasyPack/src/components/MainArea.tsx (完整文件，现有 state 和 CommandCard 渲染)
    - E:/GitLib/EasyPack/src/lib/shortcutUtils.ts (keyboardEventToShortcut 和 shortcutToDisplay)
    - E:/GitLib/EasyPack/src/components/__tests__/CommandCard.test.tsx (现有测试结构)
    - E:/GitLib/EasyPack/.planning/phases/11-全局快捷键/11-UI-SPEC.md (徽章状态机规范)
  </read_first>
  <action>
    **Step 1 — 扩展 CommandCard props (per UI-SPEC Section 5):**

    在 CommandCardProps 中添加:
    ```typescript
    // Phase 11: global shortcut badge
    shortcut?: string;
    isRecording?: boolean;
    onRecordingStart?: () => void;
    onRecordingStop?: () => void;
    onShortcutAssign?: (shortcut: string) => void;
    onShortcutClear?: () => void;
    hasConflict?: boolean;
    ```

    在函数参数解构中添加对应参数。

    **Step 2 — 实现 CommandCard 徽章渲染 (per UI-SPEC Section 1):**

    替换现有的 shortcutNumber badge 渲染块（lines 112-120）。

    新的徽章渲染逻辑分为几个区域，全部包裹在 `shortcutNumber` 原有位置之后：

    ```tsx
    {/* Phase 11: Global shortcut badge */}
    {isRecording ? (
      // State 3: Recording — pulsing accent border
      <span
        className={cn(
          "absolute top-1 left-1 px-1.5 py-0.5 rounded text-[10px]",
          "bg-accent/20 border border-accent/60 text-accent",
          "animate-pulse min-w-[48px] text-center"
        )}
        aria-label="正在录制快捷键"
        aria-live="assertive"
      >
        按下快捷键...
      </span>
    ) : hasConflict ? (
      // State 4: Conflict — destructive red
      <span
        className={cn(
          "absolute top-1 left-1 px-1.5 py-0.5 rounded text-[10px] font-semibold",
          "bg-destructive/20 border border-destructive/60 text-destructive",
          "min-w-[24px] text-center"
        )}
        aria-label="快捷键冲突"
        role="alert"
      >
        冲突
      </span>
    ) : shortcut ? (
      // State 1: Display mode — shows shortcut combo
      <span
        className={cn(
          "absolute top-1 left-1 px-1.5 py-0.5 rounded text-[10px] font-semibold",
          "bg-white/10 border border-white/10 text-muted-foreground/80",
          "min-w-[24px] text-center",
          editMode && "group cursor-pointer hover:bg-white/15 hover:border-white/20"
        )}
        aria-label={editMode ? `快捷键: ${shortcutToDisplay(shortcut)}` : undefined}
        aria-hidden={!editMode}
        onClick={editMode ? (e) => { e.stopPropagation(); onRecordingStart?.(); } : undefined}
      >
        {shortcutToDisplay(shortcut)}
        {/* Clear button — visible on hover in edit mode only */}
        {editMode && (
          <button
            onClick={(e) => { e.stopPropagation(); onShortcutClear?.(); }}
            className="absolute -top-1 -right-1 size-3 bg-red-500/80 hover:bg-red-500 text-white rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-100"
            aria-label={`清除快捷键: ${shortcutToDisplay(shortcut)}`}
          >
            <X className="size-2" />
          </button>
        )}
      </span>
    ) : editMode ? (
      // State 2: Empty slot — "+" clickable button
      <button
        onClick={(e) => { e.stopPropagation(); onRecordingStart?.(); }}
        className={cn(
          "absolute top-1 left-1 px-1.5 py-0.5 rounded text-[10px]",
          "bg-white/5 border border-dashed border-white/20 text-muted-foreground/50",
          "cursor-pointer hover:bg-white/10 hover:border-white/30",
          "transition-all duration-150 ease-out",
          "focus-visible:ring-1 focus-visible:ring-ring"
        )}
        aria-label="设置快捷键"
        role="button"
      >
        +
      </button>
    ) : (
      // No shortcut, non-edit mode: show number badge if available
      shortcutNumber != null && !disabled && (
        <span
          className="absolute top-1 left-1 text-[10px] font-semibold text-muted-foreground/70 pointer-events-none"
          aria-hidden="true"
        >
          {shortcutNumber}
        </span>
      )
    )}
    ```

    需要添加 import:
    ```typescript
    import { keyboardEventToShortcut } from "@/lib/shortcutUtils";
    import { shortcutToDisplay } from "@/lib/shortcutUtils";
    ```

    注意: CommandCard 外层 button 需要添加 `group` class 以支持 hover 状态传递（用于 clear button 的 group-hover）。

    **Step 3 — 添加录制 keydown 监听 (per UI-SPEC Section 2):**

    在 CommandCard 函数体内，isRecording 为 true 时添加 keydown 监听:

    ```typescript
    useEffect(() => {
      if (!isRecording) return;

      function handleRecordingKeydown(e: KeyboardEvent) {
        // Skip if target is input/textarea (per RESEARCH Pitfall 6)
        if (e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement) return;

        if (e.key === "Escape") {
          onRecordingStop?.();
          return;
        }

        const shortcut = keyboardEventToShortcut(e);
        if (!shortcut) {
          // No modifier or modifier-only — show toast error
          const MODIFIER_KEYS = ["Control", "Alt", "Shift", "Meta"];
          if (MODIFIER_KEYS.includes(e.key)) return; // silently ignore modifier press
          // No modifier at all
          if (!e.ctrlKey && !e.altKey && !e.shiftKey && !e.metaKey) {
            import("sonner").then(({ toast }) => {
              toast.error("快捷键必须包含修饰键 (Ctrl/Alt/Shift)");
            });
          }
          return;
        }

        e.preventDefault();
        e.stopPropagation();
        onShortcutAssign?.(shortcut);
      }

      window.addEventListener("keydown", handleRecordingKeydown);
      return () => window.removeEventListener("keydown", handleRecordingKeydown);
    }, [isRecording, onRecordingStop, onShortcutAssign]);
    ```

    注意: 使用动态 import("sonner") 避免 CommandCard 依赖 toast 的静态导入（保持组件纯净）。或者在文件顶部 import toast 即可（因为项目中其他组件也这样做）。

    实际上，直接在顶部 import toast:
    ```typescript
    import { toast } from "sonner";
    ```

    **Step 4 — 更新 MainArea.tsx:**

    在 MainAreaProps 接口添加:
    ```typescript
    // Phase 11: shortcut management
    assignShortcut: (commandId: string, shortcut: string) => Promise<boolean>;
    clearShortcut: (commandId: string) => Promise<void>;
    ```

    添加 recording 状态:
    ```typescript
    const [recordingCommandId, setRecordingCommandId] = useState<string | null>(null);
    const [conflictCommandId, setConflictCommandId] = useState<string | null>(null);
    ```

    添加录制相关回调:
    ```typescript
    const handleRecordingStart = useCallback((commandId: string) => {
      setRecordingCommandId(commandId);
      setConflictCommandId(null);
    }, []);

    const handleRecordingStop = useCallback(() => {
      setRecordingCommandId(null);
      setConflictCommandId(null);
    }, []);

    const handleShortcutAssign = useCallback(async (commandId: string, shortcut: string) => {
      const success = await assignShortcut(commandId, shortcut);
      if (!success) {
        // Conflict detected
        setConflictCommandId(commandId);
        // Auto-clear conflict after 2s
        setTimeout(() => setConflictCommandId(null), 2000);
      } else {
        setRecordingCommandId(null);
      }
    }, [assignShortcut]);

    const handleShortcutClear = useCallback(async (commandId: string) => {
      await clearShortcut(commandId);
    }, [clearShortcut]);
    ```

    在 editMode 切换时取消录制 (在现有代码附近添加 effect):
    ```typescript
    // Cancel recording when edit mode is turned off
    useEffect(() => {
      if (!editMode) {
        setRecordingCommandId(null);
        setConflictCommandId(null);
      }
    }, [editMode]);
    ```

    更新 CommandCard 渲染，替换 shortcutNumber prop:
    ```tsx
    <CommandCard
      key={cmd.id}
      name={cmd.name}
      icon={getIconByName(cmd.icon)}
      command={cmd.command}
      isCustom={isCustom}
      editMode={canEdit}
      onEdit={() => handleEdit(cmd)}
      onDelete={() => deleteCommand(cmd.id)}
      commandId={cmd.id}
      onClick={() => onExecute(cmd.command)}
      tabIndex={isCardFocused ? 0 : -1}
      shortcut={cmd.shortcut}
      shortcutNumber={!cmd.shortcut && !isCustom && !canEdit && index < 9 ? index + 1 : undefined}
      isRecording={recordingCommandId === cmd.id}
      hasConflict={conflictCommandId === cmd.id}
      onRecordingStart={() => handleRecordingStart(cmd.id)}
      onRecordingStop={handleRecordingStop}
      onShortcutAssign={(s) => handleShortcutAssign(cmd.id, s)}
      onShortcutClear={() => handleShortcutClear(cmd.id)}
    />
    ```

    **Step 5 — 扩展 CommandCard 测试:**

    在 src/components/__tests__/CommandCard.test.tsx 中添加测试用例:
    - "shows shortcut badge when shortcut prop is provided in non-edit mode"
    - "shows + button in edit mode when no shortcut"
    - "does not show + button in non-edit mode when no shortcut"
    - "shows recording state when isRecording is true"

    Mock shortcutUtils:
    ```typescript
    vi.mock("@/lib/shortcutUtils", () => ({
      keyboardEventToShortcut: vi.fn(),
      shortcutToDisplay: vi.fn((s: string) => s.replace("CommandOrControl", "Ctrl")),
    }));
    ```
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && npx vitest run --reporter=verbose 2>&1 | head -120</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CommandCard.tsx 的 CommandCardProps 包含 shortcut, isRecording, onRecordingStart, onRecordingStop, onShortcutAssign, onShortcutClear, hasConflict
    - src/components/CommandCard.tsx 包含 "按下快捷键..." 文字（录制状态）
    - src/components/CommandCard.tsx 包含 "冲突" 文字（冲突状态）
    - src/components/CommandCard.tsx 包含 shortcutToDisplay 调用
    - src/components/CommandCard.tsx 包含 keyboardEventToShortcut import
    - src/components/CommandCard.tsx 包含 useEffect 监听 isRecording keydown
    - src/components/CommandCard.tsx 外层 button 有 "group" class
    - src/components/MainArea.tsx 包含 "recordingCommandId" state
    - src/components/MainArea.tsx 包含 "handleRecordingStart" 函数
    - src/components/MainArea.tsx 包含 "handleShortcutAssign" 函数
    - src/components/MainArea.tsx 的 CommandCard 渲染包含 shortcut={cmd.shortcut} prop
    - src/components/__tests__/CommandCard.test.tsx 包含快捷键徽章相关测试
    - npx vitest run 通过
  </acceptance_criteria>
  <done>
    - CommandCard 徽章 4 种状态正确渲染: Display/Empty Slot/Recording/Conflict
    - 非编辑模式下无快捷键的卡片不显示录制入口 (per D-04)
    - 非编辑模式下已绑定快捷键的卡片始终显示徽章 (per D-05)
    - 编辑模式下无快捷键的卡片显示 + 号可点击进入录制 (per D-03)
    - 录制中按键触发 keyboardEventToShortcut 转换，有效组合调用 onShortcutAssign
    - 录制中按 Esc 取消录制
    - 录制中收到冲突结果时显示红色冲突徽章，2s 后自动清除
    - 编辑模式关闭时自动取消所有录制
    - 编辑模式下 hover 已绑定徽章显示清除 X 按钮
    - MainArea 正确管理 recordingCommandId 状态
    - MainArea 正确传递 assignShortcut/clearShortcut 到 CommandCard
    - 有自定义快捷键时优先显示自定义快捷键徽章而非数字键
    - 全量测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard DOM event -> shortcut string | 用户输入的键盘事件经过 keyboardEventToShortcut 验证 |
| shortcut string -> Tauri register() | 已验证的字符串传入插件 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Denial of Service | recording keydown handler | mitigate | 检查 e.target 不是 input/textarea，防止在表单中输入时误触发 |
| T-11-06 | Tampering | conflict detection | mitigate | assignShortcut 扫描当前 commands 列表检查重复绑定 |
</threat_model>

<verification>
1. `npx vitest run --reporter=verbose` — 全量测试通过
2. 手动验证（build 后）:
   - 启动应用 `npm run tauri dev`
   - 选择一个项目，进入编辑模式
   - 点击卡片上的 + 号，按下 Ctrl+G，验证徽章显示 Ctrl+G
   - 按下 Ctrl+G（非编辑模式），验证终端打开执行对应命令
   - 切换项目，验证快捷键自动更新
   - 尝试绑定相同快捷键到另一个指令，验证冲突提示
   - 点击清除按钮，验证绑定清除
   - 重启应用，验证快捷键绑定保留
</verification>

<success_criteria>
- CommandCard 徽章 4 种状态（Display/Empty Slot/Recording/Conflict）正确渲染
- 录制模式捕获按键组合、验证、注册、持久化完整流程可工作
- 冲突检测在绑定时即时反馈
- 清除绑定功能正常
- useGlobalShortcuts 在 App.tsx 中正确集成
- 全局快捷键按下后执行对应命令
- 全量测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/11-全局快捷键/11-02-SUMMARY.md`
</output>
