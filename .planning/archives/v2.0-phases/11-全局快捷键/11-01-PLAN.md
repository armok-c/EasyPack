---
phase: 11-全局快捷键
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/lib/types.ts
  - src/lib/shortcutUtils.ts
  - src/hooks/useGlobalShortcuts.ts
  - src/lib/__tests__/shortcutUtils.test.ts
  - src/hooks/__tests__/useGlobalShortcuts.test.ts
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-06

must_haves:
  truths:
    - "keyboardEventToShortcut 正确将浏览器 KeyboardEvent 转换为 Tauri Accelerator 格式字符串"
    - "keyboardEventToShortcut 拒绝无修饰键组合和超过 3 键组合"
    - "useGlobalShortcuts 在项目切换时先 unregisterAll 再批量注册新项目快捷键"
    - "useGlobalShortcuts handler 检查 event.state === 'Pressed' 防止双触发"
    - "Tauri globalShortcut 插件在 Rust 端注册并配置正确的权限"
    - "CommandItem.shortcut 字段可选且向后兼容"
  artifacts:
    - path: "src/lib/shortcutUtils.ts"
      provides: "keyboardEventToShortcut() 和 shortcutToDisplay() 工具函数"
      exports: ["keyboardEventToShortcut", "shortcutToDisplay"]
    - path: "src/hooks/useGlobalShortcuts.ts"
      provides: "OS 级全局快捷键注册生命周期管理 hook"
      exports: ["useGlobalShortcuts"]
    - path: "src/lib/types.ts"
      provides: "CommandItem 接口新增 shortcut 可选字段"
      contains: "shortcut?"
    - path: "src-tauri/Cargo.toml"
      provides: "tauri-plugin-global-shortcut Rust 依赖"
      contains: "tauri-plugin-global-shortcut"
    - path: "src-tauri/src/lib.rs"
      provides: "globalShortcut 插件注册"
      contains: "tauri_plugin_global_shortcut"
    - path: "src-tauri/capabilities/default.json"
      provides: "global-shortcut 权限声明"
      contains: "global-shortcut:allow-register"
  key_links:
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "@tauri-apps/plugin-global-shortcut"
      via: "import { register, unregister, unregisterAll, isRegistered }"
      pattern: "from '@tauri-apps/plugin-global-shortcut'"
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "src/lib/shortcutUtils.ts"
      via: "uses shortcutToDisplay for display conversion"
      pattern: "shortcutToDisplay"
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "src/lib/types.ts"
      via: "imports CommandItem for type checking"
      pattern: "CommandItem"
---

<objective>
安装 tauri-plugin-global-shortcut 插件，创建快捷键工具函数和全局快捷键注册生命周期管理 hook。

Purpose: 为 Phase 11 提供基础设施——插件配置让 OS 级热键可用，工具函数处理键盘事件转换，useGlobalShortcuts hook 管理注册/注销/执行生命周期。

Output: 可工作的 Tauri 插件配置、类型扩展、快捷键工具函数库、全局快捷键 hook 及对应测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-全局快捷键/11-RESEARCH.md
@.planning/phases/11-全局快捷键/11-UI-SPEC.md
@.planning/phases/11-全局快捷键/11-VALIDATION.md

<interfaces>
<!-- 现有接口，执行器必须遵循 -->

From src/lib/types.ts:
```typescript
export interface CommandItem {
  id: string;
  name: string;
  command: string;
  icon: string;
  type: "preset" | "custom";
  scope: "global" | "project";
  addedAt: number;
}
```

From src/hooks/useProject.ts (executeCommand 签名):
```typescript
const executeCommand = useCallback(
  async (shellCommand: string) => {
    if (!currentProject) return;
    await invoke("execute_command", { projectPath: currentProject.path, shellCommand });
  },
  [currentProject]
);
```

From src/hooks/useKeyboard.ts (hook 结构模式):
```typescript
interface UseKeyboardOptions {
  commands: CommandItem[];
  currentProject: ProjectItem | null;
  onExecute: (command: string) => void;
  editMode: boolean;
}
export function useKeyboard({ commands, currentProject, onExecute, editMode }: UseKeyboardOptions) {
  // useCallback + useEffect + cleanup pattern
}
```

From src/hooks/useProject.ts (ProjectItem 类型):
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;
  color?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Tauri global-shortcut plugin + extend CommandItem type + create shortcut utilities</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/capabilities/default.json, src/lib/types.ts, src/lib/shortcutUtils.ts, src/lib/__tests__/shortcutUtils.test.ts</files>
  <read_first>
    - E:/GitLib/EasyPack/src-tauri/Cargo.toml (current deps)
    - E:/GitLib/EasyPack/src-tauri/src/lib.rs (current plugin chain)
    - E:/GitLib/EasyPack/src-tauri/capabilities/default.json (current permissions)
    - E:/GitLib/EasyPack/src/lib/types.ts (current CommandItem interface)
  </read_first>
  <behavior>
    - Test: keyboardEventToShortcut with Ctrl+G returns "CommandOrControl+G"
    - Test: keyboardEventToShortcut with Ctrl+Shift+R returns "CommandOrControl+Shift+R"
    - Test: keyboardEventToShortcut with Alt+F5 returns "Alt+F5"
    - Test: keyboardEventToShortcut with only Ctrl (no non-modifier key) returns null
    - Test: keyboardEventToShortcut with no modifier key (just "A") returns null
    - Test: keyboardEventToShortcut with Ctrl+Alt+Shift+T (4 keys) returns null (exceeds D-07 max 3)
    - Test: keyboardEventToShortcut maps ArrowUp to "Up", space to "Space"
    - Test: shortcutToDisplay("CommandOrControl+Shift+R") returns "Ctrl+Shift+R"
    - Test: shortcutToDisplay("Alt+G") returns "Alt+G" (no CommandOrControl prefix)
  </behavior>
  <action>
    **Step 1 — Rust 端插件安装 (per D-01):**

    在 src-tauri/Cargo.toml 的 [dependencies] 段末尾添加:
    ```toml
    tauri-plugin-global-shortcut = "2"
    ```

    在 src-tauri/src/lib.rs 的 `.plugin(tauri_plugin_store::Builder::default().build())` 之后、`.invoke_handler(...)` 之前添加:
    ```rust
    .plugin(tauri_plugin_global_shortcut::Builder::new().build())
    ```

    在 src-tauri/capabilities/default.json 的 permissions 数组末尾（"store:default" 之后）添加:
    ```json
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered",
    "global-shortcut:allow-unregister-all"
    ```

    **Step 2 — 类型扩展:**

    在 src/lib/types.ts 的 CommandItem 接口中，在 `addedAt: number;` 之后添加:
    ```typescript
    shortcut?: string;  // Phase 11: OS-level global shortcut in Tauri Accelerator format, e.g. "CommandOrControl+G"
    ```

    **Step 3 — 先写测试 (TDD RED):**

    创建 src/lib/__tests__/shortcutUtils.test.ts。使用 vitest describe/it/expect 结构。mock KeyboardEvent 的方式: 创建一个 `{ ctrlKey: true, altKey: false, shiftKey: false, metaKey: false, key: "g" }` 对象传入函数（因为 Node.js 没有 KeyboardEvent 构造器）。

    测试用例:
    - "converts Ctrl+G to CommandOrControl+G"
    - "converts Ctrl+Shift+R to CommandOrControl+Shift+R"
    - "converts Alt+F5 to Alt+F5"
    - "rejects modifier-only combo (just Ctrl)" -> returns null
    - "rejects no-modifier combo (just A)" -> returns null
    - "rejects 4-key combo (Ctrl+Alt+Shift+T)" -> returns null
    - "maps special keys: ArrowUp -> Up, space bar -> Space"
    - "shortcutToDisplay replaces CommandOrControl with Ctrl"
    - "shortcutToDisplay leaves Alt+G unchanged"

    运行 `npx vitest run src/lib/__tests__/shortcutUtils.test.ts` 确认测试失败（RED）。

    **Step 4 — 实现工具函数 (TDD GREEN):**

    创建 src/lib/shortcutUtils.ts，导出两个函数:

    ```typescript
    /**
     * Convert a browser KeyboardEvent to a Tauri Accelerator shortcut string.
     * Returns null if the combo is invalid (no modifier, modifier-only, or >3 keys).
     *
     * Rules per D-06: must include at least one modifier (Ctrl/Alt/Shift).
     * Rules per D-07: 2-3 key combo max.
     */
    export function keyboardEventToShortcut(e: { ctrlKey: boolean; altKey: boolean; shiftKey: boolean; metaKey: boolean; key: string }): string | null {
      const parts: string[] = [];
      if (e.ctrlKey || e.metaKey) parts.push("CommandOrControl");
      if (e.altKey) parts.push("Alt");
      if (e.shiftKey) parts.push("Shift");

      const MODIFIER_KEYS = ["Control", "Alt", "Shift", "Meta"];
      if (MODIFIER_KEYS.includes(e.key)) return null; // modifier-only

      if (parts.length === 0) return null; // no modifier (D-06)

      let mainKey: string;
      if (e.key.length === 1) {
        mainKey = e.key.toUpperCase();
      } else {
        const keyMap: Record<string, string> = {
          "ArrowUp": "Up", "ArrowDown": "Down",
          "ArrowLeft": "Left", "ArrowRight": "Right",
          " ": "Space",
        };
        mainKey = keyMap[e.key] || e.key;
      }
      parts.push(mainKey);

      if (parts.length > 3) return null; // D-07: max 3 keys

      return parts.join("+");
    }

    /** Convert Tauri shortcut string to display-friendly format. "CommandOrControl+G" -> "Ctrl+G" */
    export function shortcutToDisplay(shortcut: string): string {
      return shortcut.replace("CommandOrControl", "Ctrl");
    }
    ```

    运行 `npx vitest run src/lib/__tests__/shortcutUtils.test.ts` 确认所有测试通过（GREEN）。

    **Step 5 — 安装前端 npm 包:**

    运行 `cd E:/GitLib/EasyPack && npm install @tauri-apps/plugin-global-shortcut`。
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && npx vitest run src/lib/__tests__/shortcutUtils.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src-tauri/Cargo.toml 包含 "tauri-plugin-global-shortcut" 依赖行
    - src-tauri/src/lib.rs 包含 ".plugin(tauri_plugin_global_shortcut::Builder::new().build())"
    - src-tauri/capabilities/default.json 包含 "global-shortcut:allow-register" 和 "global-shortcut:allow-unregister-all" 权限
    - src/lib/types.ts 的 CommandItem 接口包含 "shortcut?: string" 字段
    - src/lib/shortcutUtils.ts 存在且导出 keyboardEventToShortcut 和 shortcutToDisplay
    - src/lib/__tests__/shortcutUtils.test.ts 中所有测试通过
    - @tauri-apps/plugin-global-shortcut 已安装到 node_modules
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useGlobalShortcuts hook with registration lifecycle + tests</name>
  <files>src/hooks/useGlobalShortcuts.ts, src/hooks/__tests__/useGlobalShortcuts.test.ts</files>
  <read_first>
    - E:/GitLib/EasyPack/src/hooks/useKeyboard.ts (hook 结构模式参考)
    - E:/GitLib/EasyPack/src/hooks/useProject.ts (executeCommand 签名和 commands 派生逻辑)
    - E:/GitLib/EasyPack/src/lib/shortcutUtils.ts (Task 1 产出的工具函数)
    - E:/GitLib/EasyPack/src/lib/types.ts (扩展后的 CommandItem 接口)
  </read_first>
  <behavior>
    - Test: hook calls unregisterAll then registers each command's shortcut on mount
    - Test: hook calls unregisterAll on unmount (cleanup)
    - Test: handler only executes on event.state === 'Pressed' (not 'Released')
    - Test: hook re-registers when commands array changes (project switch)
    - Test: hook does not register when enabled=false
    - Test: onShortcutTrigger callback receives the correct command string
  </behavior>
  <action>
    **Step 1 — 先写测试 (TDD RED):**

    创建 src/hooks/__tests__/useGlobalShortcuts.test.ts。

    使用 vi.hoisted() + vi.mock() 模式 mock @tauri-apps/plugin-global-shortcut:
    ```typescript
    const { mockRegister, mockUnregister, mockUnregisterAll, mockIsRegistered } = vi.hoisted(() => ({
      mockRegister: vi.fn().mockResolvedValue(undefined),
      mockUnregister: vi.fn().mockResolvedValue(undefined),
      mockUnregisterAll: vi.fn().mockResolvedValue(undefined),
      mockIsRegistered: vi.fn().mockResolvedValue(false),
    }));
    vi.mock("@tauri-apps/plugin-global-shortcut", () => ({
      register: mockRegister,
      unregister: mockUnregister,
      unregisterAll: mockUnregisterAll,
      isRegistered: mockIsRegistered,
    }));
    ```

    测试用例 (使用 renderHook + act from @testing-library/react):
    - "registers shortcuts on mount when enabled": 传入 commands 包含两个带 shortcut 的 CommandItem，enabled=true，断言 mockUnregisterAll 被调用，mockRegister 被调用 2 次
    - "does not register when disabled": enabled=false，断言 mockRegister 未被调用
    - "unregisters all on unmount": renderHook 后 unmount，断言 mockUnregisterAll 在 cleanup 中被调用
    - "handler executes command only on Pressed state": 模拟 register 的 handler 回调，调用 handler({ shortcut: "CommandOrControl+G", state: "Pressed" } as any)，断言 onShortcutTrigger 被调用；再调用 handler({ shortcut: "CommandOrControl+G", state: "Released" } as any)，断言 onShortcutTrigger 不再多调用
    - "re-registers on commands change": rerender 传入新的 commands，断言 unregisterAll + register 被再次调用

    运行 `npx vitest run src/hooks/__tests__/useGlobalShortcuts.test.ts` 确认失败（RED）。

    **Step 2 — 实现 hook (TDD GREEN):**

    创建 src/hooks/useGlobalShortcuts.ts:

    ```typescript
    import { useEffect, useRef } from "react";
    import { register, unregisterAll } from "@tauri-apps/plugin-global-shortcut";
    import type { ShortcutEvent } from "@tauri-apps/plugin-global-shortcut";
    import type { CommandItem } from "@/lib/types";

    interface UseGlobalShortcutsOptions {
      commands: CommandItem[];
      onExecute: (command: string) => void;
      enabled: boolean; // false when no project selected
    }

    /**
     * Manages OS-level global shortcut registration lifecycle.
     * - Registers all commands with shortcut fields on mount/project change
     * - Unregisters all on unmount/switch
     * - Executes commands on shortcut Pressed events
     *
     * Per RESEARCH.md: must check event.state === 'Pressed' to prevent double-fire.
     * Per RESEARCH.md Pitfall 2: must unregisterAll before re-registration on project switch.
     * Per RESEARCH.md Pitfall 5: uses version counter to prevent race conditions on rapid switching.
     */
    export function useGlobalShortcuts({
      commands,
      onExecute,
      enabled,
    }: UseGlobalShortcutsOptions) {
      const onExecuteRef = useRef(onExecute);
      onExecuteRef.current = onExecute;

      useEffect(() => {
        if (!enabled) {
          // No project selected — unregister everything
          unregisterAll().catch(console.error);
          return;
        }

        const shortcuts = commands.filter((cmd) => cmd.shortcut);

        // Version counter to prevent race conditions (Pitfall 5)
        let version = 0;
        const currentVersion = ++version;

        async function registerAll() {
          try {
            await unregisterAll();
            // Only proceed if still current (no rapid re-switch happened)
            if (version !== currentVersion) return;

            for (const cmd of shortcuts) {
              const handler = (event: ShortcutEvent) => {
                if (event.state === "Pressed") {
                  onExecuteRef.current(cmd.command);
                }
              };
              await register(cmd.shortcut!, handler);
              if (version !== currentVersion) return;
            }
          } catch (err) {
            console.error("Failed to register shortcuts:", err);
          }
        }

        registerAll();

        return () => {
          // Increment version to cancel any in-flight registrations
          version++;
          unregisterAll().catch(console.error);
        };
      }, [commands, enabled]);
    }
    ```

    注意: 使用 useRef 保存 onExecute 回调避免 stale closure 问题。version counter 处理快速项目切换的竞态条件。useEffect 依赖 [commands, enabled] 触发重注册。

    **Step 3 — 运行测试:**

    `npx vitest run src/hooks/__tests__/useGlobalShortcuts.test.ts --reporter=verbose` 确认所有测试通过。

    **Step 4 — 运行全量测试确认无回归:**

    `npx vitest run` 确认全部通过。
  </action>
  <verify>
    <automated>cd E:/GitLib/EasyPack && npx vitest run src/hooks/__tests__/useGlobalShortcuts.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/hooks/useGlobalShortcuts.ts 存在且导出 useGlobalShortcuts 函数
    - hook 接受 { commands: CommandItem[], onExecute: (cmd: string) => void, enabled: boolean } 参数
    - hook 在 enabled=false 时调用 unregisterAll 且不注册
    - hook 在 mount 时 unregisterAll + batch register 所有带 shortcut 的命令
    - hook 的 handler 检查 event.state === 'Pressed'
    - hook 在 unmount 时调用 unregisterAll 清理
    - hook 使用 version counter 防止快速切换竞态
    - src/hooks/__tests__/useGlobalShortcuts.test.ts 中所有测试通过
    - 全量测试 npx vitest run 通过（无回归）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Tauri plugin API | KeyboardEvent 数据从前端传入 Tauri 插件，需验证格式合法性 |
| Tauri plugin -> OS hotkey | register() 调用 Win32 RegisterHotKey，系统可能拒绝已被占用的快捷键 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | keyboardEventToShortcut | mitigate | 验证必须包含修饰键 (D-06)，最多 3 键 (D-07)，拒绝 modifier-only 组合 |
| T-11-02 | Denial of Service | global shortcut registration | accept | 另一应用抢占快捷键无法在应用层防护，系统决定优先级 |
| T-11-03 | Denial of Service | useGlobalShortcuts | mitigate | 检查 event.state === 'Pressed' 防止双触发 |
| T-11-04 | Tampering | capabilities/default.json | mitigate | 限制为 global-shortcut:allow-* 权限，非 core:default 全开 |
</threat_model>

<verification>
1. `npx vitest run src/lib/__tests__/shortcutUtils.test.ts --reporter=verbose` — 全部通过
2. `npx vitest run src/hooks/__tests__/useGlobalShortcuts.test.ts --reporter=verbose` — 全部通过
3. `npx vitest run` — 全量测试无回归
4. `grep -n "shortcut?" src/lib/types.ts` — 确认 shortcut 字段存在
5. `grep -n "tauri-plugin-global-shortcut" src-tauri/Cargo.toml` — 确认依赖存在
6. `grep -n "global-shortcut" src-tauri/capabilities/default.json` — 确认权限存在
7. `grep -n "tauri_plugin_global_shortcut" src-tauri/src/lib.rs` — 确认插件注册
</verification>

<success_criteria>
- Tauri global-shortcut 插件三端配置完成（Cargo.toml + lib.rs + default.json）
- @tauri-apps/plugin-global-shortcut npm 包已安装
- CommandItem.shortcut?: string 字段添加且向后兼容
- keyboardEventToShortcut 正确处理所有验证规则（D-06 修饰键、D-07 键数、特殊键映射）
- useGlobalShortcuts hook 实现完整的注册/注销/执行生命周期
- 所有新增测试通过，全量测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/11-全局快捷键/11-01-SUMMARY.md`
</output>
