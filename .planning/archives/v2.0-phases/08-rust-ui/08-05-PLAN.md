---
phase: 08-rust-ui
plan: 05
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/hooks/useProject.ts
  - src/components/MainArea.tsx
  - src/App.tsx
autonomous: true
requirements:
  - PROJ-09
  - PROJ-10

must_haves:
  truths:
    - "选中项目后信息栏显示文件夹大小（排除 node_modules/.git 等大目录）"
    - "Git 项目显示当前分支名（如 '分支: main'），非 Git 项目不显示分支"
    - "文件夹大小计算中显示'计算中...'占位文本"
    - "计算超时或失败时显示'无法计算'，且通过独立错误标志区分"
    - "切换项目时重新获取项目信息"
    - "App.tsx 正确将 projectInfo 和 projectInfoLoading 传递给 MainArea"
  artifacts:
    - path: "src/hooks/useProject.ts"
      provides: "项目信息获取逻辑（文件夹大小 + Git 分支）"
      contains: "get_project_info"
    - path: "src/components/MainArea.tsx"
      provides: "信息栏文件夹大小和 Git 分支显示"
      contains: "分支:"
    - path: "src/App.tsx"
      provides: "projectInfo/projectInfoLoading props 传递给 MainArea"
      contains: "projectInfo"
  key_links:
    - from: "src/hooks/useProject.ts"
      to: "get_project_info Tauri command"
      via: "invoke('get_project_info', { projectPath })"
      pattern: "invoke.*get_project_info"
    - from: "src/components/MainArea.tsx"
      to: "useProject hook"
      via: "props: projectInfo state"
      pattern: "projectInfo"
    - from: "src/App.tsx"
      to: "src/components/MainArea.tsx"
      via: "projectInfo + projectInfoLoading props"
      pattern: "projectInfo.*MainArea"
---

<objective>
在 MainArea 信息栏中显示文件夹大小和 Git 分支信息。通过 useProject hook 管理 get_project_info 调用，包括加载状态、超时处理和错误显示（per D-04, D-05, D-06, D-07, D-08, PROJ-09, PROJ-10）。修复 App.tsx props 传递。

Purpose: 用户选中项目后能看到项目的磁盘占用和 Git 状态，增强项目信息可见性。
Output: MainArea 信息栏新增文件夹大小和 Git 分支显示，带超时和错误处理，App.tsx 正确传递 props。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rust-ui/08-CONTEXT.md
@.planning/phases/08-rust-ui/08-UI-SPEC.md
@.planning/phases/08-rust-ui/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (Rust backend command) -->

Rust Tauri command:
```rust
// get_project_info(project_path: String) -> Result<ProjectInfo, String>
// ProjectInfo { size: String, branch: Option<String> }
```

From src/components/MainArea.tsx (current info area, lines 162-205):
```tsx
<div className="mb-6">
  <div className="flex items-center justify-between">
    <h2 className="text-sm font-medium text-foreground">
      当前项目: {currentProject.name}
    </h2>
    {/* Edit mode toggle button */}
  </div>
  <p className="text-xs text-muted-foreground mt-1">{currentProject.path}</p>
  {/* Mode label + switch entry */}
  <div className="flex items-center gap-2 mt-1">
    <span className="text-xs text-muted-foreground">
      {commandMode === "global" ? "全局指令" : "项目自定义指令"}
    </span>
    ...
  </div>
</div>
```

From src/hooks/useProject.ts (current return interface):
```typescript
return {
  currentProject, selectFolder, executeCommand,
  projects, selectedId, loading, addProject, removeProject, selectProject,
  commands, addCommand, updateCommand, deleteCommand,
  commandMode, editMode, setEditMode, enableProjectCommands, disableProjectCommands,
  updateProjectStyle, reorderProjects,
};
```

From src/App.tsx (current MainArea usage):
```tsx
<MainArea ... />
```
(需要确认当前 App.tsx 中 MainArea 接收的 props 列表)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 useProject hook 中添加项目信息获取逻辑（含正确的错误状态处理）</name>
  <files>src/hooks/useProject.ts</files>
  <read_first>
    - src/hooks/useProject.ts (当前完整实现，特别关注 selectProject 和 currentProject 部分)
    - .planning/phases/08-rust-ui/08-CONTEXT.md (D-04, D-06 超时机制)
    - .planning/phases/08-rust-ui/08-UI-SPEC.md (Section 4: Folder Size & Git Branch Display, Fetch Timing)
    - .planning/phases/08-rust-ui/08-RESEARCH.md (Frontend: 超时包装代码示例)
  </read_first>
  <action>
    在 `src/hooks/useProject.ts` 中添加项目信息获取逻辑。

    **1. 添加 ProjectInfo 接口（在 ProjectItem 接口之后）:**
    ```typescript
    /** 项目信息（文件夹大小 + Git 分支），来自 Rust get_project_info 命令 */
    export interface ProjectInfoResult {
      size: string;         // 人类可读格式，如 "12.3 MB"
      branch: string | null; // null = 非 Git 仓库或 detached HEAD
    }
    ```

    **2. 在 useProject hook 中添加状态和逻辑:**

    在现有 useState 声明区域（约 line 44-49 之后）添加:
    ```typescript
    // Phase 8: project info (folder size + Git branch)
    const [projectInfo, setProjectInfo] = useState<ProjectInfoResult | null>(null);
    const [projectInfoLoading, setProjectInfoLoading] = useState(false);
    const [projectInfoError, setProjectInfoError] = useState(false);
    ```

    **3. 添加项目信息获取函数:**
    ```typescript
    // Phase 8: fetch project info (folder size + Git branch) per D-04
    const fetchProjectInfo = useCallback(async (projectPath: string) => {
      setProjectInfoLoading(true);
      setProjectInfoError(false);
      setProjectInfo(null);
      try {
        // D-06: 8 second timeout (Claude's discretion, between 5-10s)
        // 使用 reject 而非 resolve，确保超时状态可通过 catch 区分
        const result = await Promise.race([
          invoke<ProjectInfoResult>("get_project_info", { projectPath }),
          new Promise<never>((_, reject) =>
            setTimeout(() => reject(new Error("timeout")), 8000)
          ),
        ]);
        setProjectInfo(result);
      } catch {
        // 超时或 invoke 错误：设置错误标志
        setProjectInfoError(true);
      } finally {
        setProjectInfoLoading(false);
      }
    }, []);
    ```

    关键设计决策：
    - 超时 Promise 使用 `reject` 而非 `resolve`。这样超时和 invoke 错误都走 catch 分支，统一处理。
    - 使用独立的 `projectInfoError: boolean` 标志而非在 data 中混入错误字符串。这样 UI 可以明确区分三种状态：加载中、成功（有数据）、失败（error=true）。
    - "无法计算" 文本由 MainArea 组件根据 `projectInfoError` 标志显示，不在 hook 中硬编码。

    **4. 在 selectProject 中触发项目信息获取:**

    修改现有 selectProject 函数，在选中项目后调用 fetchProjectInfo:
    ```typescript
    const selectProject = useCallback(
      async (id: string) => {
        setSelectedId(id);
        setEditMode(false);
        await store?.set(SELECTED_KEY, id);
        // Phase 8: fetch project info on project switch (per D-04)
        const project = projects.find((p) => p.id === id);
        if (project) {
          fetchProjectInfo(project.path);
        }
      },
      [store, projects, fetchProjectInfo]
    );
    ```

    **5. 在 return 对象中导出新状态和函数:**
    在 return 对象中添加（在 reorderProjects 之后）:
    ```typescript
    // Phase 8: project info
    projectInfo,           // ProjectInfoResult | null
    projectInfoLoading,    // boolean
    projectInfoError,      // boolean
    ```

    **注意事项:**
    - 超时使用前端 `Promise.race` 方案（per RESEARCH Don't Hand-Roll），不阻塞 Rust 线程
    - 超时秒数 8 秒（Claude's discretion，在 D-06 的 5-10 秒范围内）
    - 初始化时（useEffect init）如果有 savedSelectedId，也应获取对应项目的项目信息:
      在 init 函数最后，如果 savedSelectedId 有值且对应项目存在，调用 fetchProjectInfo
    - 使用不可变更新模式（新状态对象，不修改已有对象）
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/hooks/useProject.ts` 导出 `ProjectInfoResult` 接口
    - `useProject` hook 包含 `projectInfo`、`projectInfoLoading`、`projectInfoError` 三个状态
    - `useProject` hook 包含 `fetchProjectInfo` 函数
    - `fetchProjectInfo` 超时使用 `reject`（非 resolve），错误状态通过 `projectInfoError` 标志区分
    - `selectProject` 在切换项目时调用 `fetchProjectInfo`
    - 超时机制使用 `Promise.race`，超时时间 8000ms
    - 超时或错误时 `projectInfoError` 设为 true（不混合错误文本到数据中）
    - return 对象导出 `projectInfo`、`projectInfoLoading`、`projectInfoError`
    - 所有 vitest 测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 App.tsx 传递 props 并在 MainArea 信息栏显示文件夹大小和 Git 分支</name>
  <files>src/App.tsx, src/components/MainArea.tsx</files>
  <read_first>
    - src/App.tsx (当前 MainArea 的 props 传递)
    - src/components/MainArea.tsx (当前完整实现，特别关注 lines 162-205 信息栏区域)
    - .planning/phases/08-rust-ui/08-UI-SPEC.md (Section 4: Info Bar Layout, Info Display Rules)
    - .planning/phases/08-rust-ui/08-CONTEXT.md (D-04, D-07 显示规则)
    - src/hooks/useProject.ts (新增的 projectInfo、projectInfoLoading、projectInfoError)
  </read_first>
  <action>
    修改 `src/App.tsx` 和 `src/components/MainArea.tsx`，传递并显示文件夹大小和 Git 分支信息。

    **Part A: 修改 `src/App.tsx`**

    1. 在 App 组件中从 useProject() 解构新增的状态:
    ```typescript
    const { ..., projectInfo, projectInfoLoading, projectInfoError } = useProject();
    ```

    2. 找到 `<MainArea` 组件调用处，添加三个新 props:
    ```tsx
    <MainArea
      ...existingProps
      projectInfo={projectInfo}
      projectInfoLoading={projectInfoLoading}
      projectInfoError={projectInfoError}
    />
    ```

    **Part B: 修改 `src/components/MainArea.tsx`**

    1. 在 MainAreaProps interface 中添加:
    ```typescript
    // Phase 8: project info display
    projectInfo: { size: string; branch: string | null } | null;
    projectInfoLoading: boolean;
    projectInfoError: boolean;
    ```

    2. 在 MainArea 函数参数解构中添加:
    ```typescript
    projectInfo,
    projectInfoLoading,
    projectInfoError,
    ```

    3. 在信息栏中添加显示（在 `{currentProject.path}` 行之后，mode label 行之前）:

    当前结构:
    ```tsx
    <p className="text-xs text-muted-foreground mt-1">{currentProject.path}</p>
    {/* Mode label + switch entry */}
    <div className="flex items-center gap-2 mt-1">
    ```

    在 path 行和 mode label 行之间插入:
    ```tsx
    {/* Phase 8: folder size + Git branch (per D-04, D-07, D-08) */}
    {(projectInfo || projectInfoLoading) && (
      <div className="flex items-center gap-1 mt-1" aria-live="polite">
        <span className="text-xs text-muted-foreground">
          {projectInfoLoading ? "计算中..." : projectInfoError ? "无法计算" : projectInfo?.size}
        </span>
        {projectInfo?.branch && (
          <>
            <span className="text-xs text-muted-foreground">·</span>
            <span className="text-xs text-muted-foreground">
              分支: {projectInfo.branch}
            </span>
          </>
        )}
      </div>
    )}
    ```

    **UI 规格 (per UI-SPEC Section 4):**
    - 文件夹大小: text-xs text-muted-foreground，显示人类可读格式
    - 计算中: "计算中..." 文本 (per UI-SPEC Copywriting)
    - 计算失败: "无法计算" 由 `projectInfoError` 标志触发（独立于数据状态）
    - Git 分支: "分支: {name}" 格式，与前一个信息用 "·" 分隔
    - 非 Git 仓库: 不显示分支部分（projectInfo.branch 为 null）
    - Detached HEAD: 不显示分支部分（后端返回 None）
    - 整行 aria-live="polite" 用于无障碍状态更新 (per UI-SPEC Accessibility)
    - 行间距 gap-1 (4px)，mt-1 与上方 path 行保持紧凑 (per UI-SPEC Phase 8 Specific Spacing)
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/App.tsx` 从 useProject 解构 `projectInfo`、`projectInfoLoading`、`projectInfoError`
    - `src/App.tsx` 将三个新 props 传递给 MainArea 组件
    - `src/components/MainArea.tsx` 的 MainAreaProps 包含 `projectInfo`、`projectInfoLoading`、`projectInfoError`
    - 信息栏在 path 行下方显示文件夹大小
    - 计算中显示 "计算中..." 文本
    - 错误时显示 "无法计算"（由独立 projectInfoError 标志控制）
    - Git 项目显示 "分支: {branch}" 格式
    - 非 Git 项目不显示分支信息
    - 整行有 `aria-live="polite"` 属性
    - 所有 vitest 测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust get_project_info | 通过 invoke 调用，参数为项目路径 |
| 用户看到的信息 | 文件夹大小和 Git 分支为信息展示，不构成安全风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | 文件夹大小信息显示 | accept | 用户查看自己的项目信息，无泄露风险 |
| T-08-11 | Denial of Service | 超大目录计算耗时 | mitigate | 前端 Promise.race 8 秒超时，超时设置 projectInfoError=true 显示"无法计算"，不阻塞 UI |

</threat_model>

<verification>
1. `npx vitest run` 所有测试通过
2. `grep "get_project_info" src/hooks/useProject.ts` 输出匹配
3. `grep "projectInfoError" src/hooks/useProject.ts` 输出匹配
4. `grep "projectInfo" src/components/MainArea.tsx` 输出匹配
5. `grep "分支:" src/components/MainArea.tsx` 输出匹配
6. `grep "计算中" src/components/MainArea.tsx` 输出匹配
7. `grep "无法计算" src/components/MainArea.tsx` 输出匹配
8. `grep "projectInfo" src/App.tsx` 输出匹配
</verification>

<success_criteria>
- 选中项目后信息栏显示文件夹大小和 Git 分支
- 加载中有"计算中..."占位
- 超时/错误有独立的 projectInfoError 标志和"无法计算"回退
- 非 Git 项目不显示分支
- App.tsx 正确传递所有 props
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-rust-ui/08-05-SUMMARY.md`
</output>
