---
phase: 16-版本管理
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/hooks/useUpdateCheck.ts
  - src/components/TitleBar.tsx
  - src/components/SettingsDialog.tsx
  - src/App.tsx
autonomous: true
requirements:
  - VER-01
  - VER-03
  - VER-04

must_haves:
  truths:
    - "SettingsDialog 底部显示当前版本号文字（如 v0.1.0）"
    - "应用启动时自动检查更新，useUpdateCheck hook 在 store 就绪后触发"
    - "发现新版本时齿轮图标右上角显示红色小圆点"
    - "发现新版本时 SettingsDialog 内显示更新提示条"
    - "点击更新提示条后系统浏览器打开 GitHub Releases 页面"
    - "红点持续显示直到用户更新版本，不因关闭弹窗而消失"
  artifacts:
    - path: "src/hooks/useUpdateCheck.ts"
      provides: "更新检查 hook（启动检查、缓存、状态管理、openReleasePage）"
      exports: ["useUpdateCheck"]
    - path: "src/components/TitleBar.tsx"
      provides: "齿轮图标红点指示器"
      contains: "updateAvailable"
    - path: "src/components/SettingsDialog.tsx"
      provides: "版本号显示 + 更新提示条"
      contains: "latestVersion"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useUpdateCheck.ts"
      via: "useUpdateCheck(store) hook 调用"
      pattern: "useUpdateCheck"
    - from: "src/App.tsx"
      to: "src/components/TitleBar.tsx"
      via: "updateAvailable prop 传递"
      pattern: "updateAvailable"
    - from: "src/App.tsx"
      to: "src/components/SettingsDialog.tsx"
      via: "updateAvailable + latestVersion + currentVersion + onOpenReleasePage props"
      pattern: "latestVersion"
    - from: "src/hooks/useUpdateCheck.ts"
      to: "check_for_updates command"
      via: "invoke('check_for_updates')"
      pattern: "invoke.*check_for_updates"
    - from: "src/hooks/useUpdateCheck.ts"
      to: "open_release_page command"
      via: "invoke('open_release_page')"
      pattern: "invoke.*open_release_page"
---

<objective>
实现前端 UI 的版本号显示、更新检查状态管理和更新通知界面。

Purpose: 用户可以在设置弹窗底部看到当前版本号；应用启动时自动检查更新；发现新版本时齿轮图标显示红点、设置弹窗内显示更新提示条；点击提示条打开浏览器下载页面。

Output: useUpdateCheck hook + TitleBar 红点 + SettingsDialog 版本号和提示条 + App.tsx 状态传递。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-版本管理/16-CONTEXT.md
@.planning/phases/16-版本管理/16-RESEARCH.md
@.planning/phases/16-版本管理/16-01-SUMMARY.md

<interfaces>
<!-- Rust command contracts from Plan 01 -->

From src-tauri/src/commands/update.rs (created by Plan 01):
```rust
#[derive(Serialize)]
pub struct UpdateCheckResult {
    has_update: bool,
    latest_version: Option<String>,
}

#[tauri::command]
pub fn check_for_updates(app: tauri::AppHandle) -> Result<UpdateCheckResult, String>;

#[tauri::command]
pub fn open_release_page() -> Result<(), String>;
```

From src/components/TitleBar.tsx (current props):
```typescript
interface TitleBarProps {
  onSettingsOpen: () => void;
  onFloatToggle: () => void;
  floatVisible: boolean;
  onDragWhileSnapped?: ((deltaX: number, deltaY: number) => void) | null;
  drawerSnapEdge?: string | null;
}
```

From src/components/SettingsDialog.tsx (current props):
```typescript
interface SettingsDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  trayEnabled: boolean;
  onTrayEnabledChange: (enabled: boolean) => void;
  closeToTray: boolean;
  onCloseToTrayChange: (enabled: boolean) => void;
  drawerEnabled: boolean;
  onDrawerEnabledChange: (enabled: boolean) => void;
  autostartEnabled: boolean;
  onAutostartEnabledChange: (enabled: boolean) => void;
}
```

From src/App.tsx (store and hook pattern):
```typescript
const { store, ... } = useProject();
// Hook usage pattern: hooks receive store, return state + callbacks
```

From @tauri-apps/api/app:
```typescript
function getVersion(): Promise<string>; // reads tauri.conf.json version field
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 useUpdateCheck hook</name>
  <files>src/hooks/useUpdateCheck.ts</files>
  <behavior>
    - 调用 useUpdateCheck(store) 后，store 存在时自动触发 getVersion() 和 invoke("check_for_updates")
    - 返回 updateAvailable (boolean)、latestVersion (string | null)、currentVersion (string)、openReleasePage (function)
    - store 为 null 时不发起任何调用
    - 组件卸载后 mounted flag 阻止 setState
    - openReleasePage 调用 invoke("open_release_page")
  </behavior>
  <action>
    创建 `src/hooks/useUpdateCheck.ts`，遵循现有 hook 模式（参考 useTray.ts、useRecentCommands.ts）:

    (a) 定义接口:
    ```typescript
    interface UpdateCheckResult {
      has_update: boolean;
      latest_version: string | null;
    }
    ```

    (b) 导出 `useUpdateCheck(store: Store | null)` 函数:
    - 使用 `useState` 管理 `updateAvailable` (boolean, 默认 false)、`latestVersion` (string | null, 默认 null)、`currentVersion` (string, 默认 "")
    - 使用 `useEffect` 依赖 `[store]`，在 store 非空时执行:
      - 设置 `mounted` flag（cleanup 时设为 false）
      - 异步调用 `getVersion()` 获取当前版本号（import from `@tauri-apps/api/app`）
      - 异步调用 `invoke<UpdateCheckResult>("check_for_updates")` 获取更新检查结果
      - 所有异步操作后检查 mounted flag 再 setState
      - catch 块静默忽略错误（网络错误不应中断 UX）
    - 导出 `openReleasePage` async 函数: 调用 `invoke("open_release_page")`，catch 静默忽略

    (c) 返回值: `{ updateAvailable, latestVersion, currentVersion, openReleasePage }`

    **重要注意事项:**
    - store 依赖确保 hook 在 store 初始化后才发起检查
    - mounted flag 防止组件卸载后的 setState 警告
    - 不需要 store 作为缓存读取——缓存由 Rust 端 check_for_updates command 处理
    - 遵循现有 hook 的 import 风格: `import { invoke } from "@tauri-apps/api/core"`
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | grep -i "useUpdateCheck" || echo "No type errors for useUpdateCheck"</automated>
  </verify>
  <done>
    - useUpdateCheck.ts 文件存在
    - 导出 useUpdateCheck 函数，接受 Store | null 参数
    - store 非空时调用 getVersion + invoke check_for_updates
    - 返回 updateAvailable、latestVersion、currentVersion、openReleasePage
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TitleBar 添加红点 + SettingsDialog 添加版本号和提示条 + App.tsx 状态传递</name>
  <files>src/components/TitleBar.tsx, src/components/SettingsDialog.tsx, src/App.tsx</files>
  <behavior>
    - TitleBar: 当 updateAvailable=true 时，齿轮按钮右上角显示 6px 红色圆点
    - TitleBar: 当 updateAvailable=false 时，不显示任何红点
    - SettingsDialog: 底部显示当前版本号文字（如 "v0.1.0"），样式 text-xs text-muted-foreground
    - SettingsDialog: 当 updateAvailable=true 时，版本号上方显示蓝色/绿色左边框提示条
    - SettingsDialog: 点击提示条调用 onOpenReleasePage
    - App.tsx: 调用 useUpdateCheck(store)，传递状态给 TitleBar 和 SettingsDialog
  </behavior>
  <action>
    **Step 1: 修改 TitleBar.tsx** (per D-03)

    (a) 在 TitleBarProps 接口添加 `updateAvailable?: boolean`

    (b) 解构 props 中添加 `updateAvailable = false`

    (c) 在齿轮按钮上添加红点——修改现有 Settings 按钮的 className 添加 `relative`:
    ```tsx
    <button
      className="titlebar-button relative"
      onClick={onSettingsOpen}
      aria-label="设置"
    >
      <Settings className="w-[14px] h-[14px]" />
      {updateAvailable && (
        <span className="absolute -top-0.5 -right-0.5 w-1.5 h-1.5 bg-red-500 rounded-full" />
      )}
    </button>
    ```

    **Step 2: 修改 SettingsDialog.tsx** (per D-01, D-04, D-06)

    (a) 在 SettingsDialogProps 接口添加:
    - `currentVersion: string`
    - `updateAvailable: boolean`
    - `latestVersion: string | null`
    - `onOpenReleasePage: () => void`

    (b) 解构这些新 props

    (c) 在 DialogContent 内，在现有设置内容（`div.py-4.space-y-6`）之后，添加版本号和更新提示区域:
    ```tsx
    {/* 版本号 + 更新提示 */}
    <div className="border-t border-white/10 pt-3 mt-2">
      {updateAvailable && latestVersion && (
        <button
          onClick={onOpenReleasePage}
          className="w-full text-left px-3 py-2 mb-2 rounded-md border-l-2 border-blue-400 bg-blue-400/10 text-sm text-blue-300 hover:bg-blue-400/20 transition-colors cursor-pointer"
        >
          发现新版本 v{latestVersion}，点击下载
        </button>
      )}
      <p className="text-xs text-muted-foreground text-center">
        v{currentVersion || "..."}
      </p>
    </div>
    ```

    **Step 3: 修改 App.tsx**

    (a) 添加 import: `import { useUpdateCheck } from "@/hooks/useUpdateCheck";`

    (b) 在 App 组件函数体内（`useTray` 之前或之后），调用 hook:
    ```typescript
    const { updateAvailable, latestVersion, currentVersion, openReleasePage } = useUpdateCheck(store);
    ```

    (c) 修改 TitleBar 组件调用，添加 updateAvailable prop:
    ```tsx
    <TitleBar
      onSettingsOpen={() => setSettingsOpen(true)}
      onFloatToggle={toggleFloat}
      floatVisible={floatVisible}
      onDragWhileSnapped={drawerEnabled ? handleDragWhileSnapped : null}
      drawerSnapEdge={snapEdge}
      updateAvailable={updateAvailable}
    />
    ```

    (d) 修改 SettingsDialog 组件调用，添加版本相关 props:
    ```tsx
    <SettingsDialog
      open={settingsOpen}
      onOpenChange={setSettingsOpen}
      trayEnabled={trayEnabled}
      onTrayEnabledChange={handleTrayEnabledChange}
      closeToTray={closeToTray}
      onCloseToTrayChange={handleCloseToTrayChange}
      drawerEnabled={drawerEnabled}
      onDrawerEnabledChange={handleDrawerEnabledChange}
      autostartEnabled={autostartEnabled}
      onAutostartEnabledChange={handleAutostartEnabledChange}
      currentVersion={currentVersion}
      updateAvailable={updateAvailable}
      latestVersion={latestVersion}
      onOpenReleasePage={openReleasePage}
    />
    ```

    **重要注意事项:**
    - 红点样式严格按 D-03：6px 红色圆点，absolute 定位在齿轮图标右上角
    - 版本号样式按 D-01：text-xs text-muted-foreground，居中显示
    - 提示条样式参考 shadcn/ui Alert，使用蓝色左边框 + 半透明背景
    - updateAvailable 状态持久化由 Rust 端缓存保证（24h 内重复检查返回缓存结果），不需要前端额外持久化
    - 红点不因关闭弹窗消失（D-06）：状态存在 App.tsx 中，独立于 SettingsDialog 的 open/close 生命周期
  </action>
  <verify>
    <automated>cd E:/git/EasyPack && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TitleBar 齿轮按钮在 updateAvailable=true 时显示红色小圆点
    - SettingsDialog 底部显示 "v{currentVersion}" 文字
    - SettingsDialog 在 updateAvailable=true 时显示 "发现新版本 v{latestVersion}，点击下载" 提示条
    - 点击提示条调用 open_release_page command 打开浏览器
    - App.tsx 正确调用 useUpdateCheck hook 并传递状态
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust IPC | invoke() 调用跨 IPC 边界，参数不可信 |
| SettingsDialog -> open_release_page | 用户点击触发浏览器打开，URL 硬编码在 Rust 端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Spoofing | updateAvailable 状态伪造 | accept | 前端状态由 Rust 端返回结果驱动，无用户输入路径。即使伪造也仅导致红点显示/隐藏，无安全影响 |
| T-16-06 | Information Disclosure | 版本号暴露 | accept | 版本号为公开信息，在 tauri.conf.json 和 GitHub releases 中均可见 |
</threat_model>

<verification>
1. `cd E:/git/EasyPack && npx tsc --noEmit` — TypeScript 编译通过
2. `cd E:/git/EasyPack && npx vite build` — 前端构建通过（如有 Tauri 构建环境）
3. 代码 review:
   - useUpdateCheck hook 正确调用 getVersion + invoke check_for_updates
   - TitleBar 传入 updateAvailable prop 并条件渲染红点
   - SettingsDialog 传入 currentVersion、updateAvailable、latestVersion、onOpenReleasePage 并渲染版本号和提示条
   - App.tsx 调用 useUpdateCheck(store) 并传递所有 props
4. 功能验证（需运行应用）:
   - 打开设置弹窗，底部显示当前版本号
   - 齿轮图标无红点（如无新版本）
   - 如需测试红点：可临时修改 check_for_updates 返回 has_update: true 验证 UI
</verification>

<success_criteria>
- TypeScript 编译零错误
- useUpdateCheck hook 正确管理更新状态（currentVersion, updateAvailable, latestVersion）
- TitleBar 齿轮图标在 updateAvailable 时显示红色 6px 圆点
- SettingsDialog 底部显示版本号文字
- SettingsDialog 在 updateAvailable 时显示更新提示条
- 点击提示条触发 open_release_page 打开浏览器
- 网络错误不导致 UI 崩溃或错误提示
</success_criteria>

<output>
After completion, create `.planning/phases/16-版本管理/16-02-SUMMARY.md`
</output>
