---
phase: 09-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/shell.rs
  - src-tauri/src/lib.rs
  - src/hooks/useProject.ts
  - src/App.tsx
  - src/components/MainArea.tsx
autonomous: true
requirements: [PROJ-11, UI-10]

must_haves:
  truths:
    - "用户点击'打开文件夹'按钮后 Windows 文件资源管理器打开项目目录"
    - "Toggle Group 按钮紧邻拼合显示'全局指令'和'项目指令'两个选项"
    - "当前激活模式对应按钮使用 secondary variant 高亮"
    - "项目无自定义指令时'项目指令'按钮灰显禁用不可点击"
    - "'打开文件夹'按钮使用 outline variant 右对齐，与 Toggle Group 在同一行"
  artifacts:
    - path: "src-tauri/src/commands/shell.rs"
      provides: "open_folder Tauri command"
      contains: "pub async fn open_folder"
    - path: "src-tauri/src/lib.rs"
      provides: "open_folder command registration"
      contains: "commands::shell::open_folder"
    - path: "src/hooks/useProject.ts"
      provides: "openFolder function"
      contains: "invoke(\"open_folder\""
    - path: "src/components/MainArea.tsx"
      provides: "Toggle Group + 打开文件夹 button row"
      contains: "justify-between"
  key_links:
    - from: "src/components/MainArea.tsx"
      to: "src/hooks/useProject.ts"
      via: "onOpenFolder prop + openFolder function"
      pattern: "onOpenFolder"
    - from: "src/hooks/useProject.ts"
      to: "src-tauri/src/commands/shell.rs"
      via: "invoke('open_folder')"
      pattern: "invoke\\(\"open_folder\""
    - from: "src-tauri/src/commands/shell.rs"
      to: "explorer.exe"
      via: "StdCommand::new + raw_arg"
      pattern: "explorer\\.exe"
---

<objective>
将 MainArea 中文本链接切换器替换为 Toggle Group 按钮行（全局指令/项目指令拼合按钮 + 打开文件夹 outline 按钮），并在 Rust 后端新增 open_folder 命令。

Purpose: 完成需求 PROJ-11（打开文件夹）和 UI-10（按钮化切换），提升操作一致性和便捷性。
Output: 修改后的 MainArea.tsx（Toggle Group + 打开文件夹按钮行）、useProject.ts（openFolder 函数）、shell.rs（open_folder 命令）、lib.rs（命令注册）、App.tsx（props 传递）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-ui/09-CONTEXT.md
@.planning/phases/09-frontend-ui/09-UI-SPEC.md
@.planning/phases/08-rust-ui/08-05-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/button.tsx:
```typescript
// Button component with variants: default, destructive, outline, secondary, ghost, link
// Sizes: default, sm (h-8 text-xs), lg, icon
// Import: import { Button } from "@/components/ui/button";
```

From src/hooks/useProject.ts:
```typescript
export interface ProjectItem {
  id: string;
  name: string;
  path: string;
  addedAt: number;
  icon?: string;
  color?: string;
}

// Key functions already exported from useProject:
// enableProjectCommands: () => Promise<void>
// disableProjectCommands: () => Promise<void>
// commandMode: "global" | "project"
// projectCommandsMap: Record<string, CommandItem[]>
// currentProject: ProjectItem | null
// selectedId: string | null
```

From src/components/MainArea.tsx:
```typescript
interface MainAreaProps {
  currentProject: ProjectItem | null;
  onExecute: (command: string) => void;
  commands: CommandItem[];
  commandMode: "global" | "project";
  editMode: boolean;
  setEditMode: (editMode: boolean) => void;
  addCommand: (name: string, command: string, icon?: string) => Promise<void>;
  updateCommand: (id: string, data: { name: string; command: string; icon: string }) => Promise<void>;
  deleteCommand: (id: string) => Promise<void>;
  enableProjectCommands: () => Promise<void>;
  disableProjectCommands: () => Promise<void>;
  activeZone: "sidebar" | "main";
  onZoneSwitch: () => void;
  projectInfo: { size: string; branch: string | null } | null;
  projectInfoLoading: boolean;
  projectInfoError: boolean;
}
// Lines 207-228: text link switcher to be replaced entirely
```

From src-tauri/src/lib.rs:
```rust
// invoke_handler registration pattern:
.invoke_handler(tauri::generate_handler![
    commands::shell::execute_command,
    commands::project_info::scan_project_icons,
    commands::project_info::get_project_info,
    // Add: commands::shell::open_folder,
])
```

From src-tauri/src/commands/shell.rs:
```rust
use std::process::Command as StdCommand;
// raw_arg pattern (verified in Phase 6 for path handling):
// StdCommand::new("cmd").args([...]).spawn()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust 后端 open_folder 命令 + 前端 openFolder hook 函数</name>
  <files>src-tauri/src/commands/shell.rs, src-tauri/src/lib.rs, src/hooks/useProject.ts</files>
  <read_first>
    - src-tauri/src/commands/shell.rs (当前 shell 命令实现模式)
    - src-tauri/src/lib.rs (invoke_handler 注册列表)
    - src/hooks/useProject.ts (hook 完整结构，return 对象)
    - .planning/phases/09-frontend-ui/09-CONTEXT.md (D-04: explorer.exe + raw_arg 决策)
  </read_first>
  <action>
**1. 在 src-tauri/src/commands/shell.rs 末尾（mod tests 之前）添加 open_folder 命令：**

```rust
/// 在 Windows 文件资源管理器中打开项目文件夹 (per D-04)
/// 使用 explorer.exe + raw_arg() 模式，路径用双引号包裹处理空格
#[tauri::command]
pub async fn open_folder(path: String) -> Result<(), String> {
    StdCommand::new("explorer.exe")
        .raw_arg(format!("\"{}\"", path))
        .spawn()
        .map_err(|e| format!("Failed to open folder: {}", e))?;
    Ok(())
}
```

注意：
- 使用 `StdCommand::new("explorer.exe")` 不是 `"explorer"`（per D-04）
- 使用 `.raw_arg(format!("\"{}\"", path))` 传路径，双引号包裹处理空格
- 不用 `.arg()` 因为 explorer.exe 传参需要双引号包裹（PITFALLS.md 516-555 行的注意事项）
- 异步函数签名与 execute_command 保持一致
- 错误消息格式与 execute_command 一致

**2. 在 src-tauri/src/lib.rs 的 invoke_handler 中注册新命令：**

在 `commands::project_info::get_project_info,` 之后添加：
```rust
commands::shell::open_folder,
```

**3. 在 src/hooks/useProject.ts 中添加 openFolder 函数：**

在 return 语句之前（约第 416 行 `// Phase 5 Plan 02` 注释之后），添加 openFolder 函数：

```typescript
// Phase 9: open project folder in Windows Explorer (per D-04)
const openFolder = useCallback(async (path: string) => {
  try {
    await invoke("open_folder", { path });
  } catch (error) {
    toast.error("无法打开文件夹", {
      description: "路径无效或文件夹不存在",
    });
  }
}, []);
```

然后在 return 对象中添加（在 `// Phase 8: project info` 区块之后）：

```typescript
// Phase 9: open folder
openFolder, // (path: string) => Promise<void>
```

注意：
- 函数使用 `useCallback` 包装，依赖数组为空 `[]`
- invoke 调用参数名 `{ path }` 必须与 Rust 函数参数名 `path` 匹配
- 错误时使用 toast.error 展示中文提示，与 UI-SPEC Copywriting Contract 一致
- 成功时不显示 toast（UI-SPEC: "No toast — explorer.exe opens visibly"）
  </action>
  <verify>
    <automated>cd "E:/GitLib/EasyPack" && grep -c "pub async fn open_folder" src-tauri/src/commands/shell.rs && grep -c "commands::shell::open_folder" src-tauri/src/lib.rs && grep -c 'invoke("open_folder"' src/hooks/useProject.ts</automated>
  </verify>
  <done>
    - shell.rs 包含 `pub async fn open_folder(path: String) -> Result<(), String>` 函数
    - shell.rs 中 open_folder 使用 `StdCommand::new("explorer.exe").raw_arg(format!("\"{}\"", path))`
    - lib.rs invoke_handler 包含 `commands::shell::open_folder`
    - useProject.ts 包含 `openFolder` 函数，调用 `invoke("open_folder", { path })`
    - useProject.ts return 对象导出 `openFolder`
    - useProject.ts 错误处理包含 toast.error("无法打开文件夹")
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MainArea Toggle Group 按钮行 + App.tsx props 传递</name>
  <files>src/components/MainArea.tsx, src/App.tsx</files>
  <read_first>
    - src/components/MainArea.tsx (完整文件，特别关注 207-228 行的切换器区域和 MainAreaProps 接口)
    - src/App.tsx (完整文件，关注 useProject 解构和 MainArea props 传递)
    - src/components/ui/button.tsx (shadcn Button 组件的 variant 和 size 定义)
    - .planning/phases/09-frontend-ui/09-UI-SPEC.md (Toggle Group CSS 规范、按钮行完整布局代码)
    - .planning/phases/09-frontend-ui/09-CONTEXT.md (D-01 到 D-06 全部锁定决策)
  </read_first>
  <behavior>
    - MainArea 接受 onOpenFolder prop 并在按钮行中调用
    - Toggle Group 两个按钮紧邻拼合（中间无 gap），激活项用 secondary variant，非激活项用 ghost variant
    - "项目指令" 按钮在 selectedId 为空或项目无自定义指令时 disabled
    - "打开文件夹" 按钮使用 outline variant，右对齐，在无选中项目时 disabled
    - MainArea.tsx 不再包含文本链接切换器的任何代码（"使用项目自定义指令" / "使用全局指令" 文本链接）
    - App.tsx 传递 onOpenFolder prop 到 MainArea
  </behavior>
  <action>
**1. 修改 MainArea.tsx MainAreaProps 接口：**

在 `projectInfoError: boolean;` 之后添加：
```typescript
// Phase 9: open folder in explorer
onOpenFolder: () => void;
```

**2. 在 MainArea 函数签名中解构新 prop：**

在 `projectInfoError,` 之后添加 `onOpenFolder,`。

**3. 在 MainArea.tsx 顶部添加 import：**

确保已导入 `Button` 和 `FolderOpen`：
- `import { Button } from "@/components/ui/button";`（新增）
- `import { FolderOpen, Settings, Plus } from "lucide-react";`（FolderOpen 已存在）

**4. 完整替换 MainArea.tsx 第 206-227 行（`{/* Mode label + switch entry (per D-09) */}` 注释到 `</div>`）为以下代码：**

```tsx
        {/* Phase 9: Toggle Group + 打开文件夹 button row (per D-01, D-02, D-03, D-05, D-06) */}
        <div className="flex items-center justify-between mt-2">
          <div
            className="inline-flex rounded-md overflow-hidden border border-white/10"
            role="radiogroup"
            aria-label="指令模式切换"
          >
            <Button
              variant={commandMode === "global" ? "secondary" : "ghost"}
              size="sm"
              role="radio"
              aria-checked={commandMode === "global"}
              aria-label="全局指令"
              className={cn(
                "rounded-none border-r border-white/10",
                commandMode === "global" && "rounded-l-md"
              )}
              onClick={commandMode !== "global" ? disableProjectCommands : undefined}
            >
              全局指令
            </Button>
            <Button
              variant={commandMode === "project" ? "secondary" : "ghost"}
              size="sm"
              role="radio"
              aria-checked={commandMode === "project"}
              aria-label="项目指令"
              aria-disabled={(() => {
                const hasProjectCommands = currentProject
                  ? (projectCommandsMap?.[currentProject.id]?.length ?? 0) > 0
                  : false;
                return !currentProject || !hasProjectCommands;
              })()}
              className={cn(
                "rounded-none",
                commandMode === "project" && "rounded-r-md"
              )}
              disabled={(() => {
                const hasProjectCommands = currentProject
                  ? (projectCommandsMap?.[currentProject.id]?.length ?? 0) > 0
                  : false;
                return !currentProject || !hasProjectCommands;
              })()}
              onClick={commandMode !== "project" ? enableProjectCommands : undefined}
            >
              项目指令
            </Button>
          </div>
          <Button
            variant="outline"
            size="sm"
            onClick={onOpenFolder}
            disabled={!currentProject}
            aria-label="打开项目文件夹"
          >
            <FolderOpen className="size-3.5" />
            打开文件夹
          </Button>
        </div>
```

注意：
- 外层容器使用 `flex items-center justify-between mt-2`（per D-03 右对齐）
- Toggle Group 容器使用 `inline-flex rounded-md overflow-hidden border border-white/10`（per D-01 紧邻拼合）
- 每个按钮使用 `size="sm"`（per D-02）
- "全局指令" 按钮在全局模式时用 `secondary` variant，否则用 `ghost`
- "项目指令" 按钮在项目模式时用 `secondary` variant，否则用 `ghost`
- 按钮圆角处理：左按钮只有 `rounded-l-md`（激活时），右按钮只有 `rounded-r-md`（激活时），内部全部 `rounded-none`
- 分隔线：左按钮的 `border-r border-white/10`（per UI-SPEC）
- disabled 逻辑：`!currentProject || !hasProjectCommands`（per D-05 空状态禁用）
- onClick：仅在非当前模式时调用切换函数（per D-06 点击当前活跃按钮无操作）
- 无障碍：`role="radiogroup"`、`role="radio"`、`aria-checked`、`aria-label`（per UI-SPEC Accessibility）
- "打开文件夹" 使用 `outline` variant + `FolderOpen` icon `size-3.5`（per D-03）
- onClick 仅传 `onOpenFolder`，不需要参数（路径在 App.tsx 中绑定）

**但是**：projectCommandsMap 不在 MainAreaProps 中。需要从 useProject 传递到 MainArea。

有两个选择：
A) 传递 projectCommandsMap prop 到 MainArea
B) 在 MainArea 中根据 commandMode 和 commands 推断 disabled 状态

选择 B 更简洁：当 `commandMode === "global"` 且项目无自定义指令集时，说明需要禁用。但准确判断需要知道是否有项目指令集。

实际上，更简单的方式：在 useProject 中已经通过 `projectCommandsMap[selectedId]` 管理。最干净的做法是在 useProject 中导出一个 derived boolean，或传递 projectCommandsMap。

**最终决定：在 App.tsx 中计算 isProjectToggleDisabled 并作为 prop 传递。**

修改方案：

**MainAreaProps 新增：**
```typescript
onOpenFolder: () => void;
isProjectToggleDisabled: boolean;
```

**MainArea 函数参数新增：**
```typescript
onOpenFolder,
isProjectToggleDisabled,
```

**Toggle Group 中 "项目指令" 按钮的 disabled 和 aria-disabled 直接使用 `isProjectToggleDisabled`：**
```tsx
disabled={isProjectToggleDisabled}
aria-disabled={isProjectToggleDisabled}
```

**5. 修改 App.tsx：**

从 useProject 解构中添加 `openFolder`：
```typescript
// Phase 9: open folder
openFolder,
```

在 App 函数体中，useProject() 调用之后计算 isProjectToggleDisabled：
```typescript
// Phase 9: derive project toggle disabled state (per D-05)
const isProjectToggleDisabled = useMemo(() => {
  if (!currentProject) return true;
  // 通过 commands 长度或 commandMode 推断不可靠，需要直接检查 store
  // 但 App 无法直接访问 projectCommandsMap — 需要从 useProject 导出
  return false; // placeholder
}, [currentProject]);
```

等等，这不对。需要重新思考。

**更好的方案：从 useProject 导出 projectCommandsMap。**

在 useProject.ts return 对象中添加（Phase 4 Plan 03 区块）：
```typescript
projectCommandsMap, // Record<string, CommandItem[]>
```

然后在 App.tsx 中：
```typescript
const {
  // ...existing
  openFolder,
  projectCommandsMap,
} = useProject();

const isProjectToggleDisabled = useMemo(() => {
  if (!currentProject) return true;
  const projectCmds = projectCommandsMap[currentProject.id];
  return !projectCmds || projectCmds.length === 0;
}, [currentProject, projectCommandsMap]);
```

**最终修改清单（综合以上所有分析）：**

1. **useProject.ts**：添加 openFolder 函数 + 导出 projectCommandsMap + 导出 openFolder
2. **shell.rs**：添加 open_folder 命令
3. **lib.rs**：注册 open_folder
4. **MainArea.tsx**：替换 206-227 行为 Toggle Group + 打开文件夹按钮行，新增 onOpenFolder 和 isProjectToggleDisabled props
5. **App.tsx**：解构 openFolder + projectCommandsMap，计算 isProjectToggleDisabled，传递新 props 到 MainArea

需要确保 App.tsx 添加 `useMemo` import（已有：`import { useState, useCallback } from "react"` — 需要加 `useMemo`）。
  </action>
  <verify>
    <automated>cd "E:/GitLib/EasyPack" && grep -c "role=\"radiogroup\"" src/components/MainArea.tsx && grep -c "justify-between" src/components/MainArea.tsx && grep -c "onOpenFolder" src/components/MainArea.tsx && grep -c "onOpenFolder" src/App.tsx && grep -c "isProjectToggleDisabled" src/App.tsx && grep -c "projectCommandsMap" src/App.tsx</automated>
  </verify>
  <done>
    - MainArea.tsx 不再包含 "使用项目自定义指令" 或 "使用全局指令" 文本链接
    - MainArea.tsx 包含 Toggle Group 容器（role="radiogroup", aria-label="指令模式切换"）
    - MainArea.tsx 包含两个 Button（"全局指令" 使用 secondary/ghost variant，"项目指令" 使用 secondary/ghost variant）
    - MainArea.tsx "项目指令" 按钮 disabled 绑定到 isProjectToggleDisabled prop
    - MainArea.tsx 包含 "打开文件夹" outline Button（FolderOpen icon size-3.5 + 文字）
    - MainArea.tsx 按钮行使用 flex justify-between mt-2 布局
    - App.tsx 从 useProject 解构 openFolder 和 projectCommandsMap
    - App.tsx 计算 isProjectToggleDisabled（currentProject 为空或无项目指令集时为 true）
    - App.tsx 传递 onOpenFolder 和 isProjectToggleDisabled props 到 MainArea
    - useProject.ts return 对象导出 projectCommandsMap 和 openFolder
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust open_folder | 用户触发的路径字符串传入后端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | E (Elevation of Privilege) | open_folder command | accept | explorer.exe 以当前用户权限运行，无权限提升路径；路径来自内部存储的 project.path，非用户自由输入 |
| T-09-02 | T (Tampering) | open_folder path param | accept | 路径来自 useProject 内部 projects 状态（通过文件夹选择器添加），不可被外部篡改；explorer.exe 对非法路径会显示错误对话框而非崩溃 |

此阶段无高危威胁：open_folder 是只读操作（打开资源管理器），不修改任何文件或状态；Toggle Group 是纯前端 UI 切换，不涉及数据处理。
</threat_model>

<verification>
1. Rust 编译通过：`cd src-tauri && cargo check`
2. 前端编译通过：`pnpm build`
3. 现有测试通过：`pnpm test`
4. grep 验证关键代码存在（见各 task verify）
5. 无残留旧文本链接代码
</verification>

<success_criteria>
- "打开文件夹" 按钮使用 outline variant + FolderOpen 图标 + sm size
- Toggle Group 两个按钮紧邻拼合（无 gap），激活项 secondary variant，非激活项 ghost variant
- "项目指令" 按钮在项目无自定义指令时 disabled + aria-disabled
- 按钮行使用 justify-between：左 Toggle Group，右 "打开文件夹" 按钮
- 点击 "全局指令" 调用 disableProjectCommands
- 点击 "项目指令" 调用 enableProjectCommands
- 点击 "打开文件夹" 调用 Rust open_folder 命令，explorer.exe 打开项目目录
- 错误时显示 toast "无法打开文件夹"
- MainArea 中不再有旧文本链接切换器代码
- cargo check 通过，pnpm build 通过，pnpm test 通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-ui/09-01-SUMMARY.md`
</output>
